# Q6 — normalized import acceptance and manual candidates

Program `gpt-study-platform-2026-09-11`, worker Q, 2026-09-11.
Branch `codex/gpt-imports`; worktree
`/Users/connor/.codex/worktrees/f0c7/oms-study-automation`.
Accepted parent `a0dd67a5f76f73206aee5451f4c864ebc60d74e2`; clean rebase
confirmed before edits. Exact commit/tree are supplied to O after committing.

## Scope and entrypoint

Updated `docs/question-bank-import-format.md`,
`src/oms_hub/question_bank/routes.py`,
`src/oms_hub/web/templates/question_bank_import.html`,
`tests/question_bank/test_routes.py`, and
`tests/question_bank/test_native_review.py`; this receipt is the only new file.
No shared app/config/models/migrations/navigation changes. O approved the additional
route proposal before edits and owns the C2 chat entry link.

`GET /question-bank/anki-candidates` renders the dedicated manual form.
`POST /question-bank/anki-candidates` requires owner and existing form CSRF,
accepts `query`, validates with Q4's `parse_candidate_query`, and resolves with
`preview_candidate_notes` using the router's optional injected index. The constructor
contract is unchanged. Invalid queries fail 422 before index access; unavailable
index/snapshot is 503, changing snapshot is 422. No persistence, index refresh or
RPC is invoked. Missing IDs, all card IDs, pending review and unverified snapshot
are displayed. User-pasted AMBOSS provenance does not establish entitlement or
question-to-note mapping. There is no arbitrary Anki search forwarding.

O's final wiring passes only an already configured `app.state.anki_companion_index`
without enabling Anki. `tag_rules=()` stays unchanged pending observed/reviewed real
prefix mapping. Manual NIDs use that snapshot independently; QID tag matching has
fixture acceptance only. No actual source-to-tag match or profile freshness claimed.

## Synthetic reconciliation

The new combined acceptance test submits eight original rows. Eight are retained,
zero record writes blocked, one duplicate separately reported. Seven identities
produce four attempts: correct, incorrect, omitted and unknown. Original Unicode
notes, tags, nullable time and row order are preserved. Body reconciliation is
one ready + two held + four metadata-only + one duplicate = eight rows.
Replay returns the original receipt and preserves four attempts.

A second source using the same exact `00123` adds its own fifth attempt. A later
two-row export that conflicts at row two rolls back both rows, preserves five
attempts, and exposes no persisted receipt. Review stages only the three applicable
bodies; missing answer/image gates prevent publication. No published row exists.
The existing adjacent test separately verifies successful reviewed publication
after explicit answer verification and synthetic image upload, then exercises
the existing public quiz player, content and image endpoints.

## Checks

Configured Python 3.13 interpreter, no install or upgrade:

```sh
PYTHONPATH=src /Users/connor/Developer/oms-study-automation/.venv/bin/python -m pytest -o addopts='' tests/question_bank tests/study_generation/test_native_quiz.py tests/anki/test_companion_index.py tests/study_generation/test_practice_review.py tests/study_generation/test_bank_review_integration.py tests/v2/test_public_quiz_routes.py -q
/Users/connor/Developer/oms-study-automation/.venv/bin/ruff check src/oms_hub/question_bank tests/question_bank
/Users/connor/Developer/oms-study-automation/.venv/bin/mypy --follow-imports=silent src/oms_hub/question_bank
git diff --check
```

Full listed regression run: **304 passed in 58.71s**. Ruff and mypy passed
(seven Q source files). Scoped diff and whitespace inspected. Existing tests
also cover concurrent duplicate writes, injected insert failure, independent
source/product/learner identity, forged previews, immutable body versions,
unknown results, owner/CSRF boundaries and O's Unicode wire-size fix.

Synthetic browser check: fixture served the actual router/template/static styles
at `http://127.0.0.1:65361/question-bank/anki-candidates`. Submitted
`nid:101,999 OR nid:101`; actual DOM showed two candidates, note 101 cards 201/202,
normalized cloze text, explicit missing note 999, and unverified provenance and
snapshot. Desktop screenshot visually inspected. No mobile acceptance claimed
for the new panel. Browser tab closed; exact fixture PID 53411 verified and stopped,
execution session exited 143. Fixture script/database/index remain under
`/var/folders/09/115hy48x3nn6mcm86v5rbq6c0000gn/T/q6-browser.jpaec9lf/` for audit.
No main app or port 8765 operation occurred.

## Shared contracts and remaining acceptance

Existing Q source modules: `contracts.py`, `imports.py`, `repository.py`,
`anki_links.py`, `native_review.py`, `routes.py`, and package `__init__.py`.
No new storage model or migration in Q6. The accepted O schema supplies:

- `bank_imports`: unique learner/source/product/export; digest, receipt/provenance.
- `bank_questions`: unique source/product/external string QID; immutable non-null
  content hash and structural readiness; reviewed topics.
- `bank_import_rows`: unique import/original row number; raw canonical row and
  notes/tags/topics/issues, question FK and row readiness.
- `bank_attempts`: unique learner/question/attempt; nullable time, four-state
  outcome, original row FK; ascending autoincrement cursor.
- `bank_topic_reviews`: content hash and previous/new topics with actor/time.
- `bank_review_runs` and `bank_review_questions`: exact import/batch/run and
  original row/question links maintained by O's existing Studio bridge.

`AttemptFact` remains frozen with id, learner_id, exact QuestionKey, attempt_id,
result, nullable occurred_at/elapsed_ms, imported_at, import_id and effective topics.
`BankRepository.iter_attempts(*, learner_id, after_id=0, limit=500)` returns a tuple
ordered by stable ascending id. Unknown and omitted results remain separate from
incorrect; QID-only rows create no attempt. External topic claims remain pending;
trusted server-recorded native provenance is handled by the accepted shared path.

O explicitly accepted Q6's consumer boundary: C3 is not present on the approved
parent. Actual correctness-denominator/first-attempt analytics acceptance remains
for O's cross-consumer suite after C3 is accepted. Planned C3 API is
`ProgressService(bank).report(owner_id, first_only=False)` and
`summarize(tuple[AttemptFact, ...], first_only=...)`. Q adds no duplicate aggregator.

Normalized format acceptance is synthetic only. UWorld and TrueLearn adapters/Q2
remain independently pending actual samples already requested once. No account,
credential, provider, retained GitHub dataset, real Anki profile/index, live release,
full curation, or Windows acceptance is claimed. Q call-path inspection found no
provider/network/credential/refresh/publish operation in question-bank modules;
review staging reaches the accepted local Studio bridge, and publication remains
the existing explicitly reviewed service path.
