# C2 — unavailable-only reference capability

Local deliverable: explicit `AmbossReference.search` / `AmbossUnavailable` boundary and plan-defined `ReferencePassage`; medical-reference chat uses it and never falls back to GPT. Existing official AMBOSS link and no-AMBOSS provenance message remain visible. An unexpected returning adapter fails closed until an access-specific amendment exists.

Read the approved access research and sent inquiry. O reports only an automatic acknowledgment in the exact sent thread; this establishes no endpoint, entitlement, or content rights. No email, provider probe, login, or live access occurred. **C2-live external pending**.

Validation: `PYTHONPATH=src /Users/connor/Developer/oms-study-automation/.venv/bin/python -m pytest tests/study_chat -q` — 38 passed. Owned chat Ruff and focused mypy — pass. Initial unavailable-boundary test failed because the module was absent, then passed. Existing C1 UI already supplies the official link; no template/app/navigation changes.

The accepted parent contains Q's exact local AnKing resolver but not Q5's candidate route; no route was guessed. A manual candidate entry link remains an O/Q5 integration seam, independent of unavailable-only C2 and live access.

Next: O review/integration; C proceeds to the separately dispatched C3 local session/progress work.
