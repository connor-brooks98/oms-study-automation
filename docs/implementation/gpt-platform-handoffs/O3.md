# O3 final candidate receipt

Code candidate: `9e54da4188abaa5694c57b0d59aaec7c4e216a4f`.
Tree: `35aae67a78ab911608b378b924345df9467ae1cc`.
Integration branch/worktree: `codex/gpt-study-platform`, `/Users/connor/Developer/worktrees/oms-gpt-study-platform`.
Later receipt and operations-guide edits do not change the candidate code, tests, runtime configuration or release archive.

## Verification

The configured existing environment was used; no dependencies were installed or upgraded.

```sh
/Users/connor/Developer/oms-study-automation/.venv/bin/ruff check src tests scripts
/Users/connor/Developer/oms-study-automation/.venv/bin/mypy src
PYTHONPATH=src PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring /Users/connor/Developer/oms-study-automation/.venv/bin/python -m pytest -o addopts='' -q -m 'not windows_office'
/opt/homebrew/bin/node --test 'tests/js/*.test.js'
```

Ruff passed; mypy passed 230 source files; Node 284 passed. Full Python verification: **3,821 passed, 3 skipped, 1 deselected in 800.87 seconds; process exit 0**. Logs and SHA256 values are recorded in the program status.
The one excluded Windows Office marker and opt-in browser/runtime skips are not actual Windows acceptance. The B6 opt-in browser test was run separately and passed; its 12 stages and C4 desktop/mobile browser proof are recorded in `O3-browser-evidence.json`.

Independent final code review passed at the exact candidate, including both repaired C4 race conditions and app/private-export boundaries. Separate documentation review verified archive manifests, fonts, all 17 browser evidence files, links and explicit operational limits. Detailed corrections and task receipts remain in O1/B/Q/C handoffs.

## Immutable artifacts and preserved state

`O3-archives.json` binds both deterministic local archives to the exact candidate. Both repeat builds were byte-identical; all 309 runtime and 700 source manifest entries and ZIP integrity passed. Bundled font files equal frozen Git bytes. These are local artifacts only and have not been copied to the NUC.

Original checkout dirty/untracked status matches the bootstrap list. Runtime/test/config paths remained byte-identical to the reviewed candidate throughout final verification. All B/Q/C worker worktrees and retained resources remain in place. No push, main merge, live restart/deployment, Anki mutation, private-source provider test, paid fallback, reset-credit use, purchase or retained-resource deletion occurred.

## Acceptance boundary

B1–B7, Q1/Q3–Q6, C1/C3/C4 are locally implemented and reviewed. Q2 vendor adapters await the exact supported per-question exports; AMBOSS remains explicitly unavailable pending entitlement evidence in the already identified email thread. Real Anki QID prefix rules remain unconfigured; full curation is deferred. The specifically authorized AMBOSS thread `1a0924aab334e5da` was refreshed at `2026-09-12T00:42:22Z`: still only the sent inquiry and automatic acknowledgement `1a0924ac6f289fe0`; nothing was sent or followed up.

Production Codex generation remains `capability_unverified`. Windows restricted execution/readable roots, actual Office conversion, subscription login persistence and text/image/schema behavior have not been accepted. The one managed-login attempt expired; no retry was started. Full evidence over 20 images or 100,000 serialized source characters fails before dispatch with affected-objective diagnostics; no truncation or automatic partition acceptance is implied. Existing imported NotebookLM outline replacement remains unavailable through GPT.

See `../gpt-platform-release-proposal.md` for the capability matrix, exact read-only old-Hub snapshot, three bounded source-free provider cases and deployment/rollback proposal. No deployment approval is requested while activation is blocked.
