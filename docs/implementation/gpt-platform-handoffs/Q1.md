# Q1 — normalized import preview

- Program: `gpt-study-platform-2026-09-11`; worker Q; 2026-09-11.
- Worktree: `/Users/connor/.codex/worktrees/f0c7/oms-study-automation`.
- Branch: `codex/gpt-imports`.
- Base: `96ceec56c4b8f9df0018fe090710631adb29a2a2`.
- Base tree: `86f10d154cbbba4494ee3b249d40a94bb76a014d`.
- Head/tree: the commit containing this receipt; exact post-commit hashes are
  delivered directly to O with the task-tool completion message (avoids a
  self-referential commit hash in this file).
- Status: initial independent review blocked P2 on numeric timestamp strings;
  corrected locally and ready for re-review. No approval, integration, Windows
  or provider acceptance claimed here.

## Files and interfaces

- `src/oms_hub/question_bank/__init__.py`: package marker.
- `contracts.py`: strict frozen Pydantic inputs and frozen dataclass outputs with
  the Q1 plan names, including `QuestionKey`, `TopicLabel`, `ImportRow`,
  `Provenance`, `ImportEnvelope`, `RowIssue`, `ImportPreview`, `ImportReceipt`,
  `AttemptFact`.
- `imports.py`: `parse_import(raw: bytes) -> ImportEnvelope` and
  `preview_import(raw: bytes) -> ImportPreview`.
- `tests/question_bank/test_imports.py` and synthetic `fixtures/normalized-v1.json`.
- `docs/question-bank-import-format.md`: format, readiness/duplicate semantics,
  support matrix, and exact vendor sample request.

Reuses `parse_native_quiz` unchanged. No shared app/config/models/migrations edits.
Preview records original row positions, classifies exact duplicate/conflicting
identities, and holds malformed or image-dependent bodies without discarding
metadata. No publication or database writes are implemented. Nested question
dictionaries retain the plan's raw JSON representation; Q3 must revalidate and
recompute the expected digest before any write, never trust a constructed preview.

## Verification

Configured interpreter is the existing Python 3.13 environment at
`/Users/connor/Developer/oms-study-automation/.venv/bin/python`; no dependencies
were installed or upgraded. Commands run in the worktree with `PYTHONPATH=src`.

1. RED: `python -m pytest tests/question_bank/test_imports.py -q` using that full
   interpreter path failed collection with `ModuleNotFoundError:
   No module named 'oms_hub.question_bank'`, before production files existed.
2. The first implemented run exposed four failing raw-body nonfinite tests
   (`NaN`, `Infinity`, `-Infinity`, `1e999`). Pydantic JSON serialization masks
   those values as null; validation now checks the raw body before serialization.
3. GREEN: `python -m pytest -o addopts='' tests/question_bank/test_imports.py
   tests/study_generation/test_native_quiz.py -q`: **124 passed** after review fix.
4. `.venv/bin/ruff check src/oms_hub/question_bank tests/question_bank`: passed.
5. `PYTHONPATH=src .venv/bin/mypy --follow-imports=silent
   src/oms_hub/question_bank`: passed (3 source files; scoped type check).
6. `git diff --check`: passed; final staged scope checked before commit.

Tests cover strict identity/types, timezone requirements, attempt invariants,
confidence/size limits, nested invalid JSON numbers, canonical digest, original
duplicate positions, native readiness, and socket/database/publication guards.
All evidence is local and synthetic. No provider, accounts, credentials, Anki,
retained resources, production listener, deployment or push actions occurred.

## Coordination and next steps

O received and accepted Q3's exact model/table requirements separately and owns
their schema migration. O also accepted the `set_topics(..., reviewer_context:
str)` amendment; request-bound actor context is stored in audit JSON. Q3 will use
`BankRepository(database.session)` after O supplies the reviewed shared commit.
Future private routes consume O-validated `request.state.study_owner_id`, reject
its absence with HTTP 401, and never trust a form-supplied learner ID.

Q2 is independently blocked on real supported samples, not Q1: provide each
vendor's sanitized actual export or user-made results/QID/notes file with product,
export steps, timezone and stable question/attempt identity. No proprietary format
was guessed and no vendor adapter is advertised. Full content requires established
authorized provenance; aggregate-only reports cannot become invented attempts.

Next: O's independent Q1 review, then exact next-task dispatch. No helpers spawned.

## Independent review correction

The reviewer independently passed the original 117 tests but reproduced a P2:
`AwareDatetime` accepted numeric strings as Unix timestamps even in strict JSON
mode. The correction starts at `95730e8d753f955d555c60973a851fa1c26a0225`, tree
`765ac4e9f110a3fe7547578840f236feb5b52b5f`.

Added regressions for integer/date-like/fractional/negative numeric strings, plus
positive tests for UTC, negative offset and fractional time with a positive offset.
RED: Q1-only suite **4 failed, 80 passed**, all four failures were numeric strings
incorrectly accepted. A before-validator now requires calendar date/time syntax
for strings and parses it with `datetime.fromisoformat`; the existing
`AwareDatetime` check still requires a timezone. Parsing inside the before-validator
also preserves strict-mode support for legitimate JSON timestamps. GREEN: the
combined focused suite **124 passed**. Scoped Ruff,
mypy and diff checks passed. The correction commit's exact head/tree are sent to O
after commit. The shared schema candidate remains unconsumed pending Q3 dispatch.
