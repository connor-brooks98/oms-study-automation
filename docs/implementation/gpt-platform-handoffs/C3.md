# C3 — private attempts and descriptive progress

Local implementation complete; O review, shared-player application, factory/navigation integration, and integrated acceptance remain pending. No C4 work started.

- Accepted integration ancestor: `763248746e3cd19b65f2d93dfd4bac7f56d32dc8`.
- Code commit: `16693769aa7330dd20e8a6c02f2d09b23c034d70`.
- Code tree: `f00ca7e90c855e251a314f1f88a7bfc46c74b260`.
- Branch/worktree: `codex/gpt-chat`, `/Users/connor/.codex/worktrees/5b09/oms-study-automation`.
- Owned production files: `src/oms_hub/study_progress/{__init__,sessions,service,routes,taxonomy}.py` and `web/templates/study_{session,progress}.html`; tests under `tests/study_progress`.
- No shared app, config, schema, migration, repository, navigation, public endpoint, or player file was edited. `C3-player.patch` is the exact proposed O-owned player/test change.

## O integration

```python
from oms_hub.study_progress import routes
from oms_hub.study_progress.sessions import StudySessionService
from oms_hub.study_progress.service import ProgressService

app.state.study_session_service = StudySessionService(
    database.session,
    bank=bank_repository,
    load_quiz=load_quiz,
    topics_for=topics_for,
    media_for=media_for,
)
app.state.study_progress_service = ProgressService(bank_repository)
app.include_router(routes.router)
```

All three callbacks are required, as approved:

- `load_quiz(owner_id, quiz_token) -> PublishedQuizRecord`: fixed private owner/access check and current active, accepted native publication. C also compares the current DB publication's active flag, version, and canonical native content inside its transaction.
- `topics_for(owner_id, QuestionKey) -> tuple[TopicLabel, ...]`: accepted reviewed metadata only; return `()` when unreviewed. C never infers accepted taxonomy from objective text. Accepted labels are staged with the immutable outcome so a retry sends the same bank inputs.
- `media_for(owner_id, PublishedQuizRecord) -> tuple[PublishedQuizMediaRecord, ...]`: O verifies trusted publication paths/readiness. C checks unique keys, publication binding, every required image, and on-disk SHA-256; missing or changed media fails delivery. URLs use existing public media paths and generic `Question image` alt text.

Private entry: `/study/sessions/new?quiz_token=<accepted token>` provides a CSRF-protected native start form. JSON creation: `POST /study/sessions` with `{"quiz_token":"..."}`. Session page/content: `/study/sessions/{uuid}` and `/content`. Answer: `POST /study/sessions/{uuid}/answers/{attempt_uuid}` with `{"kind":"choice","choice_id":"c1","elapsed_ms":null}` or `{"kind":"matching","matches":{"p1":"c2","p2":"c1"},"elapsed_ms":null}`. Close: POST `/study/sessions/{uuid}/close`. Progress page/API: `/study/progress` and `/study/progress/data`, optional `?first_only=true`.

Public anonymous grading remains unchanged and writes no personal events. Private routes use only `request.state.study_owner_id`, enforce CSRF on writes, and return private/no-store data. Session answers reject client correctness, ownership, question identity, and timestamp fields. The server binds the path attempt ID to its issued question and grades through existing native functions. Duration is optional, strictly nonnegative, and never fabricated; staged duration is restored for exact retry.

Apply **C3-player.patch together with the private route/factory integration**. The new private template requires this patch to submit/restore correctly. It opts in only with `data-personal-session="true"`, reuses existing rendering and grading feedback validation, restores server-saved selections/acknowledged feedback, retains staged selections for retry, omits browser history and public flags, uses the issued attempt ID and duration, disables new submissions in closed sessions, and displays correct personal-storage copy. Public behavior stays covered by the original tests.

Final patch SHA-256: `6791377bad54c3bbe005c9be5e5a8f7943944814a961f4d52efada6a232a96c8`. Base public player SHA-256: `0bfbd78fa5a08cbbddeaf06d3efbf8b86146db3a34fe3c9ad6757517b9b48dff`. `git apply --check docs/implementation/gpt-platform-handoffs/C3-player.patch` passes on C's tree. The patch also adds four public-player tests; it was materialized and exercised in an isolated temporary copy, not applied to shared files here.

## Persistence and reporting

Uses the approved `study_sessions` and `study_session_questions` tables and Q's actual bank contracts. A server UUID is issued per session and per delivered question. Full `(quiz token, version, canonical native content SHA-256, stable question ID)` is serialized and hashed into a 71-character `QuestionKey.question_id`, preserving the shared 200-character limit.

Before returning grading feedback, C commits `{submission,result,feedback,topics}` to `selected_answer_json` with server submission time and optional duration. It releases that transaction, invokes Q's atomic `record_attempt`, validates the returned owner/key/identity/result/time/duration, and links `bank_attempt_id` in a new transaction. This uses the existing approved columns; it adds no outcome/aggregate table. A crash before or after the bank commit retries the same staged inputs, even after the publication changes, without regrading. A different selection/duration conflicts. Only acknowledged feedback is restored as graded UI state. Closing blocks new answers but still allows exact staged replay.

Progress scans one owner's actual `AttemptFact` records with Q's cursor. Global event deduplication precedes source/topic groups. Only accepted mapped labels group events. First selection occurs globally before topic grouping, so a repeat with a new label cannot become a first attempt for that topic. Repeats count extra events for the same immutable question key. A group with multiple events and any missing occurrence time, or a tied earliest time, has ambiguous ordering and is excluded from first-only accuracy. A single undated event can count as first; undated coverage remains explicit. Omitted/unknown imported results stay distinct, and only correct+incorrect enter accuracy. No pass/readiness model, aggregate store, browser-history migration, or pass-tracker replacement.

## Official taxonomy metadata

Top-level metadata only: 17 NBOME dimension/category IDs and 18 USMLE content headings, with separate versioned namespaces, original source identifiers, source/specification URLs, and verification date `2026-09-11`. No question mappings or crosswalks are manufactured. USMLE has heading identifiers rather than official numeric codes; local ordinals are explicitly only part of the versioned canonical IDs.

- `nbome_comlex_level1:2025-02`: [February 2025 official blueprint](https://www.nbome.org/app/uploads/2025/05/COMLEX-USA-Blueprint-2025.pdf), dimensions 1 and 2. The [NBOME applicability page](https://www.nbome.org/assessments/comlex-usa/comlex-usa-blueprint/) lists this version for Level 1 through April 8, 2028, with Blueprint 2.0 from May 2028.
- `usmle_step1:2026`: [official 2026 content outline](https://www.usmle.org/sites/default/files/2022-01/USMLE_Content_Outline_0.pdf), 18 table-of-contents headings; [Step 1 specifications](https://www.usmle.org/exam-resources/step-1-materials/step-1-content-outline-and-specifications). The PDF's URL directory is 2022-01, but its actual release/copyright text is 2026. This shared outline is labeled explicitly; Step 1 specifications determine emphasis.

## Verification

Existing runtime: `/Users/connor/Developer/oms-study-automation/.venv/bin/`, `PYTHONPATH=src`; Node `/opt/homebrew/bin/node`.

- `python -m pytest tests/study_progress tests/question_bank tests/study_generation -q`: passed all **834 collected tests**. Initial new-module checks failed before implementation as expected.
- Final strengthened C3-only check: `python -m pytest -q -o addopts='' tests/study_progress`: **12 passed**. Includes real SQLite migrations/bank records, two simultaneous identical answers, owner/session/attempt separation, response-loss replay with duration, changed version/media rejection, matching grading, private CSRF, anonymous no-write grading, first/repeat/unknown/omitted counts, global multi-label deduplication, and taxonomy identity checks.
- Original public player/library: `node --test tests/js/public_quiz.test.js tests/js/public_quiz_library.test.js`: **68 passed**.
- Final proposed player patch in isolated temporary copy: **38 passed** (34 existing + 4 personal-session regressions). Final copy: `/var/folders/09/115hy48x3nn6mcm86v5rbq6c0000gn/T/c3-player-check-muho205o/`.
- Owned `ruff check` and focused `mypy --follow-imports=silent src/oms_hub/study_progress`: pass, **5 source files**. Staged `git diff --check` and normal `git apply --check` for the patch: pass.
- Installed Chrome + Playwright intercepting requests to a synthetic `TestClient`, all external requests blocked: start form, answer submission, durable server reload, progress, first-attempt toggle, 390px mobile overflow check, **0 JS errors**. Repeated against final candidate player. Script `/tmp/c3_browser_check.py`; screenshots `/tmp/c3-session-desktop.png`, `/tmp/c3-progress-desktop.png`, `/tmp/c3-progress-mobile.png`. Desktop session and final mobile progress screenshots visually inspected.

All evidence is local/offline. No helpers, provider/account/Anki/Windows actions, push, deployment, or live service changes. C2 live access remains externally pending; Q6 manual/Anki candidate route linkage awaits its accepted handoff. C is idle for O review/dependencies; C4 is not authorized yet.
