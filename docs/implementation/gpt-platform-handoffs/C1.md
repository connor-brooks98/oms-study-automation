# C1 local delivery — chat sources, persistence, service and UI

State: C1 local implementation and raw-output amendment complete; independent full C1 review remains pending. This is not provider or release acceptance.

- Integration parent: `8de567845e4ba53be558f876aa3dffb1fd195cf8` (O-approved schema38).
- Foundation after rebase: `a250a1bd`.
- Service/UI and replay correction: `540528d1`.
- Code head: `96f5eccc7f7dd595c7ce324378f675f3ce913c26`.
- Code tree: `5798e3362024b04dd2271373a10a7af1d67a9309`.
- Branch/worktree: `codex/gpt-chat`, `/Users/connor/.codex/worktrees/5b09/oms-study-automation`.

## Scope and contracts

Owned changes: `src/oms_hub/study_chat/{__init__,contracts,sources,repository,service,routes}.py`, the focused chat template/JS/CSS, `tests/study_chat/`, and `tests/js/study_chat.test.js`. No shared app/config/models/migrations/navigation edits.

Uses the actual B `SessionRequest`, `SessionResult`, `SessionLifecycle`, `SessionError`, and injected `CodexSessionClient`. No separate client, scheduler, login, capability probe, or error DTO. Actual B generation remains blocked by `capability_unverified`; local tests do not bypass production readiness.

O wiring:

```python
chat_sources = ChatSources(
    database.session,
    LectureSourceExtractor(app.state.ingestion_repository),
    authorize_revision=authorize_revision,
)
chat_repository = ChatRepository(database.session, sources=chat_sources)
app.state.study_chat_service = ChatService(
    chat_repository, shared_codex_session_client, model=selected_model,
)
app.include_router(study_chat.routes.router)
```

O supplies the required owner/revision callback and existing singleton. Leave `study_chat_service=None` when no executable/pin is configured: the page displays unavailable HTML and mutation APIs return 503. Constructors perform no external action. Model selection changes affect new requests; persisted request models remain fixed. O alone calls `interrupt_pending(owner_id=...)` after reconciling the owned process on restart.

Repository additions: owner-scoped `load`, `load_request`, `recent_requests`, `clear`, `begin(request, model=...) -> BeginResult(record, created)`, `fail`, and explicit startup interruption. `record_lifecycle(request_id, event, owner_id=...)` requires the owner-bound callback. Only `begin.created=True` can generate. A provider-completed phase leaves application state running until raw persistence, answer validation, and append commit. `record_output(request_id, SessionResult, owner_id=...)` checks completed phase and matching thread/turn before privately committing text bounded by B's `MAX_OUTPUT_BYTES`; the service then parses and validates.

Source selection uses case-folded token overlap with stable passage-ID ordering and a 24,000-character whole-passage limit. It checks authorized current revision/lecture metadata and immutable artifact hashes before/after extraction, before dispatch, and before answer acceptance. It preserves exact passage evidence and bounded owner/conversation history. The documented lexical paraphrase miss remains a limitation, not a fabricated answer.

Private routes provide conversation creation/reload/clear, answer submission, request state, cancellation, and plain-text cited excerpts. Owner identity comes only from `request.state.study_owner_id`; mutations reuse `require_form_csrf`. Plain-text DOM rendering and server-generated citation paths exclude executable markup/model links. AMBOSS mode is explicitly unavailable and never falls back to general generation.

## Verification

Using the existing `/Users/connor/Developer/oms-study-automation/.venv/bin/` runtime and `PYTHONPATH=src`:

- `python -m pytest tests/study_chat tests/study_generation/test_chat_schema.py tests/anki/test_sources.py tests/v2/test_gpt_owner_boundary.py -q`: **47 passed**. The subsequently strengthened malformed-output test also passed independently, including request/conversation route privacy assertions.
- `node --test --test-timeout=5000 tests/js/study_chat.test.js`: **5 passed**.
- `ruff check src/oms_hub/study_chat tests/study_chat`: pass.
- `mypy --follow-imports=silent src/oms_hub/study_chat`: pass, six source files. This is focused typing evidence, not a full-repository mypy claim.
- `git diff --check`: pass before commit.
- Isolated installed Chrome, Playwright interception to synthetic `TestClient`, no provider/network requests: source selection, answered citation, durable reload, clear, 390px overflow check, and no JS errors pass. Screenshots: `/tmp/c1-chat-desktop.png`, `/tmp/c1-chat-mobile.png` (temporary local evidence).

Foundation reviewer P1 is corrected: repeated dispatch was incorrectly accepted by exact-event replay. Regressions first failed, then passed after terminal-state/repeated-dispatch checks moved before harmless notification replay. Both interrupted and failed requests reject repeated last events and dispatch. Fresh full C1 review remains O-owned.

## Limitations and next action

The new B2 raw-output contract is consumed. Malformed JSON is retained in O's private `raw_response_text` column while the request becomes failed with no accepted answer. Raw content never enters normal repository views, routes, history, or UI; storage failure prevents answer acceptance. The current B2 parent still has its earlier JSON parse, but C's fake transport regression proves that C handles raw malformed output when B removes that redundant parse.

Evidence classification: local/offline only; Windows and provider acceptance absent; AMBOSS live access unavailable. No helpers, live Anki, account, provider, Windows, push, deployment, or production actions. O wires the constructors/routes/navigation and owns independent review and integration. C is idle pending review or the next exact assignment; C2/C3/C4 were not started.
