# Task 2.8 Program Sol-0 fresh private-shadow composition

Status: **CORRECTED / COMPOSED / HASH-VERIFIED / NOT EXECUTED / REVIEWED SHIP /
PROVIDER RECONCILIATION EMPTY / GATE_NOT_OPEN**

Program Sol-0 built a brand-new protected private-shadow composition after cleanup result
`9b36301726b1dfd068aee549654f3c9adcee7d57` and independently reviewed result
`2cef97899a371ba5e72d831e31194ce14e07b387`. The exact source archive is pinned to Sol-2
handoff `909785131b113fb5b2083b74bd2757c2fc0dbf4b`, tree
`d7eb0dae14d915d24b47a6a865ffe6eb31ab5c6a`, plus corrected entrypoint review GREEN
`4bc82fbb258cb1192323fe16f1123c8b2a5fdb82`, tree
`df0871b3777c65f76ca93cc9385591793e481e86`, and handoff `0d3cd44`.

The reviewed provider-agnostic scheduled-task contract is borrowed byte-for-byte from candidate
`76d876ec43ee90074650c2eb11bc68329ea71575` in final reviewed handoff `e1ca272`; it is not
silently claimed as ancestry of the current branch. Its SHA-256 remains
`013fb6a834514ef15cd7ae0e738288446aa1a8e83bfe70f8c3dc46de27600e23`.

## Exact composition

The new root is
`C:\Users\conbr\AppData\Local\Temp\sol0-task28-private-shadow-2cef97899a371ba5e72d831e31194ce1`.
It is canonically disjoint from, does not alias, and is neither ancestor nor descendant of the
permanent `9097851` tombstone or retained `06848e2` root. Both retained roots were checked only
for root presence, canonical identity, disjointness, and root-level reparse state; their contents
and evidence were not read or changed.

- source archive:
  `389f70b675477ef15fe581903693395f93913ed7951a293d08cd138e18dd92a6`;
- fresh locked requirements:
  `c6bd5cbebb0d87841f66b506f034ed39b8b18c1ec8973e738f258f95afd6758a`;
- source manifest:
  `d36c6a64ef342ff0d4e88c370c794a2add46ef2f98fbdfb9dcabd6bd86f702b0`
  over 674 files;
- runtime manifest:
  `ce88fe3f371b0e2ca7938d8156d784c99094274d6c750fea35ca1a7700ec1ded`
  over 11,868 files;
- controller:
  `b5e56187978c155b1925925aeb840766a85241599ef21be103eead130cd08279`;
- launcher:
  `ccdff556d4880fa93762facd662ec797d00da2bea9746a1f463139d23446ff90`;
- corrected entrypoint / composition-bound operator:
  `96c77c083d665fe945cde5a31265d83276fe07778a1bb732bccee1b28f1acad2` /
  `ff80bb0fcbe48853394ac61bdbabbc92e11a3e65fb9fb303a2795e1175b9580f`;
- exact scratch-binding test:
  `e561388902da485f8e77ea4f2582938d2aa9897eb0efbd0f8d686ae398bcdd3c`;
- normalized-Markdown shared adapter / smoke operator / evidence validator:
  `10c5fd206aa926e883d00bc970a82c11273dae8fb57f7145b33cff6613e392eb` /
  `ef4f74cd39ae4a272a1164246e0ba58989ae849bea2102d922c0e76e3069c489` /
  `6220323896c4b6d9d5eae62bc4d69e2efcdba046d633df99afbcc0b6ba557c72`.

The root has 20 top-level entries. Root and evidence DACLs are protected,
current-SID-only FullControl with OI/CI and inheritance disabled; `icacls /verify` passed and the
composition contains zero reparse points. The evidence directory, exact task, scratch, and
diagnostic are absent/empty as required. Hub is `Running`; both health endpoints report `ok` on
schema 29. Pinned imports passed for `google-genai==2.14.0`, `pydantic==2.13.4`,
`httpx==0.28.1`, and `keyring==25.7.0`.

## Contract and verification

The controller invokes `Assert-ScheduledTaskAbsentBeforeRegistration` immediately before any
Task Scheduler mutation. The launcher invokes `Assert-CurrentLimitedInteractiveScheduledTask`
before scratch creation, private opt-in, wrapper invocation, credential read, source access, or
provider access. The corrected entrypoint retains the reviewed 15-key blocked record and its
fail-closed sentinel fallback. The committed reconciliation serializer correction `df9e6f2`
was replayed separately and proved future empty `wrapper_warnings` serialize as JSON `[]`;
reconciliation code is not part of or callable from this private-shadow composition.

Local focused verification passed `155` with one intentional live skip; the affected suite passed
`250` with one intentional live skip. Ruff passed, strict mypy passed on five source files, and
PowerShell parsing passed. Real Windows PowerShell 5.1 emitted:

- `SCHEDULED_TASK_RUN_CONTRACT_HARNESS_VERIFIED`;
- `PRIVATE_SHADOW_SCRATCH_BINDING_VERIFIED`;
- `PRIVATE_SHADOW_EVIDENCE_HARNESS_VERIFIED`;
- `PRIVATE_SHADOW_ENTRYPOINT_HARNESS_VERIFIED`;
- `RECONCILIATION_EVIDENCE_HARNESS_VERIFIED`.

The launcher was not executed and no task was registered or started. No credential, private
source, retained evidence, or provider API was accessed; no cleanup, reconciliation, or private
shadow ran; no prior authorization was used; and no production, Gate, merge, push, or deployment
action occurred.

The first independent reviews correctly **BLOCKED** composition commit `5bd9779` because the
byte-identical prior operator still expected obsolete scratch
`sol2-task28-private-live-0aab1c3`, while the fresh launcher created the new exact scratch. A
composition-owned test reproduced that mismatch before the operator edit. The minimum correction
changes only the composition operator's `SCRATCH_NAME` to
`sol2-task28-private-live-2cef97899a371ba5e72d831e31194ce1`. The corrected test binds the exact
launcher literal to the exact operator constant and exercises the real `_validate_scratch`
implementation on Windows after using the launcher's PowerShell directory/DACL initialization;
it removes the empty synthetic scratch and re-proves final scratch absence. Source and runtime
archives/manifests are unchanged.

Independent specification and quality/security re-reviews both **SHIP** corrected exact
composition `427d1dd573f9f0d07b1f9c7267a87f428e9792f3`, tree
`5d6b1eec45ff5d32a273094d89b1425d0967a6cc`, with no findings. Final read-only state recheck
matched controller `b5e5618`, launcher `ccdff55`, operator `ff80bb0`, and scratch test `e561388`;
the root still has 20 top-level entries and exact 674/11,868 source/runtime counts, evidence is
empty, task/scratch/diagnostic are absent, and Hub plus both schema-29 endpoints remain healthy.

## Exact fresh one-shot authorization required

No live authorization is present. The following is the precise scope required before any run:

> Connor authorizes exactly one Limited/Interactive invocation of Program Sol-0 Task 2.8
> private-shadow composition `427d1dd573f9f0d07b1f9c7267a87f428e9792f3`, tree
> `5d6b1eec45ff5d32a273094d89b1425d0967a6cc`, controller SHA-256
> `b5e56187978c155b1925925aeb840766a85241599ef21be103eead130cd08279`, launcher SHA-256
> `ccdff556d4880fa93762facd662ec797d00da2bea9746a1f463139d23446ff90`, source handoff
> `909785131b113fb5b2083b74bd2757c2fc0dbf4b`, source-manifest SHA-256
> `d36c6a64ef342ff0d4e88c370c794a2add46ef2f98fbdfb9dcabd6bd86f702b0`, and runtime-manifest
> SHA-256 `ce88fe3f371b0e2ca7938d8156d784c99094274d6c750fea35ca1a7700ec1ded`.
> It may register/start the one exact named task once with no retry; read the stored Gemini key
> exactly once; read only the exact private Lecture 13 normalized-Markdown and PDF inputs selected
> by the reviewed schema-29 preflight; perform only the reviewed prior-state lists, create one
> disposable store whose display name full-matches `task-2-8-private-[0-9a-f]{32}`, upload/import
> only that run's bounded inputs with file display names full-matching
> `task-2-8-private-[0-9a-f]{32}-[0-9]{3}`, perform the reviewed positive and wrong-scope queries,
> and delete/relist only the exact documents, files, and store created by that invocation for
> cleanup verification. It must retain only sanitized allowlisted evidence, never raw/private or
> provider content. It may not retry, reuse prior authorization, inspect or mutate either retained
> private root, operate on nonmatching provider resources, mutate production or Gate data, merge,
> push, or deploy. Authorization is consumed when controller registration/start is attempted.
> Acceptance requires provider cleanup and reconciliation empty; independent evidence-review
> SHIP; exact task/scratch/diagnostic absence; the new protected composition/evidence root retained
> pending separately authorized disposition; both prior roots unchanged; and Hub Running with both
> endpoints `ok` on schema 29. Any ambiguity fails closed without retry.
