# 1.3 handoff

## Status

Task 1.3 is implemented and locally verified on `sol1/source-trust`. This fix
round hardens the prior candidate without amending it. Sol-0 still owns
startup wiring and shared integration.

## Baseline and scope

- Authorized base: `eab21fdc8ff2ed562e147b7f10d8d8ec786f8668`.
- Authorized base tree: `94ebf606aaa9753247e8cb0cf2f0ff6ff48014dd`.
- Worktree: `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol1`.
- Branch: `sol1/source-trust`.
- The tracked manifest still describes Task 1.3 as blocked; the explicit Sol-0
  delegation in the task brief is the local readiness ruling. The manifest was
  not edited.
- No central startup, migration, ORM metadata, schema exporter/snapshot,
  contract proposal, dependency, route, settings, provider, Anki, primary
  checkout, integration, production, or private-data file was changed.

## TDD evidence

### RED

Before `repository.py` existed:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
python -m pytest tests/knowledge/test_repository.py \
tests/knowledge/test_repository_migration.py -q
```

Collection failed as expected with two `ModuleNotFoundError` errors for
`oms_hub.knowledge.repository`.

### GREEN

After the minimal direct-SQL implementation:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
python -m pytest tests/knowledge/test_repository.py \
tests/knowledge/test_repository_migration.py -q
```

Result: `18 passed`.

The tests cover fresh initialization, repeated initialization, exact source
trust tables/columns/constraints/indexes/foreign keys, preservation of an old
table and row, source and revision CRUD, canonical ID persistence, duplicate
source/hash revision reuse, deterministic evidence ordering, immutable evidence
retry, source/revision dependency failures, authority mismatch, the exact
normalized-text content digest, all-or-nothing evidence writes, retirement,
and the currently safe empty artifact-dependency result.

## Implementation

`KnowledgeRepository` uses `Database.engine.begin()` for every write and does
not extend SQLAlchemy's central ORM metadata. `initialize()` is additive and
idempotent:

- `knowledge_sources.id` is the logical source ID and stores its authority.
- `source_revisions.id` stores the canonical `source_revision_id`; its
  `source_document_id` references `knowledge_sources.id`, and
  `(source_document_id, file_sha256)` is unique.
- `evidence_units.id` stores `evidence_id`; `source_revision_id` references
  `source_revisions.id`.
- Evidence scope and locator indexes are exactly
  `(course_id, exam_id, lecture_id, authority_class)` and
  `(source_revision_id, locator_kind, locator_value)`.

`create_source()` and `create_revision()` accept the domain objects as well as
the task's keyword forms. Duplicate logical sources return the matching source
and reject authority changes. Duplicate source/hash revisions return the first
stored revision without changing its state. Missing source dependencies raise
`KeyError` before any revision write.

`put_evidence_units()` resolves authority through the revision and source,
requires every unit to belong to the requested revision, and compares
`content_sha256` to `sha256_text(normalized_text)`. All validation occurs before
inserts, so a late invalid unit cannot leave earlier units persisted. Exact
retries are idempotent; changed content under an existing evidence ID is
rejected. `list_evidence()` returns all audit rows in stable `created_at, id`
order. `retire_revision()` marks the revision retired and stamps previously
unstamped evidence retirement times, preserving the records for audit.

`dependent_artifact_ids()` returns `()` because no artifact-provenance table or
shared schema is available in Task 1.3. Task 5.2 owns that future contract; no
artifact columns or tables were invented, and no artifact-provenance proposal
is required for this safe empty behavior. Central startup activation is a
separate held Sol-0 proposal recorded below.

## Fix round 1

Terra quality review identified four Important findings: the mandated test
module collided with the existing default pytest import mode; check-then-insert
races exposed `IntegrityError` for concurrent source/revision/evidence creates;
retired revisions accepted new evidence; and callers could persist a
non-deterministic revision ID.

### RED

After adding the package marker and regression tests, before changing the
repository implementation:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
python -m pytest tests/knowledge/test_repository.py \
tests/knowledge/test_repository_migration.py -q
```

Result: `6 failed, 17 passed`. The failures were the deterministic-ID
mismatch, retired exact/new evidence boundary, and three real SQLite
concurrency races that surfaced the prior unhandled uniqueness conflicts.

### GREEN

The minimal fix adds `tests/knowledge/__init__.py`, catches only relevant
unique conflicts outside their failed transactions and re-reads in a fresh
transaction, retries an evidence batch once as a whole, rejects new evidence
for retired revisions while permitting only exact stored-row retries, and
derives/validates `source_revision_id` from the source/hash identity.

The focused command then passed with `23 passed`. The affected lane passed with
`123 passed`:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
python -m pytest tests/knowledge tests/contracts tests/providers -rA
```

The exact configured broad lane now passes after the authorized package marker
fix (exit 0; the known PDF adapter remained deselected and the
`windows_office` marker remained excluded):

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring python -m pytest -q \
-m "not windows_office" --deselect tests/document_processing/test_pdf_adapter.py
```

No importlib-mode workaround or global pytest configuration change was used.

## Fix round 2

Terra re-review found one remaining Important race: an evidence writer could
read `ready`, lose the retirement race, and then insert an active-looking row.

### RED

After adding deterministic concurrent retirement/evidence regressions and
before the transaction-boundary fix:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
python -m pytest tests/knowledge/test_repository.py \
tests/knowledge/test_repository_migration.py -q
```

Result: `2 failed, 23 passed`: the retirement-winning interleaving inserted
evidence after retirement, and the insertion-winning serialization hook had no
state claim to observe.

### GREEN

The minimal fix conditionally updates `source_revisions` with `SET state =
state` and `state <> retired` before evidence validation/insertion. This
transaction-local write claim serializes with retirement on the existing
SQLite/Postgres-capable SQLAlchemy engine: retirement winning rejects the
batch; insertion winning lets retirement wait and stamp the inserted evidence.
`retire_revision()` now claims the row with its state update before stamping,
avoiding a read-to-write upgrade race. Exact stored-row retries on an already
retired revision remain no-ops, while new rows remain rejected.

Focused result: `25 passed`.

Affected knowledge/contracts/providers result: `125 passed`.

The exact broad lane was not repeated in this round: the prior round's exact
default-import broad command passed after `tests/knowledge/__init__.py`, and
this round's changed production path is fully covered by the focused and
affected lanes. This classification does not claim a new broad execution.

## Primary-candidate verification

Focused and affected lanes:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
python -m pytest tests/knowledge tests/contracts tests/providers -rA
```

Result: `118 passed` before Fix Round 1; Fix Round 1 raised the affected lane
to `123 passed`, and Fix Round 2 raises it to `125 passed` as recorded above.

Targeted quality checks:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check \
src/oms_hub/knowledge/repository.py tests/knowledge/test_repository.py \
tests/knowledge/test_repository_migration.py
```

Result: all checks passed.

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src/oms_hub/knowledge/repository.py
PATH=/tmp/studyhub-task01-venv/bin:$PATH MYPYPATH=$PWD/src mypy \
tests/knowledge/test_repository.py tests/knowledge/test_repository_migration.py
```

Both commands passed with no issues. The fix-round full repository lint,
source mypy, frozen exporter/v1 proof, diff/private scans, and final clean
scope checks are recorded in the Task 1.3 report. No native Windows
acceptance is claimed.

## Safety and handoff

No push, merge, deployment, provider call, Anki mutation, production/private
data access, destructive cleanup, reset, stash, or state deletion was
performed. Sol-0 must wire `KnowledgeRepository.initialize()` at its approved
startup boundary and own any later artifact-provenance integration.

## Review provenance and held central activation

- Terra specification review (`task-1.3-terra-spec-review.md`): **APPROVED**
  for `7d1e405ed9d5e638776bbd2fbb421cf25201eed6` / tree
  `99a6a4473e1200a7de82e4809d02d4ccc4c455a5`, with no Critical, Important, or
  Minor findings. It accepted the direct-SQL additive schema, the local
  repository invariants, and the empty Task 5.2 dependency result; CP-0001
  remained proposed and unapplied.
- Primary implementation: `7d1e405ed9d5e638776bbd2fbb421cf25201eed6`
  (`feat: persist canonical sources revisions and evidence`).
- Initial Terra quality/reliability/security review
  (`task-1.3-terra-quality-review.md`): **NEEDS FIXES**, with four Important
  findings (default pytest collection collision, uniqueness races, retired
  revision evidence admission, and deterministic revision identity) and one
  non-blocking overload Minor.
- Fix round 1: `200d738944008e948ff3c4651a9f0660e44f6164` (`fix: harden source
  trust repository integrity`) addressed the four Important findings except
  the later-discovered retirement/evidence interleaving.
- Fix round 2: `562bfaaca0dc698e50f867f2f053fe78d0cab7b8` (`fix: serialize
  evidence writes with retirement`) added the portable transaction-local
  serialization claim and deterministic concurrent regressions.
- Final Terra quality re-review
  (`task-1.3-terra-quality-rereview-2.md`): **APPROVED** at
  `562bfaaca0dc698e50f867f2f053fe78d0cab7b8` / tree
  `bee5d664431691be22a99af879587bd7dcc0971a`; the remaining race was
  addressed, with no new Critical or Important breakage.
- Workstream Sol final-review finding: the prior handoff did not satisfy the
  requirement that every central registration need have a complete HELD
  proposal. Although it named Sol-0 startup wiring, it incorrectly implied no
  proposal was needed. This documentation-only correction adds
  `docs/implementation/shared-file-proposals/1.3-knowledge-repository-activation.md`
  with status **HELD / unapplied**, the exact `src/oms_hub/app.py:create_app`
  target, a minimal un-applied patch, default-off and failure-degraded startup
  behavior, integration/regression commands, rollback impact, and the Task 5.2
  artifact-provenance boundary. No central file or implementation behavior is
  changed by Task 1.3.
