# 0.1 handoff

- Base SHA: `749d729010aa75bf160f996d39e11edccb883a58`
- Head SHA: `c188fbf5dc6e56d23c6f0a260da06b778bbf714a` before this documentation-only correction; verify the checked-out containing commit with `git rev-parse HEAD`.
- Owner: Program Sol-0 (`sol0`); Task 0.1 title: Capture the immutable execution baseline.
- Files changed: baseline delivery created the frozen map/schema, reader/tests, baseline captures, human map, and this handoff. Fix Round 1 modifies this handoff and the human map and creates `artifacts/acceptance/grounded-learning/baseline/windows-ci-32254255685.json`.
- Contracts consumed: Task 0.1 brief/context, `.github/workflows/ci.yml`, `pyproject.toml`, and the approved planning package.
- Contracts produced: `artifacts/implementation/repo-map-v1.json` and `repo-map-v1.schema.json`; `scripts/frozen_paths.py` reads that static map only.
- Tests run: the five Gate 0 lanes, frozen-reader RED/GREEN evidence, map/path/log structural verification, and `git diff --check`.
- Test result: lint, types, JavaScript, reader, and structural checks pass; full Python and local Windows-equivalent suites retain their captured macOS `environment_only` PyMuPDF signal-11 baseline.
- Live-provider test run: no.
- Known limitations: native Windows was not available locally; the historical Windows snapshot failure was not reached locally because PyMuPDF/`fitz` crashed earlier.
- Data migration impact: none.
- Feature flag: none added or changed.
- Rollback: documentation/evidence only; revert the correction commit, then `c188fbf5dc6e56d23c6f0a260da06b778bbf714a` if the baseline delivery itself must be removed. No runtime or data rollback is required.
- Reviewer findings: Terra specification review is **CHANGES REQUIRED**; Fix Round 1 is committed pending Terra re-review. Terra quality review is pending.

## Task provenance and scope

- Task ID/title: `0.1` — Capture the immutable execution baseline.
- Program Sol owner: Sol-0; branch: `sol0/contracts-and-integration`.
- Worktree: `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol0`.
- Planning package SHA-256: `6b063d3effb378f6f1289fa64d0211d695db183ff3c749ba5d1cd95dd943294e`.
- Gate 0 base tree: `c20af8e2df4991852014ec9f1f66462e5363d71d`.
- Baseline metadata: `artifacts/acceptance/grounded-learning/baseline.json`.
- Path contract/schema: `artifacts/implementation/repo-map-v1.json` and `artifacts/implementation/repo-map-v1.schema.json`.
- Human path map: `docs/superpowers/plans/2026-08-20-study-hub-repo-map.md`.

Created by the baseline delivery (`c188fbf5dc6e56d23c6f0a260da06b778bbf714a`):

```text
artifacts/acceptance/grounded-learning/baseline.json
artifacts/acceptance/grounded-learning/baseline/
artifacts/acceptance/grounded-learning/frozen-paths-red.log
artifacts/acceptance/grounded-learning/frozen-paths-green.log
artifacts/implementation/repo-map-v1.json
artifacts/implementation/repo-map-v1.schema.json
docs/implementation/handoffs/0.1.md
docs/superpowers/plans/2026-08-20-study-hub-repo-map.md
scripts/frozen_paths.py
tests/scripts/test_frozen_paths.py
```

Modified by Fix Round 1: this handoff and the human path map. Created by Fix Round 1: `artifacts/acceptance/grounded-learning/baseline/windows-ci-32254255685.json`.

## Ruling

Ruling: The repository root supplied in the original kickoff prompt was stale. /Users/connor/Developer/oms-study-automation is authoritative because it exists and its configured GitHub remote matches connor-brooks98/oms-study-automation. The corrected path changes only filesystem location, not repository identity, implementation scope, or baseline requirements.

## Containing implementation identity

The baseline implementation is contained in `c188fbf5dc6e56d23c6f0a260da06b778bbf714a`, with tree `07605065c1a217403d3c215dcf84c6392b9b006f`. To reproduce those values exactly, check out that commit and run:

```bash
git rev-parse HEAD
git rev-parse 'HEAD^{tree}'
```

Fix Round 1 is a separate narrow correction commit; use the same two commands at its checked-out `HEAD` to identify that containing correction commit and tree.

## Exact commands and evidence

```text
Install: python -m pip install -e ".[dev,document-processing,pdf-inspection]"
Python lint: ruff check src tests scripts
Python types: mypy src
Python tests: PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring pytest -q -m "not windows_office"
JavaScript tests: node --test "tests/js/*.test.js"
Windows CI: $env:PYTEST_ADDOPTS='-m "not windows_office"'; .\.venv\Scripts\python.exe -m pytest tests\test_office_converter.py tests\document_processing tests\study_generation tests\v2 -q
Local macOS equivalent: PYTEST_ADDOPTS='-m "not windows_office"' python -m pytest tests/test_office_converter.py tests/document_processing tests/study_generation tests/v2 -q
Reader: python -m pytest tests/scripts/test_frozen_paths.py -q
Reader command: python scripts/frozen_paths.py quiz_page_files
Deployment interface (documented, not run): .\scripts\install-windows.ps1
```

| Lane | Result/classification | Evidence |
| --- | --- | --- |
| Python lint | PASS, exit 0 | `baseline/01-python-lint.json` |
| Python types | PASS, exit 0 | `baseline/02-python-types.json` |
| Python tests | signal 11 at 36%; `environment_only` | `baseline/03-python-tests.json` |
| JavaScript tests | PASS, 168 tests | `baseline/04-javascript-tests.json` |
| Local macOS Windows equivalent | signal 11 in PyMuPDF/`fitz`; `environment_only` | `baseline/05-windows-document-tests-local-equivalent.json` |
| Historical hosted Windows CI | failed assertion; `pre_existing_nonreproducible` in this macOS task | `baseline/windows-ci-32254255685.json` |

The frozen reader has RED evidence in `artifacts/acceptance/grounded-learning/frozen-paths-red.log` and GREEN evidence in `artifacts/acceptance/grounded-learning/frozen-paths-green.log`. It never searches the repository at runtime.

## Windows evidence and repair ruling

The committed redacted hosted-CI evidence is `artifacts/acceptance/grounded-learning/baseline/windows-ci-32254255685.json`. It records CI workflow/run/job identity, timestamps, base SHA, failed step/test/assertion/annotation, a safe minimal failed-log excerpt, and the exact read-only retrieval command/provenance.

No local Windows pass is claimed. The local macOS equivalent cannot establish the hosted assertion because it segfaults earlier in native PyMuPDF. The historical Windows failure is therefore a non-regression baseline for future Windows work. Repair commit: none. A repair would require a currently reproducible, understood, behavior-preserving, necessary failure plus a focused regression; those four gates are not met.

## Boundaries and next gate

- Public quiz grading is not learner quiz-attempt persistence; the current grading response is transient and no learner attempt record exists at Gate 0.
- No feature flag, schema, provider, prompt, migration, production/private data, or Anki state changed in Task 0.1 or Fix Round 1.
- Next allowed task: **Task 0.2 only**. Gate 1 remains blocked until Task 0.2 installs the approved design, plan, and manifest and the required reviews accept the corrected Task 0.1 record.
