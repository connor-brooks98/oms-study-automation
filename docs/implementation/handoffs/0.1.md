# 0.1 — Immutable execution baseline handoff

Ruling: The repository root supplied in the original kickoff prompt was stale. /Users/connor/Developer/oms-study-automation is authoritative because it exists and its configured GitHub remote matches connor-brooks98/oms-study-automation. The corrected path changes only filesystem location, not repository identity, implementation scope, or baseline requirements.

## Frozen input

- Base SHA: `749d729010aa75bf160f996d39e11edccb883a58`
- Base tree: `c20af8e2df4991852014ec9f1f66462e5363d71d`
- Baseline metadata: `artifacts/acceptance/grounded-learning/baseline.json`
- Path contract: `artifacts/implementation/repo-map-v1.json`
- Schema: `artifacts/implementation/repo-map-v1.schema.json`
- Human map and current learner-attempt boundary: `docs/superpowers/plans/2026-08-20-study-hub-repo-map.md`

The containing Task 0.1 implementation commit can be reproduced with
`git rev-parse HEAD`; this handoff deliberately does not claim a SHA that did
not exist when the frozen map was authored.

## Baseline lanes and classifications

| Lane | Result | Classification | Evidence |
| --- | --- | --- | --- |
| Python lint | PASS, 0 | Not applicable | `baseline/01-python-lint.json` |
| Python types | PASS, 0 | Not applicable | `baseline/02-python-types.json` |
| Python tests | signal 11 after progress through 36% | `environment_only` | `baseline/03-python-tests.json` |
| JavaScript tests | PASS, 168 tests | Not applicable | `baseline/04-javascript-tests.json` |
| Windows document local equivalent on macOS | signal 11 in `fitz`/PyMuPDF while executing `tests/document_processing/test_pdf_adapter.py::test_scanned_pdf_returns_ocr_blocker_instead_of_empty_success` | `environment_only` | `baseline/05-windows-document-tests-local-equivalent.json` |

All evidence files record the command, UTC start/end, duration, stdout,
stderr, and exit status. The local Python failures are reproducible on this
macOS ARM64 / CPython 3.12.13 environment and terminate in PyMuPDF native code;
they are not safe evidence for a product-code repair.

Historical GitHub Windows evidence remains a separate non-regression baseline:
workflow `32254255685`, job `96072169375`, failed
`tests/document_processing/test_snapshots.py::test_url_snapshot_hard_deadline_stops_hanging_stream_read_without_writing`
at `assert completed.wait(1)`. It is `pre_existing_nonreproducible` in this
macOS task because native Windows execution was unavailable and the local
equivalent crashed before that test. Future Windows work must show no additional
failures and must retest this exact assertion on Windows. No repair was made:
the reproducible, understood, behavior-preserving, necessary repair gates were
not all satisfied.

## Frozen reader

`scripts/frozen_paths.py PATH_KEY` reads only the committed map and never
searches the repository. Its RED proof is
`artifacts/acceptance/grounded-learning/frozen-paths-red.log`: all four tests
failed because the script did not yet exist. Green proof is recorded in the
Task 0.1 report after the implementation test run. The tests cover a known key,
an unknown key, an empty list, and a path containing spaces.

## Next-task constraints

- Do not treat the public quiz grading response as learner-attempt persistence.
- Use the frozen map rather than rediscovering paths during parallel work.
- Do not modify current CI or document-processing code unless a separate,
  focused regression proves a repair meets every baseline-repair gate.
- Windows document processing is not locally passed on macOS; native Windows
  acceptance remains required before final release.
