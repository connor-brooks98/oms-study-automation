# Task 2.8 schema-29 source-trust compatibility handoff

## Status

**PREREQUISITE MERGE-READY / PRIVATE SHADOW NOT RUN / GATE_NOT_OPEN.** This Program
Sol-0 change provides only the reviewed read-only schema-29 projection required before
Sol-2 may resume its separately authorized private shadow preflight.

## Commit

- Branch: `codex/sol0-schema29-source-trust-compat`.
- Base integration SHA: `8fa42f461ec63dd7b7f52d06754d21a18f0068b6`.
- Initial RED: `0ff01ece6b021e2db3e91b6e8872a5f191780a81`, tree
  `ee2cb1064c21aa06e6e64a3485107ac895d4f298`.
- Initial GREEN: `d1f88b52873dbd381a8c305d8ad60e5a548d5a8d`, tree
  `0424e80d6973103959ee5b2ad2dc558681f9eba1`.
- Security-finding RED: `62fe966c499c88a95237b7fa8e35e26f1c7884de`, tree
  `610ab55ec21efc645b95e36aa14c8c2c9175f13b`.
- Final GREEN: `03539d0a21d82f5beaf9f695ef8c2aaa868bfc24`, tree
  `a5e07e03fd2d7182c9cb45593cc027da9095eec5`.
- Handoff commit: `SELF` (resolve from the commit containing this file).
- Ancestry: the base integration SHA is an ancestor of final GREEN.
- Final worktree status: clean after the containing handoff commit; Program Sol-0 reports
  the resolved SHA and status externally without amending this self-referential file.

## Deliverable

- `project_schema29_index_input(...) -> IndexInputView` accepts only exact schema 29.
- Reuses Task 1.6's reviewed current-slide eligibility, deterministic identity, scope,
  parser, and normalized-evidence preparation through a read-only preparer with no Source
  Trust repository/write member.
- Reuses Task 1.7's reviewed `KnowledgeService.resolve_index_input` validation and exact
  CP-0002 v2 frozen view.
- Projects `legacy-study-revision:<id>`, deterministic `sr_` identity, `legacy_slides`,
  `ready`, `course_material`, bounded course/exam/lecture scope, verified PPTX/PDF,
  deterministic Markdown/evidence, and sorted asset metadata.
- The compatibility projection exposes no database handle and never initializes, inserts,
  updates, deletes, or migrates Source Trust or legacy records.
- Normalized Markdown is the sole content artifact; it and its containing directories are
  forced beneath an explicit existing canonical scratch root. Descendant symlinks, Windows
  reparse points, path escapes, and a linked/reparse `normalized.md` fail closed before the
  write.

## Files

- Created: `src/oms_hub/source_trust_schema29.py`,
  `tests/test_source_trust_schema29_compat.py`, and this handoff.
- Intentionally unchanged: Sol-2 indexing/smoke code, source-trust schema/repository/service,
  migrations, production database/files, app/startup, provider settings, credentials, plan,
  manifest, acceptance artifacts, CI, and Gate 2B state.

## TDD evidence

- Initial RED command:
  `python -m pytest tests/test_source_trust_schema29_compat.py -q`.
  Result: collection stopped at the intended missing
  `oms_hub.source_trust_schema29` module before production code existed.
- Initial GREEN: the same target passed `2 passed` after the minimum projection.
- Independent quality/security review found that a descendant `index-inputs` symlink could
  redirect Markdown outside scratch and that the initial implementation falsified a
  `KnowledgeRepository` type with `None`.
- Review RED command:
  `python -m pytest tests/test_source_trust_schema29_compat.py::test_projection_rejects_descendant_symlink_before_materializing -q`.
  Result: `1 failed` because no exception was raised and the escape was possible.
- Final GREEN: the complete focused target passed `3 passed`; the outside directory remained
  empty, and the false repository contract was removed.

## Verification

- Focused plus Task 1.6/1.7/consumer lane:
  `python -m pytest tests/test_source_trust_schema29_compat.py tests/knowledge/test_backfill.py tests/knowledge/test_service.py tests/indexing/test_source_input_contract.py -q`
  — `50 passed`.
- Affected knowledge/contracts/providers/indexing lane with the inherited aggregate
  `ResourceWarning` suppression — exit `0`.
- Safe broad Python with `ResourceWarning` suppression, non-Windows marker, and the documented
  macOS PDF-adapter deselection — exit `0`, one expected skip.
- `ruff check src tests scripts` — passed.
- `mypy src` — passed for 230 source files.
- `git diff --check` — passed.
- Live/private/provider/credential/native-Windows tests — not run.

## Independent review

- Terra specification: **APPROVED** at exact `03539d0`; no specification or ownership
  findings. The reviewer confirmed CP-0002 identities/scope/authority/evidence/artifact/view
  semantics, Sol-0-only scope, valid RED/GREEN, and sufficiency for Sol-2's separate retry.
- Terra quality/security: initial **CHANGES REQUIRED** at `d1f88b5` for descendant scratch
  redirection and the false repository contract; final **APPROVED** at `03539d0` after
  RED/GREEN correction. No remaining quality/security blockers.
- Residual directory-swap TOCTOU requires a concurrently hostile actor controlling the already
  protected/trusted scratch root; the quality/security reviewer classified it non-blocking for
  this local preflight adapter.

## Data, rollback, and next boundary

- Production/private data changes: none.
- Database migration/backfill: none.
- Provider resources/credentials: none accessed or created.
- Rollback: revert this handoff and both GREEN/RED pairs in reverse order. Git history retains
  the TDD and review evidence.
- Next owner/action: after Program Sol-0 integrates this prerequisite and Sol-2 lands any
  necessary reviewed consumer change, Sol-2 may resume the already-authorized private-shadow
  preflight within its existing scope using the public projection and protected scratch root.
  Only actions beyond that existing authorization require new authority. Gate 2B remains closed.
