# Task 2.8 Sol-2 private-root 9097851 evidence disposition

Status: **OWNER DECISION REVIEWED SHIP / ABSENCE ACCEPTED ONLY AS TERMINAL
TOMBSTONE / RESTORATION NOT REQUIRED / GATE_NOT_OPEN**

Program Sol-0 handoff `dcc603c1077cbb6dd01a0477312a924692903b4c` stopped because the
protected private-shadow `9097851` root exists but its exact `evidence` directory is absent.
Baseline handoff `896ded575f8ec44256152592b84ad7774e482d45` recorded the directory
present and empty. Sol-0 did not read, remove, recreate, or otherwise mutate either protected
private root or its evidence and correctly refused to stage cleanup.

## Exact read-only current state

Sol-2 inspected only allowlisted filesystem hashes and metadata. It did not enumerate or read
private or retained evidence content.

- protected root: present; exact current-SID-only FullControl OI/CI ACL; zero reparse points;
- evidence directory: absent; evidence file count therefore not applicable;
- source/runtime: 674 / 11,868 files; root top-level inventory count 19;
- source manifest:
  `d36c6a64ef342ff0d4e88c370c794a2add46ef2f98fbdfb9dcabd6bd86f702b0`;
- runtime manifest:
  `ad8e00b852d32c3b1216452e25e62160a68fb07745f3589321b20fec3ccfc5a7`;
- controller: `5a955d65feb3adf03759bd62c8e2f842b2e81a27abfc5c9e10b8912c72796587`;
- launcher: `0795af225426707b9a49454b19538b6b0eb420a9f05ab74280d1d541fd87fffa`;
- corrected entrypoint:
  `96c77c083d665fe945cde5a31265d83276fe07778a1bb732bccee1b28f1acad2`;
- exact 9097851 task, diagnostic, and shared scratch: absent;
- Hub: `Running`; ports 8765 and 8788 both `status=ok`, `schema_version=29`.

The machine-readable record is
`artifacts/acceptance/grounded-learning/task-2.8-sol2-private-root-9097851-disposition.json`.

## Owner invariant decision

**Approve the absent evidence directory only as a permanent non-executable tombstone state.
Restoration is not required and is not authorized by this disposition.**

Absence is not a valid executable or post-run evidence invariant. The reviewed controller and
launcher both call `Assert-ProtectedDirectory` for the root and evidence directory before task
registration or any credential, private-source, or provider boundary. The launcher additionally
requires the directory to be empty. The durable wrapper requires it as the existing parent for
safe result and status files and never removes it. A root with no evidence directory therefore
fails closed before the protected live boundary.

The 9097851 controller and launcher were never executed, and baseline `896ded5` recorded the
directory empty. There is no durable 9097851 result to restore. Recreating an empty directory
would create new ACL/timestamp/filesystem provenance without restoring the reviewed composition.
The safer and smaller disposition is to keep the root unchanged, forbid all future reuse, and
bind its current absent-evidence state before and after cleanup of the separate reconciliation
root.

This decision does not establish private cleanup or acceptance. Provider state remains
`unknown`, and Gate 2B remains closed.

## Exact downstream Sol-0 contract

Program Sol-0 must add fresh committed RED/GREEN before rebuilding the cleanup composition:

1. RED must model the exact root-present/evidence-absent state and fail under the current
   present-empty invariant.
2. GREEN may accept it only for the exact 9097851 tombstone and only while cleaning the separate
   e71eace reconciliation task/root.
3. Before and after that cleanup it must require the exact five hashes above, 674/11,868 file
   counts, 19 top-level entries, exact root ACL, zero reparse points, 9097851 task absence,
   diagnostic/scratch absence, evidence-path absence, Hub `Running`, and both schema-29 health
   endpoints `ok`.
4. It must fail closed if the 9097851 root disappears, the evidence path reappears, any bound
   hash/count/inventory/ACL changes, any task or residue appears, or Hub health changes.
5. It must prove the cleanup never reads, creates, writes, removes, or otherwise mutates any part
   of the 9097851 root. Only the separately named reconciliation task/root may be removed.
6. Before any mutation, it must resolve the existing reconciliation cleanup root and retained
   9097851 root through reparse-safe canonical paths and reject equality, ancestor/descendant
   overlap, or any alias. Reparse checks must cover each approved-base-to-target ancestor and
   every enumerated deletion descendant, not only the starting directories.
7. Before any mutation, it must bind the exact reconciliation task path, principal, executable,
   arguments, and working directory. Every path-bearing task value and every deletion target must
   resolve inside the canonical reconciliation root and remain disjoint from the 9097851 root.
   A mismatch must retain both the reconciliation task/root and the private tombstone.

Fresh exact-composition specification and quality/security **SHIP** remain required before any
cleanup-only authorization. The 9097851 root may be removed only under a separate explicit
authorization; it must never be reused. Any private retry requires a new protected root, a fresh
reviewed composition, and separate authorization.

No key, provider API/inventory, private source, retained evidence content, cleanup,
reconciliation, task mutation, protected-root mutation, production/Gate mutation, merge, push,
or deployment occurred. Gate 2B remains closed.

## Independent review

- Specification: **SHIP** exact candidate
  `26fa70a47f6b6d1cb32683ec530aa4e23e876fc7`, tree
  `542281913bd263d6fca32744f04eac029d9d6dfe`.
- Quality/security initially **BLOCKED** the first candidate because it did not require
  canonical/reparse-safe disjointness and exact scheduled-task action binding before deletion.
  Correction `26fa70a` added those requirements; exact-candidate re-review returned **SHIP** with
  no residual finding.

Both reviews were read-only and did not access the NUC, retained evidence, private source,
credential, or provider. They performed no filesystem, task, production, or Gate mutation.
