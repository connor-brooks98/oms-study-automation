# Task 2.8 Program Sol-0 authorized reconciliation result

Status: **ONE SHOT CONSUMED / SCHEMA-2 EMPTY RESULT / CLEANUP BLOCKED /
EXACT TASK AND ROOT RETAINED / NO RETRY / GATE_NOT_OPEN**

Connor authorized exactly one invocation of reviewed composition
`2a799fb098ed790029b358385658b2be13207d58`, final composition handoff
`91eeda434b72b6af423ed71a144d293db2693088`, and source
`e71eacec05c085409aa41ce4f06b1809630e9124`. The preflight replayed the exact
controller, launcher, source-manifest, and runtime-manifest hashes; 682 source and 11,868 runtime
files; protected ACLs; zero reparse points; empty reconciliation outputs; exact task absence;
both preserved private roots; and Hub `Running` with both endpoints healthy on schema 29.

Exactly one Limited/Interactive task was registered and started. No retry occurred. The task
completed at `2026-08-28T23:52:37Z` with `LastTaskResult=0`. The reviewed code path read the
stored key once and performed the authorized global inventories. The sanitized schema-2 result
reports `passed`, ordered states `inventory_complete`, `deletes_attempted`, and
`reconciliation_empty`, with every inspected, matched, delete-attempt, remaining, and relist
count equal to zero. No provider deletion was required. The safe status reports exit zero,
usable evidence, provider cleanup complete, operator artifacts deleted, no raw diagnostic
retained, and Hub health `ok` before and after.

Exact sanitized evidence hashes:

- safe result: `bd26cc4b7b3eeb34e875caaa043b4118bea375bc4473a3dab34dd65dffdf5a7d`;
- safe status: `e5ec9fcf1fb790b62a18939d78d350ea1572a903e02fae6e4e916ad5f4661c3f`.

## Fail-closed cleanup blocker

The immutable safe-result envelope serialized empty `wrapper_warnings` as `{}`, a zero-property
`System.Management.Automation.PSCustomObject`, rather than the required JSON array `[]`. Under
PowerShell array coercion that object has count one. The exact controller therefore did not
claim acceptance or unregister the task; its cleanup mode uses the same predicate and cannot
remove the root.

The exact retained task is `OMS Sol0 Task28 Reconciliation e71eace`, path `\`, state `Ready`,
enabled, with one action invoking the reviewed launcher through Windows PowerShell as
`Limited` / `Interactive`; `ExecutionTimeLimit=PT0S`. The protected composition root and both
sanitized evidence files remain. No raw diagnostic remains.

Independent specification and quality/security reviews both **BLOCK cleanup acceptance**. They
agree the nested schema-2 operator record is internally consistent, sanitized, and proves an
empty exact reconciliation, but overriding the envelope type mismatch would weaken the reviewed
fail-closed controller contract.

Both private roots remain present. The 9097851 private composition still has empty evidence; the
prior 06848e2 root still has one retained sanitized evidence file. Neither retained evidence
content was read or mutated. Hub remains `Running`; ports 8765 and 8788 remain `ok` on schema
29. No private source, provider creation/upload/import/query, private retry, production/Gate
mutation, merge, push, or deployment occurred.

## Next required action

No provider retry or additional key read is justified or authorized. Program Sol-0 must first
commit offline PowerShell 5.1 RED/GREEN proving empty wrapper warnings serialize as `[]`, then
build and independently review a hash-bound cleanup-only disposition that accepts only this
exact immutable zero-property legacy `wrapper_warnings` object without rewriting either evidence
file. That disposition may unregister only the exact retained reconciliation task and remove
only the exact reconciliation root after rechecking both evidence hashes, task action/principal,
Hub/schema-29 health, and both untouched private roots. Fresh cleanup-only authorization is
required before executing it. Gate 2B and any private retry remain closed.
