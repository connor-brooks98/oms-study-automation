# Task 2.8 Program Sol-0 reconciliation cleanup disposition

Status: **OFFLINE CONTRACT SHIP / EXACT COMPOSITION BLOCKED / CLEANUP UNSTAGED /
NOT EXECUTED / AUTHORIZATION NOT CONSUMED / NO RETRY / GATE_NOT_OPEN**

The offline PowerShell 5.1 correction and cleanup-only disposition are complete at reviewed
candidate `2a0a0fc9f6548a980d5c7ff7234ba501a270e524`, tree
`ba288fc37bd91d6f51eff6d51d6eaebd5aa1ba03`. The change does not perform reconciliation,
read a key, contact a provider, read private evidence, or alter either immutable legacy evidence
file.

## Committed TDD ancestry

- serializer RED `7b463fb510ad32e79a2654adb7b1ce09c6f632b5` reproduced the PowerShell 5.1 empty-object defect;
- serializer GREEN `df9e6f256e465f914488a3e5dec4751e244aabc6` makes future empty
  `wrapper_warnings` serialize as JSON `[]`;
- cleanup RED `c08325c9b73b4a700712c3729ffe3fc21151b895` specified the exact legacy-only disposition;
- initial cleanup GREEN `8e1dc6d7f18cdb9c76de6fb0bc5fb8ef888310f4` implemented it;
- review RED `235eaf689cf4c50b6940e8f4812a41807f79b397` proved that an unexpected root file or a
  starting-root reparse point must retain the task and root;
- review GREEN `2a0a0fc9f6548a980d5c7ff7234ba501a270e524` binds the complete root inventory and checks
  the starting root plus descendants for reparse points.

The intervening test-only fixture corrections are `ad26bdd`, `bca6dc6`, `f503802`, `1dff765`,
`a0c88d0`, and `b5b5afa`. They change only PowerShell mock scope, temporary-directory setup,
mock signatures, and exact fixture-manifest construction; they do not relax acceptance behavior.

## Exact hashes and verification

- immutable safe result: `bd26cc4b7b3eeb34e875caaa043b4118bea375bc4473a3dab34dd65dffdf5a7d`;
- immutable safe status: `e5ec9fcf1fb790b62a18939d78d350ea1572a903e02fae6e4e916ad5f4661c3f`;
- corrected serializer: `a0f0800e51885a6c8a7abe3c655639a57505192a6bee75932d8bdc7e5c1c8254`;
- cleanup script: `61214964551ab86572712284dbd471a672232b3d4638faf14e656a7450aeee01`;
- cleanup PowerShell harness: `6e2249042ecd7477dcf60fbca1a9e7f420c375615f9f2a9a558d6791b9f6e8b8`;
- cleanup Python test: `8defb91bd0feb666cf299f0f523075e9f3b23f761e96b0b88d279efa7a966a36`;
- 12,560-file cleanup root manifest: `ea671e594d9494aec7be240e322baa382dc954bab8f1de9ca196c24052887184`.

Focused tests passed 29. The affected suite passed 332 with one intentional live skip. Ruff,
strict mypy, diff hygiene, and boundary scans passed. Real Windows PowerShell 5.1 emitted both
`RECONCILIATION_EVIDENCE_HARNESS_VERIFIED` and
`TASK28_RECONCILIATION_CLEANUP_HARNESS_VERIFIED`.

Independent specification and quality/security reviews both **SHIP** the exact offline candidate.
They both **BLOCK** an executable cleanup composition on the current protected state.

## Exact composition blocker

Read-only preflight stopped before staging the cleanup script. The reconciliation root and exact
two evidence hashes remain valid; the exact task exists once in `Ready` state; no diagnostic is
present; Hub remains `Running`; and ports 8765 and 8788 report `ok` on schema 29. The prior
`06848e2` private root and its evidence directory remain, with one retained evidence file.

However, the protected `9097851` private root exists while its exact `evidence` directory is now
absent. Baseline handoff `896ded575f8ec44256152592b84ad7774e482d45` recorded that directory
as present and empty. Nothing in this cleanup turn read, removed, recreated, or otherwise mutated
either private root or its evidence. Recreating the directory would be a protected-root mutation;
accepting its absence would change the reviewed invariant. Neither is authorized by this task.

The exact owner is the Sol-2 private-shadow evidence owner. That owner must provide a durable,
independently reviewed disposition of the missing `9097851` evidence directory without reading or
mutating retained evidence. If absence is approved as the new invariant, Sol-0 must commit fresh
offline RED/GREEN binding that state, then rebuild and independently review a new cleanup
composition. If absence was unintended, restoration requires separate explicit authorization.

## Authorization boundary

There is no valid cleanup-only authorization to issue for the current state. The following is a
**non-executable future template** and becomes usable only after the owner disposition, fresh exact
composition hashes, and two exact-composition `SHIP` verdicts replace every placeholder:

> Connor authorizes exactly one cleanup-only invocation of reviewed Task 2.8 composition
> `<composition-commit>`, cleanup script SHA-256 `<cleanup-script-sha256>`, and root-manifest
> SHA-256 `<root-manifest-sha256>` against immutable safe result
> `bd26cc4b7b3eeb34e875caaa043b4118bea375bc4473a3dab34dd65dffdf5a7d` and safe status
> `e5ec9fcf1fb790b62a18939d78d350ea1572a903e02fae6e4e916ad5f4661c3f`. It may unregister only
> task `OMS Sol0 Task28 Reconciliation e71eace` at path `\` and remove only root
> `C:\Users\conbr\AppData\Local\Temp\sol0-task28-reconciliation-e71eace`, after every reviewed
> task, evidence, diagnostic, Hub/schema-29, ACL, reparse, manifest, and untouched-private-root
> check passes. On ambiguity it must retain the task and root. It may not read a key, access
> provider APIs or inventory, access or mutate private source/evidence, retry reconciliation or
> private shadow, create/upload/import/query, mutate production or Gate data, merge, push, or
> deploy. This authorization is consumed when cleanup execution is attempted and authorizes no
> provider or private retry.

No cleanup was staged or executed, and no authorization was consumed.
