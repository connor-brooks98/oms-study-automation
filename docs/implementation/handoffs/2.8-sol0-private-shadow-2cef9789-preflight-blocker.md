# Task 2.8 Program Sol-0 private-shadow preflight blocker

Status: **BLOCKED BEFORE CONTROLLER / AUTHORIZATION NOT CONSUMED / NO RETRY /
NO PRIVATE OR PROVIDER BOUNDARY CROSSED**

Connor authorized one invocation of reviewed composition
`427d1dd573f9f0d07b1f9c7267a87f428e9792f3`, tree
`5d6b1eec45ff5d32a273094d89b1425d0967a6cc`, controller SHA-256
`b5e56187978c155b1925925aeb840766a85241599ef21be103eead130cd08279`, and launcher SHA-256
`ccdff556d4880fa93762facd662ec797d00da2bea9746a1f463139d23446ff90`.

The mandatory read-only preflight failed closed before controller invocation. The reviewed
composition requires an empty protected `evidence` directory and 20 top-level entries. The exact
path
`C:\Users\conbr\AppData\Local\Temp\sol0-task28-private-shadow-2cef97899a371ba5e72d831e31194ce1\evidence`
was absent, and the root therefore contained 19 top-level entries. The controller and launcher
still matched their authorized hashes.

A single read-only blocker snapshot confirmed:

- the exact private-shadow task remained absent;
- exact scratch and diagnostic paths remained absent;
- Hub remained `Running`;
- both health endpoints remained `ok` on schema 29.

The snapshot enumerated only the new composition root's non-private top-level names. It did not
access either retained prior private root. The temporary preflight helper was removed afterward.

The controller was never invoked, so registration/start was never attempted and the authorization
was not consumed under its stated consumption rule. No task was registered or started; no stored
key, private source, private evidence, or provider API was accessed; no provider, production,
Gate, retained-root, merge, push, or deployment mutation occurred; and no retry was attempted.

Program Sol-0 must not recreate the missing directory ad hoc under the reviewed authorization.
The minimum next action is to establish a fresh protected composition state, re-prove its exact
ACL/reparse/manifest/task/output/Hub predicates, obtain fresh independent specification and
quality/security reviews, and request fresh authorization naming that exact reviewed state before
any later controller invocation.
