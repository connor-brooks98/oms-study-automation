# 1.2 handoff

## Status

Task 1.2 is implemented and locally verified on `sol1/source-trust`.

## Commit

- Branch: `sol1/source-trust`
- Commit: symbolic containing the Task 1.2 implementation; resolve with
  `git rev-parse HEAD` after commit.
- Tree: symbolic containing the Task 1.2 implementation; resolve with
  `git rev-parse 'HEAD^{tree}'` after commit.
- Base: `9c2ba724e02e0b6cbcd8ca1151c370deee5f18eb` (tree
  `36ef4ef881853e957341ffe0ba1ab64868fddf65`).
- Commit message: `feat: define source trust domain models`.

## Deliverable

Produced in `oms_hub.knowledge.models`:

- `KnowledgeSource(source_document_id, authority_class)`.
- `SourceRevision(source_document_id, source_revision_id, file_sha256, state)`.
- `EvidenceLocator(kind, value)`.
- `EvidenceUnit` with the specified identity, authority, course/exam/lecture
  scope, locator, text, optional image, content hash, source priority, and
  creation/retirement timestamps.
- `SourceRevisionState` and `EvidenceLocatorKind` with every required value.

All models are frozen, slotted stdlib dataclasses. `EvidenceUnit` requires
`course_id` for `course_material`; `supports_medical_claims` is derived and is
true only for course material or published journal. Defaults are
`image_asset_id=None`, `source_priority=0`, UTC ISO `created_at`, and
`retired_at=None`. No speculative locator, ID, or hash validation was added.
`SourceRevision.revision_id` is a read-only compatibility property that returns
the canonical `source_revision_id`; it is not a second persisted field.

## TDD and verification

- RED:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge/test_models.py -q`
  — expected collection failure: `ModuleNotFoundError: No module named
  'oms_hub.knowledge.models'` before implementation.
- Focused GREEN:
  the same command — `12 passed`.
- Knowledge/contracts/providers:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge tests/contracts tests/providers -q`
  — `68 passed`.
- Ruff:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check src tests scripts` — all
  checks passed.
- Source typing:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src` — success, no issues in
  179 source files.
- Task-owned test typing:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH MYPYPATH=$PWD/src mypy tests/knowledge/test_models.py`
  — success, no issues in 1 source file.
- Safe broad Python lane:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring python -m pytest -q -m "not windows_office" --deselect tests/document_processing/test_pdf_adapter.py`
  — exit 0 and 100% completion. The known crashing PDF adapter was excluded;
  native Windows acceptance was not claimed.
- `git diff --check` passed. The frozen
  `scripts/export_grounded_contract_schemas.py` and
  `schemas/knowledge-v1.json` are unchanged, and existing schema snapshot
  tests pass in the contracts lane.
- Changed-file privacy/scope scan found no private paths, credentials, Anki
  collections, URLs, DOI/PMID/PubMed identifiers, or personal identifiers.

## Schema and field ruling

Controller ruling was followed: no exporter or v1 snapshot change was made.
The minimum SourceRevision fields are the four fields supplied by the Task 0.5
builder and needed by the Task 1.3 repository consumer. The minimum
KnowledgeSource is logical source identity plus authority class; evidence scope
stays on EvidenceUnit. This is reversible through the unapplied additive
proposal `docs/implementation/contract-proposals/CP-0001-knowledge-domain-models.md`.
It proposes `knowledge-v2.json`, lists producers/consumers, preserves v1
backward compatibility, and requires Sol-0 plus every consuming-owner review
before any exporter or activation change. It does not claim approval or
activation.

## Files and safety

- Created: `src/oms_hub/knowledge/models.py`,
  `tests/knowledge/test_models.py`, this handoff, and the unapplied CP-0001
  proposal.
- Intentionally not modified: provider contracts, the schema exporter, all
  schema snapshots, IDs, builders, plans/design/ledger, routes/UI, databases,
  dependencies, CI, Task 1.3 files, Anki, private data, production, and the
  primary checkout.
- No provider calls, live/native acceptance, deployment, push, merge, tag,
  cleanup, reset, stash, or destructive action was performed.

## Limitations and rollback

The proposal is unapplied; v1 remains the only generated schema. Native Windows
acceptance and live provider checks remain outside this task. Rollback is
reverting the Task 1.2 commit, with no persisted-data migration to undo.

## Fix Round 1

Quality review findings addressed in a separate fix commit; `04742bd` and
`27f21f3` were not amended.

- RED:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge/test_models.py -q`
  — `19 failed, 19 passed`; failures covered direct enum strings/unknowns,
  blank or non-string course scope, and malformed/naive/non-UTC timestamps in
  direct and TypeAdapter construction.
- GREEN: the same command — `38 passed`.
- Enum boundaries now coerce every direct enum field through its declared
  `StrEnum` with `object.__setattr__`; unknown values raise `ValueError`.
- Course material now rejects non-string or whitespace-only `course_id`.
- `created_at` and non-null `retired_at` now require parseable,
  timezone-aware UTC ISO-8601 values; `Z` and `+00:00` are accepted and the
  supplied spelling is preserved. `retired_at=None` remains valid.
- Affected lane:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge tests/contracts tests/providers -q`
  — `94 passed`.
- Quality after the fix: `ruff check src tests scripts` passed; `mypy src`
  succeeded with no issues in 179 source files; task-owned mypy succeeded with
  no issues in 1 source file; `git diff --check` passed.
- Frozen proof:
  `git diff --exit-code -- scripts/export_grounded_contract_schemas.py schemas/knowledge-v1.json`
  passed. The v1 exporter and snapshot remain unchanged.
- The safe broad Python lane was not repeated in this round; it already passed
  before the fix, and the amended code is fully covered by the focused and
  affected lanes above. No native Windows or live provider acceptance is
  claimed.
- CP-0001 now states the literal v2 course-material `if`/`then` JSON Schema,
  exact EvidenceUnit required keys, defaulted/omittable keys and ordinary
  default emission, UTC date-time format/pattern constraints, and exclusion of
  `supports_medical_claims` and `revision_id`. It remains proposed and
  unapplied.

## Fix Round 2

Four consumer-compatibility blockers were addressed in a separate fix commit;
no prior commit was amended.

- RED:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge/test_models.py -q`
  — `6 failed, 38 passed`; direct and TypeAdapter cases proved that `+00`,
  `+0000`, and `-00:00` were still accepted before the suffix gate.
- GREEN: the same command — `44 passed`.
- Runtime timestamp parsing now accepts exactly `Z` or `+00:00` before
  `datetime.fromisoformat`; the valid spellings remain preserved.
- CP-0001 now requires repository/import authority equality between every
  EvidenceUnit and its referenced KnowledgeSource, with a separate logical
  source/revision for differing authority and an exact rejection test.
- CP-0001 defines all six revision-state transitions, READY-only eligibility
  for new indexing/retrieval/citation/question flows, stale/retired audit
  preservation, and exact transition/consumer-exclusion tests.
- CP-0001 defines lowercase 64-hex `content_sha256` as
  `sha256_text(normalized_text)`, the direct EvidenceUnit-to-EvidenceRef
  mapping and `sha256:` checksum, an exact mapping round-trip test, and a
  separate future excerpt-integrity requirement.
- Affected lane:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge tests/contracts tests/providers -q`
  — `100 passed`.
- Quality: `ruff check src tests scripts` passed; `mypy src` succeeded with no
  issues in 179 source files; task-owned mypy succeeded with no issues in 1
  source file; `git diff --check` passed; frozen exporter/v1 diff proof passed.
- Refined scope/privacy/secret scan was silent. The safe broad Python lane was
  not repeated; Fix Round 1 documented its prior exit-0 result, and this
  round's amended runtime code is covered by the focused and affected lanes.
- Non-blocking activation recommendations recorded in CP-0001: use an already
  available real JSON-Schema instance validator at activation and have Sol-2
  review `source_priority` direction before provider ranking. No dependency was
  added.

## Review provenance

- Implementation commit: `04742bd`; verification-count correction:
  `27f21f3`. The handoff retains symbolic self-reference for the containing
  candidate rather than claiming a commit contains its own SHA.
- Terra specification reviewer `/root/task_1_2_terra_spec`: **APPROVED** at
  `27f21f3`, with no findings.
- Terra quality reviewer `/root/task_1_2_terra_quality`: initially **CHANGES
  REQUIRED** (`0 Critical, 3 Important, 1 Minor`); Fix Round 1 `ce15f71`
  addressed all Important findings. Final scoped quality after Fix Round 2 is
  **QUALITY APPROVED**, with the prior Minor addressed and no breakage.
- Consumer technical reviewer `/root/task_1_2_consumer_review`: initially
  **CHANGES REQUIRED** with four blockers; Fix Round 2 `20d57f8` addressed all
  four. Consumer re-review is **APPROVED** with no breakage.
- CP-0001 remains proposed and unapplied. The frozen schema exporter and v1
  snapshot remain unchanged. Actual Program Sol-0 and named consuming-owner
  approvals remain pending before activation.
- Reviewer-host proof was read-only; no shared activation or integration claim
  is made. Workstream Sol final review remains pending, and Task 1.3 is not
  authorized.
