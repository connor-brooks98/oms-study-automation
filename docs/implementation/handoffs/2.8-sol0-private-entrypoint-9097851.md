# Task 2.8 Program Sol-0 private-shadow entrypoint correction

Status: **OFFLINE COMPLETE / INDEPENDENT REVIEWS SHIP / RETAINED ROOT PRESERVED /
PROVIDER STATE UNKNOWN / GATE_NOT_OPEN**

Exact Sol-2 blocker handoff `909785131b113fb5b2083b74bd2757c2fc0dbf4b` proved that the
separately bundled entrypoint at SHA-256
`cbfded5324a57c0f5af9fd283bee806c19ba5cfeeb6096ba42a23fed10218164` retained an obsolete
11-key blocked-evidence allowlist and collapsed corrected evidence to an invalid three-key
fallback.

## TDD and correction

- test-only RED `4051a54` failed because no shared composition entrypoint was committed;
- minimum GREEN `1dd7dec` committed the shared entrypoint, preserved the exact corrected 15-key
  blocked record, and replaced the three-key fallback with a complete blocked sentinel whose
  cleanup and reconciliation remain `unknown`;
- criteria-neutral typing/lint commits: `b18e30b`, `49a222f`;
- review RED `84ac912` reproduced an unguarded database-close failure that skipped scratch
  cleanup and emitted a traceback;
- review GREEN `4bc82fbb258cb1192323fe16f1123c8b2a5fdb82`, tree
  `df0871b3777c65f76ca93cc9385591793e481e86`, independently guards database close, always
  attempts scratch cleanup, and replaces any close/cleanup ambiguity with the 15-key
  unknown-cleanup sentinel.

Exact final hashes:

- entrypoint: `96c77c083d665fe945cde5a31265d83276fe07778a1bb732bccee1b28f1acad2`;
- corrected/fallback fixture:
  `e78b8681cfdc066cd244db1d8e3df9a8aacad07d1ca9cf9dcdeeef117254a124`;
- PowerShell 5.1 harness:
  `942be010290e323301774941a603ac69c9094184d15a3a4d63bbbd6313381858`;
- Python contract test:
  `1ebcc475c921d55f12d841d70afd655bdf6271fe67d8ae9e325307fd72e49011`;
- real evidence validator:
  `6220323896c4b6d9d5eae62bc4d69e2efcdba046d633df99afbcc0b6ba557c72`.

The actual entrypoint process was exercised in corrected, ordinary fallback, database-close
failure, and scratch-cleanup failure modes. Every mode exited blocked with one 15-key JSON record
that passed the real PowerShell validator. Windows PowerShell 5.1 emitted
`PRIVATE_SHADOW_ENTRYPOINT_HARNESS_VERIFIED`.

Verification passed: `112 passed, 1 intentional live skip` focused; `300 passed, 1 intentional
live skip` affected; Ruff, strict mypy, Python AST, diff hygiene, and exact ancestry passed.
Independent specification and quality/security reviews both **SHIP** exact `4bc82fbb` with no
findings.

## Preserved boundary

The retained `06848e2` protected root and its single sanitized evidence file were not read,
changed, or removed. Read-only checks found exact root/evidence ACLs, zero reparse points, and
task, scratch, and diagnostic absence. Hub remained `Running`; both health endpoints remained
`ok` on schema 29.

No credential, private source, provider API/inventory, reconciliation, task registration/start,
prior authorization, retry, production/Gate mutation, merge, push, or deployment occurred.
Provider resource state remains unknown. Any inventory/reconciliation must receive fresh,
separate authorization before a later independently authorized private retry.
