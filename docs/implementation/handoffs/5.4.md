# 5.4 handoff

- Branch: `sol5/board-questions`.
- Base SHA: `d9c2d3e86753ea058190120828d84c2cef48d7b2` (clean verified Task 5.2
  completion, descending from accepted integration `1129754`).
- RED commit: `e978f58233f8c37ad94a61667ae7eea9b1951c7c`, tree
  `af08ba1ee8bc2ddd8c1a72f4e5cb8fd97aa50e56`.
- Accepted-checksum RED correction: `d0c7ce6474f657df11d969148e258136750615ba`,
  tree `b49cd5495ab924c48a5ec1bdee0609d6c9bf0535`; no amend, squash, or
  history rewrite.
- GREEN candidate: `40ea435ad5064c2a78865749595437fe8a968028`, tree
  `b771b62c4dccc4d7a851b664592d08c9b2b082a7`.
- Independent-review remediation RED: `2cb99f518e0ad49e1d0661c8736f6b38bd0d7cb3`,
  tree `3469b1adf6a164f1b54b077ac58e192458ecaaef`.
- Current remediation GREEN candidate:
  `c28c92b23b5fba13603741723bf9f3edfc15ca7d`, tree
  `926364bc5cc70add465132cf150adee1726ff6d8`.
- Second renewed-review RED: `ac9af20ecf85b3f1c0815b02e098aa0e62fb1623`,
  tree `964b7f4e55b631c0cdec15ee049982aff8e463f3`.
- Current second-remediation GREEN candidate:
  `61caca368a8301c87284f37dc003f60a246566d9`, tree
  `32e91223256942b3ee6e4996204c5c26b7ed0c77`.
- Deterministic pruning RED: `19f7c5614474af8f32f2f981c5c117a210d456ea`,
  tree `6e3df7fba4876ab7aa4cb8dc662331651785697e`.
- Current exact-selection GREEN candidate:
  `6f7bcb0bceba1e93d337a387587c6d73a316e159`, tree
  `142664d767ed3123746fdba6f26b93d65cbe88bb`.
- Third renewed-review RED: `67a6b466b9c889c561ef2ab6c2e96c3de1785c3c`,
  tree `2ecc34495d3af819ea41dd1fe9cd44504966373a`.
- Current priority-preserving GREEN candidate:
  `93c28985197867033304d8c1fccd12ad82c407b9`, tree
  `308d7f762f28fbf8b69bdcbae3186771e8b2aaa6`.
- Fourth renewed-review RED: `ef1f0ef0b7273cccdf9e783f84e43667d8290132`,
  tree `0776625e4e924384dbbc276d564dbadd49223cc2`.
- Current character/Pareto GREEN candidate:
  `d79ff6f083dcad17477b9c698643d645f0b392aa`, tree
  `960b3bae27e4b1c5c5353a5371b93638c56f7adc`.
- Fifth quality-review performance RED:
  `dbb95a4cc62fb68dd20b7c6df7dea284bad56a3e`, tree
  `adfa2082d0a3d24ce6bb60580a1fcfa2f683d064`.
- Current performance GREEN candidate:
  `5e4d92b7bcb6564d64618619ba8f3bc0099853a9`, tree
  `5215c46810e6a1444ee13d95bc1be3f6c3b00bd0`.
- Final reviewed evidence head:
  `6acfe8926d2f03cfadda05eccd862c4e68c1be66`, tree
  `0d14257e62d9de170f229376bdd91da61a481b95`.
- Owner: Sol-5 with fresh Luna committed TDD implementation.
- Initial independent Terra review status: **CHANGES REQUIRED** on evidence head
  `0a02382093b58a6fb6dce45c17197bd0ed339c11` and code candidate `40ea435`.
  Findings were provider-versus-canonical cap ambiguity, semantic-operator
  signature collisions, incomplete exact excerpt binding, lifecycle
  interleaving, greedy false insufficiency, and incomplete provider-result
  shape validation. The remediation below has committed regressions and fixes
  for all six findings.
- Second independent Terra review status: **CHANGES REQUIRED** on evidence head
  `3bc43fa5b6b10344a1eb6e833c4292d21fd3465e` and code candidate `c28c92b`.
  Findings were required-cover overflow for unbounded non-integrated objective
  counts, character budgets disagreeing with emitted representatives, Unicode
  symbol/mark signature collisions, and exhaustive Cartesian cover selection.
- A deterministic pre-re-review audit of `61caca3` found that its 128-state
  Pareto truncation could discard the only signature that makes a later role
  affordable. The additional committed RED/GREEN round below removes that
  heuristic entirely.
- Third independent Terra review status: **CHANGES REQUIRED** on evidence head
  `d2c09a86cbac9f804f31bd262842571d8bddd395` and code candidate `6f7bcb0`;
  all earlier findings were closed. Remaining findings were minimum-character
  required-cover selection discarding a higher-priority feasible cover and a
  full packet retaining a now-redundant low-priority required locator instead
  of a higher-priority omitted candidate.
- Fourth independent Terra review status: **CHANGES REQUIRED** on evidence head
  `62512f714011ad6c981fc6aafabbb685b89d09ab` and code candidate `93c2898`;
  all earlier findings were closed. Remaining findings were a character-bound
  skipped candidate not being reconsidered after a later candidate made its
  blocking required locator redundant, and quadratic all-pairs reconstruction
  of the exact Pareto frontier after every insertion.
- Fifth independent Terra specification review: **APPROVED** on evidence head
  `35bdc0c3abc135b6d3f92898a554823ad662beb0` and code candidate `d79ff6f`;
  all functional findings were closed.
- Fifth independent Terra quality review: **CHANGES REQUIRED** on the same
  evidence and code. Its only remaining finding was performance: the exact
  frontier still copied a complete tuple twice on every insertion, and the
  reviewer contract-maximum probe measured `17.4276s` CPU.
- Final independent Terra specification reviewer `/root/terra_5_4_spec`:
  **APPROVED** exact evidence head `6acfe8926d2f03cfadda05eccd862c4e68c1be66`
  / tree `0d14257e62d9de170f229376bdd91da61a481b95` and code candidate
  `5e4d92b7bcb6564d64618619ba8f3bc0099853a9` / tree
  `5215c46810e6a1444ee13d95bc1be3f6c3b00bd0`. The scoped confirmation found
  no API, deterministic-selection, cap, omission, ownership, or activation
  regression from the performance-only refactor.
- Final independent Terra quality reviewer `/root/terra_5_4_quality`:
  **APPROVED** the same exact evidence head, code candidate, and trees. Its
  contract-maximum concept-plus-four-objective, sixteen-reference-per-role
  probe completed in `0.5842s`, down from `17.4276s`, while preserving exact
  Pareto behavior. It reported no blocking or nonblocking quality finding.

## Deliverable

- Produced `QuestionObjective`, `QuestionGenerationRequest`,
  `QuestionEvidenceLocator`, `QuestionPacketEvidence`,
  `QuestionEvidencePacket`, `QuestionEvidenceError`, and async
  `QuestionEvidencePacketBuilder.build(request)`.
- The builder makes one bounded retrieval for the correct-answer concept and
  one for each objective, then re-resolves every provider `EvidenceRef` against
  canonical `KnowledgeRepository` evidence and source-revision records.
- Every accepted reference must bind exactly to a READY revision, unretired
  evidence, requested scope, allowed truth-mode authority, canonical locator,
  byte-for-byte canonical excerpt, and the accepted direct provider checksum
  form `sha256:<EvidenceUnit.content_sha256>`.
- Generation is refused for no concept evidence, no evidence for any requested
  objective, provider-declared insufficiency, generated-artifact authority,
  disallowed literature in `course_only`, unknown evidence/revisions,
  stale/retired revisions, retired evidence, scope or canonical-integrity
  mismatch, more than four objectives in an integrated item, a forbidden target
  signature, or a bound that cannot retain required evidence.
- Equivalent claims deduplicate by an operator-safe Unicode/case/punctuation/
  whitespace-normalized SHA-256 signature while preserving every retained
  evidence ID, revision, authority, checksum, and locator. `<`, `>`, `+`, and
  `-` remain semantically distinct; Unicode comparators/minus and `+/-`
  typographic forms normalize to their ASCII equivalents.
- Every provider result is validated before repository reads and is limited to
  16 references. A completed packet also contains at most 16 total canonical
  evidence IDs/locators after claim grouping, at most 18,000 normalized
  evidence characters, and at most four objectives in every packet mode. A
  request with more than four objectives is refused before retrieval; this
  applies the manifest integration ceiling to every packet because any item
  spanning five objectives is necessarily integrated regardless of its mode
  label.
- Required-cover selection first groups equivalent claim signatures and derives
  exact representative/member options. Its global DP is keyed by at most 32
  role masks (one concept plus four objective bits) and 17 canonical-ID counts,
  retaining the exact nondominated character/priority alternatives per key.
  Each frontier is a mutable pair of state and character-index lists. For every
  claim-signature stage, the state map is shallow-copied and a destination
  frontier is cloned lazily at most once for that stage/key, leaving prior-stage
  frontiers unchanged. In-place insertion is indexed by character count: an
  existing lower/equal-character, higher/equal-priority state rejects the
  candidate, and only higher/equal-character, lower/equal-priority states are
  removed, with stable ID ties. Complete feasible covers are selected by highest
  total canonical source priority, then character count, unit count, and stable
  IDs. Group costs are additive, so no signature-dependent future cost,
  per-insertion full-frontier copying, all-pairs reconstruction, or arbitrary
  frontier pruning remains. Required cover and final output independently remain
  at or below 16 canonical IDs.
- Character projection uses the exact highest-priority text that grouping emits,
  so adding an equivalent locator can replace the representative only when the
  resulting packet remains within 18,000 characters. Optional fill remains in
  descending canonical priority order and retains candidates skipped for either
  the ID or character bound. After a later successful addition or replacement,
  it deterministically retries those deferred higher-priority candidates and
  attempts only a one-for-one priority-improving replacement of the lowest
  selected locator. A swap is accepted only when required coverage and the
  exact character/unit bounds remain valid. Truncation never slices text and
  records every omitted evidence ID.
- Every selected canonical evidence record is re-resolved immediately before
  packet return, failing closed if revision lifecycle, evidence retirement,
  authority, scope, locator, excerpt, or checksum changed during the build.
- The source snapshot hash is deterministic over the selected canonical
  evidence identities, revisions, authorities, checksums, and locators.
  Prior-tested signatures, forbidden-repeat signatures, and style constraints
  are retained unchanged for downstream generation.

## Contract boundary and scope

- Consumed accepted interfaces: Task 5.3 `QuestionMode`; Gate 2A
  `EvidenceUnit`, `SourceRevisionState`, `KnowledgeRepository`, Source Trust
  scope policy, and provider `RetrievalProvider` / `RetrievalRequest` /
  `RetrievalResult` / `RetrievalScope` contracts.
- The plan names a generic "retrieval gateway", but the accepted integration
  tree does not contain Sol-3 Task 3.4 `src/oms_hub/ask/retrieval.py`, and the
  manifest does not make Task 5.4 depend on 3.4. This implementation therefore
  consumes only the integrated provider contract and canonical Source Trust
  repository; it does not borrow, recreate, or depend on unintegrated Sol-3
  code.
- `src/oms_hub/questions/__init__.py` was modified only to export Task 5.4's
  produced public interfaces through the established Task 5.3 package pattern.
  No shared, central, schema-exporter, flag, route, provider, or migration
  surface changed.
- Activation remains `ACTIVATION_HELD`. No question-v2 schema or runtime flag
  was activated.

## Files

- Created `src/oms_hub/questions/evidence_packets.py`.
- Created `tests/questions/test_evidence_packets.py`.
- Created this handoff.
- Modified `src/oms_hub/questions/__init__.py` only for the package export
  justification above.

## TDD and verification evidence

- RED command:
  `.venv/bin/python -m pytest tests/questions/test_evidence_packets.py --override-ini addopts= -q`.
  At `e978f58`, collection reached the new owned test and failed with the one
  intended `ModuleNotFoundError` for the absent
  `oms_hub.questions.evidence_packets` boundary. The same intended RED remained
  after the committed `d0c7ce6` checksum-contract correction.
- Focused GREEN: the unchanged command reported `22 passed in 0.36s`.
- Coverage:
  `.venv/bin/python -m pytest tests/questions/test_evidence_packets.py --override-ini addopts= --cov=oms_hub.questions.evidence_packets --cov-report=term-missing -q`
  reported `22 passed` and `92%` (`243` statements, `19` missed). The generated
  `.coverage` file was removed before commit.
- Affected question/Source Trust suite:
  `.venv/bin/python -m pytest tests/questions tests/knowledge/test_policy.py tests/knowledge/test_repository.py --override-ini addopts= -q`
  reported `140 passed`.
- Contract/provider suite, with the task venv first on `PATH` because the frozen
  Gate-1 record invokes `python` in a subprocess:
  `PATH="$PWD/.venv/bin:$PATH" .venv/bin/python -m pytest tests/contracts tests/providers --override-ini addopts= -q`
  reported `122 passed`.
- Ruff:
  `.venv/bin/ruff check src/oms_hub/questions/evidence_packets.py src/oms_hub/questions/__init__.py tests/questions/test_evidence_packets.py`
  reported `All checks passed!`.
- Changed-source mypy:
  `.venv/bin/mypy src/oms_hub/questions/evidence_packets.py src/oms_hub/questions/__init__.py`
  reported success for 2 source files.
- Task-test mypy:
  `MYPYPATH=$PWD/src .venv/bin/mypy tests/questions/test_evidence_packets.py`
  reported success for 1 source file.
- Source mypy in the recorded full offline environment:
  `/private/tmp/sol1-task16-full-venv/bin/mypy src` reported success for 214
  source files. The smaller task-local `.venv` lacks optional `pdf_inspector`
  and `anydoc`, so its failed full-source attempt is not counted; changed-source
  and task-test mypy also pass as recorded above.
- `git diff --check` passed. Task fixtures use temporary SQLite databases and
  synthetic text only.

## Independent-review remediation TDD evidence

- RED commit: `2cb99f518e0ad49e1d0661c8736f6b38bd0d7cb3`, tree
  `3469b1adf6a164f1b54b077ac58e192458ecaaef`. The unchanged focused
  command reported `13 failed, 26 passed`. The intended failures reproduced:
  one provider-result overlimit accepted before repository reads; 32 retained
  canonical locators hidden inside one deduplicated claim; four semantic-
  operator collisions; CRLF/LF excerpt acceptance; stale-revision and retired-
  evidence interleavings; one feasible compact required cover falsely refused;
  and three invalid result-shape cases accepted. Ruff and task-test mypy passed
  at RED.
- GREEN commit: `c28c92b23b5fba13603741723bf9f3edfc15ca7d`, tree
  `926364bc5cc70add465132cf150adee1726ff6d8`. The unchanged focused
  command reported `39 passed in 0.80s`.
- Remediation coverage reported `92%` (`273` statements, `21` missed) with
  `39 passed`; the generated `.coverage` file was removed before commit.
- Remediation affected question/Source Trust suite reported `157 passed`.
  Contract/provider suite reported `122 passed`.
- Remediation Ruff reported `All checks passed!`; full-source mypy reported no
  issues in 214 source files; task-test mypy reported no issues in 1 source
  file; `git diff --check` passed.

## Second renewed-review and pruning remediation TDD evidence

- Renewed-review RED commit:
  `ac9af20ecf85b3f1c0815b02e098aa0e62fb1623`, tree
  `964b7f4e55b631c0cdec15ee049982aff8e463f3`. The unchanged focused
  command reported `6 failed, 39 passed`: one non-integrated 17-role overflow,
  one oversized highest-priority representative emitted past 18,000
  characters, three Unicode symbol/combining-mark collisions, and one
  deterministic accounting guard against exhaustive cover work.
- First GREEN for that round: `61caca368a8301c87284f37dc003f60a246566d9`,
  tree `32e91223256942b3ee6e4996204c5c26b7ed0c77`. It reported `45 passed`,
  but the pre-re-review audit found the arbitrary-frontier feasibility gap
  recorded next; this commit is historical evidence, not the current candidate.
- Pruning RED commit: `19f7c5614474af8f32f2f981c5c117a210d456ea`,
  tree `6e3df7fba4876ab7aa4cb8dc662331651785697e`. The unchanged focused
  command reported `1 failed, 45 passed`; a feasible high-character signature
  needed by a later objective was deterministically discarded by the heuristic
  frontier. Ruff and task-test mypy passed at RED.
- Exact-selection GREEN commit:
  `6f7bcb0bceba1e93d337a387587c6d73a316e159`, tree
  `142664d767ed3123746fdba6f26b93d65cbe88bb`. The unchanged focused
  command reported `46 passed in 1.34s`.
- Current coverage reported `94%` (`353` statements, `22` missed) with
  `46 passed`; the generated `.coverage` file was removed before commit.
- Current affected question/Source Trust suite reported `164 passed`.
  Contract/provider suite reported `122 passed`.
- Current Ruff reported `All checks passed!`; full-source mypy reported no
  issues in 214 source files; task-test mypy reported no issues in 1 source
  file; `git diff --check` passed.

## Third renewed-review remediation TDD evidence

- RED commit: `67a6b466b9c889c561ef2ab6c2e96c3de1785c3c`, tree
  `2ecc34495d3af819ea41dd1fe9cd44504966373a`. The unchanged focused
  command reported `2 failed, 46 passed`: a priority `100` concept with an exact
  18,000-character feasible cover lost to a priority `1` shorter concept, and a
  full 16-ID packet retained a redundant priority `-100` locator instead of the
  omitted priority `887` candidate after an equivalent priority `1000` locator
  joined the packet. Ruff and task-test mypy passed at RED.
- GREEN commit: `93c28985197867033304d8c1fccd12ad82c407b9`, tree
  `308d7f762f28fbf8b69bdcbae3186771e8b2aaa6`. The unchanged focused
  command reported `48 passed in 1.35s`.
- Current coverage reported `93%` (`382` statements, `28` missed) with
  `48 passed`; the generated `.coverage` file was removed before commit.
- Current affected question/Source Trust suite reported `166 passed`.
  Contract/provider suite reported `122 passed`.
- Current Ruff reported `All checks passed!`; full-source mypy reported no
  issues in 214 source files; task-test mypy reported no issues in 1 source
  file; `git diff --check` passed.

## Fourth renewed-review remediation TDD evidence

- RED commit: `ef1f0ef0b7273cccdf9e783f84e43667d8290132`, tree
  `0776625e4e924384dbbc276d564dbadd49223cc2`. The unchanged focused
  command reported `2 failed, 48 passed`: a priority `999` concept skipped at
  the character bound was not retried after a priority `99` objective made the
  blocking priority `100` objective redundant, and the adversarial exact
  frontier crossed the deterministic 5,000-dominance-operation guard because
  it rebuilt all pairwise dominance after every insertion. Ruff and task-test
  mypy passed at RED.
- GREEN commit: `d79ff6f083dcad17477b9c698643d645f0b392aa`, tree
  `960b3bae27e4b1c5c5353a5371b93638c56f7adc`. The unchanged focused
  command reported `50 passed in 1.82s`.
- Current coverage reported `95%` (`403` statements, `22` missed) with
  `50 passed in 3.19s`; the generated `.coverage` file was removed before
  commit.
- Current affected question/Source Trust suite reported `168 passed in 2.03s`.
  Contract/provider suite reported `122 passed in 4.07s`.
- Current Ruff reported `All checks passed!`; full-source mypy reported no
  issues in 214 source files; task-test mypy reported no issues in 1 source
  file; `git diff --check` passed.

## Fifth quality-review performance remediation TDD evidence

- RED commit: `dbb95a4cc62fb68dd20b7c6df7dea284bad56a3e`, tree
  `adfa2082d0a3d24ce6bb60580a1fcfa2f683d064`. The contract-maximum fixture
  supplies the concept and four objectives with 16 distinct valid references
  each, using character/priority tradeoffs that retain exact Pareto alternatives.
  Its non-wall-clock guard counts tuple-frontier elements at the narrow insertion
  boundary and stops after 20,000 elements rather than waiting for the complete
  slow path. The unchanged focused command reported `1 failed, 50 passed in
  1.61s`, failing only on that deterministic copy guard. Ruff and task-test
  mypy passed at RED.
- GREEN commit: `5e4d92b7bcb6564d64618619ba8f3bc0099853a9`, tree
  `5215c46810e6a1444ee13d95bc1be3f6c3b00bd0`. The unchanged focused
  command reported `51 passed in 2.87s`; all 50 pre-existing functional tests
  remained unchanged and GREEN.
- Current coverage reported `95%` (`418` statements, `22` missed) with
  `51 passed in 7.61s`; the generated `.coverage` file was removed before the
  evidence commit.
- Current affected question/Source Trust suite reported `169 passed in 3.41s`.
  Contract/provider suite reported `122 passed in 3.38s`.
- Current Ruff reported `All checks passed!`; full-source mypy reported no
  issues in 214 source files; task-test mypy reported no issues in 1 source
  file; `git diff --check` passed.

## Data, rollback, and limitations

- Data migration impact: none. No production/private database or source was
  read or written.
- Rollback: revert the Task 5.4 GREEN/export unit before any later Sol-0-owned
  runtime composition. No schema or stored data rollback is required.
- Known limitation: until an accepted retrieval gateway is integrated, the
  builder directly orchestrates the accepted retrieval provider and canonical
  Source Trust repository. A later owner may adapt an accepted gateway without
  changing packet policy. Exact required-cover work is bounded by the packet-
  wide four-objective policy, 16-reference provider-result cap, 16-ID output
  cap, 18,000-character cap, 32 role masks, and 17 ID-count keys. Optional fill
  deliberately implements one-for-one priority improvement only, not a general
  combinatorial optimizer. Revisit those fixed bounds only if the manifest
  contract grows. Task 5.5 provider generation/persistence was not started.

No network, live provider, private data, production write, Anki write, push,
merge, rebase, deployment, manifest/plan edit, later-task implementation, or
question-v2 activation was performed.
