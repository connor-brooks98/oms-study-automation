# 1.6 handoff

## Status

Task 1.6 remains **BLOCKED / NOT STARTED**. This is proposal-only status
evidence; no backfill implementation, test, source-trust record, or activation
exists.

## Commit

- Branch: `sol1/source-trust`.
- Proposal base: `d3a44089f1ddf6f13a154e791653b59fe462d044`, tree
  `8d6bb46dedf4ff67a65552400b1e5b20c5a37227`.
- Initial proposal commit: `df026d0648abf6c73d5bf74f4ea9086e0012bea3`,
  tree `3c5a8191e79e8683ccedd5bb03507252538a51d8`.
- Sol-2 BLOCK amendment base: the initial proposal commit above.
- Containing proposal/status commit: symbolic; resolve externally after commit
  because a committed file cannot contain its own final commit/tree identity.

## Deliverable

- Produced proposal:
  `docs/implementation/contract-proposals/CP-0002-legacy-slide-revision-adapter.md`.
- Requested ruling: Program Sol-0 approval or rejection of the exact legacy
  source identity, hash, bounded StoreKey-safe scope, knowledge-owned index
  view, parser, locator/preview, atomic supersession, retry, and ownership
  mappings.
- Consumed evidence: the recovered Task 1.1-1.5 handoffs, frozen repository map,
  current ingestion/catalog read models, artifact preview boundary, document
  parser, and Source Trust models.
- Feature flags/schema/prompt/model versions: unchanged.

## Sol-2 consuming review amendment

Sol-2 returned **BLOCK** on the initial CP-0002 proposal with three required
corrections. The amended proposal now:

1. freezes bounded slug-safe scope IDs with 96-bit full-input digests and a
   composed `StoreKey.course` parse round-trip gate;
2. requires Task 1.7's read-only `resolve_index_input(source_revision_id)` view
   to own all legacy resolution and expose verified canonical artifacts,
   hashes, state, authority, scope, evidence, and assets to Sol-2; and
3. requires one atomic Source Trust activation that marks prior ready scope
   revisions stale before the replacement becomes ready/indexable.

The amendment preserves the compatibility `sr_` identity,
`course_material` authority, and `source_sha256` file-hash meaning. Its exact
evaluation is that no Sol-0-owned shared JSON/provider schema, central database
migration, or wiring change is required if Sol-0 and Sol-2 approve CP-0002.
Failure to prove unique ready-per-scope activation with existing columns is the
explicit stop that would require a Sol-0-owned additive schema change.

## Files

- Proposal series: CP-0002 and this blocked handoff/status record.
- Current amendment modifies only those same two documentation files.
- Intentionally not modified: `src/**`, `tests/**`, shared contracts/schemas,
  exporter, app/startup, routes, settings/flags, dependency lock, migrations,
  CI, plan, manifest, prior handoffs, artifacts, private/production data, and
  Anki.

## Verification

- Repository state before the amendment: clean detached worktree at
  `df026d0`; `sol1/source-trust` matched that exact initial proposal commit.
- Dependency evidence: approved Task 1.4 candidate `db1ab54` and approved Task
  1.5 candidate `efca477` are ancestors of the proposal base.
- Adapter audit: independent read-only review returned **BLOCKED** pending an
  exact Sol-0 ruling; consuming Sol-2 then returned **BLOCK** with the three
  findings recorded above.
- Documentation checks for the amendment: staged `git diff --check` passed;
  the allowlist contained only CP-0002 and this handoff; the scoped
  absolute-path, credential, private-data, and Anki-collection scan was silent.
- Runtime tests: not run; no production code or test changed, and Task 1.6 TDD
  must not begin before the ruling.

## Data and migration

- New records/backfill: none.
- Idempotency key: proposed only; no implementation exists.
- Stale/rollback behavior: revert the proposal-only commit if rejected. There
  is no runtime or persisted-data rollback.

## Blocker and required owner action

Program Sol-0 must record one complete outcome from amended CP-0002's `Exact
Sol-0 ruling required` section. The recommended ruling is that the internal
view and existing state/scope columns require no Sol-0-owned shared-file change.
Approval must include Sol-2 re-review, the exact StoreKey/view/supersession
contract, and explicit Task 1.6 implementation authority. If transactional
unique-ready supersession cannot be proved, Sol-0 must instead own the additive
schema/migration contract before Task 1.6 returns to Sol-1.

Until then, Task 1.6 is blocked and Tasks 1.7 and 1.8 remain transitively
blocked. No push, merge, deployment, worktree deletion, production/private-data
access, or Anki mutation is authorized or performed.
