# 0.3 handoff

## Commit

- Branch: `sol0/contracts-and-integration`
- Commit: symbolic containing Task 0.3 commit; resolve after commit with `git rev-parse HEAD`.
- Tree: symbolic containing Task 0.3 tree; resolve after commit with `git rev-parse 'HEAD^{tree}'`.
- Base integration SHA: `45a23d238025d09e6f101367145d25f25c39ec62` (Task 0.2 final review-record commit).

## Deliverable

- Produced interfaces: `FeatureFlag` (`StrEnum`) and immutable `FeatureFlags`, publicly exported by `oms_hub.features`.
- Consumed interfaces: Pydantic `Settings`, `RuntimeSettingsRepository.model_copy`, and existing application construction.
- Feature flags: `source_trust_v1`, `gemini_file_search_v1`, `ask_studyhub_v1`, `ask_quiz_context_v1`, `board_question_v1`, `adaptive_practice_v1`, `practice_modes_v1`, `error_notebook_v1`, `timed_blocks_v1`, `anki_learning_loop_v1`, `board_runway_v1`, `journal_evidence_v1`, and `legacy_notebooklm_generation`.
- Schema/prompt/model versions: none added or changed.

All 12 new grounded-learning flags default off in `FeatureFlags.from_mapping({})` and in `Settings`. Unknown names fail closed with `ValueError`; non-bool values fail with `ValueError`; copied `MappingProxyType` state prevents both input aliasing and consumer mutation.

The legacy NotebookLM flag remains false for the standalone `FeatureFlags.from_mapping({})` contract, but the `Settings` adapter supplies `legacy_notebooklm_generation=True` whenever the environment mapping omits it. This preserves current behavior: `create_app` unconditionally constructs the existing NotebookLM connection, gateway, generation worker, and routers. An explicit `false` mapping remains false; Task 0.3 does not consume the value to alter generation or routing.

## Files

- Created: `src/oms_hub/features/__init__.py`, `src/oms_hub/features/flags.py`, `tests/features/test_flags.py`, and this handoff.
- Modified: `src/oms_hub/config.py` and `.env.example` only from the frozen configuration inventory.
- Intentionally not modified: `src/oms_hub/security/secret_store.py`, `src/oms_hub/web/settings_routes.py`, route registration, UI, generation, providers, persistence, migrations, dependencies, CI, manifests, Anki, Task 0.1/0.2 handoffs, and Task 0.4 files.

`OMS_HUB_FEATURE_FLAGS` is the sole added environment key. Its documented value is `{}`; its runtime adapter is `Settings.parse_feature_flags`, which constructs the one typed immutable `Settings.feature_flags` value. No persisted key was introduced. `Settings.serialize_feature_flags` serializes the immutable value only for the existing `model_dump` -> `RuntimeSettingsRepository.model_copy` path, preserving flags through the existing runtime port override.

## Verification

- Focused final: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/features/test_flags.py -q` — `45 passed`.
- Plan affected selection: at implementation commit `0b87287d5dbb58362dc1bfb54eb9cc822c97fd33`, `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/features/test_flags.py tests -q -k "settings or feature"` — `41 passed`; the controller will rerun it at final HEAD.
- Existing settings/generation parent evidence: `26 passed`. Settings/runtime post-fix evidence: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/anki/test_settings.py tests/v2/test_runtime_settings.py -q` — `17 passed`.
- Quality: `ruff check src tests scripts` and `mypy src` passed for `172` source files. `node --test "tests/js/*.test.js"` passed with `168` tests.
- Full workstream: not run. The frozen baseline records macOS CPython 3.12.13 / PyMuPDF 1.28.2 signal 11; this task ran only the safe, affected suites and did not exercise PDF processing.
- Live/manual: no provider, production, private-data, Anki, or native Windows action. The focused app smoke confirms existing `/health/live` and `/settings` routes remain available.
- Git status: clean before this review record. Reproduce the final clean symbolic containing identity with `git status --short`, `git rev-parse HEAD`, and `git rev-parse 'HEAD^{tree}'` after this commit.

Task 0.3 range whitespace, scope, and secret scans passed. The 33 Markdown hard-break warnings from Task 0.2 remain inherited and are not modified here.

## Data and migration

- New records: none.
- Backfill: none.
- Idempotency key: not applicable; configuration is parsed at settings construction.
- Stale/rollback behavior: revert the Task 0.3 commit to remove the configuration surface; no route, provider, schema, migration, or data rollback is needed.

## Risks and follow-up

- Known limitations: native Windows acceptance remains outstanding. The macOS CPython 3.12.13 / PyMuPDF 1.28.2 signal-11 baseline remains unresolved and out of scope. The Task 0.1 parked handoff-inventory Minor remains parked. Task 0.2's 33 Markdown hard-break warnings remain inherited.
- Contract proposals: none. No dependency, route, UI, schema, migration, or persisted settings system was added.
- Review commits: implementation `0b87287d5dbb58362dc1bfb54eb9cc822c97fd33`; immutability correction `b8b692310fc6cf8c9fbdc9a64c757ca373bf27a1`; strict direct-validation correction `6babe2a3fe6aa5e411af62f1d5e2b42ab4eaf56f`.
- Terra specification review: **CHANGES REQUIRED** at `0b87287` for mutable direct construction; `b8b6923` copies and freezes every direct-constructor input, and the scoped re-review is **APPROVED**.
- Terra quality review: **CHANGES REQUIRED** at `b8b6923` for unvalidated direct construction/truthiness bypass; `6babe2a` validates declared `FeatureFlag` keys and exact bool values in the shared constructor boundary, and the scoped re-review is **APPROVED**.
- Corrections made: immutable direct construction, then strict direct key/value validation. No route, generation, dependency, schema, migration, or Task 0.4 work was added.
- Next permitted task: Task 0.4 only. Gate 1 — Shared Contracts remains blocked.
