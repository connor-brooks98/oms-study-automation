# 7.1 handoff

- Base SHA: `82e22d7aa109a29a5fe4d7c26eb1672966317fb`
- Head SHA: `SELF` (repository self-resolution convention: resolve to the commit containing this handoff with `git rev-parse HEAD`; the final resolved correction identity is recorded in `task-7.1-report.md`)
- Head tree: `SELF_TREE` (resolve from that same commit with `git rev-parse "HEAD^{tree}"`; the final resolved correction tree is recorded in `task-7.1-report.md`)
- Owner: Sol-7 / Luna
- Files changed: `src/oms_hub/anki/learning_contracts.py`, `tests/anki/test_learning_contracts.py`, `docs/implementation/handoffs/7.1.md`
- Contracts consumed: `AnkiConnectClient` typed AnkiConnect v6 read methods, `AnkiPreflight`, `AnkiRuntime.preflight()`, and existing note/card response shapes.
- Contracts produced: `AnkiNoteLearningState`, `AnkiLearningSnapshot`, `AnkiSyncHealth`, and read-only `AnkiLearningReader`.
- Tests run: required contract tests, focused Anki read-path tests, full `tests/anki tests/agent`, Ruff, source mypy, and task-owned mypy.
- Test result: task-owned contract tests `7 passed`; focused affected Anki tests `44 passed`; full Anki/agent suite `527 passed`; Ruff and both mypy lanes clean.
- Live-provider test run: no
- Known limitations: AnkiConnect does not expose a reliable review timestamp in the reused response surface; `mod` is a modification timestamp and is ignored, so review time is `None` unless an explicit review field is present. Numeric `cardsInfo.due` and queue values are not interpreted as due/overdue status; those statuses are `None` unless Anki explicitly returns a boolean status. Note-level interval uses the maximum card interval, retrievability uses the minimum available value, and lapse count uses the maximum card lapse count. The adapter does not persist or upload snapshots; that is Task 7.2.
- Data migration impact: none; no database, collection, private Anki data, or `collection.anki2` access.
- Feature flag: `anki_learning_loop_v1` remains unwired/off; this task only defines the local read-only contract.
- Rollback: remove the three task-owned files or revert the implementation commit; no runtime or data rollback is needed.
- Reviewer findings: Terra specification review initially required two fixes and approved at `deb94d6`; quality review initially required two Medium and one Minor fixes and approved at `c02ea18`. Both reviewer hosts were behaviorally read-only. Final Sol review remains the next gate.

## Existing Anki method reuse inventory

| Existing method | Classification | Use in Task 7.1 |
| --- | --- | --- |
| `AnkiConnectClient.version()` | read-only allowed | Fallback health check when no runtime preflight source is supplied. |
| `AnkiConnectClient.get_active_profile()` | read-only allowed | Fallback health check. |
| `AnkiConnectClient.find_notes()` | read-only allowed | Collection accessibility probe and selected note lookup. |
| `AnkiConnectClient.notes_info()` | read-only allowed | Reads note IDs, card IDs, and tags; fields/media are intentionally discarded. |
| `AnkiConnectClient.cards_info()` | read-only allowed | Reads deck and explicitly exposed scheduling/status metadata needed for minimized learning state; numeric `due` positions are not interpreted. |
| `AnkiRuntime.preflight()` | read-only allowed | Preferred health/lifecycle boundary; the adapter never calls `ensure_running()`. |
| `AnkiConnectClient.find_cards()` | not needed | Note card IDs already identify the cards in the selected notes. |
| `AnkiConnectClient.model_field_names()` | not needed | Full model/field metadata is outside the learning contract. |
| `AnkiConnectClient.retrieve_media_file()` | not needed | Media is explicitly excluded from hosted learning snapshots. |
| `AnkiConnectClient.add_tags()` | write-capable prohibited in hosted flow | Not exposed or called. |
| `AnkiConnectClient.remove_tags()` | write-capable prohibited in hosted flow | Not exposed or called. |
| `AnkiConnectClient.add_notes()` | write-capable prohibited in hosted flow | Not exposed or called. |
| `AnkiConnectClient.sync()` | write-capable prohibited in hosted flow | Not exposed or called; synchronization remains local/agent-owned. |
| `AnkiConnectClient.aclose()` | not needed | The application lifecycle owns gateway cleanup. |
| `AnkiRuntime.ensure_running()` | not needed | Calling it could launch Anki; this adapter is read-only and uses preflight only. |
| `AnkiRuntime.aclose()` | not needed | The application lifecycle owns runtime cleanup. |

The existing `oms_anki_agent.FullSnapshotExporter` and `SnapshotNote` remain available for the separate local snapshot/export flow but are not reused here because they retain full fields and media. This adapter emits only the approved learning fields.
