# 1.1 handoff

## Commit

- Branch: `sol1/source-trust`
- Commit: symbolic containing the Task 1.1 implementation; resolve after commit with `git rev-parse HEAD`.
- Tree: symbolic containing the Task 1.1 implementation; resolve after commit with `git rev-parse 'HEAD^{tree}'`.
- Base integration SHA: `82e22d7aa109a29a5fe4d7c26eb1672966317fbf` (tree `bc719a8644765e360c1a47c4a21b0747d8df8f64`).

## Deliverable

- Produced interfaces: `source_revision_id`, `evidence_id`, `sha256_file`, and `sha256_text` in `oms_hub.knowledge.ids`.
- Identity format: SHA-256 of UTF-8 input, base32 encoded lowercase without padding, truncated to 26 characters, with `sr_` and `ev_` namespaces. Component inputs are separated with NUL bytes as specified.
- Text hashing normalizes CRLF and CR to LF only; surrounding whitespace and all other text content are preserved.
- File hashing reads binary bytes in 1 MiB chunks and does not apply text normalization.
- No provider, schema, persistence, route, feature-flag, dependency, migration, or runtime behavior changed.

## Files

- Created: `src/oms_hub/knowledge/__init__.py`, `src/oms_hub/knowledge/ids.py`, `tests/knowledge/test_ids.py`, and this handoff.
- Intentionally not modified: provider contracts, fixtures, schemas, settings, routes/UI, databases/migrations, dependencies, CI, plans/design/manifest/ledger, prior handoffs, Anki, production/private data, and the primary checkout.

## Verification

- RED: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge/test_ids.py -q` — expected collection failure: `ModuleNotFoundError: No module named 'oms_hub.knowledge.ids'` before implementation.
- Focused GREEN: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge/test_ids.py -q` — `4 passed`.
- Affected contracts/fixtures/providers: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts/test_fixture_integrity.py tests/contracts/test_provider_contracts.py tests/contracts/test_schema_exports.py tests/providers/test_fake_provider.py -q` — `42 passed`.
- Contract/provider/feature regression: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts tests/providers tests/features -q` — `96 passed`.
- Quality: `PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check src tests scripts` — passed; `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src` — `Success: no issues found in 178 source files`; `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src/oms_hub/knowledge tests/knowledge/test_ids.py` — `Success: no issues found in 3 source files`.
- JavaScript: `node --test "tests/js/*.test.js"` — `168 passed`, 0 failed.
- Safe broad Python lane: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring python -m pytest -q -m "not windows_office" --deselect tests/document_processing/test_pdf_adapter.py` — exit 0; `1516/1521` selected, one skip observed. The known PyMuPDF crash file was excluded; no native Windows acceptance is claimed.
- Safety/scope: `git diff --check` passed after staging; changed-file scan found no private paths, credentials, Anki collections, URLs, or publication identifiers; base-to-HEAD scope was checked against the four Task 1.1-owned paths after commit.

## Data and migration

- New records: none.
- Backfill: none.
- Idempotency key: not applicable; all identifiers and hashes are deterministic.
- Rollback: remove the Task 1.1 files and handoff; no runtime or persisted-data rollback exists.

## Risks and follow-up

- Known limitations: native Windows acceptance remains outstanding; the inherited macOS/PyMuPDF signal-11 path remains excluded; no live provider, production, private-data, or Anki action was performed.
- The implementation intentionally performs no input validation beyond the requested hashing contract; callers provide source IDs, locators, and SHA-256 strings according to their owning domain contracts.
- Next task: Task 1.2 may consume these stable identity helpers.

## Fix Round 1

- Reviewer findings: the initial `sha256_file` body duplicated the existing `oms_hub.files.atomic.sha256_file`, and the initial tests did not pin the NUL-separated composition or independently prove that every identity component changes the result.
- Correction: `oms_hub.knowledge.ids.sha256_file` now directly re-exports `oms_hub.files.atomic.sha256_file`; `src/oms_hub/files/atomic.py` was not modified. Tests add independently derived literal source/evidence IDs plus source-document, file-digest, source-revision, locator, and content-digest sensitivity assertions.
- Reuse RED: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/knowledge/test_ids.py -q` — `5 passed, 1 failed`; the failure was `assert sha256_file is atomic_sha256_file`.
- Coverage baseline: `... python -m pytest tests/knowledge/test_ids.py -q -k 'uses_every_component'` — `2 passed` on the correct implementation.
- Temporary mutation checks (restored before commit): omitting `file_sha256` made `test_source_revision_id_uses_every_component_and_nul_separation` fail; omitting `locator` made `test_evidence_id_uses_every_component_and_nul_separation` fail. These were real failures, not fabricated RED evidence.
- Correction GREEN: `... python -m pytest tests/knowledge/test_ids.py -q` — `7 passed`.
- Correction remains stdlib-only and within the original four owned paths; no shared/central file, provider, production, Anki, or private data was changed.

## Review provenance

- Terra specification reviewer `/root/task_1_1_terra_spec`: **APPROVED** candidate `21969e175440f4a2a383b1bd92ffe1269b66da5e`; no correction findings. Historical RED and suite execution are evidenced by the report and handoff rather than the final diff.
- Terra quality/reliability/security reviewer `/root/task_1_1_terra_quality`: initial **CHANGES REQUIRED** on `21969e1` for duplicate `sha256_file` and insufficient exact/component ID tests; findings: `0 Critical, 2 Important, 0 Minor`.
- Fix Round 1 commit `a74c8d65ed5316c9008146c8142a0c1c01713103` addressed both findings. The same Terra scoped re-review found both **ADDRESSED**, with no new breakage or out-of-scope observations, and returned **APPROVED**.
- Reviewer-host behavioral read-only proof: HEAD, tree, and status were unchanged across reviews.
- No cross-workstream review was required.
- Next state: Task 1.1 awaits Workstream Sol final review and Program Sol-0 manifest transition. Task 1.2 is not claimed ready by this handoff.
