# Task 3.1 handoff — Ask StudyHub contracts

## Commit

- Branch: `sol3/ask-backend`
- Commit: `SELF` (resolve with `git rev-parse HEAD` after the implementation commit)
- Tree: `SELF_TREE` (resolve with `git rev-parse 'HEAD^{tree}'` after the implementation commit)
- Base SHA: `82e22d7aa109a29a5fe4d7c26eb1672966317fbf`
- Base tree: `bc719a8644765e360c1a47c4a21b0747d8df8f64`
- Planned subject: `feat: define Ask StudyHub contracts`

## Deliverable

Implemented strict, frozen Pydantic Ask contracts in the owned package:

- `AskMode` with `global`, `lecture`, `exam`, `quiz_pre_submit`, and `quiz_post_submit`.
- `AskPageContext` and `QuizPageContext` with immutable objective IDs and strict extra
  rejection.
- `AskRequest` preserving the supplied frozen `RetrievalScope` and its explicit
  `TruthMode.COURSE_ONLY` value.
- `AskThread` and `AskMessage` for scoped conversation identity.
- `GroundedClaim`, `CitationView`, and `GroundedAnswer` with claim evidence IDs,
  citation evidence IDs, insufficient-evidence state, safe-response reason, and provider /
  retrieval provenance IDs.

`QuizPageContext` rejects all known answer-bearing fields before submission, including
`correct_option_id`, `correct_answer_text`, `rationale`, and `is_correct`; `extra="forbid"`
rejects unknown fields as well. Post-submit context can carry those grading fields. Lists
accepted at the test/wire boundary are normalized to immutable tuples. The isolated
implementation imports and uses frozen provider `RetrievalScope`. Frozen shared
`EvidenceRef` and `AnswerEvent` remain unchanged; they are referenced by the formal v2
proposal and are not imported by `models.py`.

## TDD evidence

RED was captured before production files existed:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
  python -m pytest tests/ask/test_models.py -q
```

Result: test collection failed with exit `2`,
`ModuleNotFoundError: No module named 'oms_hub.ask'`.

After implementing only the owned Ask package, the focused suite was GREEN:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD \
  python -m pytest tests/ask/test_models.py -q
```

Result: `12 passed`.

## Verification

- Focused and affected contract tests: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/ask/test_models.py tests/contracts -q` — `53 passed`.
- Exact Ruff: `PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check src tests scripts` — passed.
- Source mypy: `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src` — passed, `178 source files`.
- Task-owned test mypy: `PATH=/tmp/studyhub-task01-venv/bin:$PATH MYPYPATH=$PWD/src mypy tests/ask/test_models.py` — passed, `1 source file`.
- Current schema reproducibility: exported all six schemas to a temporary directory and ran `diff -ru schemas <temporary-dir>` — exit `0`; current `schemas/ask-v1.json` remains SHA-256 `f7517bd19572724401e0b9c56adb03ce217d768d2a94092296497c59dcb2153c`.
- `git diff --check` — run after staging; must remain clean.
- Secret/private-content checks — run after staging; no credentials, private lecture text, Anki data, or production identifiers are present in the owned diff.
- The inherited full PyMuPDF crash path and native Windows acceptance were not run or claimed.

## Shared-file schema proposal status

The original in-place `ask-v1.json` rewrite is **REJECTED**, not pending. Program Sol-0
accepts the isolated implementation but rejects changing the frozen Gate-1 snapshot. The
formal versioned proposal is
[`docs/contracts/change-requests/3.1-ask-wire-schema-v2.md`](../../contracts/change-requests/3.1-ask-wire-schema-v2.md).

That proposal requests a new `schemas/ask-v2.json` with `$id` `ask-v2.json`, preserving
`schemas/ask-v1.json` byte-for-byte at SHA-256
`f7517bd19572724401e0b9c56adb03ce217d768d2a94092296497c59dcb2153c` and preserving its
public `AnswerEvent` envelope. It names only the Sol-0-owned future exporter/test/schema
targets, records exact v2 structural output and commands, and proposes
`studyhub-grounded-contracts-v2`.

Sol-0 approval remains **REQUIRED and PENDING** for shared integration. Sol-4 consumer-owner
reviewer `/root/task_3_1_sol4_consumer_review` returned **SOL-4 CONSUMER APPROVED** for exact
candidate `5c12301be439590079f03401c22e6ec1de1838b9` / tree
`658b5f49f2098b423a1234a508d3334920304b18`, additive `ask-v2` only, subject to the formal
proposal's conditions: Task 4.1 selects named `$defs` rather than the root union; Task 3.8
freezes schema version, endpoint request/response refs, SSE/NDJSON framing/content-type/
delimiters and known event payload shapes before Sol-4 integration; unknown events are
ignored/logged; malformed JSON and known events fail safely; missing shapes use the contract
protocol; and there is no browser-only split-brain validation. Frontend/schema integration
remains blocked until Sol-0 lands and verifies the shared changes. No exporter, contract test,
v1 snapshot, or v2 snapshot was applied here.

## Files

- Created: `src/oms_hub/ask/__init__.py`
- Created: `src/oms_hub/ask/models.py`
- Created: `tests/ask/test_models.py`
- Created: this handoff
- Not modified: `scripts/export_grounded_contract_schemas.py`, `tests/contracts/**`,
  `schemas/ask-v1.json`, provider/shared contracts, plans, manifest, dependency/lock files,
  central wiring, database/migrations, private data, Anki, production, and native Windows
  state.

## Data, security, and rollout

No database records, migrations, provider calls, network calls, Anki collection writes,
production writes, or feature-flag wiring were introduced. The pre-submit validator is a
fail-closed boundary: known answer/rationale fields and all unknown extras are rejected
when `submitted=False`. No answer text or private source content is logged or embedded in
these contracts.

## Self-review and concerns

- The implementation stays within the Task 3.1 owned model/package/test surface and uses
  the existing strict Pydantic 2 pattern.
- The frozen provider contracts are consumed unchanged; `AskRequest` structurally satisfies
  the empty `GroundedAnswerRequest` marker.
- No speculative persistence, route, provider, exporter, or schema infrastructure was added.
- Concern: Sol-4 approval is recorded for the exact additive candidate and conditions, but
  Sol-0 shared approval remains required and downstream browser/schema consumption remains
  blocked until Sol-0 lands/verifies the shared changes and the ordered Task 3.8 mappings are
  complete.

## Review record

The initial implementation candidate was:

- Commit: `02a9a3bb146ab1402f749b8dbe0211307740daae`
- Tree: `aedf5aaadab2530d3fc4880d409e066a8d2e2c23`

Terra specification reviewer `/root/task_3_1_spec_review` performed a behaviorally
read-only review. The initial result was **CHANGES REQUIRED** for two contract findings:

1. Quiz mode and submission state could disagree at the request/thread boundary, allowing
   a pre-submit mode to carry post-submit grading fields.
2. The base `AskPageContext` accepted `quiz_question` without the required quiz fields.

The corrected candidate is:

- Commit: `4ffca79d51ef24050dc48be8fd8d272a74466a57`
- Tree: `cd4deef4ed1dcf79ae3b0d39022191cb4c456afb`
- Affected tests: `57 passed`

The scoped Terra specification re-review found both findings **ADDRESSED**, no new
breakage, and unchanged candidate identity. A different Terra quality/reliability/security
reviewer `/root/task_3_1_quality_review` returned **APPROVED** with no issues; focused Ask
tests were `16 passed`, and identity was unchanged. Both reviewer hosts were behaviorally
read-only despite broad tool permissions.

The original Sol-0 in-place `ask-v1.json` rewrite is **rejected**, not pending. The formal
versioned v2 proposal is linked above and remains unapplied. Terra specification reviewer
`/root/task_3_1_proposal_spec` returned **APPROVED** after two fix rounds: the complete v2
schema finding and premature approval claim were addressed, with no Critical or Important
residuals; identity was behaviorally read-only. A different Terra quality/reliability/security
reviewer `/root/task_3_1_proposal_quality` returned **APPROVED** after the active/reserved
index, fail-fast, and pinned-v1-hash corrections, with no Critical or Important residuals;
identity was behaviorally read-only. Sol-4 consumer-owner reviewer
`/root/task_3_1_sol4_consumer_review` returned **SOL-4 CONSUMER APPROVED** for exact candidate
`5c12301be439590079f03401c22e6ec1de1838b9` / tree
`658b5f49f2098b423a1234a508d3334920304b18`, additive `ask-v2` only, with the conditions
listed in the formal proposal. Sol-0 approval remains **REQUIRED and PENDING**. This review
record does not authorize shared implementation, tag creation, integration, Task 4.1 or 3.2
work, push, or deploy. Candidate `4415402`'s conditional review comments and candidate
`51a89e1`'s **CHANGES REQUIRED** rollout-order finding remain review history, not current
approval. Native Windows acceptance and the inherited PyMuPDF crash lane remain unrun.
Inherited baseline concerns remain: `13` test-typing errors and `33` Markdown warnings. No
provider/live/private/production/Anki action, push, merge, or deploy was performed.

This review-record documentation commit uses the required subject
`docs: record Task 3.1 review evidence`. Its own commit and tree remain symbolic here
because a commit cannot embed its own SHA; resolve `SELF` and `SELF_TREE` after creation
with `git rev-parse HEAD` and `git rev-parse 'HEAD^{tree}'`.

This final proposal review-record update uses the required subject
`docs: record Ask v2 proposal approvals`. Its containing commit and tree remain symbolic
here because a commit cannot embed its own SHA; resolve `SELF` and `SELF_TREE` after
creation with `git rev-parse HEAD` and `git rev-parse 'HEAD^{tree}'`.
