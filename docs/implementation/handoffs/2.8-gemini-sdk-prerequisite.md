# Task 2.8 Gemini SDK prerequisite handoff

This is the Program Sol-0 shared-dependency prerequisite only. It is not the Task 2.8 live
smoke-test handoff and does not open Gate 2B.

## Commit

- Branch: `codex/sol0-gemini-sdk-prerequisite`
- Base integration SHA: `37c1b6ac572ecdc4549f4688ab5868c4a9a85240`
- RED commit: `715cb41a6801dc5f40a4ef06b1fcfda9fd1dd2a4`
- RED tree: `ff4ad6abf8488783e2388e8b2c25ca76148b2562`
- GREEN commit: `1ca3293c2eab803c5e697ec521c4f61370e509ed`
- GREEN tree: `5fdf174629fa05257e8b8cb30546075d98fed750`
- Handoff commit: `SELF` (resolve from the commit containing this file)
- Ancestry: the base integration SHA is an ancestor of GREEN.
- Final worktree status: clean after the containing handoff commit; verified SHA/status are reported
  by Program Sol-0 without amending this self-referential file.

## Deliverable

- Produced interface: exact runtime dependency `google-genai==2.14.0`.
- Lock convention: new root `uv.lock`, as directed by the reviewed Task 2.1 shared-file proposal;
  generated with `uv 0.11.32` using `uv lock` and containing artifact SHA-256 hashes.
- Contracts consumed: plan Global Constraints, Task 2.1 shared-file proposal, Task 2.8 boundary,
  machine-readable manifest, and `docs/implementation/handoffs/2.1.md`.
- Feature flags: none changed.
- Provider/model/API behavior: none implemented or invoked.
- Gate state: `LIVE_BLOCKED / GATE_NOT_OPEN`; Sol-2 still owns the opt-in live smoke adapter.
  Task 2.8 acceptance remains jointly owned by Sol-2 and Sol-9, with Terra review and Sol-0
  integration.

## Files

- Created: `uv.lock`, `tests/contracts/test_gemini_sdk_dependency.py`, this handoff.
- Modified: `pyproject.toml`, `docs/implementation/THIRD_PARTY_CODE_REUSE.md`.
- Intentionally not modified: provider/runtime/config/secret code, Task 2.8 smoke tests/scripts,
  CI, acceptance artifacts, manifest gate state, schemas, production data, and private fixtures.

## TDD evidence

- Guarantee: the official SDK is an exact direct dependency, the deprecated SDK is absent, the
  exact version is locked, and root lock metadata preserves the exact requirement.
- RED: `PYTHONPATH="$PWD/src:$PWD" /private/tmp/sol1-task16-full-venv/bin/python -m pytest tests/contracts/test_gemini_sdk_dependency.py -q` failed because the manifest lacked
  `google-genai==2.14.0`; checkpointed in the test-only RED commit.
- GREEN: the same focused test passed after the minimum manifest and lock change.
- The GREEN commit also contains Ruff's import-only formatting correction to the committed RED
  test; no assertion changed.

## Verification

- Focused dependency/release command,
  `pytest tests/contracts/test_gemini_sdk_dependency.py tests/v2/test_anydoc_release.py::test_release_metadata_pins_document_processors_and_keeps_python_312_range -q`:
  passed (`2 passed`).
- Affected contract suite, `pytest tests/contracts -q`: passed (`42 passed`).
- `UV_CACHE_DIR=/private/tmp/uv-cache-gemini-sdk uv lock --check`: passed; 94 packages resolved.
- `uv tree --locked --package google-genai --depth 1`: resolved `google-genai v2.14.0`.
- `ruff check src tests scripts`: passed.
- `mypy src`: passed for 229 source files.
- Full Python command,
  `python -m pytest -q -m "not windows_office"`, exited nonzero with 1 failed and 1 skipped; a
  separate collection check reported 2,286 selected tests, so 2,284 completed as passed. The sole
  failure was an unrelated, order-dependent
  `PytestUnraisableExceptionWarning` for an unclosed SQLite connection, reported against
  `tests/knowledge/test_routes.py::test_knowledge_routes_are_private_and_no_store`. The exact test
  passed immediately with
  `python -m pytest tests/knowledge/test_routes.py::test_knowledge_routes_are_private_and_no_store -q`
  (`1 passed`). An earlier run without the venv on `PATH` also exposed the inherited
  embedded-`python` environment dependency; its exact failure and the first transient SQLite
  warning both passed together after correcting `PATH` (`2 passed`). No changed runtime file is
  loaded by these tests, and the independently reviewed dependency scope has no database behavior.
- Live/manual provider test: not run; prohibited for this prerequisite.
- Network/private data: package metadata was resolved only for `uv lock`; no Gemini endpoint,
  credential, or private source was accessed.

## License and rejection analysis

- Official upstream tag `v2.14.0` declares version `2.14.0`, Python `>=3.10`, and Apache-2.0:
  `https://github.com/googleapis/python-genai/blob/v2.14.0/pyproject.toml`.
- This prerequisite neither vendors nor redistributes SDK source. Any future bundled redistribution
  must preserve applicable Apache-2.0 license and NOTICE materials. Example terms must be reviewed
  if a later task copies example code.
- Existing standard-library and repository HTTP/validation/retry dependencies do not provide the
  official typed File Search stores, files, operations, interactions, or compatibility surface.
  Hand-rolling it would duplicate the approved maintained SDK.
- Deprecated `google-generativeai` was rejected and is guarded by the dependency contract test.

## Independent review

- Specification Terra: `APPROVED` on `37c1b6ac..1ca3293`; scope, ownership, TDD, exact pin/lock,
  and license/rejection requirements satisfied. Required boundary retained: do not claim Task 2.8
  or Gate 2B completion.
- Quality/security Terra: `APPROVED` on `37c1b6ac..1ca3293`; no Critical, High, Medium, or Low
  findings. Offline lock checks passed for Python 3.12 and 3.13; hashes, platform markers, exact
  metadata, supply-chain provenance, and secret/private-data boundaries were accepted.

## Data and migration

- New records/backfill/idempotency key: none.
- Data or schema migration: none.
- Rollback: revert the handoff, GREEN, and RED commits in reverse order (or otherwise remove the
  contract test with the dependency). The revert commits preserve the RED checkpoint in Git history.

## Risks and follow-up

- The first root lock captures the whole project graph; future dependency changes must run
  `uv lock` and review the resulting diff.
- Task 2.8 remains blocked on Sol-2's adapter/live-contract work, approved secret injection, and
  the plan's other Gate 2B acceptance requirements.
- No older HELD shared proposal was applied.
