# 5.3 handoff

## Commit

- Branch: `sol5/board-questions`.
- Base integration SHA: `4f5c687edffbabb08321515c3e505e41d3ee3888`.
- Base integration tree: `cd12cb0896a66986dd4d7bcd9bb7902836128079`.
- RED test checkpoint: `890ed64` (`test: add question-v2 boundary cases`).
- Implementation/test commit: `0796cfc` (`feat: enforce question-v2 authority boundary`).
- Boundary fix commit: `ac8b6221a625a69b9104f755ea3e05da4f8500bd` (tree
  `7cbc18cd9b7f369535997094795bd17b16aeb246`); no amend.
- Governance documentation is committed separately after this implementation
  handoff; the final report records its exact SHA/tree without self-reference.
- Prior direct question-v1 implementation remains superseded and unaccepted:
  `95304880dd7c1abce0733d792001bfbbded3ad0d`.
- Owner: Sol-5 board-question workstream; fresh Luna correction.
- Contract owner: Sol-0 for any shared activation.

## Deliverable

- Produced interfaces:
  `QuestionMode`, `QuestionStatus`, `QuestionClaimRole`, `QuestionOption`,
  `QuestionClaim`, `BoardQuestionDraft`, `QuestionValidationResult`,
  `QuestionVersion`, frozen `QuestionResolution`,
  `QuestionResolutionProvider`, `QuestionResolutionFailure`,
  `QuestionResolutionError`, and `resolve_question_version`.
- `QuestionVersion.question_version_id` is required, nonblank, and immutable;
  `schema_version` is required and constrained to literal `question-v2`.
- The resolver accepts one canonical ID, obtains one authoritative resolution
  or absence through a Protocol, and fails closed for absent, mismatched,
  stale, unapproved, unverifiable, missing-objective, missing-hash, and
  malformed-record states. Approved objective IDs must be an actual tuple of
  nonblank strings; malformed containers are `INVALID_RECORD` and are not
  normalized.
- Contracts consumed: existing Pydantic question models, Gate-1 frozen schema
  record, in-memory `TypeAdapter` generation, and plan §§3.4, 12.2, and 14.
- Contracts produced: strict in-memory question-v2 model/schema boundary and
  the abstract question-resolution record/provider/adapter boundary; no shared
  active contract is registered.
- Feature flags: none; no flag is added, changed, or enabled.
- Schema/prompt/model versions: frozen `question-v1.json` remains reserved;
  `question-v2.json` is an in-memory candidate only and is not activated.
- Candidate v2: 7,557 UTF-8 bytes, SHA-256
  `a3de074e74d19066078cb4eb857a5b29a5d78db19edced24310ad6637b1778d6`.
- Frozen v1: 293 bytes, SHA-256
  `968449d9dca8da71a28658360fe6a2d8e61cf35e49c5d8a9ab6e7a4564e7eb9d`.
- Activation: candidate remains unapplied; renewed Terra, Sol-6, Sol-10, and
  Sol-0 review fields are `PENDING` in CP-0001.

## Files

- Created:
  `src/oms_hub/questions/resolution.py`,
  `tests/questions/test_resolution.py`.
- Modified:
  `src/oms_hub/questions/models.py`,
  `src/oms_hub/questions/__init__.py`,
  `tests/questions/test_models.py`,
  `docs/implementation/contract-proposals/CP-0001-question-v2.md`, and this
  handoff.
- Ignored evidence updated:
  `.superpowers/sdd/2026-08-20-study-hub-grounded-adaptive-learning/task-5.3-v2-boundary-report.md`
  and `progress.md`.
- Intentionally not modified: `schemas/question-v1.json`,
  `scripts/export_grounded_contract_schemas.py`,
  `tests/contracts/test_schema_exports.py`, shared contracts, routes,
  persistence, generation, dependencies, flags, manifest, integration or
  primary checkout, Task 5.2, Task 5.4, providers, private/production data,
  Anki, deployment, and external state.

## Verification

- TDD RED:
  `PYTHONPATH=$PWD/src:$PWD /tmp/studyhub-task01-venv/bin/python -m pytest tests/questions/test_models.py --override-ini addopts= -q`
  — `6 failed, 35 passed`; the failures were the intentionally missing
  canonical model field/strict v2 boundary. Resolver collection also failed
  before implementation because the exported boundary did not exist.
- Focused GREEN:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD /tmp/studyhub-task01-venv/bin/python -m pytest tests/questions/test_models.py tests/questions/test_resolution.py --override-ini addopts= -q`
  — `56 passed`.
- Candidate generation: two independent in-memory `json_schema()` calls,
  `$schema`/`$id` assignment, sorted serialization, and byte comparison pass;
  exact candidate is 7,557 bytes with the hash above.
- Central schema exporter tests (exporter and central tests unmodified):
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD /tmp/studyhub-task01-venv/bin/python -m pytest tests/contracts/test_schema_exports.py --override-ini addopts= -q`
  — `2 passed in 0.19s`; exporter and central test are unmodified.
- Relevant contract/provider/feature suites:
  `... pytest tests/contracts tests/providers tests/features --override-ini addopts= -q`
  — `96 passed in 1.97s`.
- Ruff: `ruff check src tests scripts` — all checks passed.
- Source typing: `mypy src` — success, no issues in 181 source files.
- Task-test typing: `MYPYPATH=$PWD/src mypy tests/questions/test_models.py tests/questions/test_resolution.py`
  — success, no issues in 2 files.
- Frozen-v1 identity: `git diff --exit-code 82e22d7:schemas/question-v1.json schemas/question-v1.json`
  — exit 0; `sha256sum` is
  `968449d9dca8da71a28658360fe6a2d8e61cf35e49c5d8a9ab6e7a4564e7eb9d`.
- `git diff --check` — pass; scope/secret scans are recorded in the final
  v2-boundary report with actual results.
- Full workstream: relevant contracts/providers/features suite passed; no
  broader unrelated workstream or integration suite was required.
- Live-provider test run: no. Manual/native/Windows/production/Anki/deployment
  lanes were not run by design.
- Git status: clean after the separate governance documentation commit.

## Data and migration

- Data migration impact: none.
- New records: none; the resolution record is an in-memory immutable value.
- Backfill: none; Gate-1 v1 was reserved and inactive.
- Idempotency key: not applicable.
- Stale/rollback behavior: resolver rejects stale or unverifiable authority;
  retain frozen v1 and reject/defer candidate v2 before activation. If a later
  activation is approved, revert exporter/test/v2-snapshot changes as one unit
  without rewriting v1.
- Rollback: before activation, reject/defer the candidate and retain frozen v1;
  after a future activation, revert the exporter/test/v2-snapshot unit without
  rewriting v1.

## Risks and follow-up

- Known limitation: no runtime consumer adopts the resolver in this lane; Sol-6
  and Sol-10 must complete renewed consuming-owner reviews and their own
  product-boundary tests before activation.
- Contract proposal: `docs/implementation/contract-proposals/CP-0001-question-v2.md`.
- Review fields: renewed Terra specification, Terra quality, Sol-6, Sol-10,
  Workstream Sol, and Sol-0 reviews are all `PENDING`; prior findings are
  retained only as resolved-target context in CP-0001.
- Next unblocked tasks: none from this correction; Task 5.2 and Task 5.4 remain
  blocked on Gate 2A / Task 1.8.
