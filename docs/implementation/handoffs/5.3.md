# 5.3 handoff

## Commit

- Branch: `sol5/board-questions`.
- Base integration SHA: `fa0ca5c0d9fce5366d1e724e5c9017069ea0dccc`.
- Base integration tree: `554bcfb450c8666116ea9b340c81f559987b9e68`.
- Correction implementation/test/snapshot commit:
  `873154c517fe2b0166779c1122fd691652d6b485`.
- Correction implementation/test/snapshot tree:
  `6ec0116579af3b4deb51acd0d2b98e16124b79b9`.
- Governance documentation commit: `SELF` (this documentation commit; resolve
  with `git rev-parse HEAD` after commit, without amending).
- Prior direct question-v1 implementation is superseded and unaccepted:
  `95304880dd7c1abce0733d792001bfbbded3ad0d`.

## Owner

- Owner: Sol-5 board-question workstream; fresh Luna correction.
- Contract owner: Sol-0 for any shared activation.
- Review state: PENDING independent Terra specification review, Terra quality
  review, consuming-owner approval, and final Workstream Sol review.

## Deliverable

- Produced interfaces: the previously reviewed isolated question models remain
  complete: `QuestionMode`, `QuestionStatus`, `QuestionClaimRole`,
  `QuestionOption`, `QuestionClaim`, `BoardQuestionDraft`,
  `QuestionValidationResult`, and `QuestionVersion`.
- Consumed interfaces: Gate-1 frozen schema record, existing Pydantic models,
  existing deterministic direct-schema helper, and the contract-change rules in
  the implementation plan (§3.4, §12.2, §14).
- Feature flags: none; no flag is added, changed, or enabled.
- Schema/prompt/model versions: tracked `question-v1.json` remains the reserved
  Gate-1 snapshot; a `question-v2.json` schema is a deterministic in-memory
  candidate only and is not activated.
- Activation state: isolated models are complete; question-v2 activation is
  unapplied pending the CP-0001 review/approval protocol.
- Current frozen v1: 293 bytes, SHA-256
  `968449d9dca8da71a28658360fe6a2d8e61cf35e49c5d8a9ab6e7a4564e7eb9d`.
- Candidate v2: 7,379 bytes, SHA-256
  `0f535c43fc1de3eadc61970f615370d3b23bc1046c7bef5f7bdeb01419a8294d`.

## Files

- Created: `docs/implementation/contract-proposals/CP-0001-question-v2.md`.
- Modified in the implementation commit: `schemas/question-v1.json` and
  `tests/questions/test_models.py`.
- Modified in this governance commit: this handoff.
- Ignored evidence files updated with this correction:
  `.superpowers/sdd/2026-08-20-study-hub-grounded-adaptive-learning/task-5.3-report.md`
  and `progress.md`.
- Intentionally not modified: `src/oms_hub/questions/models.py`,
  `scripts/export_grounded_contract_schemas.py`,
  `tests/contracts/test_schema_exports.py`, shared provider contracts,
  dependencies, routes, flags, manifest, integration checkout, Task 5.1,
  Task 5.2, Task 5.4, provider/network paths, private/production data, Anki,
  deployment, and all other files.

## Verification

- TDD RED against the authorized pre-correction head: after the direct test was
  adjusted, `tests/questions/test_models.py` reported `1 failed, 33 passed`;
  the failure was the required frozen-v1 assertion (`7379` actual bytes versus
  `293` expected).
- TDD GREEN after restoration:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/questions/test_models.py --override-ini addopts= -q`
  — `34 passed in 0.11s`.
- Frozen snapshot identity:
  `git diff --exit-code 82e22d7:schemas/question-v1.json schemas/question-v1.json`
  — exit `0`, no output; `sha256sum schemas/question-v1.json` —
  `968449d9dca8da71a28658360fe6a2d8e61cf35e49c5d8a9ab6e7a4564e7eb9d`.
- Candidate direct generation: two independent in-memory
  `_candidate_v2_schema_bytes()` calls each run `json_schema()`, apply
  `$schema`/`$id`, and sort-serialize a fresh `$id=question-v2.json` candidate;
  their bytes are equal. The candidate is `7,379` bytes with SHA-256
  `0f535c43fc1de3eadc61970f615370d3b23bc1046c7bef5f7bdeb01419a8294d`.
- Central exporter/schema test, with exporter and central test unmodified:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts/test_schema_exports.py --override-ini addopts= -q`
  — `2 passed in 0.42s`.
- Relevant contract/provider/fixture/features suite:
  `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts tests/providers tests/features --override-ini addopts= -q`
  — `96 passed in 2.70s`.
- Ruff: `PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check src tests scripts`
  — `All checks passed!`.
- Source typing: `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src` —
  `Success: no issues found in 180 source files`.
- Task-test typing: `PATH=/tmp/studyhub-task01-venv/bin:$PATH MYPYPATH=$PWD/src mypy tests/questions/test_models.py` —
  `Success: no issues found in 1 source file`.
- `git diff --check` passed; current correction scope is the two implementation
  files plus the proposal and handoff. No secret/private-content markers were
  found in the correction diff.
- Not run by design: Windows/PyMuPDF crash paths, live provider/network calls,
  private or production data, Anki mutation, routes, deployment, push, merge,
  integration, primary-checkout mutation, Task 5.2, and Task 5.4.

## Data and migration

- New records: none.
- Backfill: none; v1 was reserved and inactive.
- Idempotency key: not applicable.
- Stale/rollback behavior: keep v1 frozen; reject/defer v2 until CP-0001
  approvals; if later activation lands, revert the exporter/test/v2 snapshot as
  one reviewed unit without rewriting v1.
- No persistence, route, provider, feature flag, production, or Anki state was
  changed.

## Risks and follow-up

- Known limitation: the candidate v2 is not a shared active contract. The
  central exporter intentionally continues emitting reserved question-v1 until
  Sol-0 approves and lands the exact future targets in CP-0001.
- Contract proposal: `docs/implementation/contract-proposals/CP-0001-question-v2.md`.
- Required reviewers: independent Terra specification review, independent
  Terra quality review, Sol-0 contract-owner review, one consuming owner from
  Sol-6 or Sol-10, and final Workstream Sol review.
- Consuming-owner approval: `PENDING`; reviewer, result, evidence commit/tree,
  and scope confirmation remain explicit fields in CP-0001.
- Next unblocked task: none from this correction; Task 5.2 and Task 5.4 remain
  blocked on Source Trust Gate 2A / Task 1.8.

## Governance correction record

- Gate-1 record: `artifacts/acceptance/grounded-learning/gate-1.json`, state
  `open`.
- Contract tag target: `studyhub-grounded-contracts-v1`, annotated object
  `a2636bbeb84d2143685c3555e9a3f74ccb8380d0`, peeled target
  `60a5f3ec873f982bca14d3507d719eb9927a8f1a`.
- Original direct `question-v1` edit: explicitly **superseded and not accepted
  shared state**. The generated model schema is not restored under v1 and is
  not a migration source.
- Conflict ruling: §3.4 requires a versioned contract proposal after Gate 1;
  §12.2 requires Sol-0 plus one affected consuming Sol approval; §14 requires
  exact handoff evidence. The correction preserves the frozen v1 bytes and
  moves the reviewed model output to an unapplied, proposal-only v2 candidate.
- Review fields: Terra specification `PENDING`; Terra quality `PENDING`;
  Workstream Sol `PENDING`; Sol-0 `PENDING`; consuming owner `PENDING`.
