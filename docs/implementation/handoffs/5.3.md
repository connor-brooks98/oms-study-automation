# 5.3 handoff

## Commit

- Branch: `sol5/board-questions`.
- Task-final implementation/fix commit: `95304880dd7c1abce0733d792001bfbbded3ad0d`.
- Task-final implementation/fix tree: `5c6f3d16c2cfa4ff302d563f46eabbef9ccab0ed`.
- Base commit: `b887601e624da99c8f546b77445c6a2cec95b5a5`.
- Base tree: `86425e9d7601d1000fb952e6e268a88d24a1272d`.
- Implementation history: initial `78ac7ef9e8475230dcc840046dc7a0bdecf0444c` (`6c7bc932e9699b9ec6c259a8c732c492b5501fd6`), provenance correction `0de56d4a27a0d39f724b3b53e64dd01ae3ccec31` (`f740be927535b56d6fa1412cb263944ab5c9c89c`), and this final nonblank-schema correction `95304880dd7c1abce0733d792001bfbbded3ad0d` (`5c6f3d16c2cfa4ff302d563f46eabbef9ccab0ed`).
- The handoff is a separate documentation/review record; its commit does not replace the implementation identity above.
- Specification review history: initial CHANGES REQUIRED by `/root/task_5_3_terra_spec` because `QuestionVersion` permitted missing derivative provenance; correction round 1 was re-reviewed APPROVED. Fresh quality review by `/root/task_5_3_terra_quality` was APPROVED with identity unchanged and clean before this final review wave.

## Deliverable

- Added immutable Pydantic contracts `QuestionMode`, `QuestionStatus`, `QuestionClaimRole`, `QuestionOption`, `QuestionClaim`, `BoardQuestionDraft`, `QuestionValidationResult`, and `QuestionVersion`.
- Modes/statuses/claim roles use the exact approved values. Options and claims require nonblank IDs/text/rationale and nonempty evidence ID arrays with nonblank items. Drafts require four or five options, unique option IDs, an existing `correct_option_id`, difficulty 1–5, and at least one nonblank objective ID.
- `QuestionVersion` requires nonempty source revisions and nonblank question/prompt/schema/model/hash provenance; evidence IDs remain optional until the evidence-packet task supplies them. Models remain strict, frozen, extra-forbidden, and normalize JSON lists to tuples.
- The final fix reuses `_NonblankString = Annotated[str, Field(min_length=1, pattern=r"\S")]` for every scalar and string-array item already rejected by runtime `_nonblank`/`_nonblank_ids` validation. `schemas/question-v1.json` therefore carries `minLength: 1` plus `pattern: "\\S"` for those values, including provenance, IDs, and optional string arrays.
- The deterministic schema root is an `anyOf` union of `BoardQuestionDraft`, `QuestionValidationResult`, and `QuestionVersion`; current bytes are `7,379`, SHA-256 `6c4424e681da3b544d8efeaeb544d489eab11cfb9abba92a3940c9553210a8fe`.

## Files

- Implementation/fix commit files: `src/oms_hub/questions/models.py`, `tests/questions/test_models.py`, and `schemas/question-v1.json`.
- Task 5.3 implementation files retained from the earlier commits: `src/oms_hub/questions/__init__.py`, plus the same model, test, and schema files.
- Separate documentation/review record: `docs/implementation/handoffs/5.3.md`.
- Ignored reports: `.superpowers/sdd/2026-08-20-study-hub-grounded-adaptive-learning/task-5.3-report.md` and the final fix-wave report.
- No provider contracts, dependencies, exporter, central tests, manifest, routes, storage, private fixtures, production data, Anki data, or Task 5.1 implementation files changed.

## Verification

- Initial implementation RED: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/questions/test_models.py -q` failed during collection with `ModuleNotFoundError: No module named 'oms_hub.questions'`; initial GREEN was `15 passed in 0.07s`.
- Provenance correction RED against `0de56d4`'s parent: the focused suite reported `7 failed, 21 passed`; GREEN after that correction was `28 passed in 0.07s`. The seven failures covered missing source revisions, prompt/schema/model versions, input/output hashes, and an empty source revision tuple. The prior corrected schema was `6,451` bytes with SHA-256 `785e35fc3deccf8ac7c9e36ebf56c18f91a7704db4b59f2cc7d2d07f96b5e405`.
- Final fix-wave TDD RED before changing model generation: after adding six representative JSON-schema-boundary invalid cases, the current candidate reported `6 failed, 28 passed in 0.10s`; whitespace-only scalar and string-array items were accepted by the old generated schema.
- Final fix-wave GREEN: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/questions/test_models.py --override-ini addopts= -q` — `34 passed in 0.08s` on the final rerun. The boundary helper validates object/array/string/integer/boolean/ref/anyOf constraints against the generated schema without a new dependency.
- Direct reproducibility: `question-v1 direct model reproducibility: pass (7379 bytes, sha256 6c4424e681da3b544d8efeaeb544d489eab11cfb9abba92a3940c9553210a8fe)`.
- Relevant contract/provider/fixture/features suite: `87 passed in 1.82s`.
- Task 5.1 focused compatibility suite: `31 passed in 1.23s`; affected outline/quiz/worker compatibility suite: `125 passed in 13.64s`.
- Ruff: `All checks passed!`; `mypy src`: `Success: no issues found in 180 source files`; both task test files: `Success: no issues found in 2 source files`.
- `git diff --check` and the changed-file scope/safety scan were clean; no credential/private-path/Anki markers were found. Native Windows, provider/network, private-data, production, Anki, deployment, and the known PyMuPDF crash path were not run.
- Workstream review history retained: the full prior five-file base-to-head diff was inspected with owned scope only; structural/provenance validation, direct schema snapshot, strict/frozen behavior, and review identities were clean before this final fix wave.

## Data and migration

- No persistence, database, migration, route, provider, production, Anki, or external-service mutation was added. The schema/model correction is wire-contract-only and deterministic.
- JSON list inputs continue to normalize to immutable tuples; generated question output remains derivative and no private/source content is embedded in the schema or tests.

## Risks and follow-up

- The Sol-0-owned central exporter remains an explicit integration hold. It must import `BoardQuestionDraft`, `QuestionValidationResult`, and `QuestionVersion`; add the TypeAdapter union to `_active_schemas()` with the existing draft-2020-12 `$schema`/`$id`; remove only `question-v1.json` from `_RESERVED`; preserve mastery/practice/journal reserved output; and update `tests/contracts/test_schema_exports.py` for active question output and repeated byte equality.
- The hold is reproducible and intentionally retained: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts/test_schema_exports.py --override-ini addopts= -q` reports `1 failed, 1 passed` because the unchanged exporter emits the reserved question namespace. It is a shared Sol-0 integration concern, not an owned-lane pass or defect.
- `QuestionValidationResult.codes` remains generic because Task 5.6 owns its vocabulary; claims may remain empty because semantic coverage belongs to Task 5.6; evidence IDs remain optional until Task 5.4. Each is a bounded later schema/model change if requirements change.
