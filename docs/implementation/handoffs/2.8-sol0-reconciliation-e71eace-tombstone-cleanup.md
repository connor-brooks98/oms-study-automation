# Task 2.8 Sol-0 terminal-tombstone cleanup correction

Status: **OFFLINE GREEN / INDEPENDENT REVIEWS BLOCK / OWNER CORRECTION REQUIRED /
NO COMPOSITION / NOT EXECUTED / AUTHORIZATION NOT CONSUMED / GATE_NOT_OPEN**

Sol-0 continued from exact reviewed Sol-2 disposition handoff
`8b10617290e62e6276e6f448c11670cdc83b8c03`, candidate
`26fa70a47f6b6d1cb32683ec530aa4e23e876fc7`, tree
`542281913bd263d6fca32744f04eac029d9d6dfe`. Its exact content is retained locally through
cherry-picked commits `76f964f`, `03cb134`, and `a3e2e8d`; no merge occurred.

## Offline TDD and verification

- RED `a69a2c4046e99735058d018027d0231fdaba3dc8` modeled root-present,
  evidence-path-absent and failed on real PowerShell 5.1 with
  `cleanup_directory_unavailable` under the old present-directory invariant.
- Test-only fixture corrections `dce3f69` and `64f4246` materialized the already-required
  synthetic launcher and isolated `%TEMP%` residue between fault cases.
- Review RED `95b3b1ce3dfaf7d703fb8ec46303ba9a047bc1f6` proved an approved-base
  junction was incorrectly accepted, failing with `ambiguous_cleanup_did_not_retain_task_and_root`.
- GREEN `da5a7a38f51a4f075380ff2c576a9f0a06e04f09`, tree
  `50fca253a9687a6c00b2fd1d51de2c3dd9658098`, binds the terminal tombstone,
  five published hashes, 674/11,868 manifest lines, 19 top-level entries, exact ACL, zero
  reparse points, private task/diagnostic/scratch/evidence-path absence, Hub/schema-29 health,
  reconciliation task fields, and canonical target disjointness.

Exact offline hashes:

- cleanup script: `4526603656c97ea7cc064d1f7cffbae486896470363a5929a376eee2a88f7560`;
- PowerShell harness: `5584ab0bb935fd9ab75dd5fffaf512e9a266c1686b53acc1316a0afa7542c712`;
- Python test: `15ce84919c105e100f562eaafba5100a9a013a152bd76a6735706171fe7591b7`.

The focused cleanup tests passed 2. The affected Task 2.8 lane passed 136 with one intentional
live skip. Real Windows PowerShell 5.1 emitted
`TASK28_RECONCILIATION_CLEANUP_HARNESS_VERIFIED`. Ruff, strict file-target mypy, and diff
hygiene passed. A repository-wide diagnostic run reached 2,409 passed and three skipped with 18
unrelated environment/order failures from an unavailable `python` shim, absent optional `anydoc`,
sandboxed macOS Keychain access, and an order-dependent provider-import assertion.

## Independent exact-candidate review

Specification and quality/security both **BLOCK** exact candidate `da5a7a3`, tree `50fca253`.
Their blockers cannot be resolved by Sol-0 without changing the reviewed owner contract:

1. The immutable retained task executes exact system
   `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`. It therefore cannot also satisfy
   the disposition's requirement that every path-bearing task value be within the reconciliation
   root. The owner must exempt only this exact canonical executable, while requiring exact file
   identity, trusted-base ancestor reparse checks, no alias, and tombstone disjointness. The
   launcher argument must remain inside the canonical reconciliation root and the working
   directory equal to or inside it.
2. Task Scheduler unregistration and recursive filesystem removal are separate, non-transactional
   mutations. After successful unregistration, Sol-0 cannot guarantee that both task and root
   remain on a later failure without an independently authorized re-registration contract, which
   is itself fallible. The owner must define retain-both as applying through final pre-unregister
   validation. After unregistration, a failure must retain the root and tombstone, stop before
   further mutation, and record sanitized failure-stage evidence.
3. The owner must explicitly accept the narrow check-to-call race under the exact protected-ACL,
   single-invocation threat model or require a separately reviewed handle/locking design.

After that correction, Sol-0 still owns a new committed review RED/GREEN that repeats the complete
root/canonical-path/reparse/manifest/result/status/task-or-task-absence/tombstone/Hub checks
immediately before each mutation.

No cleanup composition was built or staged because both required offline reviews are `BLOCK`.
There is consequently no truthful composition hash or authorization wording yet. No cleanup ran;
only allowlisted tombstone metadata was inspected; no private/source/evidence content was read;
the tombstone was not restored or mutated; no key/provider boundary was crossed; and no
authorization was consumed.
