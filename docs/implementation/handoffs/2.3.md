# Task 2.3 handoff — Gemini store and document lifecycle

## Identity

- Worktree: `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol2`
- Branch: `sol2/gemini-indexing`
- Base commit: `3b25cd080d32dde12929aee2aa1b7dc676c2f88d`
- Base tree: `5bf6733e093d14d5afc96a66b18068138ce5eda4`
- Implementation commit: `SELF` (resolve from the commit containing this handoff)
- Implementation tree: `SELF_TREE` (resolve from the commit containing this handoff)
- Self-reference rule: do not amend the containing commit to insert its own identity.

## Produced interfaces

- `oms_hub.indexing.models.IndexState`
- `oms_hub.indexing.models.validate_transition`
- `oms_hub.indexing.models.StoreKey`
- `oms_hub.indexing.models.ProviderStore`
- `oms_hub.indexing.models.ProviderDocument`
- `oms_hub.indexing.models.IndexJob`
- `oms_hub.indexing.repository.IndexRepository`
- `oms_hub.providers.gemini.file_search.GeminiFileSearchAdmin`

`StoreKey.course(course_id, exam_id)` renders exactly
`course:{course_id}:exam:{exam_id}`. `StoreKey.literature(course_id)` renders
exactly `literature:{course_id}`. Identifiers are bounded, slug-safe, and
cannot contain the namespace delimiter. Display names are derived only from
the validated local key, sanitized, and capped at 100 characters. Provider
store/document identities are persisted separately and are never derived from
or reused as local names.

The explicit state graph covers the required indexing phases, retryable and
terminal failure, stale generation recovery, and deletion. A failed remote
document delete deliberately leaves the durable document in `DELETING`; a
later retry can finish `DELETING -> DELETED` without falsely claiming success.
Every unlisted transition, including self-transitions, is rejected.

The repository uses the existing `Base` and `Database.session()` conventions.
`ProviderStoreModel`, `ProviderDocumentModel`, and `IndexJobModel` are additive
owned rows. Store generations use a unique `(store_key, generation)`, a unique
provider identity, and a partial unique current-key index, allowing a stale
orphan and a different current replacement to coexist. Document and job
identity/idempotency constraints are database-enforced. Metadata is validated
JSON and round-tripped canonically.

`GeminiFileSearchAdmin` uses one `async with GeminiClientFactory.client()` per
operation. `ensure_store` checks the current opaque provider identity, commits
the old generation `STALE` before replacement after a translated 404 (or a
missing result), and leaves that stale state committed when replacement
creation fails. Non-404 provider failures do not mutate local state. Listing
only maps remote documents into local records; upload/import/wait behavior is
not included. Store ensures use one lazy event-loop-local lock per admin to
serialize the full check/create/persist critical section; this fake/local
ceiling serializes all keys for that admin and should become per-key or
distributed coordination only if throughput or multi-process activation
requires it. Delete calls the injected async document surface and only marks
`DELETED` after the actual delete invocation succeeds, or after that invocation
returns translated 404 remote absence. Client acquisition, API resolution, and
context-close failures propagate redacted and leave the document `DELETING`.

## TDD evidence

RED, before the owned indexing package existed:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH="$PWD/src:$PWD" \
  python -m pytest tests/indexing/test_repository.py -q
```

Collection failed with `ModuleNotFoundError: No module named
'oms_hub.indexing'`. The remote-admin suite produced the same expected RED
collection failure before `file_search.py` and the indexing package existed.

Initial implementation GREEN focused run:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH="$PWD/src:$PWD" \
  python -m pytest tests/indexing/test_repository.py \
  tests/providers/gemini/test_file_search_store.py -q
```

Result: `25 passed` on the initial implementation tree. The latest focused
run, including all bounded corrections, is recorded below as `39 passed`.

Coverage includes the exhaustive state matrix, exact key parsing and bounds,
display-name safety, schema registration without constructor DDL, round-trip
store/document/job persistence, generation history, uniqueness/idempotency,
remote-present idempotency, 404 orphan replacement, failed replacement,
non-404 fail-closed behavior, replacement identity difference, repeated
ensure, deterministic document listing, successful delete, failed delete,
one client context per operation, and provider-payload/config-secret redaction.

## Verification and safety

The following local checks passed on the latest implementation tree:

- Focused Task 2.3 tests: `39 passed`.
- Affected Gemini/provider-contract, migration, and LLM repository tests:
  `96 passed`.
- Ruff on the owned source/tests: passed.
- Strict source mypy on `src/oms_hub/indexing` and
  `src/oms_hub/providers/gemini/file_search.py`: passed.
- Strict task mypy on the owned source and tests: passed.
- Local model-registration test after explicit owned-model import: passed.
- No SDK import or live/network call was made; the fake SDK seam was used.
- No API key, raw provider payload, uploaded/private source, Anki collection,
  production data, deployment, secret store, or central file was touched.
- The exact tracked implementation scope is seven files: the three indexing
  files, `file_search.py`, the two focused test files, and this handoff.

## Held central persistence proposal

Production activation, migration registration, and provider settings remain
`HELD`. The current application imports the
central `Base` and calls `Database.migrate()`, but it does not import the new
owned ORM module. Local tests explicitly import `oms_hub.indexing.models`
before `Database.create_schema()` as authorized. Sol-0 must review this exact,
unapplied proposal before production/bootstrap use:

Target: `src/oms_hub/migrations.py`, import block and schema constant.

```diff
@@
 import oms_hub.anki.models  # noqa: F401
+import oms_hub.indexing.models  # noqa: F401
@@
-LATEST_SCHEMA_VERSION = 22
+LATEST_SCHEMA_VERSION = 23
```

Reason: importing the owned ORM module registers `provider_stores`,
`provider_documents`, and `index_jobs` on `Base.metadata` before the existing
`database.create_schema()` call. Advancing the version records the additive
schema boundary for existing databases; no destructive alteration or data
backfill is required. `app.py` and `cli.py` already reach `Database.migrate()`
through `migrations.py`, so no application bootstrap hunk is proposed.

Compatibility/conflict: this is additive and leaves existing tables/data
untouched. Sol-0 owns central migrations and must compose it with any other
schema-version proposal, rerun migration/bootstrap and existing DB tests, and
review the generated schema. Rollback is a separate reviewed migration action:
disable the feature, restore a verified backup if tables must be removed, and
then revert the import/version hunk; do not delete live tables as part of this
local task. Proposal is held and unapplied here.

## Downstream boundary

Task 2.4 may consume these records and the admin client lifecycle for resumable
upload/import/wait behavior. This handoff intentionally does not implement
`upload_file`, `import_file`, operation polling, ingestion workers,
reconciliation, rebuild, health, live contract tests, application wiring, or
production activation. Task 2.4 has not begun.

## Held CP-0002 acceptance-test-8 consumer patch

CP-0002 at accepted proposal commit
`7dd3fc69a279ad3def9a736000b0908a4941098a` requires one composed Sol-2
consumer test after Sol-1 provides `IndexInputView`. That type does not exist on
this isolated branch, so the manifest's no-cross-branch-borrowing rule prevents
committing a skipped test or a copied stand-in. The exact integration-only
patch is held for Sol-0 to apply after composing the accepted Sol-1 and Sol-2
branches; this does not begin Task 2.4.

Target: `tests/indexing/test_source_input_contract.py`

```diff
--- /dev/null
+++ b/tests/indexing/test_source_input_contract.py
@@
+from __future__ import annotations
+
+import ast
+import hashlib
+import inspect
+from pathlib import Path
+
+from oms_hub.artifacts import ArtifactRole
+from oms_hub.indexing.models import StoreKey
+from oms_hub.knowledge.models import SourceRevisionState
+from oms_hub.knowledge.service import CanonicalInputArtifact, IndexInputView
+from oms_hub.providers.contracts import AuthorityClass
+
+
+def _provider_indexing_input(view: IndexInputView) -> dict[str, object]:
+    store_key = StoreKey.course(view.course_id, view.exam_id)
+    return {
+        "store_key": store_key,
+        "path": view.pptx.path,
+        "display_name": view.pptx.path.name,
+        "metadata": [
+            {"key": "authority_class", "string_value": view.authority_class.value},
+            {"key": "course_id", "string_value": view.course_id},
+            {"key": "exam_id", "string_value": view.exam_id},
+            {"key": "lecture_id", "string_value": view.lecture_id},
+            {"key": "source_revision_id", "string_value": view.source_revision_id},
+        ],
+    }
+
+
+def test_sol2_consumer_uses_only_index_input_view(tmp_path: Path) -> None:
+    revision_id = "sr_aaaaaaaaaaaaaaaaaaaaaaaaaa"
+    pptx = tmp_path / "lecture.pptx"
+    pdf = tmp_path / "lecture.pdf"
+    pptx.write_bytes(b"pptx")
+    pdf.write_bytes(b"pdf")
+    view = IndexInputView(
+        source_document_id="opaque-source-document",
+        source_revision_id=revision_id,
+        source_family="legacy_slides",
+        revision_state=SourceRevisionState.READY,
+        authority_class=AuthorityClass.COURSE_MATERIAL,
+        course_id="heme-lymph-0123456789abcdef01234567",
+        exam_id="exam-2-0123456789abcdef01234567",
+        lecture_id="lecture-13-0123456789abcdef01234567",
+        pptx=CanonicalInputArtifact(
+            artifact_id=f"{revision_id}:pptx",
+            role=ArtifactRole.PPTX,
+            path=pptx,
+            sha256=hashlib.sha256(pptx.read_bytes()).hexdigest(),
+            media_type=(
+                "application/vnd.openxmlformats-officedocument."
+                "presentationml.presentation"
+            ),
+        ),
+        pdf=CanonicalInputArtifact(
+            artifact_id=f"{revision_id}:pdf",
+            role=ArtifactRole.PDF,
+            path=pdf,
+            sha256=hashlib.sha256(pdf.read_bytes()).hexdigest(),
+            media_type="application/pdf",
+        ),
+        evidence_units=(),
+        assets=(),
+    )
+
+    provider_input = _provider_indexing_input(view)
+    store_key = provider_input["store_key"]
+
+    assert isinstance(store_key, StoreKey)
+    assert StoreKey.parse(store_key.value) == store_key
+    assert provider_input["path"] == pptx
+    assert provider_input["metadata"] == [
+        {"key": "authority_class", "string_value": "course_material"},
+        {"key": "course_id", "string_value": view.course_id},
+        {"key": "exam_id", "string_value": view.exam_id},
+        {"key": "lecture_id", "string_value": view.lecture_id},
+        {"key": "source_revision_id", "string_value": revision_id},
+    ]
+    assert "opaque-source-document" not in repr(provider_input)
+
+    consumer_source = inspect.getsource(_provider_indexing_input)
+    assert "source_document_id" not in consumer_source
+    assert "legacy-study-revision" not in consumer_source
+
+    tree = ast.parse(Path(__file__).read_text(encoding="utf-8"))
+    imported_modules = {
+        node.module
+        for node in ast.walk(tree)
+        if isinstance(node, ast.ImportFrom) and node.module is not None
+    }
+    imported_modules.update(
+        alias.name
+        for node in ast.walk(tree)
+        if isinstance(node, ast.Import)
+        for alias in node.names
+    )
+    called_names = {
+        node.func.id
+        for node in ast.walk(tree)
+        if isinstance(node, ast.Call) and isinstance(node.func, ast.Name)
+    }
+    called_names.update(
+        node.func.attr
+        for node in ast.walk(tree)
+        if isinstance(node, ast.Call) and isinstance(node.func, ast.Attribute)
+    )
+    assert not any(
+        module == forbidden or module.startswith(f"{forbidden}.")
+        for module in imported_modules
+        for forbidden in ("oms_hub.ingestion", "oms_hub.repositories")
+    )
+    assert called_names.isdisjoint(
+        {
+            "IngestionRepository",
+            "CatalogRepository",
+            "get_study_revision",
+            "list_current_revisions",
+            "list_lectures",
+            "get_lecture",
+        }
+    )
```

The exact composed GREEN command is:

```text
PYTHONPATH="$PWD/src:$PWD" python -m pytest \
  tests/indexing/test_source_input_contract.py -q
```

The composed static checks are:

```text
ruff check tests/indexing/test_source_input_contract.py
MYPYPATH="$PWD/src" mypy tests/indexing/test_source_input_contract.py
```

An isolated import attempt confirms this patch cannot collect before Sol-1 is
composed (`ModuleNotFoundError: No module named 'oms_hub.knowledge'`). That is a
composition blocker, not behavioral RED evidence, and no RED claim is made.
Sol-0 must record the actual GREEN result after composing the real Sol-1 type.
The held file compiles as Python, and the unchanged isolated Sol-2 lifecycle
suite remains green (`39 passed`).
The test proves StoreKey round-trip and provider metadata/path construction from
`IndexInputView`, keeps the opaque source document identity out of provider
input, and statically rejects legacy-ID decoding plus ingestion or catalog
repository imports/calls.

## Review status

- Workstream Sol final review: `SHIP` on the complete candidate
  `7cb5588294548272fdd89e851a85a16cec320ed3` / tree
  `965c5f870aa2a169a1a461fedbc38870e20b9823`.
- Final review-record commit: `SELF` / `SELF_TREE`; resolve these identities
  from the containing docs-only commit without amending it.
- Held central persistence proposal: `ACCEPTED_FOR_APPLICATION_REVIEW`, still
  `HELD` and unapplied. Sol-0 must compose and verify it against the then-current
  central migration line before activation.

Fresh Workstream Sol evidence on the reviewed candidate:

- Focused Task 2.3 lifecycle tests: `39 passed`.
- Broader affected provider/contract/migration/repository tests: `134 passed`
  with pytest importlib collection mode for the duplicate `test_repository.py`
  basenames.
- Ruff: passed.
- Strict owned mypy: passed for `6` source/test files.
- Strict full-source mypy: passed for `175` source files.
- Held-proposal simulation: importing `oms_hub.indexing.models` before the
  existing migration path registered `provider_stores`, `provider_documents`,
  and `index_jobs` without applying a central-file change.
- Base-to-candidate diff, exact seven-file scope, secret/private scan, forbidden
  Task 2.4/live-provider scan, and clean-status checks: passed.
- No Task 2.4, central migration/settings, integration, push, deploy, main,
  production, live Gemini, private-source, or Anki action occurred.

## Review chronology

- Terra specification review: initial `DO_NOT_SHIP` on
  `7a1814b906fd222437acaef108f551493413383f` /
  `69e37aeaffc7236b0493a890e12f5c0834f5f5a2` for fabricated source IDs and
  mismatched store scope fields.
- Terra specification re-review: `DO_NOT_SHIP` on
  `0f831bd18917cf4c8160ab7a3a864e52dd50d246` /
  `d9c9fffd896da52e915cb516f36f2dfa06dc6f07` for the undocumented `sr_`
  source-revision prefix.
- Terra specification final review: `SHIP` on
  `8ddd0d17637cb3dd0f0387fca5748a54567e3a7d` /
  `0fe37448de2404ae8d9bdbd122b725148383234b`, clean, `36` focused tests.
- Terra quality review: initial `DO_NOT_SHIP` on
  `8ddd0d17637cb3dd0f0387fca5748a54567e3a7d` /
  `0fe37448de2404ae8d9bdbd122b725148383234b` for delete-404 retry handling
  and concurrent ensure orphaning.
- Terra quality re-review: `DO_NOT_SHIP` on
  `7ef3fd64ce99e0997a57f17c524dd8eaedb9efc7` /
  `0972a560863d36da3d908952c3f88842f89a0ed5` because the 404 catch covered
  client context and API-resolution failures.
- Terra quality final review: `SHIP` on
  `00b039700f166addd7934ee9e35e2e0d694e4c05` /
  `c3c25795aaadba76e0c6017ba0b7d15031fcfb2f`, clean, `39` focused tests,
  no blocker.

## Correction summaries

- Specification corrections removed fabricated provider-document source IDs,
  enforced `ProviderStore` scope/key consistency, and then relaxed source
  revision validation to the documented explicit printable bounded string
  contract without an invented prefix.
- Quality correction 1 made an actual delete 404 converge `DELETING` to
  `DELETED` while preserving non-404 failures, and serialized the full
  per-admin ensure critical section with the stated fake/local ceiling.
- Quality correction 2 narrowed 404 convergence to the resolved
  `documents.delete` invocation so acquisition, API-resolution, and close
  failures remain redacted failures with durable `DELETING` state.
