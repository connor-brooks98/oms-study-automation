# Task 2.8 Program Sol-0 reconciliation composition from e71eace

Status: **COMPOSED / HASH-VERIFIED / NOT EXECUTED / REVIEWED SHIP /
PRIVATE ROOTS PRESERVED / PROVIDER STATE UNKNOWN / GATE_NOT_OPEN**

Program Sol-0 built a separate reconciliation-only composition from exact clean handoff
`e71eacec05c085409aa41ce4f06b1809630e9124`, tree
`11d800734e961ffe26e45de51f14838427d689ee`, whose required private-composition review
`2873dca02d1be09072bf79d67f14b547d89b3c1f` is an ancestor. No new repository contract code
was needed: the committed reconciliation operator, wrapper, schema-2 evidence validator, and
reviewed scheduled-task contract already implement the requested boundary.

The inert protected root is
`C:\Users\conbr\AppData\Local\Temp\sol0-task28-reconciliation-e71eace`. It has its own
source archive, locked runtime, controller, launcher, and empty evidence directory. The staged
controller and launcher were not executed and no scheduled task was registered or started.

## Exact hashes and checks

- source archive: `c8628c81569dee9882f7bafe17ffe59b61011be972de4a7eb29b11777548d500`;
- locked requirements: `400bb935d83d102eb049812dc92edeaf32d4ad43ec94b9272e07bcb36a30b76a`;
- source manifest: `7b98d0a63f16f7630fb7a510d9cc12f80cf05bb79f739e8a4ff5fce1a75d6036`
  over 682 files;
- runtime manifest: `2b497173248aad63e4ff7c708cc370d95066a70a5003c899314a76223acb2b10`
  over 11,868 files;
- controller: `e07c78e5bd3b67c9b0b6e2b2be785dc07e8475c742687218cb27a225ab9af873`;
- launcher: `10efc422963ed18d9393edd4a724c62cfd2a1794cd2f233376e3db1c494245e3`;
- operator / wrapper / schema-2 evidence validator:
  `e2e82ea648977409ef24ef83c378df42c2bed21723fdcb2b60b801c877a9e2c1` /
  `1d97f9a1d7139a84b3fda3849f38019544d459d8d82ecfb6e25e0a9ab471a003` /
  `7d86de0630a2eeaebd23baf20bddcb90661a490c760c327fc9e975b9e69d2e15`;
- scheduled-task contract:
  `013fb6a834514ef15cd7ae0e738288446aa1a8e83bfe70f8c3dc46de27600e23`.

Focused tests passed 29. The affected provider/indexing/live/evidence lane passed 332 with one
intentional live skip. Ruff, strict mypy with `MYPYPATH=src`, Python AST, PowerShell parsing,
and diff hygiene passed. Real Windows PowerShell 5.1 emitted
`SCHEDULED_TASK_RUN_CONTRACT_HARNESS_VERIFIED` and
`RECONCILIATION_EVIDENCE_HARNESS_VERIFIED` against the exact staged hashes.

Both manifests replayed exactly. The new root and evidence directory have protected
current-SID-only FullControl OI/CI ACLs and zero reparse points. Pinned imports passed for
`google-genai==2.14.0`, `pydantic==2.13.4`, `httpx==0.28.1`, and `keyring==25.7.0`.
The exact reconciliation task, diagnostic, and evidence are absent. Hub remains `Running`; both
ports 8765 and 8788 report `status=ok`, `schema_version=29`.

The existing 9097851 private-shadow composition remains present with empty evidence. The prior
06848e2 root remains present with exactly one sanitized evidence file. No retained evidence
content was read or mutated.

Independent specification and quality/security reviews initially blocked composition commit
`07cb08f2240a8ac5c7c38a67610c5112f87de4a2` because its proposed authorization sentence did
not separately name document, store, and file mutation scopes. Documentation-only correction
`2a799fb098ed790029b358385658b2be13207d58`, tree
`16be009238a11c11d1a9c72f5fffb7ab67bb5a5f`, makes those boundaries explicit. Both
independent reviewers returned **SHIP** on that exact corrected commit with no residual finding;
the staged controller and launcher hashes did not change.

## Enforced one-shot boundary

The controller requires exact task absence immediately before registration, creates only one
current-user `Interactive` / `Limited` task with an unlimited execution-time setting, and
retains the task/root on any ambiguous completion. The launcher validates that exact current
task action, arguments, principal, run level, logon type, enabled/running state before the
wrapper can set reconciliation opt-in or read the stored key.

The operator reads `gemini-api-key` exactly once and permits only global store/file inventory;
document inspection, deletion, and verification relisting within exactly matched stores; and
deletion plus final relisting of exact-matched stores and files. Store display names must
full-match `task-2-8-private-[0-9a-f]{32}` and file display names must full-match
`task-2-8-private-[0-9a-f]{32}-[0-9]{3}`. It has no private source, create, upload, import,
query, new-resource, private-retry, production, or Gate path.

Task removal requires a zero exit plus usable sanitized evidence whose schema-2 operator result
is `passed`, cleanup complete, warning-free, and has zero remaining matched stores, files, and
documents. Root cleanup is a separate controller mode and requires the task/diagnostic absent,
the same accepted evidence, both Hub endpoints healthy on schema 29, and both private roots
unchanged. Ambiguity preserves the task/root for reviewed disposition.

## Exact authorization still required

No live authorization was used. After both exact-composition reviews return **SHIP**, the
required wording is:

> Connor authorizes exactly one Limited/Interactive invocation of Program Sol-0 Task 2.8
> reconciliation composition `2a799fb098ed790029b358385658b2be13207d58`, source
> `e71eacec05c085409aa41ce4f06b1809630e9124`, controller SHA-256
> `e07c78e5bd3b67c9b0b6e2b2be785dc07e8475c742687218cb27a225ab9af873`, and launcher SHA-256
> `10efc422963ed18d9393edd4a724c62cfd2a1794cd2f233376e3db1c494245e3`.
> It may read the stored Gemini key exactly once; globally list stores and files; inspect,
> delete, and verification-relist documents only in stores whose display names full-match
> `task-2-8-private-[0-9a-f]{32}`; delete and relist only stores whose display names full-match
> `task-2-8-private-[0-9a-f]{32}`; and delete and relist only files whose display names
> full-match `task-2-8-private-[0-9a-f]{32}-[0-9]{3}`. It may not access private
> source, create/upload/import/query or perform any new-resource operation, retry the private
> shadow, mutate production or Gate data, merge, push, or deploy. Retain only sanitized
> allowlisted schema-2 evidence. Accept only zero remaining exact store/file/document matches,
> independent evidence-review SHIP, exact reconciliation task/root cleanup, Hub Running, and
> both health endpoints `ok` on schema 29; retain the task/root on ambiguity. This authorizes
> one reconciliation only and does not authorize a private retry.

Composition and review alone do not authorize execution.
