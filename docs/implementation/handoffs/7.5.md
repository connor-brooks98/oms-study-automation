# Task 7.5 handoff

## Identity

- Task: 7.5 — Define Board Runway and planning records
- Worktree: `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol7`
- Branch: `sol7/anki-runway`
- Base commit: `b438cac6a7549dac07b82a9ae4872259193e5b78`
- Base tree: `c916641f8be8b6d2b60db182182fbfa7b4a9e61d`
- Final commit/tree: resolve from the non-amended Task 7.5 commit

## Produced interfaces

- `BoardTarget` defaults to `COMLEX Level 1`, `2027-05-01` through `2027-07-31`; constructing and saving a replacement target keeps the dates user-editable.
- `StudyAllocation` and `StudyPlanDay` hold dated, transparent planned work without scheduler or score inference.
- `BoardRunwaySnapshot` keeps recall retention, application mastery, timed application, blueprint exposure, question volume, Anki due/overdue load, external-assessment history, and data freshness as separate fields.
- `ExternalAssessment` is frozen and includes assessment name, date, score/result, scale, notes, source, and replacement-retirement lineage.
- `PlanningRepository` exposes target, plan-day, snapshot, and assessment reads/writes. Assessment correction never mutates the caller's frozen object: it appends a new record, stores the prior record as retired, and links both IDs.

No field named `pass_probability`, `predicted_score`, or `guaranteed_ready` exists. No recall scheduler, pass inference, direct Anki collection write, route, feature flag, dependency, or provider call was added.

## Persistence boundary and proposal

The owned repository is an isolated in-memory implementation so this task does not edit central `src/oms_hub/models.py`, database bootstrap, migrations, configuration, routes, or shared contracts. It is suitable for deterministic contract tests but is not restart durable. Sol-0 proposal: add the logical `board_targets`, `study_plan_days`, `external_assessments`, and runway snapshot tables through the existing SQLAlchemy/migration path, preserving immutable assessment rows and replacement/retirement foreign-key lineage; then wire the same repository methods to the central database before enabling `board_runway_v1`.

## Verification

The full evidence, including exact RED/GREEN output, is recorded at:

`.superpowers/sdd/2026-08-20-study-hub-grounded-adaptive-learning/task-7.5-report.md`

Focused task tests pass. Ruff and strict mypy pass for the owned source/tests. No inherited crashing PDF path, private data, live provider, Anki Desktop, Anki collection write, network mutation, push, merge, tag, or deployment was run.

## Known limitations

- Repository state is process-local until Sol-0 supplies central persistence.
- Snapshot construction currently accepts already-computed dimensions; Task 7.6/7.7 own allocation computation and routes.
- Data freshness is represented as a timestamp; freshness labels belong to downstream dashboard work.
