# Task 7.5 handoff

## Identity

- Task: 7.5 — Define Board Runway and planning records
- Worktree: `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol7`
- Branch: `sol7/anki-runway`
- Base commit: `b438cac6a7549dac07b82a9ae4872259193e5b78`
- Base tree: `c916641f8be8b6d2b60db182182fbfa7b4a9e61d`
- Final containing task commit: `SELF` (resolve with `git rev-parse HEAD` after the containing final task commit is committed)
- Final containing task tree: `SELF_TREE` (resolve with `git rev-parse 'HEAD^{tree}'` after the containing final task commit is committed)
- Self-reference resolution: resolve `SELF` and `SELF_TREE` from this containing final task commit; do not amend that commit to insert its own identity.

## Produced interfaces

- `BoardTarget` defaults to `COMLEX Level 1`, `2027-05-01` through `2027-07-31`; constructing and saving a replacement target keeps the dates user-editable.
- `StudyAllocation` and `StudyPlanDay` hold dated, transparent planned work without scheduler or score inference.
- `BoardRunwaySnapshot` keeps recall retention, application mastery, timed application, blueprint exposure, question volume, Anki due/overdue load, external-assessment history, and data freshness as separate fields.
- `ExternalAssessment` is frozen and includes assessment name, date, score/result, scale, notes, source, and replacement-retirement lineage.
- Date fields reject `datetime` values; all timestamps require timezone awareness and normalize to UTC at construction. Snapshot writes are idempotent for equal duplicate `snapshot_id` content and reject conflicting duplicates without overwriting history.
- `PlanningRepository` exposes target, plan-day, snapshot, and assessment reads/writes. Assessment correction never mutates the caller's frozen object: it appends a new record, stores the prior record as retired, and links both IDs.

No field named `pass_probability`, `predicted_score`, or `guaranteed_ready` exists. No recall scheduler, pass inference, direct Anki collection write, route, feature flag, dependency, or provider call was added.

## Consolidated Sol-0 persistence proposal — P7.5-SOL0-PERSISTENCE-v1

- Proposal ID: `P7.5-SOL0-PERSISTENCE-v1`.
- Status: `HELD` — this metadata records an unapplied proposal; no shared apply authority exists in this correction.
- Originating owner/task: Sol-7, Task 7.5, `Define Board Runway and planning records`.
- Exact proposal-record base: `d9ee1db5346efa9c1ac458bcd8c92671cafb0bdb` / tree `838e0eb2c86a9387cd11fd7edc9c17feb1c90c86`.
- Exact metadata patch reference: `d9ee1db5346efa9c1ac458bcd8c92671cafb0bdb..SELF`, limited to `docs/implementation/handoffs/7.5.md`. `SELF` resolves to the containing non-amended metadata commit after it is created, and its tree resolves with `git rev-parse 'HEAD^{tree}'`; no executable shared patch exists or is authorized.
- Current boundary: the owned repository is intentionally isolated and in-memory, so this task does not edit central `src/oms_hub/models.py`, database bootstrap, migrations, configuration, routes, or shared contracts. It retains target and plan-day revisions by their existing IDs and exposes current plus revision-history lookups, but is not restart durable.

### Proposed production targets

Exact consolidated targets, with no wildcards:

1. `src/oms_hub/models.py`
2. `src/oms_hub/migrations.py`
3. `src/oms_hub/app.py`
4. `src/oms_hub/planning/repository.py`

### Proposed verification targets

Exact targets, with no wildcards:

1. `tests/planning/test_repository.py`
2. `tests/study_generation/test_migration.py`
3. `tests/v2/test_runtime_public_boundary.py`

### Reason and public interfaces

- Reason: replace the accepted process-local repository boundary with additive, restart-durable persistence before `board_runway_v1` can be enabled, while preserving immutable/versioned semantics and existing behavior.
- Public interfaces affected: keep `BoardTarget`, `StudyPlanDay`, `BoardRunwaySnapshot`, and `ExternalAssessment` unchanged; preserve the existing `PlanningRepository` public methods; change repository construction to consume the existing `Database`; add `app.state.planning_repository` only after schema migration; no route/wire/schema-contract change.
- The proposed persistence maps `board_targets` to append-only target revisions with a current-target pointer, `study_plan_days` to append-only `plan_id` revisions with current-by-date lookup, `external_assessments` to append-only assessment rows with replacement/retirement lineage, and runway snapshots to append-only `snapshot_id` records with latest-by-`captured_at` retrieval. These tables must use the existing SQLAlchemy and migration path before the same repository methods are wired or `board_runway_v1` is enabled.

### Exact integration test behavior

- `tests/planning/test_repository.py`: against a file-backed SQLite database, persist two target revisions/current pointer and two same-date plan revisions/current-by-date; assert that saving an exactly equal `BoardTarget` or `StudyPlanDay` with the same ID returns the stored record without creating a new revision, and that the equal target makes/keeps that target current; assert that a changed target or plan save reusing an ID creates a new revision ID while retaining the prior revision. Persist two distinct snapshots with ordered `captured_at` values; close and reconstruct `Database` plus `PlanningRepository`; assert both snapshot IDs are retrievable after restart and `latest_snapshot` selects the later snapshot. Equal snapshot duplicates stay idempotent, while a conflicting duplicate with the same `snapshot_id` rejects without overwriting. Assert that a duplicate `assessment_id` rejects, while correction creates a new assessment ID and retires/links the old record; verify all IDs, histories, current/latest lookups, UTC timestamps, retired rows, and replacement links survive restart.
- `tests/study_generation/test_migration.py`: fresh schema contains the four additive planning tables; migration from the current accepted schema creates them without changing existing rows; repeated migration is idempotent; uniqueness/current-pointer/replacement constraints and required indexes exist; no destructive downgrade or table recreation.
- `tests/v2/test_runtime_public_boundary.py`: two `create_app` lifecycles sharing one database preserve planning records across restart; `PlanningRepository` is constructed only after migration; `board_runway_v1` remains off and route inventory/current startup behavior remains unchanged.

### Historical review evidence

Task 7.5 Terra specification approved at `226358c708f38b5981d0d1413fe4b13cda9b5703`; Terra quality approved at `d99f5fb606056b3591b2cf4155058c739ead3173`; final Workstream Sol review approved the accepted candidate `d9ee1db5346efa9c1ac458bcd8c92671cafb0bdb` / tree `838e0eb2c86a9387cd11fd7edc9c17feb1c90c86`. Those isolated-task approvals are distinct from approval or apply of this `HELD` proposal.

### Exact conflict audit and ordering

- Sol-2 current head `9b6a37f35d3818339d86c469fecb4ca304c81447` / tree `0c02be5d990c29bdaea174885dd7c64c777feaa7`, proposal `CP-2.1-GEMINI-CONFIG-V1`, overlaps `src/oms_hub/app.py` only. Compose on the then-current integration base and retain its `LLMService(..., gemini_environment_key=...)` construction.
- Sol-6 current head `45752bcd149aa0a2f5ba584b869414b4f7de35da` / tree `cab8dccd0ae03e98df34ff2a2572eb12eebb736d`, proposal `P6.3-SOL0-ADAPTER-v1`, directly overlaps `src/oms_hub/app.py` and `src/oms_hub/migrations.py`. Sol-0 must create one current-base composition: register both additive model sets before schema creation/migration, express one coherent schema-version advance containing both migrations, then construct both repositories after migration. Run both proposal matrices together.
- Sol-8 current head `1a7a0ed5c70626edd9525835148a9efb947d4e12` / tree `0d96023006cbd12c0dcccaf95f9b6bda98ea886c`, Task 8.2 handoff proposes `src/oms_hub/config.py` and a future `src/oms_hub/app.py` construction point for authorized Task 8.6. It has no current model/migration overlap, but future app composition must retain planning construction and remain separately gated.
- A repository-wide audit of current non-Sol-0 workstream handoffs found no other pending exact `src/oms_hub/models.py` or `src/oms_hub/migrations.py` proposal. Sol-3's change request is a shared wire-schema proposal, not a model/migration/app persistence proposal. Future blocked tasks may later introduce conflicts; this audit does not claim otherwise.

### Rollback and required Program Sol-0 action

- Rollback of proposed persistence: require a future Sol-0 apply to separate additive schema/repository work from feature/app enablement when practical. Operational rollback disables/unwires planning construction and keeps `board_runway_v1` off while preserving additive tables and all rows unused; never drop tables, downgrade schema, or delete planning history. Revert only future app/wiring behavior when safe; correct forward for migrated schema defects. The isolated Task 7.5 implementation and this metadata commit remain intact. No current runtime/data rollback exists because this correction applies nothing.
- Required Program Sol-0 action: keep proposal `HELD`; rebase the proposal reasoning onto the then-current reviewed integration head, compose conflicts, author/review an exact executable shared patch carrying this proposal ID, run the full test matrix, verify SHA/tree/clean state, and only then decide whether to apply/enable.

## Verification

The full evidence, including exact RED/GREEN output, is recorded at:

`.superpowers/sdd/2026-08-20-study-hub-grounded-adaptive-learning/task-7.5-report.md`

Focused task tests: `13 passed`. Ruff and strict mypy pass for the owned source/tests. No inherited crashing PDF path, private data, live provider, Anki Desktop, Anki collection write, network mutation, push, merge, tag, or deployment was run.

## Reviews

- Specification review progression: the initial finding required versioned target and plan-day retention; the next finding required an explicit snapshot persistence proposal mapping; the specification was approved at `226358c708f38b5981d0d1413fe4b13cda9b5703`.
- Quality review progression: strict date/timezone validation and immutable snapshot identity were required and approved at `d99f5fb606056b3591b2cf4155058c739ead3173`.
- Reviewer hosts were behaviorally read-only; no provider, Anki, network, production, or external-state mutation was performed.
- Final Workstream Sol review: approved the accepted implementation candidate at `d9ee1db5346efa9c1ac458bcd8c92671cafb0bdb` / tree `838e0eb2c86a9387cd11fd7edc9c17feb1c90c86`; scoped review of this new `HELD` proposal-metadata correction is next/pending.

## Known limitations

- Repository state is process-local until Sol-0 supplies central persistence.
- Snapshot construction currently accepts already-computed dimensions; Task 7.6/7.7 own allocation computation and routes.
- Data freshness is represented as a timestamp; freshness labels belong to downstream dashboard work.
