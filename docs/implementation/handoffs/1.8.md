# 1.8 handoff

## Status

This is a **Gate 2A blocked assessment**, not Gate 2A acceptance. The
machine-readable record is
`artifacts/acceptance/grounded-learning/gate-2a-source-trust.json` with
`state="blocked"` and `claim="gate_not_open"`. Task 1.8 remains blocked until
every mandatory evidence gap below is closed and independently reviewed.

The prior Task 1.8 attempt left no repository changes; its exit cause is not
recoverable from repository evidence. This restart records the blocker durably
without weakening any fail-closed contract or representing a skipped check as
a pass.

## Required record

- Base SHA: `4913842837748b77ecb78f87097745ef22f9a350`.
- Head SHA: `SELF`; resolve from the commit containing this handoff and the Gate
  2A record.
- Owner: Sol-1; blocked dependencies are owned by Sol-0/Sol-2/Sol-5 and the
  controlled private-data/platform operators identified below.
- Files changed: this handoff and
  `artifacts/acceptance/grounded-learning/gate-2a-source-trust.json` only.
- Contracts consumed: Tasks 1.1-1.7; approved CP-0002; Gate 1 record; grounded
  learning plan and manifest; existing Source Trust, parser, artifact-preview,
  and platform-test contracts.
- Contracts produced: one version-1 machine-readable blocked Gate 2A
  assessment. No application, schema, route, provider, migration, or shared
  contract is produced or changed.
- Tests run: Gate 1 record; Source Trust workstream/contracts; legacy parser and
  locator contracts; safe broad Python; Ruff; source mypy; baseline JavaScript;
  and the exact native Windows Office test on this host.
- Test result: all runnable safe lanes passed. The native Windows Office test
  was skipped, and three additional mandatory checks remain blocked; Gate 2A
  is not open.
- Live-provider test run: no.
- Known limitations: native Windows Office and the inherited macOS PyMuPDF
  adapter path were not accepted; no private Lecture 13 data was accessed; no
  Sol-1/Sol-2 composition or dependent-artifact provenance exists here.
- Data migration impact: none.
- Feature flag: unchanged and not activated.
- Rollback: revert the documentation/acceptance-record commit. No runtime or
  data rollback is required.
- Reviewer findings: fresh Terra specification and independent Terra
  quality/release reviews both approved committing this blocked assessment.
  Both denied Gate-open approval. Quality review required the physical-proof
  blocker to identify that the current one-slide smoke test cannot prove
  locator `3` to PDF page `3`; the corrected record requires a Sol-0-authorized
  three-or-more-slide native fixture.

## Accepted Task 1.1-1.7 evidence

All seven task handoffs exist at the assessed input and record completed TDD,
verification, and final approvals. Their SHA-256 values were rechecked without
reading production/private data:

| Task | Handoff SHA-256 |
| --- | --- |
| 1.1 | `64ec8ce0c9518a94d723c1e080ff99cac50742bc6a3bb77106808dee2a103a87` |
| 1.2 | `9e5fc01249bb20b1efcabfda39db6242504dd8813a70cd9c1ab98c1aca1eac18` |
| 1.3 | `038824a5aeba90c9fa5da00536bdf00e7e5bf049d78dc9103bdb9cb0588fcdb3` |
| 1.4 | `144f66720c31f201c2556a7d1239ce9ac996cad40463ff211b2ca2881157c14e` |
| 1.5 | `f076c3b2bd91dfc741d76f04052f31e6f05eecaf71179cde7c107bf460344a2f` |
| 1.6 | `d5d75e1b2746121b455c95b0ac4439115920238d246f221f1dab0536592cd875` |
| 1.7 | `6b7f726e0a2946afd91a917d21f10c7f494b949c736d1df21e1b660327a46d76` |

CP-0002 is
`821026607e040665af4b28e79bd5fcbdfe2c8297fe1b678d575522e986f2d016`.
The frozen plan and manifest remain
`d934cc15b175256676d576e07b67dcbc3759252846bfcddf049217e57f51d563`
and `7c50a9bd62a07389c474fb54a22be63697bc372bf021e6ecbd99a9b90486130e`.

The checked-in synthetic Lecture 13 hashes remain
`712ab602dd9ad8b92639e087304d05ceeceb50d4eaf7d732c4526e68070d27cb`
for normalized Markdown and
`e8d0fea3a62089d1042d887367b2a8a0ab2dd5526591c56d0117a0ca11cb7905`
for page metadata. These are integrity evidence only; the fixture explicitly
must not be presented as the plan's private Lecture 13 dry run.

## Fresh verification

| Check | Result |
| --- | --- |
| Gate 1 record | `7 passed` |
| `tests/knowledge tests/contracts` | Exit `0`; all selected tests passed |
| Legacy parser/locator contracts | `22 passed` |
| Ruff | `All checks passed!` |
| Source mypy | No issues in `185` source files |
| Safe broad Python | Exit `0`; one expected skip with native Windows Office and inherited macOS PDF-adapter exclusions stated in the command |
| Baseline JavaScript | `168 passed`, `0 failed` |
| Native Windows Office physical fixture | **BLOCKED**: the current one-slide smoke test skips on this host and cannot prove locator `3` to page `3` even if run |

Exact commands for executed checks and sanitized requirements for blocked
checks are preserved in the machine-readable record. The assessed environment
is Python `3.13.5` on `macOS-26.5.2-arm64-arm-64bit-Mach-O`, architecture
`arm64`.

## Gate blockers and owner actions

### `native_windows_office_physical_proof`

CP-0002 requires physical proof that citation slide `N` maps to PDF page `N`,
including locator `3` to page `3`. The current Windows-only
`SerialOfficeConverter` smoke fixture creates one slide and asserts only
`page_count == 1`; it skips on this host and cannot close the CP requirement by
itself. Sol-0's release/platform lane must authorize a platform-test extension
that builds at least three deterministic synthetic slides, exports them with
the real Windows desktop Microsoft Office converter, asserts equal input-slide
and output-page counts, and explicitly proves slide/locator `3` maps to PDF page
`3`. Preserve only sanitized hashes, counts, and pass status.

### `lecture13_private_dry_run`

Task 1.8 Step 4 requires a private Lecture 13 dry-run report. This delegation
forbids production/private-data access. Connor or the controlled Sol-0 operator
must authorize and run it in the private environment, committing only hashes,
counts, locator distributions, and warnings.

### `storekey_cross_workstream_composition`

CP-0002 requires `StoreKey.course()`/`StoreKey.parse()` round-trip evidence on
composed accepted Sol-1 and Sol-2 contracts. `StoreKey` is absent from this
branch; `sol2/gemini-indexing` is at
`4abda3acda3e447051db9144f6d3a1cf437602ba`, and both branches diverge from
merge base `82e22d7aa109a29a5fe4d7c26eb1672966317fbf`. Sol-0 must compose the accepted
branches and run the exact command recorded in the Gate artifact. Sol-1 must
not copy or import the Sol-2 type.

### `durable_artifact_dependency_provenance`

Task 1.8 requires source changes to mark dependent artifacts stale. Task 1.7
correctly raises `DependencyProvenanceUnavailable` without mutation because
Task 5.2 owns durable provenance; the manifest currently makes Task 5.2 depend
on Task 1.8. Sol-0 must resolve this dependency cycle or formally narrow the
Gate 2A criterion. Sol-1 cannot treat an empty dependency result as proof or
weaken the invariant.

## TDD and safety

No production defect was found or changed in this assessment, so no new
RED/GREEN commit was manufactured. Tasks 1.1-1.7 retain their separate TDD
histories and reviewed candidates. This commit records evidence only.

No private/production data, live provider, Anki collection, native Windows GUI,
or deployment surface was accessed. No central wiring, shared schema, plan,
manifest, dependency, migration, feature flag, or Task 1.1-1.7 file was edited.
No integration, push, merge, deploy, tag, worktree deletion, or Task 2 work was
performed. Gate 2A remains blocked and no downstream gate is opened by this
record.
