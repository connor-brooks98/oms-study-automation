# 0.5 handoff

## Commit

- Branch: `sol0/contracts-and-integration`
- Commit: symbolic containing Task 0.5 implementation; resolve after commit with `git rev-parse HEAD`.
- Tree: symbolic containing Task 0.5 implementation; resolve after commit with `git rev-parse 'HEAD^{tree}'`.
- Base integration SHA: `60a5f3ec873f982bca14d3507d719eb9927a8f1a` (Task 0.4 final base).
- Contract tag: `studyhub-grounded-contracts-v1`; annotated tag object `a2636bbeb84d2143685c3555e9a3f74ccb8380d0`; peeled target `60a5f3ec873f982bca14d3507d719eb9927a8f1a`; unchanged.

## Deliverable

- Produced interfaces: `build_source_revision`, `build_evidence_ref`, `build_retrieval_scope`, and `build_board_question_draft`.
- Consumed interfaces: `AuthorityClass`, `EvidenceRef`, `RetrievalScope`, and `TruthMode` imported from `oms_hub.providers`.
- Feature flags: none consumed or added.
- Schema/prompt/model versions: no production model or schema was added. `SourceRevisionPayload`, `QuestionOptionPayload`, `QuestionClaimPayload`, and `BoardQuestionDraftPayload` are test-only `TypedDict` payloads.

Builder signatures and defaults:

- `build_source_revision(*, source_document_id="source_fixture_course_l13", source_revision_id="sr_fixture_course_l13_v1", file_sha256="0" * 64, state="ready") -> SourceRevisionPayload`
- `build_evidence_ref(*, evidence_id="ev_fixture_course_l13_hemophilia_a", source_revision_id="sr_fixture_course_l13_v1", authority_class=AuthorityClass.COURSE_MATERIAL, locator_kind="slide", locator_value="5", excerpt="Hemophilia A is factor VIII deficiency with prolonged PTT and normal PT.", checksum=None) -> EvidenceRef`
- `build_retrieval_scope(*, course_id="heme", exam_id="e2", lecture_ids=("l13",), truth_mode=TruthMode.COURSE_ONLY, source_revision_ids=()) -> RetrievalScope`
- `build_board_question_draft(*, option_count=4, correct_option_id="B", duplicate_option_id=False, objective_ids=("obj_fixture_hemophilia_a",), difficulty=3, blueprint_tags=("clinical_presentation:bleeding_disorder",), evidence_ids=("ev_fixture_course_l13_hemophilia_a", "ev_fixture_course_l13_ptt")) -> BoardQuestionDraftPayload`

`build_source_revision` returns a fresh JSON-ready `SourceRevisionPayload` with defaults `source_fixture_course_l13`, `sr_fixture_course_l13_v1`, 64 zeroes, and `ready`.

`build_evidence_ref` returns a real frozen `EvidenceRef` with defaults `ev_fixture_course_l13_hemophilia_a`, `sr_fixture_course_l13_v1`, `AuthorityClass.COURSE_MATERIAL`, `slide`, `5`, the hemophilia-A excerpt, and a derived UTF-8 SHA-256 checksum. An explicit empty checksum remains empty.

`build_retrieval_scope` returns a real frozen `RetrievalScope` with defaults `heme`, `e2`, `("l13",)`, `TruthMode.COURSE_ONLY`, and `()`. Iterable inputs are copied to tuples without sorting or deduplication.

`build_board_question_draft` returns a fresh deeply independent test-only payload. Defaults are four deterministic options, correct option `B`, objective `obj_fixture_hemophilia_a`, difficulty `3`, blueprint tag `clinical_presentation:bleeding_disorder`, and evidence IDs `ev_fixture_course_l13_hemophilia_a` and `ev_fixture_course_l13_ptt`. It supports option counts 0 through 5 and deterministic invalid-payload controls.

Ruling: Task 0.5 supplies reusable builders before the `SourceRevision` and `BoardQuestionDraft` production models exist. Future-model builders therefore return explicitly typed JSON-ready payloads; current shared types return real frozen Task 0.4 contract objects. No production domain model or schema is introduced early.

## Files

- Created: `tests/builders/knowledge.py`, `tests/builders/questions.py`, `tests/fixtures/grounded_learning/course/lecture-13-normalized.md`, `tests/fixtures/grounded_learning/course/lecture-13-pages.json`, `tests/fixtures/grounded_learning/literature/article-1-normalized.md`, `tests/fixtures/grounded_learning/README.md`, `tests/contracts/test_fixture_integrity.py`, and this handoff.
- Modified: none.
- Intentionally not modified: `src/oms_hub/**`, schemas, settings/feature flags, routes/UI, databases/migrations, dependencies/locks, CI, provider contracts, plans/design/manifest/ledger, prior handoffs, `THIRD_PARTY_CODE_REUSE.md`, private sources, production data, Anki, Task 0.6, and the primary checkout.

## Verification

- Focused: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts/test_fixture_integrity.py -q` — `20 passed`.
- Affected suite: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/contracts tests/providers tests/features -q` — passed with zero failures.
- Settings: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/anki/test_settings.py tests/v2/test_runtime_settings.py tests/v2/test_notebook_settings_routes.py tests/v2/test_generation_settings.py -q` — `36 passed`.
- Quality: `PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check src tests scripts` — passed; `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src` — `Success: no issues found in 176 source files`; `PATH=/tmp/studyhub-task01-venv/bin:$PATH MYPYPATH=$PWD/src mypy tests/builders/knowledge.py tests/builders/questions.py tests/contracts/test_fixture_integrity.py` — `Success: no issues found in 3 source files`; `node --test "tests/js/*.test.js"` — `168 passed`.
- Full workstream: not run; the inherited macOS CPython 3.12.13 / PyMuPDF 1.28.2 signal-11 path was deliberately not executed.
- Live/manual: no provider, production, private-data, Anki, or native Windows action.
- Git status: implementation files are pending the required Task 0.5 commit; final clean status and commit/tree identity must be recorded after commit.

## Data and migration

- New records: none.
- Backfill: none.
- Idempotency key: not applicable; builders and fixtures are deterministic test inputs.
- Stale/rollback behavior: remove the Task 0.5 commit or restore the Task 0.4 base; no runtime data or migration exists.

## Fixture evidence

- Course marker count: 8. Literature marker count: 2. Exact marker set is asserted by `test_synthetic_evidence_markers_are_unique`.
- Course markers: `course_l13_intrinsic_pathway`, `course_l13_extrinsic_pathway`, `course_l13_pt`, `course_l13_ptt`, `course_l13_hemophilia_a`, `course_l13_hit_timing`, `course_l13_hit_mechanism`, and `course_l13_dic_pattern`.
- Literature markers: `literature_a1_hit_rapid_onset` and `literature_a1_hit_antibody_persistence`.
- Course page-map schema version: `1`; every course marker has exactly one page-map location and no literature marker is present in the map.
- Required topics: intrinsic/extrinsic pathways, PT versus PTT, hemophilia A, HIT timing and prothrombotic mechanism, and DIC laboratory pattern.
- Intentionally absent fact: the course fixture contains no HIT treatment recommendation or non-heparin anticoagulant selection.
- Intentional discrepancy: course uses simplified 5-to-10-day HIT timing; literature adds a within-24-hours rapid-onset exception after recent prior exposure.
- Fixture SHA-256: `lecture-13-normalized.md` `712ab602dd9ad8b92639e087304d05ceeceb50d4eaf7d732c4526e68070d27cb`; `lecture-13-pages.json` `e8d0fea3a62089d1042d887367b2a8a0ab2dd5526591c56d0117a0ca11cb7905`; `article-1-normalized.md` `eaf0f7377c5341c8badcbab45751268e244a0c9189ba8ca14fe41ba413c98ba0`; `README.md` `fdd5f75623ee2e5061f0fa535ea2a59bba4b020b0d935c16b6809c2c5c1c2477`.
- Privacy/publication scans passed: no private paths, personal identifiers, credentials, Anki collections, URLs, DOI, PMID, PubMed, journal, or real publication identity.
- Production isolation scan passed: no `src/oms_hub` import of `tests.builders` or `grounded_learning`.
- External code reuse: none.

## Risks and follow-up

- Known limitations: native Windows acceptance remains outstanding; Task 0.5 builders and fixtures are platform-neutral but do not claim Windows acceptance. The macOS CPython 3.12.13 / PyMuPDF 1.28.2 signal-11 remains untouched. Task 0.1's parked handoff-inventory Minor, Task 0.2's 33 Markdown hard-break warnings, and Task 0.3/0.4 broad-suite limitations remain inherited.
- Contract proposals: none. Future `SourceRevision` and `BoardQuestionDraft` production fields require their owning tasks and the shared-contract proposal process.
- Terra specification review: pending Program Sol dispatch after Luna implementation.
- Terra quality/privacy review: pending Program Sol dispatch after Luna implementation.
- Corrections made: none after implementation; focused self-review covered deterministic defaults, defensive copies, deep independence, invalid-payload controls, marker/page-map integrity, privacy scans, and production isolation.
- No dependency, schema, production, database, route, UI, settings, provider, or external-source change.
- Next permitted task: Task 0.6 only. Gate 1 — Shared Contracts remains blocked.
