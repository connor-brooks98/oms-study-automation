# 5.1 handoff

## Commit

- Branch: `sol5/board-questions`.
- Task-final implementation commit: `b887601e624da99c8f546b77445c6a2cec95b5a5`.
- Task-final implementation tree: `86425e9d7601d1000fb952e6e268a88d24a1272d`.
- Base commit: `82e22d7aa109a29a5fe4d7c26eb1672966317fbf`.
- Base tree: `bc719a8644765e360c1a47c4a21b0747d8df8f64`.
- Implementation history: `ef7ab2f534ecee701290ad06d563b718f96fef95` (`5064f863e1514d546acd573ef117096e2115c736`), correction `2303f992ea131e40844ac76b677f11990b0b7ed9` (`08b9ffb352b087b097791f09de974973166c5d8c`), and final correction `b887601e624da99c8f546b77445c6a2cec95b5a5` (`86425e9d7601d1000fb952e6e268a88d24a1272d`).
- Specification review: APPROVED by `/root/task_5_1_terra_spec`; identity unchanged and clean.
- Quality review: CHANGES REQUIRED by `/root/task_5_1_terra_quality` for erased legacy static types and two related minor adapter defects; both correction rounds were re-reviewed APPROVED.

## Deliverable

- Added `ArtifactKind`, `ArtifactGenerationContext`, `ArtifactGenerationResult`, `ArtifactRecipe`, `ArtifactRecipeRegistry`, `build_recipe_registry`, and stable recipe IDs `lecture-outline-current`, `lecture-quiz-current`, `custom-quiz-current`, and `board-question-v1`.
- Adapters receive injected one-argument callables, pass `context.request` through unchanged, and do not import or edit current generators.
- Results retain recipe/version, `AuthorityClass.GENERATED_ARTIFACT`, prompt/schema versions, storage path, route, source/evidence IDs, and caller metadata. Legacy storage and routes remain untouched.
- `ArtifactGenerationContext` accepts the compatibility spelling `legacy_request` while preserving identity with `request`; registry lookup, duplicate rejection, deterministic IDs, and missing-callable errors are covered.
- Synthetic flow evidence is limited to `LegacyRequest(lecture_id="lecture-synthetic", prompt="legacy prompt unchanged")`, output `{"title": "Synthetic lecture quiz", "questions": []}`, metadata path `data/artifacts/synthetic/quiz.json`, and route `/lectures/42/quiz`; no database write occurs.

## Files

- `src/oms_hub/artifacts/__init__.py`
- `src/oms_hub/artifacts/models.py`
- `src/oms_hub/artifacts/recipes.py`
- `tests/artifacts/test_recipes.py`
- `docs/implementation/handoffs/5.1.md`
- Ignored full report: `.superpowers/sdd/2026-08-20-study-hub-grounded-adaptive-learning/task-5.1-report.md`.
- No current generator, route, storage, central, provider contract, exporter, dependency, schema, manifest, private fixture, or production file changed.

## Verification

- Initial RED before implementation: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/artifacts/test_recipes.py -q` failed during collection with `ImportError: cannot import name 'ArtifactGenerationContext' from oms_hub.artifacts`.
- Initial GREEN: the same focused command reported `7 passed`.
- Correction round 1 expected RED: focused recipes reported `1 failed, 9 passed` because a valid falsy callable selected the unconfigured fallback; the identity guard passed against the pre-existing direct-pass behavior. Strict typing also exposed erased `Any` compatibility declarations. Correction commit `2303f992ea131e40844ac76b677f11990b0b7ed9` made the focused suite `10 passed`, the focused recovery/import suite `31 passed`, and the affected generator suite `125 passed`; Ruff, source mypy, task mypy, diff, and scope/safety checks were clean.
- Correction round 2 expected RED: the new `ArtifactService.settings` type probe reported `Expression is of type "Any", not "Settings"` and missing `ArtifactService.settings`. Correction commit `b887601e624da99c8f546b77445c6a2cec95b5a5` made the probe clean; the focused recovery/import suite was `31 passed`, the affected generator suite `125 passed`, Ruff passed, and `mypy src` reported `Success: no issues found in 178 source files`.
- Final Sol-5 fix-wave rerun: focused recipes/recovery/import tests `31 passed`; affected outline/quiz/worker compatibility suite `125 passed`; Ruff `All checks passed!`; `mypy src` `Success: no issues found in 180 source files`; `MYPYPATH=$PWD/src mypy tests/artifacts/test_recipes.py tests/questions/test_models.py` `Success: no issues found in 2 source files`.
- `git diff --check` and scoped secret/private-content scans were clean. No provider/network, private-data, production, Anki, deployment, or native Windows action was run; the known macOS PyMuPDF crash path remains unrun.

## Data and migration

- No persistence, migration, database, route, provider, production, Anki, or external-service behavior was added or changed.
- Generated artifacts remain derivative and are forced to `AuthorityClass.GENERATED_ARTIFACT`; no authority data is written.

## Risks and follow-up

- The package compatibility shim executes the adjacent unchanged legacy artifact source so `oms_hub.artifacts` imports and monkeypatch targets remain compatible while the new submodules expose static types. Existing recovery/import and affected route tests cover this judgment call.
- The board recipe is registered for stable identity but intentionally requires an injected generator; persistence, hashes, board validation, routes, and native Windows acceptance belong to later tasks.
- The separate Task 5.3 question-schema correction and its Sol-0 exporter hold are recorded in `docs/implementation/handoffs/5.3.md`; the shared exporter remains outside this task's owned files.
