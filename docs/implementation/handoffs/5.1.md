# 5.1 handoff

## Commit

- Branch: `sol5/board-questions`
- Commit: `SELF` (resolve with `git rev-parse HEAD` after the candidate is committed).
- Tree: `SELF_TREE` (resolve with `git rev-parse 'HEAD^{tree}'` after the candidate is committed).
- Base integration SHA: `82e22d7` (Gate 1 implementation base; resolve the full SHA with `git rev-parse 82e22d7`).

## Deliverable

- Produced interfaces: `ArtifactKind`, `ArtifactGenerationContext`, `ArtifactGenerationResult`, `ArtifactRecipe`, `ArtifactRecipeRegistry`, `build_recipe_registry`, and the stable recipe IDs `lecture-outline-current`, `lecture-quiz-current`, `custom-quiz-current`, and `board-question-v1`.
- Consumed interfaces: `AuthorityClass.GENERATED_ARTIFACT` from `oms_hub.providers`; no shared contract or schema changes.
- Current generators are not edited. The recipes receive injected one-argument callables and pass `context.request` through unchanged.
- Current recipe versions are `current-v1`; the reserved board recipe version is `v1`. New result records retain prompt/schema versions, source/evidence IDs, storage path, route, and caller metadata while legacy storage and routes remain untouched.

## Synthetic flow snapshot

The test-only `LegacyRequest(lecture_id, prompt)` fixture represents one synthetic lecture and contains no private source content:

- Request schema: `{"lecture_id": "lecture-synthetic", "prompt": "legacy prompt unchanged"}`.
- Output schema: `{"title": "Synthetic lecture quiz", "questions": []}`.
- Prompt/version: caller-supplied `legacy-prompt-sha256`; output schema version `legacy-output-v1`.
- Storage path/record: `data/artifacts/synthetic/quiz.json` in result metadata; no database record is created by Task 5.1.
- UI route: `/lectures/42/quiz` in result metadata; no route is added or changed.

## Verification

- RED: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/artifacts/test_recipes.py -q` — collection failed as expected before implementation with `ImportError: cannot import name 'ArtifactGenerationContext' from oms_hub.artifacts`.
- GREEN: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/artifacts/test_recipes.py -q` — `7 passed`.
- Affected current flow: `PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH=$PWD/src:$PWD python -m pytest tests/artifacts/test_recipes.py tests/test_artifact_recovery.py tests/study_generation/test_outline_pdf.py tests/study_generation/test_native_quiz.py tests/study_generation/test_service.py tests/study_generation/test_worker.py tests/study_generation/test_studio_service.py tests/study_generation/test_studio_worker.py tests/study_generation/test_studio_repository.py tests/v2/test_outline_artifact.py tests/v2/test_generation_routes.py tests/v2/test_lecture_generation_ui.py tests/v2/test_quiz_builder_acceptance.py tests/v2/test_quiz_builder_routes.py -ra` — `122 passed in 15.28s`.
- Ruff: `PATH=/tmp/studyhub-task01-venv/bin:$PATH ruff check src tests scripts` — `All checks passed!`.
- Source typing: `PATH=/tmp/studyhub-task01-venv/bin:$PATH mypy src` — `Success: no issues found in 178 source files`.
- Task typing: `PATH=/tmp/studyhub-task01-venv/bin:$PATH MYPYPATH=$PWD/src mypy tests/artifacts/test_recipes.py` — `Success: no issues found in 1 source file`.
- No known macOS PyMuPDF crash path was run. No provider, network, private-data, production, Anki, or native Windows action was performed.

## Compatibility and limitations

- Adding `src/oms_hub/artifacts/__init__.py` creates a package alongside the frozen legacy `src/oms_hub/artifacts.py` module. The package loads the unchanged legacy implementation into its namespace so existing artifact preview/promotion imports and monkeypatch targets continue to work; type-checking declarations cover those legacy exports statically.
- Task 5.1 does not persist artifact runs, compute hashes, validate board questions, or wire routes. Those behaviors belong to later Task 5.x work.
- The default registry intentionally raises a clear runtime error if a recipe is generated without its injected callable; no current generator is guessed or imported implicitly.
- Native Windows acceptance remains outside this macOS lane and is not claimed.

## Safety and scope

- Only the task-owned artifact package, adapter tests, and this handoff are changed. No current generator, route, storage, central, provider contract, exporter, dependency, schema, manifest, private fixture, or production file is changed.
- Generated results are always `AuthorityClass.GENERATED_ARTIFACT`; they cannot be constructed as an authority source.
- Synthetic test and source scans contain no credentials, private course content, Anki collection, or external provider call.

## Correction round 1

- Separate correction commit: `SELF_CORRECTION` (resolve with `git rev-parse HEAD` after the correction commit); the original Task 5.1 commit remains unchanged.
- The legacy artifact compatibility declarations now mirror the real `ArtifactRole`, recovery exceptions, diagnostics, `ResolvedArtifact`, and `ArtifactService` signatures for static checking instead of declaring them as `Any`. Runtime execution still loads the unchanged legacy implementation so imports and monkeypatch targets remain real.
- Added an exact identity assertion using `is` for `context.request`, a valid falsy callable regression test, and a static enum-member assertion.
- Expected RED before the correction: the focused recipe test was `1 failed, 9 passed` because a falsy callable selected the fallback; the identity assertion was added as a copy-regression guard and passed against the existing direct-pass behavior. A stricter pre-fix type probe reported the legacy `Any` expression in the package compatibility declarations.
- Correction GREEN: focused recipe tests `10 passed`; legacy artifact/import tests `31 passed`; final Ruff, source mypy, and task-owned mypy results are recorded in the Task 5.1 report.
