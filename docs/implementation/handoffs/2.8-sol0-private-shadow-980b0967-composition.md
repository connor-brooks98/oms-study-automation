# Task 2.8 Program Sol-0 post-blocker private-shadow composition

Status: **FRESH ROOT / CORRECTED / HASH-VERIFIED / NOT EXECUTED / REVIEW PENDING /
GATE_NOT_OPEN**

Program Sol-0 built a new protected composition after reviewed preflight blocker handoff
`980b0967fca0b9138a83d210505882ead7b60cc5`. It did not repair or reuse the drifted
`2cef9789` root. The new root is
`C:\Users\conbr\AppData\Local\Temp\sol0-task28-private-shadow-980b0967fca0b9138a83d210505882ea`.
It is canonically disjoint from the permanent `9097851` tombstone, retained `06848e2` root, and
drifted `2cef9789` root. Those prior roots were checked only for root presence, root-level reparse
state, and disjointness; their contents and evidence were not inspected or changed.

The source remains exact Sol-2 handoff `909785131b113fb5b2083b74bd2757c2fc0dbf4b`, tree
`d7eb0dae14d915d24b47a6a865ffe6eb31ab5c6a`, with corrected entrypoint lineage through
`4bc82fbb258cb1192323fe16f1123c8b2a5fdb82` and handoff `0d3cd44`. The reviewed scheduled-task,
wrapper, evidence, normalized-Markdown, schema-29, and 15-key blocked-evidence contracts remain
hash-bound.

## Exact composition

- source archive:
  `389f70b675477ef15fe581903693395f93913ed7951a293d08cd138e18dd92a6`;
- fresh lock:
  `c6bd5cbebb0d87841f66b506f034ed39b8b18c1ec8973e738f258f95afd6758a`;
- source manifest:
  `d36c6a64ef342ff0d4e88c370c794a2add46ef2f98fbdfb9dcabd6bd86f702b0`
  over 674 files;
- runtime manifest:
  `cf1344be7003a65d7e4863b0dfbbccc34f56d21c93e0bb7ff6d126df827b2a42`
  over 11,868 files;
- controller:
  `d3d5dfbb57df4718741030a9629a41968705875f74c74c0d081a3a5bb0055528`;
- launcher:
  `48189dbe4fb4defd5e71f5657d5d2e6f370d82d79e19e0a7cf4a2593b4665bf4`;
- composition-bound operator:
  `9ab7703f02c5338379bf02279716b98f43e13eae4f51176c25d8761e73ccc3d3`;
- scratch-binding test:
  `82999619aa2161e98e2a73995e16f47e4eb9ec2050b4f4c51ef50d88047bcdf5`;
- evidence-state contract / evidence-directory harness:
  `2c3ef4158e5f7118e36ea1d273ee2382931780ea3851345a27f4468360637dff` /
  `ac97101dc0ad72d1973552603875d9a171a265c3a59bfe448ddc77b634d7072d`.

The root has exactly 22 top-level entries. Root and empty evidence-directory ACLs are protected,
current-SID-only FullControl with OI/CI and inheritance disabled. `icacls /verify` passed, zero
reparse points exist, the exact task/scratch/diagnostic are absent, and the evidence directory is
present and empty. Hub is `Running`; both endpoints report `ok` on schema 29.

## Minimum post-blocker guard

The exact-head specification review **SHIPPED**, but quality/security correctly **BLOCKED**
candidate `b1fa7817` because its evidence check could become stale before task registration,
consuming one-shot authorization even though the launcher would still protect the later
scratch/key/source/provider boundary.

The minimum correction adds one no-self-heal evidence-state contract shared by controller and
launcher. It requires evidence presence and emptiness, exact protected ACLs, top-level count 22,
and zero reparse points. Both scripts call it initially. The controller calls it again immediately
before the final exact task-absence assertion and registration; the launcher calls it again
immediately before the current-task assertion. Neither script nor the helper creates, replaces,
or repairs evidence state.

The review RED emitted `evidence_guard_missing_revalidation_or_self_heals`. The corrected real
PowerShell 5.1 harness proves missing, nonempty, and disappear-after-initial-check states never
reach a registration sentinel, then emits
`PRIVATE_SHADOW_EVIDENCE_DIRECTORY_CONTRACT_VERIFIED`.

End-to-end tracing also found a stale launcher assertion for nonexistent
`scheduled-task-contract-handoff.md`. It was removed rather than inventing a file; the exact
provider-agnostic scheduled-task contract and its committed harness remain independently
hash-bound.

During construction, the new root's initially empty evidence directory was absent at the first
full verification. The new guard failed closed before any executable boundary. Disposable SCP
and matching-name probes both preserved empty protected directories, so no automatic deletion
was reproduced. Because the root was still an unreviewed build artifact, the required directory
was created once during finalization, protected, and immediately subjected to the complete
immutable verification. The final and later state snapshots both passed; this history is retained
for reviewer judgment rather than hidden.

## Verification and boundary

Local focused verification passed 155 with one intentional live skip; the affected suite passed
250 with one intentional live skip. Ruff, strict mypy, Python AST, PowerShell parsing, hash replay,
and diff hygiene passed. Windows PowerShell 5.1 emitted:

- `PRIVATE_SHADOW_EVIDENCE_DIRECTORY_CONTRACT_VERIFIED`;
- `PRIVATE_SHADOW_SCRATCH_BINDING_VERIFIED`;
- `SCHEDULED_TASK_RUN_CONTRACT_HARNESS_VERIFIED`;
- `PRIVATE_SHADOW_EVIDENCE_HARNESS_VERIFIED`;
- `PRIVATE_SHADOW_ENTRYPOINT_HARNESS_VERIFIED`.

The controller and launcher were not executed. No task was registered or started; no key,
private source/evidence, provider API, or prior-root content was accessed; no live retry occurred;
and no provider, production, Gate, merge, push, or deployment mutation occurred.

Independent exact-head specification and quality/security reviews remain pending. This
composition is not authorized for live execution. Exact fresh authorization wording will be
added only if both reviews **SHIP** this state without an unresolved blocker.
