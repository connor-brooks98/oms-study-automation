# Task 2.8 Program Sol-0 private-shadow 980b0967 result

Status: **BLOCKED / AUTHORIZATION CONSUMED / NO RETRY / CLEANUP COMPLETE /
PROVIDER RECONCILIATION EMPTY / REVIEW PENDING / GATE_NOT_OPEN**

Connor authorized exactly one invocation of reviewed composition
`ba141f403ed54335ce6282adbdcfa86524fb74e9`, tree
`dfaac0883464d8767982460c82eed19162ed715d`, controller SHA-256
`d3d5dfbb57df4718741030a9629a41968705875f74c74c0d081a3a5bb0055528`, and launcher SHA-256
`48189dbe4fb4defd5e71f5657d5d2e6f370d82d79e19e0a7cf4a2593b4665bf4`.
The exact controller was invoked once. Registration/start was attempted, so authorization is
consumed. No retry occurred.

## Sanitized result

The validated 15-key blocked record reports:

- failure stage: `import_input`;
- input identity: `normalized_markdown`;
- provider category/status/reason: `provider` / `400` / `unknown_provider`;
- source revision hash:
  `c6add553313ed9fbdfffa58b9a1c0a0fbd9d74e1281881c98d989859a1e38bbd`;
- document types: `markdown`, `pdf`, `pptx`;
- 49 pages, 49 slides, and 7,359,907 input bytes;
- cleanup: `complete`;
- provider reconciliation: `empty`;
- warning: `private_shadow_failed`.

The allowlisted lifecycle proves prior provider state was empty, one disposable store was created,
cleanup attempted two documents, three files, and one store, and the final file/store
reconciliation states were empty. The safe-result SHA-256 is
`8b01b431f674257dbfe1e683d47fc0437c40a20c2d6eb928a6b4231e25728679` over 763 bytes.

The safe status reports wrapper stage `complete`, exit code `1`, usable evidence, deleted operator
artifacts, and no retained raw content. Its SHA-256 is
`e3075593d2c45fc18e1f8d536dc3d1a7973f63fda58c61513cdaf73d6d47ae47` over 148 bytes. Exactly
these two sanitized evidence files remain in the protected new evidence directory.

## Post-run boundary

The exact temporary task, scratch, and diagnostic are absent. The new protected composition root
is retained pending separately authorized disposition. All three prior roots remain present at
the reviewed metadata-only predicate; their contents were not accessed or mutated. Hub remains
`Running`, and both endpoints remain `ok` on schema 29.

The stored key was read only through the reviewed single-read path; only the authorized private
inputs were accessed. No raw/private/provider content was retained. No nonmatching provider
resource, production data, Gate state, merge, push, or deployment boundary was crossed.

This result does not authorize another provider attempt. The exact blocker is the provider HTTP
400 during normalized-Markdown import. The owning implementation must diagnose that request
contract offline and produce a newly reviewed composition before requesting any fresh provider
authorization.

Independent exact-result specification and quality/security reviews remain pending.
