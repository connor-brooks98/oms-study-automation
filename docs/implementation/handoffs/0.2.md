# 0.2 handoff

## Commit

- Branch: `sol0/contracts-and-integration`
- Commit: symbolic containing Task 0.2 commit; resolve after commit with `git rev-parse HEAD`.
- Tree: symbolic containing Task 0.2 tree; resolve after commit with `git rev-parse 'HEAD^{tree}'`.
- Base integration SHA: `bc7a0f6f10d725449e36df7096c33c347ef81fe0`

## Deliverable

- Produced interfaces: repository-local approved design, execution plan, and machine-readable workstream manifest.
- Consumed interfaces: verified planning ZIP/extraction; Task 0.1 frozen path map and accepted baseline rulings.
- Feature flags: none added or changed.
- Schema/prompt/model versions: manifest version `1`; planned `google-genai` baseline `2.14.0`; AnkiConnect request/API version `6`; no runtime schema, prompt, or model change.

## Files

- Created: `docs/superpowers/specs/2026-08-20-study-hub-grounded-adaptive-learning-design.md`, `docs/superpowers/plans/2026-08-20-study-hub-grounded-adaptive-learning.md`, `docs/superpowers/plans/study-hub-parallel-workstream-manifest.yaml`, `docs/implementation/THIRD_PARTY_CODE_REUSE.md`, and this handoff.
- Modified: none of the five deliverables existed at the Task 0.2 base.
- Intentionally not modified: `HANDOFF.md`, `docs/implementation/handoffs/0.1.md`, runtime code, tests, dependencies, configuration, CI, acceptance artifacts, and manifest task state. Task 0.3 was not started.

## Verification

- Focused: source/copy SHA-256 and `cmp` matched before the path patch; asserted patch reported stale counts `4/1`, replacement counts `4/1`, and no stale strings. `file`, `git diff --check`, source-vs-installed unified diff, stale-path scan, manifest graph validation, frozen-path command, focused frozen-path tests, exact-file diff inspection, and secret scan were run before commit.
- Affected suite: `PYTHONPATH=$PWD/src:$PWD /tmp/studyhub-task01-venv/bin/python -m pytest tests/scripts/test_frozen_paths.py -q`.
- Full workstream: not run; Task 0.2 is documentation-only and must not execute the known crashing PyMuPDF path or broad suites.
- Live/manual: no provider, production, private-data, or Anki call; no native Windows acceptance claimed.
- Git status: clean before Task 0.2; final containing commit/tree are intentionally symbolic above and recorded in the ignored Task 0.2 report after commit.

## Data and migration

- New records: none.
- Backfill: none.
- Idempotency key: not applicable; tracked documentation-only installation.
- Stale/rollback behavior: revert the Task 0.2 documentation commit; no runtime, provider, database, production, or Anki state exists to roll back.

## Risks and follow-up

- Known limitations: native Windows acceptance remains outstanding. macOS CPython 3.12.13 / PyMuPDF 1.28.2 reproducibly crashes with signal 11; PDF-dependent implementation or acceptance must not claim a clean macOS Python 3.12 path until resolved or explicitly waived. The Task 0.1 handoff inventory omission of Fix Round 3 evidence filenames remains parked and non-load-bearing.
- Contract proposals: none.
- Next unblocked tasks: none by this handoff. Gate 1 — Shared Contracts remains blocked; Task 0.3 is the next permitted task only after the required review gates.

## Provenance and authorized correction

- Original planning ZIP SHA-256: `6b063d3effb378f6f1289fa64d0211d695db183ff3c749ba5d1cd95dd943294e`.
- Delivered design SHA-256: `37c06f8f177afc44cb7a2a4159aad206b241c4122113d4842e9c0fc24c2835f9`.
- Delivered plan SHA-256: `cdfb62316db26bd7e0c655e8635e8008d29ac321a02d683ac1fff3e66720e448`.
- Patched repository-local plan SHA-256: `d934cc15b175256676d576e07b67dcbc3759252846bfcddf049217e57f51d563`.
- Delivered manifest SHA-256: `1e8385945e9c95201cc195751b6ee586dfc0093f838ca76af9448fee2cebdf99`.
- Byte identity proof: `cmp -s` passed for delivered versus installed design and manifest before commit.
- Plan-difference proof: `diff -u` showed exactly five changed path lines: four repository-path substitutions and one worktree-root substitution, with no other change.
- Ruling: Task 0.1 verified that the authoritative repository is /Users/connor/Developer/oms-study-automation. The delivered plan retained the stale $HOME/Projects location in five command examples. Task 0.2 is authorized to correct only those five strings before committing the repository-local execution plan. The design specification and manifest remain unchanged.
- Ruling: Preserve the approved package bytes and five-substitution-only plan diff. Plain `git diff --check` must still be run and its 33 inherited Markdown hard-break warnings recorded as an expected source-owned exception. Acceptance additionally requires a deterministic line-by-line proof that every warning is inherited from the delivered design/plan after the authorized transformation, no trailing whitespace exists in the new reuse register or handoff, the manifest remains byte-identical, and a one-shot `git -c core.whitespace=-blank-at-eol diff --cached --check` reports no other whitespace error.

## Review state

- Terra specification review: pending independent review after this implementation commit.
- Terra quality-review result: pending independent review after this implementation commit.
