# Task 2.1 handoff — Gemini provider configuration

## Identity

- Worktree: `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol2`
- Branch: `sol2/gemini-indexing`
- Base commit: `82e22d7aa109a29a5fe4d7c26eb1672966317fbf`
- Base tree: `bc719a8644765e360c1a47c4a21b0747d8df8f64`
- Prior implementation commit: `7eba39d02519f86b962b65e2b00a8231ba0cf359`
- Fix-round base tree: `2d8c483e775558d16b8f0f2b1c3da72d0c87dd44`
- Review-record commit: `SELF` (resolve from the commit containing this handoff)
- Review-record tree: `SELF_TREE` (resolve from the commit containing this handoff)
- Self-reference rule: do not amend the containing commit to insert its own identity; the full report records the resolved SHA/tree.

## Produced interfaces

- `oms_hub.providers.gemini.GeminiConfig`
- `oms_hub.providers.gemini.GeminiProviderError`
- `oms_hub.providers.gemini.GeminiAuthenticationError`
- `oms_hub.providers.gemini.GeminiQuotaError`
- `oms_hub.providers.gemini.GeminiTransientError`
- `oms_hub.providers.gemini.GeminiContractError`

`GeminiConfig` is a provider-local Pydantic model. Its API key is a `SecretStr`, excluded from normal Pydantic serialization and `repr`; `to_redacted_dict()` returns JSON-safe settings plus only an `api_key_configured` boolean. Blank and whitespace-only keys are rejected. Positive timeout/size validation is enforced, the document limit cannot exceed 100 MiB, and the internal store-input limit cannot exceed 6,442,450,944 bytes.

The normalized exception classes retain `category`, `retryable`, `provider_status_code`, `provider_request_id`, and `redacted_message`. Defaults are authentication/non-retryable, quota/retryable, transient/retryable, and contract/non-retryable. The constructor accepts only an already-redacted message; provider payload redaction belongs to the Task 2.2 client translator.

## Required configuration values

| Field | Value |
| --- | --- |
| `api_key_secret_name` | `gemini_api_key` |
| `sdk_version` | `2.14.0` |
| `file_search_model` | `gemini-3.7-flash` |
| `embedding_model` | `models/gemini-embedding-2` |
| `api_version` | `v1beta` |
| `request_timeout_seconds` | `120` |
| `operation_poll_seconds` | `2` |
| `operation_timeout_seconds` | `900` |
| `maximum_document_bytes` | `104857600` |
| `maximum_store_input_bytes` | `6442450944` |

## TDD evidence

RED, before provider files existed:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH="$PWD/src:$PWD" \
  python -m pytest tests/providers/gemini/test_models.py -q
```

Result: collection failed with the expected `ModuleNotFoundError: No module named 'oms_hub.providers.gemini'`.

GREEN, after the minimum implementation:

```text
PATH=/tmp/studyhub-task01-venv/bin:$PATH PYTHONPATH="$PWD/src:$PWD" \
  python -m pytest tests/providers/gemini/test_models.py -q
```

Result: `9 passed`.

## Unapplied, file-targeted shared patches for Sol-0

These are exact proposals for the current checkout. Sol-0 applies and reviews them separately; this fix commit edits none of the target files.

### 1. Dependency and lock

Target: `pyproject.toml`, `[project].dependencies` (the current list has no Gemini entry).

```diff
@@
   "fastapi>=0.115,<1",
+  "google-genai==2.14.0",
   "httpx>=0.28,<1",
```

The current checkout has no tracked `uv.lock` (and no other lock file). The approved Sol-0 action after applying the hunk is:

```text
uv lock                         # from repository root; creates root uv.lock
git add pyproject.toml uv.lock
```

This uses the available `uv` workflow; if Sol-0 selects a different approved package manager, it must record that exact lock-file path and command instead. Do not add deprecated `google-generativeai`, and do not claim lock regeneration until the resulting lock is reviewed.

### 2. Secret-store key and legacy alias

Target: `src/oms_hub/security/secret_store.py`, directly after `VOYAGE_API_KEY_SECRET`.

```diff
@@
 VOYAGE_API_KEY_SECRET = "voyage-api-key"
+GEMINI_API_KEY_SECRET = "gemini_api_key"
+LEGACY_GEMINI_API_KEY_SECRET = "gemini-api-key"
+GEMINI_API_KEY_SECRET_ALIASES = (
+    GEMINI_API_KEY_SECRET,
+    LEGACY_GEMINI_API_KEY_SECRET,
+)
```

Add the resolver after the `SecretStore` protocol (the protocol is defined immediately below these constants):

```diff
@@
 class SecretStore(Protocol):
@@
     def delete(self, key: str) -> None: ...

+def resolve_gemini_api_key(
+    secrets: SecretStore,
+    *,
+    environment_value: str | None = None,
+) -> str | None:
+    for key_name in GEMINI_API_KEY_SECRET_ALIASES:
+        value = secrets.get(key_name)
+        if value and value.strip():
+            return value.strip()
+    if environment_value is None:
+        return None
+    return environment_value.strip() or None
```

This is the one resolver used by both the new Gemini path and legacy `LLMService`: canonical keyring `gemini_api_key` first, legacy keyring `gemini-api-key` only as an existing-install fallback, and environment last. A keyring read error propagates to the caller's fail-closed boundary; it is not converted into an environment fallback.

### 2a. Legacy consumer wiring

Targets: `src/oms_hub/llm/service.py` and the existing `LLMService` construction in `src/oms_hub/app.py`.

```diff
@@ src/oms_hub/llm/service.py
-from oms_hub.security.secret_store import SecretStore
+from oms_hub.security.secret_store import SecretStore, resolve_gemini_api_key
@@
     def __init__(
         self,
         settings: LLMSettingsRepository,
         secrets: SecretStore,
         providers: Mapping[ProviderName, LLMProvider],
+        *,
+        gemini_environment_key: str | None = None,
     ) -> None:
@@
         self.settings = settings
         self.secrets = secrets
         self.providers = dict(providers)
+        self.gemini_environment_key = gemini_environment_key
@@
     def credential_configured(self, provider: ProviderName) -> bool:
         try:
-            value = self.secrets.get(SECRET_KEYS[provider])
+            value = self._credential_value(provider)
         except Exception:  # noqa: BLE001 - read-only status fails closed
             return False
         return bool(value and value.strip())

+    def _credential_value(self, provider: ProviderName) -> str | None:
+        if provider is ProviderName.GEMINI:
+            return resolve_gemini_api_key(
+                self.secrets,
+                environment_value=self.gemini_environment_key,
+            )
+        return self.secrets.get(SECRET_KEYS[provider])
@@
     def _credential(self, provider: ProviderName) -> str:
-        value = self.secrets.get(SECRET_KEYS[provider])
+        value = self._credential_value(provider)
         if not value or not value.strip():
```

Pass the same server-only accessor to the legacy consumer; do not let it read environment state independently:

```diff
@@ src/oms_hub/app.py
     app.state.llm_service = LLMService(
         app.state.llm_settings,
         app.state.secrets,
         {
@@
             ProviderName.OPENROUTER: OpenRouterProvider(),
         },
+        gemini_environment_key=resolved.gemini_api_key_value,
     )
```

This keeps both consumers on one resolver and one canonical write. Existing installs continue to work through the legacy read fallback, while a saved canonical value always wins over both the legacy value and environment bootstrap.

### 3. Server-only `Settings` field and accessor

Target: `src/oms_hub/config.py`, adjacent to the existing `voyage_api_key` field and `voyage_api_key_value` property.

```diff
@@
     voyage_api_key: SecretStr | None = Field(
         default=None,
         validation_alias="VOYAGE_API_KEY",
         exclude=True,
         repr=False,
     )
+    gemini_api_key: SecretStr | None = Field(
+        default=None,
+        validation_alias="GEMINI_API_KEY",
+        exclude=True,
+        repr=False,
+    )
@@
     def voyage_api_key_value(self) -> str | None:
         if self.voyage_api_key is None:
             return None
         return self.voyage_api_key.get_secret_value().strip() or None

+    @property
+    def gemini_api_key_value(self) -> str | None:
+        if self.gemini_api_key is None:
+            return None
+        return self.gemini_api_key.get_secret_value().strip() or None
```

`exclude=True` and `repr=False` keep an optional server environment bootstrap secret out of settings serialization and diagnostics. The shared resolver always checks canonical and legacy keyring values before using this accessor, so an environment value cannot shadow a saved replacement.

### 4. Settings response, writes, and redaction

Targets: `src/oms_hub/web/settings_routes.py` imports, `_voyage_configured` neighborhood, and `save_ai_credential`.

```diff
@@
-from oms_hub.security.secret_store import VOYAGE_API_KEY_SECRET, SecretStore
+from oms_hub.security.secret_store import (
+    GEMINI_API_KEY_SECRET,
+    resolve_gemini_api_key,
+    VOYAGE_API_KEY_SECRET,
+    SecretStore,
+)
@@
 def _voyage_configured(request: Request) -> bool:
@@
         return False

+def _gemini_api_key(request: Request) -> str | None:
+    environment_value = request.app.state.settings.gemini_api_key_value
+    secrets = cast(SecretStore, request.app.state.secrets)
+    try:
+        return resolve_gemini_api_key(
+            secrets,
+            environment_value=environment_value,
+        )
+    except Exception:
+        return None

+def _gemini_status(request: Request) -> dict[str, object]:
+    value = _gemini_api_key(request)
+    return {
+        "configured": value is not None,
+        "provider": "gemini",
+        "key_suffix": f"…{value[-4:]}" if value else None,
+    }
@@
     selected = _provider(provider)
     secrets = cast(SecretStore, request.app.state.secrets)
-    if update.credential.strip():
-        secrets.set(SECRET_KEYS[selected], update.credential)
+    credential = update.credential.strip()
+    if credential:
+        secrets.set(
+            GEMINI_API_KEY_SECRET
+            if selected is ProviderName.GEMINI
+            else SECRET_KEYS[selected],
+            credential,
+        )
         _llm_settings(request).clear_test(selected)
+    if selected is ProviderName.GEMINI:
+        return _no_store(_gemini_status(request))
+    return _no_store(
+        {
+            "provider": selected.value,
+            "configured": _llm_service(request).credential_configured(
+                selected
+            ),
+        }
+    )
```

For a configured key this returns exactly `{"configured": true, "provider": "gemini", "key_suffix": "…1234"}` with the real final four characters; an unconfigured response uses `false` and `null`. The key itself never appears in the payload, headers, or logs. Preserve the existing generic response shape for non-Gemini providers.

### 5. Required shared-file tests

Targets: `tests/anki/test_settings.py` and `tests/v2/test_llm_settings_routes.py`.

```diff
@@ tests/anki/test_settings.py
+def test_gemini_key_accessor_is_server_only(monkeypatch: pytest.MonkeyPatch) -> None:
+    monkeypatch.setenv("GEMINI_API_KEY", "local-gemini-secret")
+    settings = Settings(_env_file=None)
+    assert settings.gemini_api_key_value == "local-gemini-secret"
+    assert "local-gemini-secret" not in repr(settings)
+    assert "local-gemini-secret" not in settings.model_dump_json()
```

Update the existing `test_credentials_are_saved_independently_and_blank_retains_existing` assertions for the Gemini response and canonical write:

```diff
@@ tests/v2/test_llm_settings_routes.py
-    assert gemini.json() == {"provider": "gemini", "configured": True}
+    assert gemini.json() == {
+        "configured": True,
+        "provider": "gemini",
+        "key_suffix": "…cret",
+    }
+    assert "gemini-secret" not in gemini.text
@@
     assert secrets.values == {
         "openai-api-key": "openai-secret",
+        "gemini_api_key": "gemini-secret",
     }
```

Add these directly applicable hunks to `tests/v2/test_llm_settings_routes.py` (the existing `MemorySecrets` fixture is used by `prepared_client`):

```diff
@@
 class MemorySecrets:
@@
     def delete(self, key):
         self.values.pop(key, None)

+class UnavailableSecrets(MemorySecrets):
+    def get(self, key):
+        raise RuntimeError("secret backend unavailable")
@@
 def test_credentials_are_saved_independently_and_blank_retains_existing(tmp_path):
@@
     assert openai.headers["cache-control"] == "no-store"

+def test_gemini_status_is_unconfigured_without_a_key(tmp_path):
+    client, _, secrets = prepared_client(tmp_path)

+    response = client.post(
+        "/settings/ai/gemini/credential",
+        json={"credential": "   "},
+    )

+    assert response.json() == {
+        "configured": False,
+        "provider": "gemini",
+        "key_suffix": None,
+    }
+    assert secrets.values == {}
+    assert response.headers["cache-control"] == "no-store"
+    assert "secret" not in response.text.casefold()

+def test_gemini_status_fails_closed_when_secret_store_is_unavailable(tmp_path):
+    client, app, _ = prepared_client(tmp_path)
+    unavailable = UnavailableSecrets()
+    app.state.secrets = unavailable
+    app.state.llm_service.secrets = unavailable

+    response = client.post(
+        "/settings/ai/gemini/credential",
+        json={"credential": "   "},
+    )

+    assert response.json() == {
+        "configured": False,
+        "provider": "gemini",
+        "key_suffix": None,
+    }
+    assert "secret backend unavailable" not in response.text

+def test_gemini_credential_replacement_uses_one_canonical_write(tmp_path):
+    client, _, secrets = prepared_client(tmp_path)
+    first = client.post(
+        "/settings/ai/gemini/credential",
+        json={"credential": "gemini-old-1234"},
+    )
+    second = client.post(
+        "/settings/ai/gemini/credential",
+        json={"credential": "gemini-new-5678"},
+    )

+    assert first.json()["key_suffix"] == "…1234"
+    assert second.json() == {
+        "configured": True,
+        "provider": "gemini",
+        "key_suffix": "…5678",
+    }
+    assert "gemini-new-5678" not in second.text
+    assert secrets.values == {
+        "gemini_api_key": "gemini-new-5678",
+    }
```

Add these directly applicable precedence/failure hunks to `tests/llm/test_service.py` and `tests/v2/test_llm_settings_routes.py`:

```diff
@@ tests/llm/test_service.py
@@
 def test_missing_credential_is_an_authentication_diagnostic(tmp_path):
@@
     assert raised.value.http_status is None

+def test_gemini_resolver_prefers_canonical_keyring_over_legacy_and_environment(tmp_path):
+    settings, service = prepared_service(tmp_path)
+    settings.set_assignment(LLMTask.TRANSCRIPTS, ProviderName.GEMINI, "gemini-model")
+    service.gemini_environment_key = "environment-secret"
+    service.secrets.values["gemini_api_key"] = "canonical-secret"
+    service.secrets.values["gemini-api-key"] = "legacy-secret"

+    _, _, resolved = service.for_task(LLMTask.TRANSCRIPTS)

+    assert resolved == "canonical-secret"

+def test_gemini_resolver_uses_legacy_keyring_before_environment(tmp_path):
+    settings, service = prepared_service(tmp_path)
+    settings.set_assignment(LLMTask.TRANSCRIPTS, ProviderName.GEMINI, "gemini-model")
+    service.gemini_environment_key = "environment-secret"
+    service.secrets.values.pop("gemini_api_key", None)

+    _, _, resolved = service.for_task(LLMTask.TRANSCRIPTS)

+    assert resolved == "gemini-secret"

+def test_gemini_resolver_uses_environment_only_when_keyring_is_empty(tmp_path):
+    settings, service = prepared_service(tmp_path)
+    settings.set_assignment(LLMTask.TRANSCRIPTS, ProviderName.GEMINI, "gemini-model")
+    service.gemini_environment_key = "environment-secret"
+    service.secrets.values.pop("gemini_api_key", None)
+    service.secrets.values.pop("gemini-api-key", None)

+    _, _, resolved = service.for_task(LLMTask.TRANSCRIPTS)

+    assert resolved == "environment-secret"
```

The route replacement and single-write failure tests are:

```diff
@@ tests/v2/test_llm_settings_routes.py
@@
 def test_gemini_credential_replacement_uses_one_canonical_write(tmp_path):
@@
     assert secrets.values == {
         "gemini_api_key": "gemini-new-5678",
     }

+def test_gemini_replacement_prefers_saved_key_over_environment(tmp_path, monkeypatch):
+    monkeypatch.setenv("GEMINI_API_KEY", "environment-0000")
+    client, _, secrets = prepared_client(tmp_path)

+    response = client.post(
+        "/settings/ai/gemini/credential",
+        json={"credential": "saved-1234"},
+    )

+    assert response.json()["key_suffix"] == "…1234"
+    assert secrets.values == {"gemini_api_key": "saved-1234"}
+    assert "environment-0000" not in response.text

+def test_gemini_single_write_failure_cannot_split_canonical_state(tmp_path):
+    client, app, _ = prepared_client(tmp_path)

+    class FailingCanonicalWrite(MemorySecrets):
+        def __init__(self):
+            super().__init__()
+            self.calls = []

+        def set(self, key, value):
+            self.calls.append((key, value))
+            raise RuntimeError("keyring write failed")

+    failing = FailingCanonicalWrite()
+    app.state.secrets = failing
+    app.state.llm_service.secrets = failing
+    client = TestClient(app)

+    with pytest.raises(RuntimeError, match="keyring write failed"):
+        client.post(
+            "/settings/ai/gemini/credential",
+            json={"credential": "saved-1234"},
+        )

+    assert failing.calls == [("gemini_api_key", "saved-1234")]
+    assert failing.values == {}
```

These exact hunks cover canonical-over-legacy/environment precedence, legacy fallback, environment-only bootstrap, replacement while an environment value exists, one canonical write, and no split state when that write fails. No dual-write behavior remains in the proposed runtime or tests.

## Program Sol-0 shared proposal metadata

- Proposal ID: `CP-2.1-GEMINI-CONFIG-V1`; origin: Sol-2, Task 2.1.
- Proposal base: `82e22d7aa109a29a5fe4d7c26eb1672966317fbf` / tree `bc719a8644765e360c1a47c4a21b0747d8df8f64`.
- Accepted isolated source: `c3a22bf3fc1562efd7401f9a205d0e11f6ff0181` / tree `bc572d714b3e541c3e52a8e0309e7ee474eeae2c`. Exact proposal reference: this handoff's `## Unapplied, file-targeted shared patches for Sol-0` sections 1–5 and 2a. The metadata-correction commit containing this section uses `SELF`/`SELF_TREE`; it does not modify any proposed Git target file.
- Consolidated target files: `pyproject.toml`; new root `uv.lock`; `src/oms_hub/security/secret_store.py`; `src/oms_hub/llm/service.py`; `src/oms_hub/app.py`; `src/oms_hub/config.py`; `src/oms_hub/web/settings_routes.py`; `tests/anki/test_settings.py`; `tests/llm/test_service.py`; `tests/v2/test_llm_settings_routes.py`.
- Reason and affected interfaces: add the pinned `google-genai==2.14.0` dependency/lock; add `GEMINI_API_KEY_SECRET`, the legacy alias, and `resolve_gemini_api_key`; expose server-only `Settings.gemini_api_key_value`; pass the optional environment value into `LLMService` while resolving Gemini canonical-keyring -> legacy-keyring -> environment; return the redacted Gemini credential status `{configured, provider, key_suffix}`; preserve legacy credential compatibility.
- Proposed tests: the exact hunks cover `tests/anki/test_settings.py::test_gemini_key_accessor_is_server_only`; `tests/llm/test_service.py` canonical-over-legacy/environment, legacy-fallback, and environment-only resolver tests; and `tests/v2/test_llm_settings_routes.py` configured/redacted, unconfigured, unavailable-store, canonical replacement, saved-over-environment, and single-write-failure cases. Prior isolated replay evidence remains `9` focused Task 2.1 tests, `29` affected provider/contract tests, `17` legacy Gemini/error tests, broad Ruff, source mypy, task mypy, scope, diff, and secret scans all passing on the isolated implementation.
- Review result: Terra specification `FIX-FIRST` on `7eba39d02519f86b962b65e2b00a8231ba0cf359`, scoped specification `SHIP` on `102c3052e9705ed3195a465ede45120d02fb9788`; independent Terra quality `FIX-FIRST` on `102c3052e9705ed3195a465ede45120d02fb9788`, scoped quality `SHIP` on `dad55d67c1a8fd4b29e5f97aec924985b510d78c`. No Critical/Important findings remain; the shared hunks are still unapplied.
- Sol-6 conflict analysis: audit method/scope was a repository-wide `rg` scan of all `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol*/docs/implementation/handoffs/*.md` proposal/target references for `app.py`, `config.py`, `secret_store.py`, `llm/service.py`, `settings_routes.py`, `pyproject.toml`, and `uv.lock`, followed by inspection of the pending Sol-6 handoff. The only other current pending proposal overlap identified is Sol-6 Task 6.3 at `/Users/connor/Developer/worktrees/oms-study-automation-grounded-learning/sol6`, accepted current HEAD `76d99d6b13f581ba50044914d5c82939f0d82a10` / tree `a90f387ad1d91d84fdbfdce8baf4ff59c9ee979a`, proposal `docs/implementation/handoffs/6.3.md`: both target `src/oms_hub/app.py`. Task 2.1 adds `gemini_environment_key` at the existing `LLMService` construction; Sol-6 registers the mastery model before `Database.create_schema()`/migration and constructs `MasteryRepository` afterward. Follow manifest order: apply/review Task 2.1 first, then compose Sol-6 without dropping the Gemini argument; manually retain both independent hunks on textual conflict. Require combined app/startup, Task 2.1 settings/LLM, and Sol-6 mastery/bootstrap tests after composition. Task 0.3 is not a pending competing proposal: it modified `src/oms_hub/config.py` and `.env.example` in accepted final commit `f43e30a74861104ae7821b0abfe3edf5da296877` / tree `1b1a8c1d7322431e264478e9458af19bc1102145`, recorded as accepted provenance in Task 0.6 and included in proposal base `82e22d7`. Therefore the Task 2.1 config hunk must preserve Task 0.3's accepted `Settings.feature_flags` fields, validators, serialization, and defaults, and Sol-0 must run Task 0.3 feature/settings regressions alongside Task 2.1 integration tests. The stale Task 0.3 handoff `pending` line is not current Gate status; Task 0.6 accepted provenance is authoritative. This audit identified no other current pending cross-workstream proposal overlap in the stated scope; Sol-6 remains the only current pending overlap.
- Rollback: this docs-only correction reverts independently. Any future Task 2.1 shared apply must be a separate Sol-0 commit, reversible without reverting Sol-6 app/bootstrap changes, restoring manifest/lock/settings/secret changes while preserving legacy credential compatibility; Task 2.1 adds no schema/data migration. If later Gemini work is already integrated, disable/revert it in reverse order before removing these shared changes.
- Program Sol-0 status: `HELD` until Sol-0 reviews, applies, or rejects this proposal. No shared proposal is applied here.

## Review status

- Initial implementation under review: `7eba39d02519f86b962b65e2b00a8231ba0cf359` / tree `2d8c483e775558d16b8f0f2b1c3da72d0c87dd44`.
- Terra specification review: reviewer `Terra`; verdict `FIX-FIRST` on `7eba39d02519f86b962b65e2b00a8231ba0cf359` / tree `2d8c483e775558d16b8f0f2b1c3da72d0c87dd44`. Finding: shared-file proposals were high-level instructions rather than precise, file-targeted manifest/lock and settings/secret-store patches.
- Terra specification round-2 finding: the test proposal still left no-key, unavailable-secret-store, and second-write replacement behavior as prose; exact test hunks were added before the scoped `SHIP` below.
- Terra scoped specification re-review: verdict `SHIP` on `102c3052e9705ed3195a465ede45120d02fb9788` / tree `0b20f570db7de5021cbd65836a6ec2b881b15cba`; before identity `7eba39d02519f86b962b65e2b00a8231ba0cf359` / `2d8c483e775558d16b8f0f2b1c3da72d0c87dd44`, after identity `102c3052e9705ed3195a465ede45120d02fb9788` / `0b20f570db7de5021cbd65836a6ec2b881b15cba`. The file-targeted proposal finding was addressed.
- Terra quality review: reviewer `Terra`; verdict `FIX-FIRST` on `102c3052e9705ed3195a465ede45120d02fb9788` / tree `0b20f570db7de5021cbd65836a6ec2b881b15cba`. Findings: environment precedence could shadow a saved replacement; dual writes were non-atomic; the report identity was stale.
- Terra scoped quality re-review: verdict `SHIP` on `dad55d67c1a8fd4b29e5f97aec924985b510d78c` / tree `01f98ae5699ff6ef190145faca055d46f49b2cc1`; before identity `102c3052e9705ed3195a465ede45120d02fb9788` / `0b20f570db7de5021cbd65836a6ec2b881b15cba`, after identity `dad55d67c1a8fd4b29e5f97aec924985b510d78c` / `01f98ae5699ff6ef190145faca055d46f49b2cc1`. The precedence, single-write, consumer-resolver, and test-completeness findings were addressed.
- No Critical or Important findings remain from the completed Terra scoped reviews; shared hunks remain unapplied and all proposed runtime target files are unchanged.
- Terra metadata review: reviewer `Terra`; verdict `FIX-FIRST` on `d5ac4da5be6df4797ab87aee3fad8649be519b57` / tree `8f08076ca192a93a0554cdb9545850c42c816e47`. Important finding: the conflict audit inaccurately dismissed Task 0.3 instead of distinguishing its accepted baseline provenance from pending proposals.
- Metadata correction: replace the Task 0.3 sentence with accepted Task 0.6 provenance, preserve its Settings compatibility surface, and require its regressions during Sol-0 application. Scoped metadata re-review: `PENDING` after this containing correction commit; no final Sol `SHIP` is claimed prospectively.
- Workstream Sol final review: `PENDING` on this containing review-record commit (`SELF` / `SELF_TREE`). No final Sol `SHIP` is claimed prospectively; the resolved identity and final verdict must be recorded after this commit is reviewed.

## Verification and safety

See `task-2.1-report.md` for the resolved final identity and complete command evidence. The implementation deliberately did not install or call Gemini, access private/source data, edit `pyproject.toml`, locks, `config.py`, `secret_store.py`, settings routes, `.env` files, shared contracts, CI, or deployment state. The inherited Gate 1 native-Windows, PyMuPDF macOS signal-11, and Phase 0 test-typing exceptions remain unchanged.

## Next boundary

Task 2.2 may consume `GeminiConfig` and these error types to implement the managed async SDK client and provider-payload translation. Real SDK/live contract tests remain opt-in and require the separate dependency/secret approvals above.
