# Task 2.8 handoff — live SDK identity mismatch corrected offline

## Status

**ELEVENTH SYNTHETIC CONFIRMATION PASSED / TASK 2.8 SYNTHETIC PACKAGE MERGE-READY /
PRIVATE SHADOW EVIDENCE AND PROGRAM SOL-0 GATE ACTION REMAIN / GATE_NOT_OPEN.** The full
synthetic Task 2.8 matrix is accepted and merge-ready, but this handoff does not claim or
mutate Gate 2B. The separately authorized private Lecture 13 shadow evidence, its final
reviews, and Program Sol-0 integration/Gate action remain outstanding.

- Branch and audited base: `sol2/gemini-indexing` at exact verified integration
  head `37c1b6ac572ecdc4549f4688ab5868c4a9a85240`.
- Task 2.7 handoff commit `c54b82f` is an ancestor of the audited base.
- Central migration version 25 and `IndexJob.operation_kind`/`lease_token` are
  present on the audited base.
- Gate 2A is open. Task 2.8 depends only on completed Task 2.7 in the manifest.
- Initial offline RED checkpoint: `39ad051`.
- Initial offline GREEN checkpoint: `230cc39`.
- Independent-review RED/GREEN checkpoints: `aae069c` / `e579329` and
  `9777a5a` / `1d31885`.
- Exact independently reviewed offline candidate: `1d318852cb79cb8fbce35e698fd3b0f26bf86c6b`.
- Program Sol-0 SDK prerequisite integration: `8fa42f461ec63dd7b7f52d06754d21a18f0068b6`.
- Sol-2 history-preserving merge: `2f7cdf9d7cb8482b8b6c4f9edd1e82ebb3d98e6a`.
- Live adapter RED checkpoints: `f1ac58d`, `0333675`; initial GREEN: `bbd6fff`.
- Terra-finding RED/GREEN checkpoints: `5ec8dc2` / `1599ae9`.
- Exact corrected live adapter candidate: `1599ae9`.
- Live-failure-evidence RED/GREEN checkpoints: `e61eb1a` / `82a81c2`.
- Independent-review-finding RED/GREEN checkpoints: `b379a6c` / `8b90837`.
- Interactions API RED/GREEN checkpoints: `669cfe1` / `62ead30`.
- Interactions evidence-boundary RED/GREEN checkpoints: `69eff14` / `b962036`.
- Official wire-body RED/GREEN checkpoints: `db57294` / `14d001f`.
- Citation/timeout RED/GREEN checkpoints: `37190e5` / `ed2172f`.
- Quality-review regression coverage: `b0c18e8`.
- Positive-validation RED/GREEN checkpoints: `487418d` / `376acce`.
- Answer-validation RED/GREEN checkpoints: `80d6bd3` / `8bdcfe5`.
- Parser-diagnostic RED/GREEN checkpoints: `51eee5e` / `6a90c3d`.
- Provider-diagnostic RED/GREEN checkpoints: `d13a15c` / `ed3f25f`.
- Citation/schema split RED/GREEN checkpoints: `8bb7915` / `d95ce96`.
- Final review-finding RED/GREEN checkpoints: `206f2b5` / `0dfa0e8`.
- Diagnostic-starvation RED checkpoints: `1938abb`, `d1a227f`, `8e5fd81`;
  GREEN: `629d4ec`.
- Diagnostic review-finding RED/GREEN checkpoints: `1c4e4e5` / `50d5b97`.
- Windows portability RED/GREEN checkpoints: `bb55f10` / `dbf3a6b`.
- Windows DACL review RED/GREEN checkpoints: `c4d5349` / `05bc9cb` and
  `0ec7242` / `aac871f`.
- Live SDK identity RED/GREEN checkpoints: `a6b8b50` / `5aa394a`.
- Final identity-review RED/GREEN checkpoints: `5bbfd99` / `481a9fd`.
- Exact final offline-reviewed code head: `481a9fd8d89f5642a5a6f7c520e24da61f91c085`.
- Current handoff commit: `SELF` (resolve from the commit containing this handoff).

## Offline-safe scaffolding

`tests/live/test_gemini_file_search_contract.py` is opt-in for the actual live
test and runs deterministic fakes by default. The fake sequence specifies one
disposable `models/gemini-embedding-2` store, deterministic one-page synthetic
PDF upload/import/poll, course-only scoped query, PDF page-one citation,
minimal Pydantic structured output, omitted thinking configuration, a
wrong-lecture negative filter, document listing, and document/file/store cleanup.

The test also proves the synthetic PDF is byte-deterministic, temporary import
failure remains synthetic and still cleans up, and a separate deterministic fake
passes through the actual `IndexWorker`/`IndexRepository` path to prove durable
`RETRYABLE_FAILURE`, retry count/category, five-second backoff, and successful
resume. Provider identities are represented only by bounded hashes, and the
checked-in Gate 2B record remains explicitly `state=blocked`,
`claim=gate_not_open`.

`scripts/run-gemini-contract-smoke.py` retains the offline-safe dry-run: without
`--execute-live` it prints the boundary and performs no provider or secret-store
access. With both `--execute-live` and `RUN_LIVE_GEMINI_TESTS=1`, it reads the existing
`gemini-api-key` once through `KeyringSecretStore`, fails closed when unavailable, and
uses the pinned `google-genai==2.14.0` async File Search/Files/Operations/Interactions
surfaces. The standard redacted path does not retain the credential, raw provider
payloads, provider identities, citation text, or structured response body. The separately
gated synthetic diagnostic exception described below retains those raw synthetic details
only in its temporary outside-Git sink, protected by POSIX `0600` or the verified exclusive
current-SID Windows DACL; it still never retains the credential.

The shared dependency and root `uv.lock` were integrated by Program Sol-0 at
`8fa42f4`; Sol-2 did not edit that shared surface. Exact SDK signatures were verified
locally without a provider call before the adapter tests were written.

## TDD evidence

Test-only commit `39ad051` was run before the script or Gate record existed. It
produced four intended failures for the missing smoke scaffold and blocked record,
plus the expected skipped live test. The failure was not caused by a missing test
runner or unrelated dependency.

The GREEN command is:

```text
PATH=/private/tmp/sol1-task16-full-venv/bin:$PATH \
PYTHONPATH="$PWD/src:$PWD" \
PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring \
python -m pytest tests/live/test_gemini_file_search_contract.py -q -rs
```

The local provider/index regression command is:

```text
PATH=/private/tmp/sol1-task16-full-venv/bin:$PATH \
PYTHONPATH="$PWD/src:$PWD" \
PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring \
python -m pytest tests/providers/gemini tests/indexing -q
```

Offline verification results:

- scaffold target: `7 passed, 1 skipped`; the skip is the required live opt-in;
- local Gemini/indexing regression: `190 passed`;
- scaffold coverage: `86%`; uncovered lines are the deliberately blocked live
  entrypoint and CLI error path;
- Ruff on the script and live test: passed;
- strict mypy on the script: passed;
- Gate record JSON parse and `git diff --check`: passed;
- safe broad Python lane: exit `0`, with two expected skips (the new live opt-in
  plus the inherited safe-platform skip) and the documented Windows/PDF-adapter
  exclusions.

Live-adapter TDD added RED `f1ac58d`/`0333675` before GREEN `bbd6fff`. Initial Terra
reviews BLOCKED that GREEN on five concrete findings: eight-field metadata was compared
to a four-field identity exactly; the record retained the synthetic response body;
cleanup could mask the primary failure; operation errors lost status classification;
and a polling request was not bounded by the remaining deadline. RED `5ec8dc2`
reproduces all five; GREEN `1599ae9` requires the identity fields as a subset, records
only schema/validation/hash evidence, preserves the primary failure while attempting all
cleanup, translates status only, and uses `asyncio.timeout(remaining)`.

Corrected focused verification at `1599ae9`: live target `13 passed, 1 skipped`; combined
Gemini provider/index/live lane passed with the same single opt-in skip; focused coverage
`81%`; Ruff and strict script mypy passed. No live-provider test was run.

The first authorized synthetic attempt exposed only a fixed `GeminiProviderError`, so
RED `e61eb1a` required bounded failure-stage, normalized error, resource, and cleanup
evidence. GREEN `82a81c2` added a fixed JSON failure record and successful cleanup
evidence without retaining the credential, provider payload, raw identity, response
body, or private data.

Independent quality/security review then BLOCKED `82a81c2` on two P1 findings. A
malformed provider structured-output mapping could escape through a Pydantic traceback,
and response loss after a remote create could be misreported as no resource plus
completed cleanup. RED `b379a6c` reproduced both. GREEN `8b90837` translates malformed
structured output to a fixed `SmokeContractError` and records each resource as
`not_started`, `unknown`, or `confirmed`; cleanup becomes `unknown` when a remote outcome
cannot be proven.

Locked offline verification at exact `8b90837`:

- `tests/providers/gemini`, `tests/indexing`, and
  `tests/live/test_gemini_file_search_contract.py`: `207 passed, 1 skipped`; the skip is
  the required live opt-in;
- Ruff on the Task 2.8 script and live contract test: passed;
- strict mypy on the Task 2.8 script: passed;
- `git diff --check`: passed;
- `556b01d` and SDK integration `8fa42f4` are ancestors of `8b90837`; the branch was
  clean before this handoff-only edit.

Post-failure diagnosis and correction added two more committed RED/GREEN pairs. RED
`669cfe1` proves that the prior `models.generate_content` query does not satisfy the
current Gemini 3.7 File Search contract. GREEN `62ead30` moves only the query boundary to
the pinned SDK's `aio.interactions.create` surface, matching Google's official File
Search structured-output example at `https://ai.google.dev/gemini-api/docs/file-search`:
scoped `file_search`, `store=false`, JSON
`response_format`, and omitted generation/thinking configuration. RED `69eff14` then
reproduces the independent-review evidence findings before GREEN `b962036` adds exact
imported `document_uri` binding and hard bounds for citation metadata, locator, page, and
excerpt. It also temporarily added the SDK-generated deprecated top-level
`response_mime_type`, which the next live attempt proved remained incompatible with the
current provider contract.

Locked offline verification at exact `b962036`:

- `tests/providers/gemini`, `tests/indexing`, and
  `tests/live/test_gemini_file_search_contract.py`: `209 passed, 1 skipped`; the skip is
  the required live opt-in;
- Ruff, strict script mypy, and `git diff --check`: passed;
- Terra specification: **SHIP**, no residual findings;
- Terra quality/security: **SHIP**, no residual findings; the required MIME type,
  document binding, bounded evidence, raw-content exclusion, and cleanup/error precedence
  are satisfied;
- neither review read a credential, called the provider, or accessed private data.

The second authorized attempt used the reviewed Interactions surface but still returned
HTTP 400 at `positive_query`; three resources were confirmed, three cleanup calls were
attempted, and cleanup reported failure. The existing Hub stayed running and healthy,
and the temporary task was removed. Its bounded evidence was identical in shape to the
record below and retained no provider identity, payload, response body, request ID,
credential, or local path.

Pinned-SDK wire serialization then showed that `b962036` sent both a deprecated top-level
`response_mime_type` and `response_format.mime_type`. Google's current official Gemini
3.7 File Search structured-output Python and REST examples send MIME only inside
`response_format`. RED `db57294` requires the exact five allowed body keys; one-line GREEN
`14d001f` removes the duplicate. The locked affected lane remains `209 passed, 1 skipped`,
with Ruff, strict mypy, and diff hygiene passing. Terra specification returned **SHIP**;
Terra quality/security found no code residual and required only this handoff correction.
Neither review crossed the provider, credential, or private-data boundary.

## Authorized synthetic attempt evidence

The reviewed `8b90837` adapter was run once under separate prior authorization with the
stored key and synthetic data only. The retained record contains no credential, request
ID, provider identity, payload, response body, or local path. Its exact bounded result
was:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "provider",
  "failure_stage": "positive_query",
  "provider_status_code": 400,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

This proves only the observed cause: Gemini rejected the positive query with HTTP 400.
The deliberately redacted record does not establish the provider's more detailed reason,
so this handoff does not invent one. All three cleanup calls were attempted and cleanup
reported failure; disposable resources could therefore have remained at that point. The
temporary runner task was removed, while the existing Hub task stayed running and its
health endpoint remained `ok` before and after the attempt.

Connor later authorized one stored-key read for cleanup reconciliation only. A separately
reviewed exact-match reconciler streamed provider listings without retaining unrelated
metadata, matched only `Study Hub Task 2.8 synthetic contract` and
`task-2-8-synthetic.pdf`, and ran no query or smoke. Its bounded result was
`status=completed`, zero matching stores/documents/files, zero delete attempts, and zero
remaining matching stores/files. The Hub remained running and healthy, and the temporary
cleanup task was removed.

The first offline root-cause diagnosis corrected the query API surface from
`models.generate_content` to `interactions.create`. The second authorized attempt then
isolated the remaining mismatch to the serialized Interactions body: the deprecated
top-level MIME duplicate was not present in Google's current official example. The
retained live records do not include provider error text, so `14d001f` still requires a
separately authorized live confirmation; this handoff does not claim that confirmation.

Initial Terra review of `230cc39` returned **BLOCK** on two offline-preparation
findings: the temporary failure fake proved cleanup but not durable retry/resume,
and the runbook did not say that the hard-coded live guard still requires a later
code-wiring change. Test-only checkpoint `aae069c` reproduced both findings before
GREEN `e579329`. Terra specification review then returned
**SHIP_OFFLINE_PREPARED**. Terra quality re-review found only that the actual CLI
hard-block exception retained the superseded owner action. Test-only `9777a5a`
reproduced that operator-message gap before the one-string GREEN correction in
`1d31885`.

Final Terra specification and quality re-reviews both returned
**SHIP_OFFLINE_PREPARED** at exact `1d31885`. They confirmed the durable retry
fixture, deterministic local-only behavior, aligned operator/runbook/artifact
messages, opt-in guard, and absence of provider/Keyring/private-data access. Both
explicitly confirmed that Gate 2B remains blocked and unclaimed.

Initial live-adapter Terra specification and quality/security reviews both returned
**BLOCK** at exact `bbd6fff`; their complete findings are enumerated in the TDD section
above.

Final independent re-reviews both returned **SHIP** on exact clean candidate/handoff
`d3c6b235e4f25d3da3882823d2f0d53bc4c42abc`:

- Terra specification: **SHIP**, no residual blockers; the full-metadata identity subset,
  answer-body redaction, and truthful Gate 2B/live boundary resolve its prior findings.
- Terra quality/security: **SHIP**; corrected code `1599ae9` resolves all five findings
  covering metadata, redaction, cleanup-error precedence, operation-error classification,
  and remaining-deadline polling. It confirmed no secret/provider boundary was crossed and
  Gate 2B remains closed.

Fresh independent completion-evidence reviews of exact clean code head `8b90837` also
returned **SHIP**:

- Terra specification: **SHIP**, no residual findings; the redacted failure record,
  malformed-output boundary, conservative resource/cleanup states, and remaining
  Gate 2B boundary are correct.
- Terra quality/security: **SHIP**, no residual findings; both P1 findings are resolved,
  cleanup precedence and CLI coverage hold, and ambiguous outcomes remain `unknown`.
- Both reviewers explicitly performed read-only offline reviews without provider,
  credential, or private-data access.

Initial reviews of Interactions candidate `62ead30` BLOCKED on the required top-level
response MIME interpretation, imported-document citation binding, bounded citation
fields, and the stale handoff. The evidence findings were reproduced at RED `69eff14` and
resolved at GREEN `b962036`. Live evidence plus the current official example superseded
the MIME interpretation at RED/GREEN `db57294` / `14d001f`. Fresh specification review
returned **SHIP**; quality/security found only the stale handoff sentence corrected here.

## Third authorized attempt and root-cause correction

The next separately authorized synthetic attempt ran exact archive content from clean
`b88731e` with `google-genai==2.14.0`. The Hub remained running and its health endpoint was
`ok` before and after; the temporary runner task was removed. The request advanced past
the prior HTTP 400 and returned after approximately 281 seconds, but the local adapter
reported a contract failure while processing the positive query. Three disposable
resources were confirmed, all three cleanup calls were attempted, and cleanup reported
failure:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "contract",
  "failure_stage": "positive_query",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

That bounded record contains no provider body or local exception reason, so it cannot by
itself prove which local contract check rejected the response. Exact offline inspection
nevertheless found two concrete adapter defects. First, Google's current Interactions
contract makes `document_uri` optional and its File Search citation examples use
`file_name` plus `source`; the adapter incorrectly required `document_uri` to equal the
imported document resource. Second, `GeminiConfig.request_timeout_seconds=120` never
reached `google-genai`, whose `HttpOptions.timeout` is expressed in milliseconds. The
observed duration is therefore consistent with the missing timeout, but is not claimed as
provider proof.

Committed RED `37190e5` reproduces both defects with the documented citation shape and
the exact SDK-construction contract. GREEN `ed2172f` wires the existing timeout once in
`GeminiClientFactory`, binds a citation to the exact uploaded display name, accepts an
absent `document_uri`, still rejects a present conflicting `document_uri`, and emits only
an allowlisted fixed `contract_reason` for the newly diagnosed local failures. Independent
quality review found one regression-test gap for the retained conflicting-URI guard;
test-only commit `b0c18e8` adds that coverage.

Locked offline verification at exact clean `b0c18e8`:

- `tests/providers/gemini`, `tests/indexing`, and
  `tests/live/test_gemini_file_search_contract.py`: `210 passed, 1 skipped`; the skip is
  the required live opt-in;
- Ruff on the four affected source/test files: passed;
- strict mypy on the smoke script and shared Gemini client: passed;
- `git diff --check`: passed;
- SDK integration `8fa42f4` and reviewed base `556b01d` are ancestors;
- Terra specification: **SHIP**, no residual findings;
- Terra quality/security: initial **BLOCK** only for the conflicting-URI regression-test
  gap, then **SHIP** at `b0c18e8`, no residual findings.

Neither the offline correction nor either exact-head re-review called the provider, read
a credential, accessed private data, or changed the running Hub.

## Fourth authorized attempt and positive-validation correction

Connor separately authorized one cleanup-reconciliation key read followed by one smoke
key read. The exact-match reconciler completed first with zero matching stores, files, or
documents, zero delete attempts, no failures, and zero remaining matching stores/files.
Hub health was `ok` before and after, and the temporary cleanup task was removed.

The smoke then ran exact clean head `7d08fd2` containing independently reviewed code
`b0c18e8`, with archive SHA-256
`85e046c00da9b0484aee252816d0b1c884d71aee0fb55b8fa043e56ccf56de14`. The archived
smoke script matched the workspace, both Terra reviewers returned **SHIP** on the exact
archive/wrapper preflight, and the Hub remained `ok`. The request and local response
parsing completed, but post-query acceptance failed at `positive_validation`. Three
resources were confirmed, all three cleanup calls were attempted, and cleanup reported
failure:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "contract",
  "failure_stage": "positive_validation",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

The retained record contains no response body or fixed validation reason, so it cannot
prove whether answer equality, citation cardinality, or page/excerpt resolution triggered
the failure. Offline contract inspection found one concrete over-strict acceptance rule:
the manifest requires verification of a citation, while the runner required exactly one
citation and rejected multiple fully matching annotations from the same document.

Committed RED `487418d` reproduces that mismatch and the missing safe failure reason.
GREEN `376acce` now requires at least one citation and selects one that matches the exact
imported document, PDF page one, and synthetic excerpt; strict scope/file/document parsing
remains unchanged. It also adds fixed allowlisted reasons for answer, absent-citation, and
unresolved-citation failures. The locked affected lane passed `212 passed, 1 skipped`,
Ruff and strict mypy passed, and both independent Terra reviewers returned **SHIP** at
exact `376acce`. No further provider call or credential read occurred after the failed
smoke, and the temporary smoke task was removed.

## Fifth authorized attempt and answer-validation correction

Connor authorized the ordered five-step Gate 2B sequence. Step 1 cleanup reconciliation
completed with zero matching or remaining synthetic resources, no failures, and Hub
health `ok`. Step 2 then ran exact clean archive head `b8d7069`, containing independently
reviewed code `376acce`, after both Terra reviewers returned **SHIP** on the exact
archive/wrapper hashes. The Hub remained `ok` throughout.

The live request again completed through response and citation parsing. It failed closed
at `positive_validation` with the new fixed `contract_reason`:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "contract_reason": "positive_answer_invalid",
  "error_category": "contract",
  "failure_stage": "positive_validation",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

That evidence proves structured output parsed but the answer did not satisfy the prior
combined exact-answer/support check. Offline inspection found that the positive prompt
itself disclosed the complete expected fact while validation required an exact
full-sentence echo. This neither cleanly proved retrieval nor accepted the natural
marker-only structured answer.

Committed RED `80d6bd3` reproduces the answer disclosure, marker-only rejection, and
ambiguous fixed reason. GREEN `8bdcfe5` asks for the marker without disclosing it, requires
`supported=true` and the undisclosed marker in the structured answer, and retains the
stronger full-fact, page-one, document-, file-, and scope-bound citation checks. The
locked affected lane passed `215 passed, 1 skipped`; Ruff and strict mypy passed; both
independent Terra reviewers returned **SHIP** at exact `8bdcfe5`.

Because Step 2 did not pass, the ordered sequence stopped before Step 3 private Lecture
13 access, Step 4 Sol-9/live-evidence acceptance, and Step 5 Program Sol-0 integration.
No private data was accessed and no integration or Gate record mutation occurred. The
temporary smoke task was removed; no further provider call or credential read occurred
under that authorization.

## Sixth authorized attempt and parser-diagnostic correction

Connor authorized a new cleanup-reconciliation key read followed by one synthetic smoke.
Cleanup completed first with zero matching or remaining resources, no failures, and Hub
health `ok`. Both Terra reviewers returned **SHIP** on the exact archive/wrapper preflight
for clean head `1d8927e`, containing reviewed implementation `8bdcfe5`; all local and NUC
hashes matched.

The smoke created and imported the disposable synthetic resources, but failed closed
during `positive_query`. All three cleanup calls were attempted and cleanup reported
failure; Hub health remained `ok`:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "contract",
  "failure_stage": "positive_query",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

The absence of `contract_reason` proves that the prior candidate still had unclassified
positive-query parser exits; it does not identify which citation or usage check failed.
Committed RED `51eee5e` covers scope, metadata, document, file, excerpt, page, and usage
rejections. GREEN `6a90c3d` assigns each one a fixed allowlisted reason without relaxing
validation or retaining raw content/provider identities. The locked affected lane passed
`216 passed, 1 skipped`; Ruff and strict mypy passed; both independent Terra reviewers
returned **SHIP** at exact `6a90c3d`.

Because the synthetic Step 2 smoke did not pass, private Lecture 13 access, Sol-9 live
acceptance, Program Sol-0 integration, and Gate 2B mutation were not performed. The
temporary smoke task was removed, and no further provider call or credential read
occurred under that authorization.

## Seventh authorized attempt and provider-diagnostic correction

Connor authorized another cleanup-reconciliation key read and one synthetic smoke.
Cleanup completed with zero matching or remaining resources and Hub health `ok`. Both
Terra reviewers returned **SHIP** on the exact archive/wrapper preflight for clean head
`21f81a7`, containing reviewed implementation `6a90c3d`; local and NUC hashes matched.

The smoke created/imported the synthetic resources but failed during `positive_query`
below the local parser. All three cleanup calls were attempted and cleanup reported
failure; Hub health remained `ok`:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "provider",
  "failure_stage": "positive_query",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

This proves the citation parser was not reached and the prior shared translator collapsed
SDK transport, response-shape `ValueError`, and unknown failures into the same generic
non-retryable provider category. It does not prove which underlying exception occurred.

Committed RED `d13a15c` reproduces that ambiguity. GREEN `ed3f25f` classifies HTTPX
timeouts/transports as retryable transient failures, SDK shape/`ValueError` failures as
contract failures, and remaining unknowns as provider failures. It attaches only a fixed
allowlisted `diagnostic_code`, which the smoke emits as `provider_reason`; raw exceptions
and payloads remain excluded. The locked affected lane passed `217 passed, 1 skipped`;
Ruff and strict mypy passed; both independent Terra reviewers returned **SHIP** at exact
`ed3f25f`.

Because the synthetic smoke did not pass, private Lecture 13 access, Sol-9 live
acceptance, Program Sol-0 integration, and Gate 2B mutation were not performed. The
temporary smoke task was removed, and no further provider call or credential read
occurred under that authorization.

## Eighth authorized attempt and citation/schema-query correction

Connor authorized cleanup reconciliation followed by one synthetic smoke. Cleanup first
completed with zero matching or remaining resources and Hub health `ok`. Both Terra
reviewers returned **SHIP** on clean head `c4c3b11`; exact local/runner artifact hashes
matched.

The smoke created and imported all three disposable resources. Its positive query parsed
the answer but failed closed at `positive_validation` with
`contract_reason=positive_citation_missing`. All three cleanup calls were attempted and
cleanup reported failure; Hub health remained `ok`. The retained evidence contains no
credential, provider identity, payload, response body, request ID, or local path.

The current official File Search examples show structured output and citation-bearing
responses as separate response forms; citations are optional response annotations rather
than a guaranteed structured-output companion. The existing two-query budget therefore
now uses an unstructured positive query for marker/citation evidence and the wrong-scope
negative query for the strict `SmokeAnswer` schema. RED `8bb7915` reproduced the mismatch;
GREEN `d95ce96` made only that split. Both independent reviews then found one shared
negative-validation gap. RED `206f2b5` proves that `supported=false` with a nonempty answer
was accepted; GREEN `0dfa0e8` now requires no citations, `supported=false`, and an exactly
empty answer.

Locked verification at exact `0dfa0e8`: affected provider/index/live lane `222 passed, 1
skipped` (the required live opt-in). An isolated locked-lane reproduction of exact
`ed3f25f` yielded `219 passed, 1 skipped`, correcting its stale `217` handoff count; two
tests at `8bb7915` and one at `206f2b5` produce the current `222`. Ruff passed, strict mypy
passed, and both independent Terra specification and quality/security re-reviews returned
**SHIP**. SDK integration `8fa42f4` and verified integration head `37c1b6a` are ancestors.
No provider call, credential read, private data access, push, deploy, merge, Gate mutation,
or Hub interruption occurred during these corrections or reviews.

## Offline diagnostic-starvation correction

The accepted diagnostic plan is implemented at exact reviewed head `50d5b97`. The locked
tests assert `google-genai==2.14.0` and use actual `Interaction`, `ModelOutputStep`,
`TextContent`, `FileCitation`, `FileSearchResultStep`, and `Usage` models. Optional absent,
malformed, and mismatched fields have distinct fixed diagnoses; absent `document_uri` is
diagnosed but nonfatal when metadata/file/page/excerpt bind. Usage reads only
`total_input_tokens`/`total_output_tokens`, preserving legitimate zeroes. An actual SDK
mock transport proves the serialized Interactions body and absence of top-level
`response_mime_type`.

The optional raw diagnostic sink activates only for the hard-coded synthetic
course/exam/lecture/source revision plus exact generated fixture digest and an explicit
absolute path outside Git. An injected session, private Lecture 13 identifier, any other
identifier/digest, or an in-repository path fails before credential/provider access. The
default session creates a fresh store and imports only that synthetic fixture. The sink is
atomic and private: POSIX mode `0600`, or on Windows a protected DACL with exactly one
explicit, non-inherited current-SID FullControl Allow ACE. It is size-bounded with hard
overflow failure, strips header maps, credentials and known secret bytes, never writes
stdout/chat/handoff, and exposes explicit deletion after diagnosis.

The redacted committed result now records creation/upload/import prerequisites and
independent positive answer, citation presence, document/file/scope/page/excerpt,
negative structured output, wrong-lecture filter, document listing, usage, and per-resource
cleanup outcomes. Failures do not suppress independent downstream checks. Cleanup runs
once in `finally`; local raw diagnostics distinguish each resource's delete-request failure
from context-close failure without changing the shared error translator, IndexWorker, or
cleanup success rules. `run_temporary_failure_fixture` and Task 2.7 remain unchanged.

Locked verification at `50d5b97`: provider/index/live lane `248 passed, 1 skipped` (the
required live opt-in), focused script coverage `82%`, Ruff passed, strict mypy passed, and
diff hygiene passed. Independent Terra specification and quality/security re-reviews both
returned **SHIP**. No provider call, credential read, resource reconciliation, private data
access, push, deploy, merge, Gate mutation, or Hub interruption occurred.

The first authorized next-step preflight on exact `52b7ad3` stopped before either stored-key
read because the locked Windows interpreter has no `os.fchmod`. No reconciliation,
provider call, resource creation/deletion, or diagnostic run occurred. RED `bb55f10` and
GREEN `dbf3a6b` added a portable fallback, but quality review correctly rejected Windows
`chmod(0600)` as insufficient. RED/GREEN `c4d5349` / `05bc9cb` added a protected
current-user SID DACL; quality review then required proof that no extra explicit ACE
remained. RED/GREEN `0ec7242` / `aac871f` now inspects the literal file with `Get-Acl` and
fails before raw writes unless the DACL is protected and contains exactly one explicit,
non-inherited current-SID FullControl Allow ACE.

Locked verification at exact `aac871f`: provider/index/live lane `252 passed, 1 skipped`,
Ruff passed, strict mypy passed, and diff hygiene passed. Both independent Terra reviewers
returned **SHIP**. An isolated real-Windows create/ACL-inspect/atomic-close/delete check
then passed on the NUC; the diagnostic was deleted and Hub health remained `ok`. No key,
provider, private data, reconciliation, push, deploy, merge, or Gate action occurred. The
authorization was explicitly bound to `52b7ad3`, so corrected head `aac871f` still requires
fresh authorization for reconciliation and one final diagnostic smoke.
The isolated NUC archive, extracted scratch directory, and DACL-check script were also
removed; the post-cleanup check found none of those paths or the diagnostic remaining.

## Ninth authorized diagnostic attempt and correction

Connor authorized the exact two-step sequence on clean handoff head `d755385`. The first
stored-key read completed exact-match reconciliation with zero matching or remaining
synthetic stores/files, zero delete attempts, and no failures. The Hub was `ok` before and
after. The temporary reconciliation task and scratch were removed before step two.

The second stored-key read ran one synthetic diagnostic smoke. Store creation, PDF upload,
import completion, positive answer, citation presence/scope/page, usage, negative structured
output, and wrong-lecture filtering passed. The redacted matrix failed document/file/excerpt
citation binding, document listing, and document cleanup; file and forced-store cleanup
passed. The Hub remained running and `ok`. Private comparison-only analysis established the
google-genai 2.14.0 shapes without emitting raw values or identities: citation
`document_uri` is the exact store identity, citation `file_name` is the uploaded file's
terminal identity, the bounded citation source contains the synthetic fact wrapped in line
breaks, and the import operation's bare document identity becomes the exact listed/delete
identity when qualified under the current store. The failed bare document delete returned
not-found; the later forced-store delete passed.

RED `a6b8b50` reproduced all observed shapes; GREEN `5aa394a` qualifies the import document,
binds citation store/file identities, and permits bounded line breaks while rejecting other
controls. Terra then identified two exact edge contracts. RED `5bbfd99` reproduced them;
GREEN `481a9fd` preserves both exact store-form and exact document-form citation locators
while rejecting cross-store or nested full document identities.

Locked verification at exact `481a9fd`: provider/index `192 passed`; live contract `62
passed, 1 skipped` for the required opt-in; google-genai `2.14.0`; script coverage `82%`;
Ruff, strict mypy, diff hygiene, cleanliness, and ancestry from `8fa42f4` and `d755385`
passed. Sol-9, Terra specification, and Terra quality/security all returned **SHIP** on the
exact corrected tree. The private raw diagnostic, temporary task, archive, project/run
scratch, and analyzers were deleted after correction and review; the absence check passed
and Hub health remained `ok`. No later key read or provider call occurred under that
authorization.

## Tenth authorized confirmation attempt

Connor freshly authorized one stored-key read and one complete synthetic confirmation on
exact clean handoff head `fd3e3f0`, containing reviewed code `481a9fd`. Exact local/NUC
archive, script, and wrapper hashes matched; google-genai was `2.14.0`; the locked Windows
runner was present; and the Hub task was running with health `ok` before the key read.

The single confirmation proved store creation, PDF upload, import completion, positive
answer, every citation binding, usage, document listing, and document/file/store cleanup.
The independent negative query returned retryable provider HTTP 500, so
`negative_structured_output=negative_query_failed` and wrong-lecture filtering remained
blocked by that query. Cleanup attempted and completed all three exact resources. Hub
health was `ok` before, after, and following removal of the temporary task, archive,
wrapper, and project/run scratch. No raw diagnostic was created or retained.

Sol-9 returned **BLOCK as Gate evidence / code SHIP**; Terra specification returned
**BLOCK as Gate evidence**; Terra quality/security returned **SHIP** with no code or
security defect. All agree the remaining action is a freshly authorized complete synthetic
confirmation on the same reviewed implementation. No cleanup reconciliation is required
by this attempt because every cleanup check passed. No retry, later key read, provider
call, private-data access, or Gate action occurred.

## Eleventh authorized confirmation — synthetic evidence accepted

Connor freshly authorized one stored-key read and one complete synthetic confirmation on
exact clean handoff head `22459a5`, containing reviewed code `481a9fd`. Exact local/NUC
archive, script, and wrapper hashes matched; google-genai was `2.14.0`; the locked runner
was present; and the Hub task was running with health `ok` before the key read.

The single run exited successfully and passed the complete matrix: disposable store/file/
document create-import-wait, positive answer, citation presence plus exact document/file/
scope/page/excerpt binding, usage, structured negative output, wrong-scope non-retrieval,
document listing, and document/file/store cleanup. The bounded record reported one PDF,
page 1, operation `done`, citation resolution `1.0`, `negative_scope_retrieved=false`,
validated `SmokeAnswer`, 1,358 indexed bytes, 56 input tokens, 34 output tokens, 39,875 ms,
and no warnings. Provider identities remained hashed; no raw response, credential, private
data, or diagnostic was retained.

Cleanup attempted and completed all three resources. Hub health was `ok` before, after,
and following removal of the temporary task, archive, wrapper, and project/run scratch;
the absence check passed. No reconciliation or further synthetic retry is required.

Locked verification remained green: provider/index `192 passed`; live contract `62 passed,
1 skipped` for the required opt-in; script coverage `82%`; Ruff, strict mypy, and diff
hygiene passed. Sol-9 returned **APPROVE / SHIP**; both Terra specification and Terra
quality/security returned **SHIP**. All three reviewers declared reviewed code `481a9fd`
and the synthetic Task 2.8 evidence merge-ready. No later key read, provider call,
private-data access, or Gate action occurred.

## Exact live permission boundary and runbook

No live step may begin until all of the following are true:

1. Connor explicitly authorizes one synthetic Task 2.8 Gemini smoke, including
   disposable store/file/document creation, two queries, cleanup, and provider
   quota/cost.
2. Program Sol-0 integrates and reviews `google-genai==2.14.0` in the shared
   dependency surface. Satisfied at `8fa42f4`.
3. Sol-2 implements the live `SmokeSession` against the exact SDK/model/API combination,
   wires it into `run_authorized_live_smoke`, and obtains independent specification and
   quality/security approval. Satisfied offline by implementation lineage through
   exact reviewed head `481a9fd`.
4. The credential is already stored under the existing approved secret-store key
   `gemini-api-key`. Do not paste it into the command, environment, repository,
   logs, or this handoff. Authorization must explicitly permit the runner to read
   that stored credential for this one smoke.
5. The operator verifies the target account/project permits disposable resources
   and accepts the cost/quota impact.

Only after those conditions and a read-only preflight review may a separately authorized
synthetic command cross the provider boundary:

```text
RUN_LIVE_GEMINI_TESTS=1 \
python scripts/run-gemini-contract-smoke.py --execute-live
```

The run must fail closed if the SDK, stored credential, cleanup capability, exact
model, embedding model, metadata filtering, citation/page mapping, structured
output, or redaction contract is unavailable. Retain only the redacted record;
never retain the key, local paths, or synthetic response body in Git, stdout, chat,
or the handoff. A separately authorized diagnostic run may add an absolute
`--synthetic-diagnostic-output` path outside Git. That mode retains the full synthetic raw
SDK evidence only in its atomic local file, protected by POSIX `0600` or the verified
single-current-SID Windows DACL above, and requires deletion after diagnosis.

The private Lecture 13 shadow index is a separate authorization. Connor must
authorize private-source access and identify or approve selection of the opaque
source revision. The controlled operator may retain only source revision hash,
document types, page/slide count, provider operation states, citation resolution
rate, duration, byte/token usage, and warnings. No source text, private path, raw
provider identity, or credential may enter Git or chat.

Finally Sol-9 and both Terra reviewers must inspect the exact live evidence, and
Program Sol-0 must integrate the accepted candidate and replace the blocked record.
Only that later reviewed commit may claim Gate 2B.

## Schema-29 private-preflight consumer adaptation

Program Sol-0 integrated the reviewed schema-29 read-only projection at
`b23eed5c73c968df33d97f9c9733cecf9fea96c9`. The authorized history-preserving
composition `a65ebdf75b98723eb912b38170b37c34816e0bc5` contains that prerequisite and
the prior Sol-2 handoff `aaac70f18efc1915bf62b3abf258db88e61448b4`.

Sol-2 then added only the missing consumer path:

- RED `3b49e8db528ffcc6d92d4959942b4603080364ea` executed the focused test and failed
  because `prepare_private_shadow_index_input` did not exist.
- GREEN `17fac6cf6fa689cab980d23031e8bec448720381` delegated the private input projection
  to `project_schema29_index_input(...)`, passing only the existing artifact service's
  ingestion/catalog handles, the artifact service itself, schema version, and protected
  materialization root.
- Terra specification correctly BLOCKED that first GREEN because the helper was not yet
  consumed by a real preflight flow.
- Review RED `001f83c70468f350a25be06706cc24ece5bd7d9d` executed the allowlisted-flow test
  and failed because `run_private_shadow_preflight` did not exist.
- Review GREEN `026394916d25ce3603fa9e0faaa7f684451c7602` wired the actual preflight flow.
  It consumes the schema-29 projection, builds the accepted `IndexManifest`, validates the
  canonical PDF and slide-only evidence, and emits only source-revision hash, document
  types, page/slide counts, preflight state, byte usage, and warnings. The script contains
  no `source_revisions` SQL or `KnowledgeRepository` path.

Exact locked verification at `0263949`:

- affected Gemini provider/indexing, source-trust compatibility, knowledge, and live-offline
  lane: `305 passed, 1 skipped`; the skip is the required live-provider opt-in;
- focused live contract: `64 passed, 1 skipped`;
- smoke-script branch coverage: `82%`;
- `google-genai==2.14.0`, Ruff, strict mypy, and diff hygiene: passed;
- no diff from reviewed `481a9fd` under `src/oms_hub/providers/**` or
  `src/oms_hub/indexing/**`;
- Gate 2B record unchanged at `state=blocked`, `claim=gate_not_open`.

Independent exact-candidate re-reviews both returned **SHIP** on
`026394916d25ce3603fa9e0faaa7f684451c7602`: Terra specification found the real
preflight flow correctly wired, and Terra quality/security confirmed the bounded evidence,
schema-29/scratch fail-closed delegation, absence of schema-25 queries, and unchanged
provider/live/secret behavior.

## Current private-shadow boundary

Connor's authorization for the controlled private Lecture 13 shadow index remains active.
The next operation may be the protected, read-only schema-29 private preflight selecting the
already approved opaque current slide revision. That operation must stop before credential or
provider access unless the projection, source hashes, canonical inputs, manifest, and cleanup
all pass. A successful preflight may then continue under the existing authorization to the
single stored-key read and private shadow provider sequence; no additional permission is
required.

This offline adaptation did not access private source content, read a credential, call Gemini,
create a provider resource, push, deploy, or mutate Gate 2B. Gate 2B remains closed until the
private evidence is produced, Sol-9 and both Terra reviewers accept the exact evidence, and
Program Sol-0 performs the later integration/Gate action.

## Canonical private slide-count correction

The first protected schema-29 preflight passed authority, source and derived hashes, manifest,
canonical PDF, cleanup, and Hub health, but reported 180 slides because the shared preflight
counted distinct evidence locator strings rather than canonical slide numbers.

- RED `2e33c5561c36464bcacd20faff77d16f81b359d0` reproduced same-slide locator inflation and
  added accepted and rejected locator contracts.
- GREEN `73be11097ae7c44e6d9f06c1e8f4d8d112cbc19c` extracts the slide number from every
  slide locator, validates the complete locator, and counts unique slide numbers.
- Terra quality/security found that the first parser's `\s+` accepted tab and newline
  separators. RED `0e6593b445e88995320774660a006ad0e2b3f010` reproduced both cases; GREEN
  `381452776dbe395214efecf8e7698c92b1aebb40` requires one literal ASCII space after
  the optional `slide` prefix.

Exact locked verification at `3814527`:

- affected provider/indexing/schema-29/knowledge/live-offline lane: `322 passed, 1 skipped`;
  the skip is the required live-provider opt-in;
- focused live contract: `81 passed, 1 skipped`;
- smoke-script branch coverage: `82%`;
- `google-genai==2.14.0`, Ruff, strict script mypy with the checkout source path, and diff
  hygiene: passed;
- no diff from `ee17f4f` under shared provider/indexing code or the Gate 2B record;
- Gate 2B remains `state=blocked`, `claim=gate_not_open`.

Independent exact-candidate re-reviews returned **SHIP** on
`381452776dbe395214efecf8e7698c92b1aebb40`: Terra specification, Terra
quality/security, and the separate `Review blocked gate summary` task found no residual
in-scope issue. The latter also noted two baseline-only broad-environment observations that
reproduce at `ee17f4f` and are not introduced by this candidate.

No private source, credential, provider, resource, deployment, merge, push, or Gate action
occurred during implementation or review. The next operation remains only the already
authorized protected read-only schema-29 preflight. Credential and provider operations stay
conditional on that preflight's success.
