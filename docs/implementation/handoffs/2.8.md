# Task 2.8 handoff — live SDK identity mismatch corrected offline

## Status

**ELEVENTH SYNTHETIC CONFIRMATION PASSED / TASK 2.8 SYNTHETIC PACKAGE MERGE-READY /
PRIVATE SHADOW EVIDENCE AND PROGRAM SOL-0 GATE ACTION REMAIN / GATE_NOT_OPEN.** The full
synthetic Task 2.8 matrix is accepted and merge-ready, but this handoff does not claim or
mutate Gate 2B. The separately authorized private Lecture 13 shadow evidence, its final
reviews, and Program Sol-0 integration/Gate action remain outstanding.

- Branch and audited base: `sol2/gemini-indexing` at exact verified integration
  head `37c1b6ac572ecdc4549f4688ab5868c4a9a85240`.
- Task 2.7 handoff commit `c54b82f` is an ancestor of the audited base.
- Central migration version 25 and `IndexJob.operation_kind`/`lease_token` are
  present on the audited base.
- Gate 2A is open. Task 2.8 depends only on completed Task 2.7 in the manifest.
- Initial offline RED checkpoint: `39ad051`.
- Initial offline GREEN checkpoint: `230cc39`.
- Independent-review RED/GREEN checkpoints: `aae069c` / `e579329` and
  `9777a5a` / `1d31885`.
- Exact independently reviewed offline candidate: `1d318852cb79cb8fbce35e698fd3b0f26bf86c6b`.
- Program Sol-0 SDK prerequisite integration: `8fa42f461ec63dd7b7f52d06754d21a18f0068b6`.
- Sol-2 history-preserving merge: `2f7cdf9d7cb8482b8b6c4f9edd1e82ebb3d98e6a`.
- Live adapter RED checkpoints: `f1ac58d`, `0333675`; initial GREEN: `bbd6fff`.
- Terra-finding RED/GREEN checkpoints: `5ec8dc2` / `1599ae9`.
- Exact corrected live adapter candidate: `1599ae9`.
- Live-failure-evidence RED/GREEN checkpoints: `e61eb1a` / `82a81c2`.
- Independent-review-finding RED/GREEN checkpoints: `b379a6c` / `8b90837`.
- Interactions API RED/GREEN checkpoints: `669cfe1` / `62ead30`.
- Interactions evidence-boundary RED/GREEN checkpoints: `69eff14` / `b962036`.
- Official wire-body RED/GREEN checkpoints: `db57294` / `14d001f`.
- Citation/timeout RED/GREEN checkpoints: `37190e5` / `ed2172f`.
- Quality-review regression coverage: `b0c18e8`.
- Positive-validation RED/GREEN checkpoints: `487418d` / `376acce`.
- Answer-validation RED/GREEN checkpoints: `80d6bd3` / `8bdcfe5`.
- Parser-diagnostic RED/GREEN checkpoints: `51eee5e` / `6a90c3d`.
- Provider-diagnostic RED/GREEN checkpoints: `d13a15c` / `ed3f25f`.
- Citation/schema split RED/GREEN checkpoints: `8bb7915` / `d95ce96`.
- Final review-finding RED/GREEN checkpoints: `206f2b5` / `0dfa0e8`.
- Diagnostic-starvation RED checkpoints: `1938abb`, `d1a227f`, `8e5fd81`;
  GREEN: `629d4ec`.
- Diagnostic review-finding RED/GREEN checkpoints: `1c4e4e5` / `50d5b97`.
- Windows portability RED/GREEN checkpoints: `bb55f10` / `dbf3a6b`.
- Windows DACL review RED/GREEN checkpoints: `c4d5349` / `05bc9cb` and
  `0ec7242` / `aac871f`.
- Live SDK identity RED/GREEN checkpoints: `a6b8b50` / `5aa394a`.
- Final identity-review RED/GREEN checkpoints: `5bbfd99` / `481a9fd`.
- Exact final offline-reviewed code head: `481a9fd8d89f5642a5a6f7c520e24da61f91c085`.
- Current handoff commit: `SELF` (resolve from the commit containing this handoff).

## Offline-safe scaffolding

`tests/live/test_gemini_file_search_contract.py` is opt-in for the actual live
test and runs deterministic fakes by default. The fake sequence specifies one
disposable `models/gemini-embedding-2` store, deterministic one-page synthetic
PDF upload/import/poll, course-only scoped query, PDF page-one citation,
minimal Pydantic structured output, omitted thinking configuration, a
wrong-lecture negative filter, document listing, and document/file/store cleanup.

The test also proves the synthetic PDF is byte-deterministic, temporary import
failure remains synthetic and still cleans up, and a separate deterministic fake
passes through the actual `IndexWorker`/`IndexRepository` path to prove durable
`RETRYABLE_FAILURE`, retry count/category, five-second backoff, and successful
resume. Provider identities are represented only by bounded hashes, and the
checked-in Gate 2B record remains explicitly `state=blocked`,
`claim=gate_not_open`.

`scripts/run-gemini-contract-smoke.py` retains the offline-safe dry-run: without
`--execute-live` it prints the boundary and performs no provider or secret-store
access. With both `--execute-live` and `RUN_LIVE_GEMINI_TESTS=1`, it reads the existing
`gemini-api-key` once through `KeyringSecretStore`, fails closed when unavailable, and
uses the pinned `google-genai==2.14.0` async File Search/Files/Operations/Interactions
surfaces. The standard redacted path does not retain the credential, raw provider
payloads, provider identities, citation text, or structured response body. The separately
gated synthetic diagnostic exception described below retains those raw synthetic details
only in its temporary outside-Git sink, protected by POSIX `0600` or the verified exclusive
current-SID Windows DACL; it still never retains the credential.

The shared dependency and root `uv.lock` were integrated by Program Sol-0 at
`8fa42f4`; Sol-2 did not edit that shared surface. Exact SDK signatures were verified
locally without a provider call before the adapter tests were written.

## TDD evidence

Test-only commit `39ad051` was run before the script or Gate record existed. It
produced four intended failures for the missing smoke scaffold and blocked record,
plus the expected skipped live test. The failure was not caused by a missing test
runner or unrelated dependency.

The GREEN command is:

```text
PATH=/private/tmp/sol1-task16-full-venv/bin:$PATH \
PYTHONPATH="$PWD/src:$PWD" \
PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring \
python -m pytest tests/live/test_gemini_file_search_contract.py -q -rs
```

The local provider/index regression command is:

```text
PATH=/private/tmp/sol1-task16-full-venv/bin:$PATH \
PYTHONPATH="$PWD/src:$PWD" \
PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring \
python -m pytest tests/providers/gemini tests/indexing -q
```

Offline verification results:

- scaffold target: `7 passed, 1 skipped`; the skip is the required live opt-in;
- local Gemini/indexing regression: `190 passed`;
- scaffold coverage: `86%`; uncovered lines are the deliberately blocked live
  entrypoint and CLI error path;
- Ruff on the script and live test: passed;
- strict mypy on the script: passed;
- Gate record JSON parse and `git diff --check`: passed;
- safe broad Python lane: exit `0`, with two expected skips (the new live opt-in
  plus the inherited safe-platform skip) and the documented Windows/PDF-adapter
  exclusions.

Live-adapter TDD added RED `f1ac58d`/`0333675` before GREEN `bbd6fff`. Initial Terra
reviews BLOCKED that GREEN on five concrete findings: eight-field metadata was compared
to a four-field identity exactly; the record retained the synthetic response body;
cleanup could mask the primary failure; operation errors lost status classification;
and a polling request was not bounded by the remaining deadline. RED `5ec8dc2`
reproduces all five; GREEN `1599ae9` requires the identity fields as a subset, records
only schema/validation/hash evidence, preserves the primary failure while attempting all
cleanup, translates status only, and uses `asyncio.timeout(remaining)`.

Corrected focused verification at `1599ae9`: live target `13 passed, 1 skipped`; combined
Gemini provider/index/live lane passed with the same single opt-in skip; focused coverage
`81%`; Ruff and strict script mypy passed. No live-provider test was run.

The first authorized synthetic attempt exposed only a fixed `GeminiProviderError`, so
RED `e61eb1a` required bounded failure-stage, normalized error, resource, and cleanup
evidence. GREEN `82a81c2` added a fixed JSON failure record and successful cleanup
evidence without retaining the credential, provider payload, raw identity, response
body, or private data.

Independent quality/security review then BLOCKED `82a81c2` on two P1 findings. A
malformed provider structured-output mapping could escape through a Pydantic traceback,
and response loss after a remote create could be misreported as no resource plus
completed cleanup. RED `b379a6c` reproduced both. GREEN `8b90837` translates malformed
structured output to a fixed `SmokeContractError` and records each resource as
`not_started`, `unknown`, or `confirmed`; cleanup becomes `unknown` when a remote outcome
cannot be proven.

Locked offline verification at exact `8b90837`:

- `tests/providers/gemini`, `tests/indexing`, and
  `tests/live/test_gemini_file_search_contract.py`: `207 passed, 1 skipped`; the skip is
  the required live opt-in;
- Ruff on the Task 2.8 script and live contract test: passed;
- strict mypy on the Task 2.8 script: passed;
- `git diff --check`: passed;
- `556b01d` and SDK integration `8fa42f4` are ancestors of `8b90837`; the branch was
  clean before this handoff-only edit.

Post-failure diagnosis and correction added two more committed RED/GREEN pairs. RED
`669cfe1` proves that the prior `models.generate_content` query does not satisfy the
current Gemini 3.7 File Search contract. GREEN `62ead30` moves only the query boundary to
the pinned SDK's `aio.interactions.create` surface, matching Google's official File
Search structured-output example at `https://ai.google.dev/gemini-api/docs/file-search`:
scoped `file_search`, `store=false`, JSON
`response_format`, and omitted generation/thinking configuration. RED `69eff14` then
reproduces the independent-review evidence findings before GREEN `b962036` adds exact
imported `document_uri` binding and hard bounds for citation metadata, locator, page, and
excerpt. It also temporarily added the SDK-generated deprecated top-level
`response_mime_type`, which the next live attempt proved remained incompatible with the
current provider contract.

Locked offline verification at exact `b962036`:

- `tests/providers/gemini`, `tests/indexing`, and
  `tests/live/test_gemini_file_search_contract.py`: `209 passed, 1 skipped`; the skip is
  the required live opt-in;
- Ruff, strict script mypy, and `git diff --check`: passed;
- Terra specification: **SHIP**, no residual findings;
- Terra quality/security: **SHIP**, no residual findings; the required MIME type,
  document binding, bounded evidence, raw-content exclusion, and cleanup/error precedence
  are satisfied;
- neither review read a credential, called the provider, or accessed private data.

The second authorized attempt used the reviewed Interactions surface but still returned
HTTP 400 at `positive_query`; three resources were confirmed, three cleanup calls were
attempted, and cleanup reported failure. The existing Hub stayed running and healthy,
and the temporary task was removed. Its bounded evidence was identical in shape to the
record below and retained no provider identity, payload, response body, request ID,
credential, or local path.

Pinned-SDK wire serialization then showed that `b962036` sent both a deprecated top-level
`response_mime_type` and `response_format.mime_type`. Google's current official Gemini
3.7 File Search structured-output Python and REST examples send MIME only inside
`response_format`. RED `db57294` requires the exact five allowed body keys; one-line GREEN
`14d001f` removes the duplicate. The locked affected lane remains `209 passed, 1 skipped`,
with Ruff, strict mypy, and diff hygiene passing. Terra specification returned **SHIP**;
Terra quality/security found no code residual and required only this handoff correction.
Neither review crossed the provider, credential, or private-data boundary.

## Authorized synthetic attempt evidence

The reviewed `8b90837` adapter was run once under separate prior authorization with the
stored key and synthetic data only. The retained record contains no credential, request
ID, provider identity, payload, response body, or local path. Its exact bounded result
was:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "provider",
  "failure_stage": "positive_query",
  "provider_status_code": 400,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

This proves only the observed cause: Gemini rejected the positive query with HTTP 400.
The deliberately redacted record does not establish the provider's more detailed reason,
so this handoff does not invent one. All three cleanup calls were attempted and cleanup
reported failure; disposable resources could therefore have remained at that point. The
temporary runner task was removed, while the existing Hub task stayed running and its
health endpoint remained `ok` before and after the attempt.

Connor later authorized one stored-key read for cleanup reconciliation only. A separately
reviewed exact-match reconciler streamed provider listings without retaining unrelated
metadata, matched only `Study Hub Task 2.8 synthetic contract` and
`task-2-8-synthetic.pdf`, and ran no query or smoke. Its bounded result was
`status=completed`, zero matching stores/documents/files, zero delete attempts, and zero
remaining matching stores/files. The Hub remained running and healthy, and the temporary
cleanup task was removed.

The first offline root-cause diagnosis corrected the query API surface from
`models.generate_content` to `interactions.create`. The second authorized attempt then
isolated the remaining mismatch to the serialized Interactions body: the deprecated
top-level MIME duplicate was not present in Google's current official example. The
retained live records do not include provider error text, so `14d001f` still requires a
separately authorized live confirmation; this handoff does not claim that confirmation.

Initial Terra review of `230cc39` returned **BLOCK** on two offline-preparation
findings: the temporary failure fake proved cleanup but not durable retry/resume,
and the runbook did not say that the hard-coded live guard still requires a later
code-wiring change. Test-only checkpoint `aae069c` reproduced both findings before
GREEN `e579329`. Terra specification review then returned
**SHIP_OFFLINE_PREPARED**. Terra quality re-review found only that the actual CLI
hard-block exception retained the superseded owner action. Test-only `9777a5a`
reproduced that operator-message gap before the one-string GREEN correction in
`1d31885`.

Final Terra specification and quality re-reviews both returned
**SHIP_OFFLINE_PREPARED** at exact `1d31885`. They confirmed the durable retry
fixture, deterministic local-only behavior, aligned operator/runbook/artifact
messages, opt-in guard, and absence of provider/Keyring/private-data access. Both
explicitly confirmed that Gate 2B remains blocked and unclaimed.

Initial live-adapter Terra specification and quality/security reviews both returned
**BLOCK** at exact `bbd6fff`; their complete findings are enumerated in the TDD section
above.

Final independent re-reviews both returned **SHIP** on exact clean candidate/handoff
`d3c6b235e4f25d3da3882823d2f0d53bc4c42abc`:

- Terra specification: **SHIP**, no residual blockers; the full-metadata identity subset,
  answer-body redaction, and truthful Gate 2B/live boundary resolve its prior findings.
- Terra quality/security: **SHIP**; corrected code `1599ae9` resolves all five findings
  covering metadata, redaction, cleanup-error precedence, operation-error classification,
  and remaining-deadline polling. It confirmed no secret/provider boundary was crossed and
  Gate 2B remains closed.

Fresh independent completion-evidence reviews of exact clean code head `8b90837` also
returned **SHIP**:

- Terra specification: **SHIP**, no residual findings; the redacted failure record,
  malformed-output boundary, conservative resource/cleanup states, and remaining
  Gate 2B boundary are correct.
- Terra quality/security: **SHIP**, no residual findings; both P1 findings are resolved,
  cleanup precedence and CLI coverage hold, and ambiguous outcomes remain `unknown`.
- Both reviewers explicitly performed read-only offline reviews without provider,
  credential, or private-data access.

Initial reviews of Interactions candidate `62ead30` BLOCKED on the required top-level
response MIME interpretation, imported-document citation binding, bounded citation
fields, and the stale handoff. The evidence findings were reproduced at RED `69eff14` and
resolved at GREEN `b962036`. Live evidence plus the current official example superseded
the MIME interpretation at RED/GREEN `db57294` / `14d001f`. Fresh specification review
returned **SHIP**; quality/security found only the stale handoff sentence corrected here.

## Third authorized attempt and root-cause correction

The next separately authorized synthetic attempt ran exact archive content from clean
`b88731e` with `google-genai==2.14.0`. The Hub remained running and its health endpoint was
`ok` before and after; the temporary runner task was removed. The request advanced past
the prior HTTP 400 and returned after approximately 281 seconds, but the local adapter
reported a contract failure while processing the positive query. Three disposable
resources were confirmed, all three cleanup calls were attempted, and cleanup reported
failure:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "contract",
  "failure_stage": "positive_query",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

That bounded record contains no provider body or local exception reason, so it cannot by
itself prove which local contract check rejected the response. Exact offline inspection
nevertheless found two concrete adapter defects. First, Google's current Interactions
contract makes `document_uri` optional and its File Search citation examples use
`file_name` plus `source`; the adapter incorrectly required `document_uri` to equal the
imported document resource. Second, `GeminiConfig.request_timeout_seconds=120` never
reached `google-genai`, whose `HttpOptions.timeout` is expressed in milliseconds. The
observed duration is therefore consistent with the missing timeout, but is not claimed as
provider proof.

Committed RED `37190e5` reproduces both defects with the documented citation shape and
the exact SDK-construction contract. GREEN `ed2172f` wires the existing timeout once in
`GeminiClientFactory`, binds a citation to the exact uploaded display name, accepts an
absent `document_uri`, still rejects a present conflicting `document_uri`, and emits only
an allowlisted fixed `contract_reason` for the newly diagnosed local failures. Independent
quality review found one regression-test gap for the retained conflicting-URI guard;
test-only commit `b0c18e8` adds that coverage.

Locked offline verification at exact clean `b0c18e8`:

- `tests/providers/gemini`, `tests/indexing`, and
  `tests/live/test_gemini_file_search_contract.py`: `210 passed, 1 skipped`; the skip is
  the required live opt-in;
- Ruff on the four affected source/test files: passed;
- strict mypy on the smoke script and shared Gemini client: passed;
- `git diff --check`: passed;
- SDK integration `8fa42f4` and reviewed base `556b01d` are ancestors;
- Terra specification: **SHIP**, no residual findings;
- Terra quality/security: initial **BLOCK** only for the conflicting-URI regression-test
  gap, then **SHIP** at `b0c18e8`, no residual findings.

Neither the offline correction nor either exact-head re-review called the provider, read
a credential, accessed private data, or changed the running Hub.

## Fourth authorized attempt and positive-validation correction

Connor separately authorized one cleanup-reconciliation key read followed by one smoke
key read. The exact-match reconciler completed first with zero matching stores, files, or
documents, zero delete attempts, no failures, and zero remaining matching stores/files.
Hub health was `ok` before and after, and the temporary cleanup task was removed.

The smoke then ran exact clean head `7d08fd2` containing independently reviewed code
`b0c18e8`, with archive SHA-256
`85e046c00da9b0484aee252816d0b1c884d71aee0fb55b8fa043e56ccf56de14`. The archived
smoke script matched the workspace, both Terra reviewers returned **SHIP** on the exact
archive/wrapper preflight, and the Hub remained `ok`. The request and local response
parsing completed, but post-query acceptance failed at `positive_validation`. Three
resources were confirmed, all three cleanup calls were attempted, and cleanup reported
failure:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "contract",
  "failure_stage": "positive_validation",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

The retained record contains no response body or fixed validation reason, so it cannot
prove whether answer equality, citation cardinality, or page/excerpt resolution triggered
the failure. Offline contract inspection found one concrete over-strict acceptance rule:
the manifest requires verification of a citation, while the runner required exactly one
citation and rejected multiple fully matching annotations from the same document.

Committed RED `487418d` reproduces that mismatch and the missing safe failure reason.
GREEN `376acce` now requires at least one citation and selects one that matches the exact
imported document, PDF page one, and synthetic excerpt; strict scope/file/document parsing
remains unchanged. It also adds fixed allowlisted reasons for answer, absent-citation, and
unresolved-citation failures. The locked affected lane passed `212 passed, 1 skipped`,
Ruff and strict mypy passed, and both independent Terra reviewers returned **SHIP** at
exact `376acce`. No further provider call or credential read occurred after the failed
smoke, and the temporary smoke task was removed.

## Fifth authorized attempt and answer-validation correction

Connor authorized the ordered five-step Gate 2B sequence. Step 1 cleanup reconciliation
completed with zero matching or remaining synthetic resources, no failures, and Hub
health `ok`. Step 2 then ran exact clean archive head `b8d7069`, containing independently
reviewed code `376acce`, after both Terra reviewers returned **SHIP** on the exact
archive/wrapper hashes. The Hub remained `ok` throughout.

The live request again completed through response and citation parsing. It failed closed
at `positive_validation` with the new fixed `contract_reason`:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "contract_reason": "positive_answer_invalid",
  "error_category": "contract",
  "failure_stage": "positive_validation",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

That evidence proves structured output parsed but the answer did not satisfy the prior
combined exact-answer/support check. Offline inspection found that the positive prompt
itself disclosed the complete expected fact while validation required an exact
full-sentence echo. This neither cleanly proved retrieval nor accepted the natural
marker-only structured answer.

Committed RED `80d6bd3` reproduces the answer disclosure, marker-only rejection, and
ambiguous fixed reason. GREEN `8bdcfe5` asks for the marker without disclosing it, requires
`supported=true` and the undisclosed marker in the structured answer, and retains the
stronger full-fact, page-one, document-, file-, and scope-bound citation checks. The
locked affected lane passed `215 passed, 1 skipped`; Ruff and strict mypy passed; both
independent Terra reviewers returned **SHIP** at exact `8bdcfe5`.

Because Step 2 did not pass, the ordered sequence stopped before Step 3 private Lecture
13 access, Step 4 Sol-9/live-evidence acceptance, and Step 5 Program Sol-0 integration.
No private data was accessed and no integration or Gate record mutation occurred. The
temporary smoke task was removed; no further provider call or credential read occurred
under that authorization.

## Sixth authorized attempt and parser-diagnostic correction

Connor authorized a new cleanup-reconciliation key read followed by one synthetic smoke.
Cleanup completed first with zero matching or remaining resources, no failures, and Hub
health `ok`. Both Terra reviewers returned **SHIP** on the exact archive/wrapper preflight
for clean head `1d8927e`, containing reviewed implementation `8bdcfe5`; all local and NUC
hashes matched.

The smoke created and imported the disposable synthetic resources, but failed closed
during `positive_query`. All three cleanup calls were attempted and cleanup reported
failure; Hub health remained `ok`:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "contract",
  "failure_stage": "positive_query",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

The absence of `contract_reason` proves that the prior candidate still had unclassified
positive-query parser exits; it does not identify which citation or usage check failed.
Committed RED `51eee5e` covers scope, metadata, document, file, excerpt, page, and usage
rejections. GREEN `6a90c3d` assigns each one a fixed allowlisted reason without relaxing
validation or retaining raw content/provider identities. The locked affected lane passed
`216 passed, 1 skipped`; Ruff and strict mypy passed; both independent Terra reviewers
returned **SHIP** at exact `6a90c3d`.

Because the synthetic Step 2 smoke did not pass, private Lecture 13 access, Sol-9 live
acceptance, Program Sol-0 integration, and Gate 2B mutation were not performed. The
temporary smoke task was removed, and no further provider call or credential read
occurred under that authorization.

## Seventh authorized attempt and provider-diagnostic correction

Connor authorized another cleanup-reconciliation key read and one synthetic smoke.
Cleanup completed with zero matching or remaining resources and Hub health `ok`. Both
Terra reviewers returned **SHIP** on the exact archive/wrapper preflight for clean head
`21f81a7`, containing reviewed implementation `6a90c3d`; local and NUC hashes matched.

The smoke created/imported the synthetic resources but failed during `positive_query`
below the local parser. All three cleanup calls were attempted and cleanup reported
failure; Hub health remained `ok`:

```json
{
  "cleanup": {"attempted": 3, "status": "failed"},
  "error_category": "provider",
  "failure_stage": "positive_query",
  "provider_status_code": null,
  "resources_created": {
    "document": "confirmed",
    "file": "confirmed",
    "store": "confirmed"
  },
  "retryable": false,
  "schema_version": 1,
  "status": "failed"
}
```

This proves the citation parser was not reached and the prior shared translator collapsed
SDK transport, response-shape `ValueError`, and unknown failures into the same generic
non-retryable provider category. It does not prove which underlying exception occurred.

Committed RED `d13a15c` reproduces that ambiguity. GREEN `ed3f25f` classifies HTTPX
timeouts/transports as retryable transient failures, SDK shape/`ValueError` failures as
contract failures, and remaining unknowns as provider failures. It attaches only a fixed
allowlisted `diagnostic_code`, which the smoke emits as `provider_reason`; raw exceptions
and payloads remain excluded. The locked affected lane passed `217 passed, 1 skipped`;
Ruff and strict mypy passed; both independent Terra reviewers returned **SHIP** at exact
`ed3f25f`.

Because the synthetic smoke did not pass, private Lecture 13 access, Sol-9 live
acceptance, Program Sol-0 integration, and Gate 2B mutation were not performed. The
temporary smoke task was removed, and no further provider call or credential read
occurred under that authorization.

## Eighth authorized attempt and citation/schema-query correction

Connor authorized cleanup reconciliation followed by one synthetic smoke. Cleanup first
completed with zero matching or remaining resources and Hub health `ok`. Both Terra
reviewers returned **SHIP** on clean head `c4c3b11`; exact local/runner artifact hashes
matched.

The smoke created and imported all three disposable resources. Its positive query parsed
the answer but failed closed at `positive_validation` with
`contract_reason=positive_citation_missing`. All three cleanup calls were attempted and
cleanup reported failure; Hub health remained `ok`. The retained evidence contains no
credential, provider identity, payload, response body, request ID, or local path.

The current official File Search examples show structured output and citation-bearing
responses as separate response forms; citations are optional response annotations rather
than a guaranteed structured-output companion. The existing two-query budget therefore
now uses an unstructured positive query for marker/citation evidence and the wrong-scope
negative query for the strict `SmokeAnswer` schema. RED `8bb7915` reproduced the mismatch;
GREEN `d95ce96` made only that split. Both independent reviews then found one shared
negative-validation gap. RED `206f2b5` proves that `supported=false` with a nonempty answer
was accepted; GREEN `0dfa0e8` now requires no citations, `supported=false`, and an exactly
empty answer.

Locked verification at exact `0dfa0e8`: affected provider/index/live lane `222 passed, 1
skipped` (the required live opt-in). An isolated locked-lane reproduction of exact
`ed3f25f` yielded `219 passed, 1 skipped`, correcting its stale `217` handoff count; two
tests at `8bb7915` and one at `206f2b5` produce the current `222`. Ruff passed, strict mypy
passed, and both independent Terra specification and quality/security re-reviews returned
**SHIP**. SDK integration `8fa42f4` and verified integration head `37c1b6a` are ancestors.
No provider call, credential read, private data access, push, deploy, merge, Gate mutation,
or Hub interruption occurred during these corrections or reviews.

## Offline diagnostic-starvation correction

The accepted diagnostic plan is implemented at exact reviewed head `50d5b97`. The locked
tests assert `google-genai==2.14.0` and use actual `Interaction`, `ModelOutputStep`,
`TextContent`, `FileCitation`, `FileSearchResultStep`, and `Usage` models. Optional absent,
malformed, and mismatched fields have distinct fixed diagnoses; absent `document_uri` is
diagnosed but nonfatal when metadata/file/page/excerpt bind. Usage reads only
`total_input_tokens`/`total_output_tokens`, preserving legitimate zeroes. An actual SDK
mock transport proves the serialized Interactions body and absence of top-level
`response_mime_type`.

The optional raw diagnostic sink activates only for the hard-coded synthetic
course/exam/lecture/source revision plus exact generated fixture digest and an explicit
absolute path outside Git. An injected session, private Lecture 13 identifier, any other
identifier/digest, or an in-repository path fails before credential/provider access. The
default session creates a fresh store and imports only that synthetic fixture. The sink is
atomic and private: POSIX mode `0600`, or on Windows a protected DACL with exactly one
explicit, non-inherited current-SID FullControl Allow ACE. It is size-bounded with hard
overflow failure, strips header maps, credentials and known secret bytes, never writes
stdout/chat/handoff, and exposes explicit deletion after diagnosis.

The redacted committed result now records creation/upload/import prerequisites and
independent positive answer, citation presence, document/file/scope/page/excerpt,
negative structured output, wrong-lecture filter, document listing, usage, and per-resource
cleanup outcomes. Failures do not suppress independent downstream checks. Cleanup runs
once in `finally`; local raw diagnostics distinguish each resource's delete-request failure
from context-close failure without changing the shared error translator, IndexWorker, or
cleanup success rules. `run_temporary_failure_fixture` and Task 2.7 remain unchanged.

Locked verification at `50d5b97`: provider/index/live lane `248 passed, 1 skipped` (the
required live opt-in), focused script coverage `82%`, Ruff passed, strict mypy passed, and
diff hygiene passed. Independent Terra specification and quality/security re-reviews both
returned **SHIP**. No provider call, credential read, resource reconciliation, private data
access, push, deploy, merge, Gate mutation, or Hub interruption occurred.

The first authorized next-step preflight on exact `52b7ad3` stopped before either stored-key
read because the locked Windows interpreter has no `os.fchmod`. No reconciliation,
provider call, resource creation/deletion, or diagnostic run occurred. RED `bb55f10` and
GREEN `dbf3a6b` added a portable fallback, but quality review correctly rejected Windows
`chmod(0600)` as insufficient. RED/GREEN `c4d5349` / `05bc9cb` added a protected
current-user SID DACL; quality review then required proof that no extra explicit ACE
remained. RED/GREEN `0ec7242` / `aac871f` now inspects the literal file with `Get-Acl` and
fails before raw writes unless the DACL is protected and contains exactly one explicit,
non-inherited current-SID FullControl Allow ACE.

Locked verification at exact `aac871f`: provider/index/live lane `252 passed, 1 skipped`,
Ruff passed, strict mypy passed, and diff hygiene passed. Both independent Terra reviewers
returned **SHIP**. An isolated real-Windows create/ACL-inspect/atomic-close/delete check
then passed on the NUC; the diagnostic was deleted and Hub health remained `ok`. No key,
provider, private data, reconciliation, push, deploy, merge, or Gate action occurred. The
authorization was explicitly bound to `52b7ad3`, so corrected head `aac871f` still requires
fresh authorization for reconciliation and one final diagnostic smoke.
The isolated NUC archive, extracted scratch directory, and DACL-check script were also
removed; the post-cleanup check found none of those paths or the diagnostic remaining.

## Ninth authorized diagnostic attempt and correction

Connor authorized the exact two-step sequence on clean handoff head `d755385`. The first
stored-key read completed exact-match reconciliation with zero matching or remaining
synthetic stores/files, zero delete attempts, and no failures. The Hub was `ok` before and
after. The temporary reconciliation task and scratch were removed before step two.

The second stored-key read ran one synthetic diagnostic smoke. Store creation, PDF upload,
import completion, positive answer, citation presence/scope/page, usage, negative structured
output, and wrong-lecture filtering passed. The redacted matrix failed document/file/excerpt
citation binding, document listing, and document cleanup; file and forced-store cleanup
passed. The Hub remained running and `ok`. Private comparison-only analysis established the
google-genai 2.14.0 shapes without emitting raw values or identities: citation
`document_uri` is the exact store identity, citation `file_name` is the uploaded file's
terminal identity, the bounded citation source contains the synthetic fact wrapped in line
breaks, and the import operation's bare document identity becomes the exact listed/delete
identity when qualified under the current store. The failed bare document delete returned
not-found; the later forced-store delete passed.

RED `a6b8b50` reproduced all observed shapes; GREEN `5aa394a` qualifies the import document,
binds citation store/file identities, and permits bounded line breaks while rejecting other
controls. Terra then identified two exact edge contracts. RED `5bbfd99` reproduced them;
GREEN `481a9fd` preserves both exact store-form and exact document-form citation locators
while rejecting cross-store or nested full document identities.

Locked verification at exact `481a9fd`: provider/index `192 passed`; live contract `62
passed, 1 skipped` for the required opt-in; google-genai `2.14.0`; script coverage `82%`;
Ruff, strict mypy, diff hygiene, cleanliness, and ancestry from `8fa42f4` and `d755385`
passed. Sol-9, Terra specification, and Terra quality/security all returned **SHIP** on the
exact corrected tree. The private raw diagnostic, temporary task, archive, project/run
scratch, and analyzers were deleted after correction and review; the absence check passed
and Hub health remained `ok`. No later key read or provider call occurred under that
authorization.

## Tenth authorized confirmation attempt

Connor freshly authorized one stored-key read and one complete synthetic confirmation on
exact clean handoff head `fd3e3f0`, containing reviewed code `481a9fd`. Exact local/NUC
archive, script, and wrapper hashes matched; google-genai was `2.14.0`; the locked Windows
runner was present; and the Hub task was running with health `ok` before the key read.

The single confirmation proved store creation, PDF upload, import completion, positive
answer, every citation binding, usage, document listing, and document/file/store cleanup.
The independent negative query returned retryable provider HTTP 500, so
`negative_structured_output=negative_query_failed` and wrong-lecture filtering remained
blocked by that query. Cleanup attempted and completed all three exact resources. Hub
health was `ok` before, after, and following removal of the temporary task, archive,
wrapper, and project/run scratch. No raw diagnostic was created or retained.

Sol-9 returned **BLOCK as Gate evidence / code SHIP**; Terra specification returned
**BLOCK as Gate evidence**; Terra quality/security returned **SHIP** with no code or
security defect. All agree the remaining action is a freshly authorized complete synthetic
confirmation on the same reviewed implementation. No cleanup reconciliation is required
by this attempt because every cleanup check passed. No retry, later key read, provider
call, private-data access, or Gate action occurred.

## Eleventh authorized confirmation — synthetic evidence accepted

Connor freshly authorized one stored-key read and one complete synthetic confirmation on
exact clean handoff head `22459a5`, containing reviewed code `481a9fd`. Exact local/NUC
archive, script, and wrapper hashes matched; google-genai was `2.14.0`; the locked runner
was present; and the Hub task was running with health `ok` before the key read.

The single run exited successfully and passed the complete matrix: disposable store/file/
document create-import-wait, positive answer, citation presence plus exact document/file/
scope/page/excerpt binding, usage, structured negative output, wrong-scope non-retrieval,
document listing, and document/file/store cleanup. The bounded record reported one PDF,
page 1, operation `done`, citation resolution `1.0`, `negative_scope_retrieved=false`,
validated `SmokeAnswer`, 1,358 indexed bytes, 56 input tokens, 34 output tokens, 39,875 ms,
and no warnings. Provider identities remained hashed; no raw response, credential, private
data, or diagnostic was retained.

Cleanup attempted and completed all three resources. Hub health was `ok` before, after,
and following removal of the temporary task, archive, wrapper, and project/run scratch;
the absence check passed. No reconciliation or further synthetic retry is required.

Locked verification remained green: provider/index `192 passed`; live contract `62 passed,
1 skipped` for the required opt-in; script coverage `82%`; Ruff, strict mypy, and diff
hygiene passed. Sol-9 returned **APPROVE / SHIP**; both Terra specification and Terra
quality/security returned **SHIP**. All three reviewers declared reviewed code `481a9fd`
and the synthetic Task 2.8 evidence merge-ready. No later key read, provider call,
private-data access, or Gate action occurred.

## Exact live permission boundary and runbook

No live step may begin until all of the following are true:

1. Connor explicitly authorizes one synthetic Task 2.8 Gemini smoke, including
   disposable store/file/document creation, two queries, cleanup, and provider
   quota/cost.
2. Program Sol-0 integrates and reviews `google-genai==2.14.0` in the shared
   dependency surface. Satisfied at `8fa42f4`.
3. Sol-2 implements the live `SmokeSession` against the exact SDK/model/API combination,
   wires it into `run_authorized_live_smoke`, and obtains independent specification and
   quality/security approval. Satisfied offline by implementation lineage through
   exact reviewed head `481a9fd`.
4. The credential is already stored under the existing approved secret-store key
   `gemini-api-key`. Do not paste it into the command, environment, repository,
   logs, or this handoff. Authorization must explicitly permit the runner to read
   that stored credential for this one smoke.
5. The operator verifies the target account/project permits disposable resources
   and accepts the cost/quota impact.

Only after those conditions and a read-only preflight review may a separately authorized
synthetic command cross the provider boundary:

```text
RUN_LIVE_GEMINI_TESTS=1 \
python scripts/run-gemini-contract-smoke.py --execute-live
```

The run must fail closed if the SDK, stored credential, cleanup capability, exact
model, embedding model, metadata filtering, citation/page mapping, structured
output, or redaction contract is unavailable. Retain only the redacted record;
never retain the key, local paths, or synthetic response body in Git, stdout, chat,
or the handoff. A separately authorized diagnostic run may add an absolute
`--synthetic-diagnostic-output` path outside Git. That mode retains the full synthetic raw
SDK evidence only in its atomic local file, protected by POSIX `0600` or the verified
single-current-SID Windows DACL above, and requires deletion after diagnosis.

The private Lecture 13 shadow index is a separate authorization. Connor must
authorize private-source access and identify or approve selection of the opaque
source revision. The controlled operator may retain only source revision hash,
document types, page/slide count, provider operation states, citation resolution
rate, duration, byte/token usage, and warnings. No source text, private path, raw
provider identity, or credential may enter Git or chat.

Finally Sol-9 and both Terra reviewers must inspect the exact live evidence, and
Program Sol-0 must integrate the accepted candidate and replace the blocked record.
Only that later reviewed commit may claim Gate 2B.

## Schema-29 private-preflight consumer adaptation

Program Sol-0 integrated the reviewed schema-29 read-only projection at
`b23eed5c73c968df33d97f9c9733cecf9fea96c9`. The authorized history-preserving
composition `a65ebdf75b98723eb912b38170b37c34816e0bc5` contains that prerequisite and
the prior Sol-2 handoff `aaac70f18efc1915bf62b3abf258db88e61448b4`.

Sol-2 then added only the missing consumer path:

- RED `3b49e8db528ffcc6d92d4959942b4603080364ea` executed the focused test and failed
  because `prepare_private_shadow_index_input` did not exist.
- GREEN `17fac6cf6fa689cab980d23031e8bec448720381` delegated the private input projection
  to `project_schema29_index_input(...)`, passing only the existing artifact service's
  ingestion/catalog handles, the artifact service itself, schema version, and protected
  materialization root.
- Terra specification correctly BLOCKED that first GREEN because the helper was not yet
  consumed by a real preflight flow.
- Review RED `001f83c70468f350a25be06706cc24ece5bd7d9d` executed the allowlisted-flow test
  and failed because `run_private_shadow_preflight` did not exist.
- Review GREEN `026394916d25ce3603fa9e0faaa7f684451c7602` wired the actual preflight flow.
  It consumes the schema-29 projection, builds the accepted `IndexManifest`, validates the
  canonical PDF and slide-only evidence, and emits only source-revision hash, document
  types, page/slide counts, preflight state, byte usage, and warnings. The script contains
  no `source_revisions` SQL or `KnowledgeRepository` path.

Exact locked verification at `0263949`:

- affected Gemini provider/indexing, source-trust compatibility, knowledge, and live-offline
  lane: `305 passed, 1 skipped`; the skip is the required live-provider opt-in;
- focused live contract: `64 passed, 1 skipped`;
- smoke-script branch coverage: `82%`;
- `google-genai==2.14.0`, Ruff, strict mypy, and diff hygiene: passed;
- no diff from reviewed `481a9fd` under `src/oms_hub/providers/**` or
  `src/oms_hub/indexing/**`;
- Gate 2B record unchanged at `state=blocked`, `claim=gate_not_open`.

Independent exact-candidate re-reviews both returned **SHIP** on
`026394916d25ce3603fa9e0faaa7f684451c7602`: Terra specification found the real
preflight flow correctly wired, and Terra quality/security confirmed the bounded evidence,
schema-29/scratch fail-closed delegation, absence of schema-25 queries, and unchanged
provider/live/secret behavior.

## Current private-shadow boundary

Connor's authorization for the controlled private Lecture 13 shadow index remains active.
The next operation may be the protected, read-only schema-29 private preflight selecting the
already approved opaque current slide revision. That operation must stop before credential or
provider access unless the projection, source hashes, canonical inputs, manifest, and cleanup
all pass. A successful preflight may then continue under the existing authorization to the
single stored-key read and private shadow provider sequence; no additional permission is
required.

This offline adaptation did not access private source content, read a credential, call Gemini,
create a provider resource, push, deploy, or mutate Gate 2B. Gate 2B remains closed until the
private evidence is produced, Sol-9 and both Terra reviewers accept the exact evidence, and
Program Sol-0 performs the later integration/Gate action.

## Canonical private slide-count correction

The first protected schema-29 preflight passed authority, source and derived hashes, manifest,
canonical PDF, cleanup, and Hub health, but reported 180 slides because the shared preflight
counted distinct evidence locator strings rather than canonical slide numbers.

- RED `2e33c5561c36464bcacd20faff77d16f81b359d0` reproduced same-slide locator inflation and
  added accepted and rejected locator contracts.
- GREEN `73be11097ae7c44e6d9f06c1e8f4d8d112cbc19c` extracts the slide number from every
  slide locator, validates the complete locator, and counts unique slide numbers.
- Terra quality/security found that the first parser's `\s+` accepted tab and newline
  separators. RED `0e6593b445e88995320774660a006ad0e2b3f010` reproduced both cases; GREEN
  `381452776dbe395214efecf8e7698c92b1aebb40` requires one literal ASCII space after
  the optional `slide` prefix.

Exact locked verification at `3814527`:

- affected provider/indexing/schema-29/knowledge/live-offline lane: `322 passed, 1 skipped`;
  the skip is the required live-provider opt-in;
- focused live contract: `81 passed, 1 skipped`;
- smoke-script branch coverage: `82%`;
- `google-genai==2.14.0`, Ruff, strict script mypy with the checkout source path, and diff
  hygiene: passed;
- no diff from `ee17f4f` under shared provider/indexing code or the Gate 2B record;
- Gate 2B remains `state=blocked`, `claim=gate_not_open`.

Independent exact-candidate re-reviews returned **SHIP** on
`381452776dbe395214efecf8e7698c92b1aebb40`: Terra specification, Terra
quality/security, and the separate `Review blocked gate summary` task found no residual
in-scope issue. The latter also noted two baseline-only broad-environment observations that
reproduce at `ee17f4f` and are not introduced by this candidate.

No private source, credential, provider, resource, deployment, merge, push, or Gate action
occurred during implementation or review. The next operation remains only the already
authorized protected read-only schema-29 preflight. Credential and provider operations stay
conditional on that preflight's success.

## Corrected protected read-only preflight

After both Terra exact-tree reviews returned **SHIP** on `a05eb47740d339a209121467d5535be81a879743`,
the already-authorized protected schema-29 preflight was rerun. The verified archive contained
that exact head, and the operator differed from the previously reviewed operator only in its
bounded scratch name and verified input hashes.

The allowlisted result passed:

- opaque source revision hash:
  `c6add553313ed9fbdfffa58b9a1c0a0fbd9d74e1281881c98d989859a1e38bbd`;
- document types: Markdown, PDF, and PPTX;
- canonical PDF pages: `49`;
- canonical unique slide numbers: `49`;
- provider operation state: `private_preflight_ready`;
- index-input bytes: `7,359,907`;
- warnings: none.

The operator exited `0`, reported cleanup complete, and observed Hub health `ok` before and
after the run. The temporary task, archive, operator, protected scratch, result, and status
artifacts were deleted; a second absence check passed. The `OMS Study Hub V2` task remained
running with schema `29` health `ok`.

This preflight read only the approved private projection inside the protected operator
boundary. It did not read a credential, call Gemini, create a provider resource, push,
deploy, merge, or mutate Gate 2B. Gate 2B remains closed. Any credential/provider operation
remains a separate conditional next step.

## Authorized private-provider step blocked before credential access

Connor directly authorized one controlled private Lecture 13 Gemini shadow sequence from
exact clean preflight-approved head `febcee32bdd62cb109e0a25c4c60717f03970075`.
The required entrypoint audit failed closed before private-source, credential, or provider
access: no reviewed private live-shadow callable exists on this head.

- `run_private_shadow_preflight(...)` stops after schema-29 projection, `IndexManifest`
  validation, and the allowlisted offline aggregate.
- The only live CLI path calls `run_authorized_live_smoke(...)` and
  `run_contract_smoke(...)`, which are deliberately bound to the synthetic PDF, synthetic
  scope/revision, synthetic queries, and synthetic citation assertions.
- `map_provider_citation(...)` is not wired into a private live operator.
- `IndexingService` can create the multimodal provider index, but it does not perform the two
  shadow queries, emit the private allowlisted result, or delete every disposable provider
  document/store as one reviewed sequence.

Sol-9, Terra specification, and Terra quality/security independently returned **BLOCK** on
this exact finding. All require the same minimal Sol-2 action before any private/provider
run: TDD one guarded private-shadow operator that consumes the approved schema-29 view and
manifest, indexes every accepted input, runs the positive and wrong-scope queries, resolves
every provider citation through direct evidence IDs, emits only the approved aggregates,
and deletes every created provider resource once in `finally`. The exact implementation must
receive Sol-9 plus both Terra reviews before credential access. No new shared schema
prerequisite is currently evident; Program Sol-0 only needs to integrate/compose the reviewed
operator afterward.

The current `--execute-live` command must not be used for this authorization because it would
rerun only the synthetic smoke. No key read, Gemini call, provider resource creation, private
source access, push, deploy, merge, production mutation, or Gate 2B action occurred during
this blocked audit.

## Guarded private live-shadow composition

Sol-2 implemented the missing callable without changing the reviewed synthetic runner or
shared provider/indexing/Gate surfaces:

- RED `b93400cd4f524f2073739a295a8fbf80171457de` specified opt-in-before-projection,
  pre-key mismatch rejection, every accepted multimodal input, two queries, aggregate-only
  output, and all-resource cleanup. GREEN `32c5dd27b6c11f7cab283c93ad2b3ab53ae3fc5a`
  added the minimum composition.
- RED `717a93ace09fdb00cef38e88f6b12e4793ef7fb7` required exact equality with the
  already-approved preflight record before the stored-key read. GREEN
  `f1ade1a6b3f47e05b599043bf855ca6b54f3a5a1` added that pin and the real
  google-genai 2.14.0 input/query/citation path.
- Review RED `1e4b4466d6894ce83e0e000cf5ebc0d855249469` reproduced an uncited but
  supported wrong-scope false pass and cleanup masking the primary failure. GREEN
  `40adf049adf1491234c5f212ce9624e3957c5d35` requires structured empty/
  `supported=false` negative output and preserves primary-error precedence after every
  cleanup attempt.
- Review RED `0f4e7eadc0385d8f3530eb4ebcf7206fde286ca6` reproduced contradictory negative
  wording and an uncertain upload leak. GREEN `9ae0f6554346050a79e36686a743bc57b8f3abd6`
  aligns the prompt and uses bounded exact-run file reconciliation before and after cleanup.
- Review RED `3d3ec4e81766e68fea935db7abf8302989a74197` reproduced the equivalent uncertain
  store leak. Final GREEN `0aab1c377774c4b932da2dee7a8647642148f15b` gives the store a
  nonprivate UUID-scoped display name, proves exact-name absence before creation, reconciles
  before deletion, deletes each exact current-run match once, and proves post-delete absence.

The callable consumes the schema-29 view and exact accepted preflight, rebuilds and validates
the multimodal manifest, verifies google-genai `2.14.0` plus the pinned Gemini and embedding
models before the key read, uploads/imports PPTX, PDF, normalized Markdown, and each accepted
visual-semantic image with per-input hash metadata and Markdown `700/100` chunking, runs the
positive and wrong-scope queries, and maps each positive provider citation through
`map_provider_citation(...)` to a direct manifest evidence ID. It returns only the existing
private evidence allowlist plus citation-resolution, duration, and token aggregates; response
text, provider identities, private paths, and source text remain internal and are discarded.

Exact locked verification at `0aab1c3`:

- affected provider/indexing/schema-29/knowledge/live-offline lane before final review fix:
  `330 passed, 1 skipped`; final live contract after the last fix: `90 passed, 1 skipped`;
- smoke-script branch coverage: `82%`;
- google-genai `2.14.0`, Ruff, strict script mypy, diff hygiene, ancestry, and clean status:
  passed;
- no diff under shared provider/indexing code or the Gate 2B record.

Sol-9, Terra specification, and Terra quality/security all returned **SHIP** on exact code
candidate `0aab1c377774c4b932da2dee7a8647642148f15b`. No credential, private source,
or provider boundary was crossed during implementation or review. Connor's authorization now
permits exactly one protected private Lecture 13 sequence using that immutable code candidate;
Gate 2B remains closed until the resulting sanitized evidence receives final review and Program
Sol-0 performs the later integration/Gate action.

## Authorized private shadow result: blocked before accepted evidence

The final live boundary used immutable reviewed code
`0aab1c377774c4b932da2dee7a8647642148f15b` from pre-run handoff
`201876e2774abcd81b203a553fa6511ed4bf2d8a`. Before execution, Sol-9, Terra
specification, and Terra quality/security returned **SHIP** on the exact transient operator
`86809d1e4a45d4f0920c1d85e072e9b7cccbbe1548edc53f2982ec040677ac8d`, wrapper
`8c0742b506367f5bff55ac11685c6cc0099ada6958b5be5ffb1e13ae585b6793`, code archive
`f4dd6d78f70a754647cbe142306c37a482a5edc67af6049972d160acade7f2cf`, and complete
protected CPython/venv archive
`7d978f5d9843b7064ef7e3c20fcba0719aee7e0eaf0ff44a27f27bf401cb17fd`. The protected
runtime import probe passed google-genai `2.14.0`, Keyring/Win32, SQLAlchemy, and the
reviewed smoke module with Hub health `ok` before and after.

Exactly one authorized limited-user scheduled sequence started. It ended with task result
`1`; the only accepted sanitized result was:

- `status=blocked`;
- `provider_operation_states=[operator_failed]`;
- `warnings=[private_shadow_failed]`.

The separate safe status proves `exit_code=1`, `operator_artifacts_deleted=true`, and Hub
health `ok` before and after. Subsequent exact cleanup deleted the scheduled task and all
enumerated operator, safe-result, archive, runtime, and scratch artifacts; residue count was
zero, while the Hub task remained `Running`, health remained `ok`, and schema remained `29`.

This is blocker evidence only, not private acceptance or Gate evidence. The stage-opaque
`operator_failed` result does **not** prove whether the attempted sequence read the stored
key, created any provider resource, or ran or completed provider cleanup. No subsequent key
read, provider inspection, reconciliation, retry, or private access occurred. Provider
resource state therefore remains unknown, no private acceptance aggregate exists, and Gate
2B remains closed.

Sol-9, Terra specification, and Terra quality/security each returned **SHIP** on this exact
sanitized blocker record, subject to those limits. Before any further private live attempt,
Connor/Program Sol-0 must separately authorize a bounded stored-key provider-resource
reconciliation/diagnostic step and require fresh sanitized evidence plus independent review.

## Authorized provider-resource reconciliation: evidence-stage block

Connor authorized one bounded stored-key reconciliation after the blocked private sequence.
The reviewed operator was restricted to one stored-key read and bounded list/delete/relist
operations over exact disposable names only:

- store: `task-2-8-private-[0-9a-f]{32}`;
- file: `task-2-8-private-[0-9a-f]{32}-[0-9]{3}`.

It had no source projection, private-content access, create, upload, import, query, feature,
or Gate path. The final independently reviewed transient inputs were operator
`3ceb59a90c4cb29d1c86b7ec44be7ab97ef8ba1e021e46d7cae9cdc9aad160bc`, wrapper
`71c7d19312587b8a6d9f435e74edcd069fd174a165ac2be2e2a70d8fba8c183f`, exact code
archive `f4dd6d78f70a754647cbe142306c37a482a5edc67af6049972d160acade7f2cf`, and complete
protected runtime archive
`1993f28686d9a1dc44d606f7eb6bbfd945c6c6ddf3bddd3a3357ddd9c678d1bb`. Offline tests
proved exact-name scoping, a successful zero-residue path, a silent document-delete no-op
block, continued known file/store cleanup after document-relist failure, exact evidence-code
allowlisting, and pass/empty versus block/incomplete equivalence. The protected SDK/Keyring
import probe passed with Hub health `ok`. Sol-9 and both Terra reviewers returned **SHIP**
before execution.

Exactly one limited-user reconciliation task started. It ended with task result `1`; the only
accepted safe result was `status=blocked`, `provider_operation_states=[wrapper_failed]`, and
`warnings=[reconciliation_wrapper_evidence_failed]`. The safe status recorded
`wrapper_stage=evidence`, `exit_code=1`, `operator_artifacts_deleted=true`, and Hub health
`ok` before and after. Exact subsequent cleanup deleted the scheduled task and every
enumerated operator, safe-result, code, runtime, test, and scratch artifact; residue was zero
and the Hub remained `Running`, healthy, and on schema `29`.

This is blocker evidence only. Because no provider counts were accepted, it does not prove
whether the attempted reconciliation read the key, inventoried or found matching resources,
deleted them, re-listed them, or completed provider cleanup. No subsequent key read or
provider call occurred. Provider state remains unknown and Gate 2B remains closed.

A post-run offline Windows replay proved that the exact validator accepts realistic serialized
full pass and block records and rejects contradictory or unknown-code records. The remaining
failure is confined to the bounded evidence-stage path—parse, validation, or safe-result
write—but the raw protected stream was intentionally deleted, so its exact cause is
unavailable. Before another provider operation, Sol-2 must split safe parse, validation, and
write diagnostics, reproduce the complete Python JSON to Windows validation path with
committed tests, and obtain independent review.
Only then may Connor/Program Sol-0 separately authorize one new bounded reconciliation; this
record does not authorize a retry or a private shadow sequence.

## Committed reconciliation diagnostic recovery

The independent `Review blocked gate summary` task traced the repeated opaque
`wrapper_stage=evidence` outcomes to diagnostic starvation: the reconciliation operator,
wrapper, and validator were transient, and their cleanup deleted the only raw stream capable
of distinguishing parse, validation, and safe-result-write failures. This finding does not
establish what the prior provider operation did; provider state remains unknown until a later
bounded reconciliation produces usable evidence.

The reviewer approved an offline-only correction plan at base
`5fe35bf69888a4feb2c7b4cbe4210128979a8c94`, with two mandatory mechanics: execute a real
Python emitter through the pytest-collected Windows harness, and retain raw diagnostics with
fixed warning `unexpected_stdout_prefix` whenever stdout precedes an otherwise valid final
JSON record.

- RED `152961994030a09101aa69a6a542799b004adb7c` committed the exact-name reconciliation,
  pinned-SDK Optional-field, single-key-read, no-private-path, evidence-stage, raw-retention,
  malformed/invalid/write-fault, and Python-to-PowerShell wrapper contracts before any
  implementation existed.
- GREEN `637f1d94281a1f7f1e46a201f25715d9a61cf2a6` committed the bounded list/delete/relist-only
  operator, evidence module, and Windows wrapper. The evidence module uses schema version 1,
  fixed parse/validation/write stages and exits `41`/`42`/`43`, exact aggregate keys and
  warning codes, atomic non-clobbering safe output, and separate `evidence_usable`,
  `provider_cleanup_complete`, `operator_artifacts_deleted`, raw-retention, and Hub-health
  fields. No provider identity or response body enters safe evidence.
- Independent quality/security review then found that the wrapper did not verify the retained
  directory ACE's exact rights/inheritance and checked only the immediate diagnostic parent
  for a reparse point. RED `4984306257d1377f490d95aac839d9c7b2f70ec7` reproduced both
  defects. GREEN `b687e94fcbcaf26dac61a259e9bd43a5a3983590` now rejects a reparse
  point in every existing project or diagnostic-parent component before canonical
  outside-Git comparison, and requires one protected current-SID Allow ACE with FullControl,
  ObjectInherit, ContainerInherit, no inherited rule, and no propagation.

Exact verification of code candidate `b687e94fcbcaf26dac61a259e9bd43a5a3983590`:

- locked reconciliation/evidence set: `14 passed`;
- locked provider/index/focused-live/evidence lane: `296 passed, 1 skipped`; the skip is the
  required live Gemini opt-in;
- reconciliation operator branch coverage: `83%`;
- Ruff, strict mypy on the new Python surfaces, diff hygiene, clean status, and ancestry from
  `8fa42f4` and `5fe35bf`: passed;
- the committed real-Python-to-wrapper PowerShell 5.1 harness passed on Windows with sentinel
  `RECONCILIATION_EVIDENCE_HARNESS_VERIFIED`; it covered both ancestor-junction rejections,
  retained-prefix ACL inspection, valid evidence, malformed JSON, invalid shape/code, and
  safe-write failure. Its temporary Windows files were removed after verification.

The safe broad Python lane was also attempted but is not recorded as green: seven failures
were outside this diff—the gate command's unavailable `python` shim, four tests requiring the
absent optional `anydoc` package, one pre-existing SQLite resource-warning failure, and the
known suite-order-sensitive global `google.genai` import assertion. The focused owned and
affected lanes above are green.

Independent exact-candidate verdicts on `b687e94`:

- `Review blocked gate summary`: **SHIP**, all original findings, both mandatory plan
  corrections, and both later Windows guard findings closed;
- Terra specification: **SHIP**;
- Terra quality/security: **SHIP** after the committed guard RED/GREEN;
- Sol-9 evidence/Gate review: **SHIP** for a later separately authorized bounded
  reconciliation only.

No credential was read, no provider or private source was accessed, no provider resource was
created or deleted, and no push, merge, deploy, production mutation, or Gate action occurred
during this implementation/review cycle. Gate 2B remains closed. The committed wrapper may be
used only for one separately authorized bounded reconciliation over exact disposable Task 2.8
resource names; a private shadow retry remains a distinct later operation and is not opened by
this implementation evidence.

## Post-review authorized bounded reconciliation

Connor freshly authorized one post-review bounded stored-key reconciliation after final
handoff `ef3572d2fd15a812a172f491c2887347a6a1798f`. The live composition preserved reviewed
code `b687e94fcbcaf26dac61a259e9bd43a5a3983590` and used an isolated protected Windows
runtime rather than modifying either installed Hub environment. Before execution:

- both installed Hub runtimes were rejected because neither contained `google-genai`;
- the exact `ef3572d` archive, the root `uv.lock` export, all 670 staged project files, all
  11,868 isolated runtime files, and the three committed reconciliation scripts were
  SHA-256 verified inside an exclusive current-SID FullControl DACL with no reparse point;
- the isolated runtime was installed with hashes from the committed lock export and passed
  `pip check`, google-genai `2.14.0`, pydantic `2.13.4`, httpx `0.28.1`, keyring `25.7.0`,
  and required import checks;
- every inherited `PYTHON*` override was cleared before setting only the protected staged
  source path, no-user-site, no-bytecode, and UTF-8 values;
- the final launcher hash was
  `5f83dc2999009fa0ade983468a3ce151f46233428e57754cd20e8fcf3ca26f5a`.

Terra specification and Terra quality/security each BLOCKED earlier launcher candidates on
missing full source/runtime integrity, inherited Python variables, and then missing
`PropagationFlags=None`. Each finding was corrected before any credential/provider access;
both reviewers returned **SHIP** on the exact final launcher.

Exactly one Limited/Interactive scheduled reconciliation task then ran. The schema-1 safe
envelope was usable and wrapper-complete, but the operator blocked during inventory:

- wrapper: `exit_code=1`, `evidence_usable=true`, `wrapper_stage=complete`;
- provider cleanup: `provider_cleanup_complete=false`;
- operator state: `status=blocked`, `provider_operation_states=[inventory_failed]`;
- inspected, matched, delete-attempt, and remaining counts: all zero;
- warning: `provider_reconciliation_incomplete`;
- local diagnostic disposition: `operator_artifacts_deleted=true`,
  `raw_diagnostic_retained=false`;
- Hub health: `ok` before and after; the Hub scheduled task remained `Running`.

The zero counts are placeholders from the failed inventory path. They do **not** prove that
the provider contains no disposable resources, that the stored key was read, or that provider
cleanup completed. No provider mutation was recorded, and this list/delete/relist-only
operator has no create/upload/import/query/private-source path. Provider state remains unknown.

Terra specification and Terra quality/security returned **SHIP** on this record as truthful
blocker evidence only. Sol-9 returned **SHIP as blocker evidence / BLOCK for Gate 2B and the
private shadow**. No reviewer permits a cleanup, Gate, or private-shadow success claim.

After review, the temporary scheduled task, protected staged project, isolated runtime,
manifests, launcher, safe evidence files, and probe were deleted; exact absence checks passed.
The running Hub was neither stopped nor restarted and remained `Running` with health `ok`.
The repository worktree remained clean. Gate 2B remains closed.

The minimal next action is fresh Connor/Program Sol-0 authorization for one additional
bounded reconciliation on the same reviewed code and launcher. Acceptance requires usable
evidence, `provider_cleanup_complete=true`, zero remaining stores/files/documents, reviewed
raw-retention disposition, complete local artifact cleanup, and Hub health `ok` before and
after. If another authorized attempt again returns `inventory_failed`, stop retries and add
allowlisted inventory-failure diagnostics with committed TDD and independent review before
any further provider access.

## Final authorized reconciliation retry: repeated inventory block

Connor authorized the one additional bounded reconciliation required by the prior blocker
record. Sol-2 reconstructed the same protected exact-source composition. The fresh isolated
runtime again contained 11,868 lock-installed files and passed the same dependency/import
contract, but generated virtual-environment bytes produced a new complete runtime-manifest
digest. The launcher changed only that pinned digest:

- runtime manifest:
  `cf2d56ce6107f64179cd0ee5a144600eda88a71e247fb1ce9e2b0afbb2d2487b`;
- launcher:
  `1f9f34349745a765d87507cf602f73c48fcc075f338d6bbfe3074f62a4064ec5`.

Terra specification and Terra quality/security independently returned **SHIP** on the
hash-only launcher update before any retry credential/provider access. The staged launcher
and manifest hashes matched and PowerShell 5.1 parsing remained clean.

Exactly one retry task ran. It reproduced the first reconciliation's sanitized result:

- schema 1, `evidence_usable=true`, `wrapper_stage=complete`, `exit_code=1`;
- `status=blocked`, `provider_operation_states=[inventory_failed]`;
- `provider_cleanup_complete=false`;
- every inspected, matched, delete-attempt, and remaining count was zero;
- warning `provider_reconciliation_incomplete`;
- `operator_artifacts_deleted=true`, `raw_diagnostic_retained=false`;
- Hub health `ok` before and after; the Hub task remained `Running`.

The repeated zero counts remain inventory-failure placeholders, not absence evidence. They do
not establish provider resource state, stored-key-read occurrence, or cleanup. No provider
delete was recorded, and no provider mutation may be claimed. Because the safe record retained
no diagnostic detail, the exact inventory exception remains unknown.

After evidence capture, the scheduled task, protected root, staged code, isolated runtime,
manifests, launcher, and safe evidence were deleted. Exact absence checks passed; the Hub was
not stopped or restarted and remained `Running`/`ok`.

Terra specification returned **BLOCK** for continuation; Terra quality/security returned
**SHIP as truthful blocker evidence**; Sol-9 returned **SHIP as blocker evidence / BLOCK for
cleanup acceptance, the private shadow, and Gate 2B**. All three require the same stop: do not
run a third reconciliation.

The exact next Sol-2 owner action is committed RED/GREEN allowlisted inventory diagnostics
that distinguish at least store-list, file-list, and matched-store document-list stages plus a
safe provider error category, and retain protected diagnostic evidence on inventory failure
without exposing provider identities/payloads, credential material, or private data. The
correction requires locked Python and real Windows wrapper/harness tests, Terra specification
and quality/security review, and Sol-9 evidence review before Program Sol-0/Connor may consider
new provider authorization. Gate 2B remains closed.

## Offline inventory-diagnostic correction

The independent `Review blocked gate summary` task traced both repeated opaque records to two
candidate-owned defects: a usable blocked record deleted its protected raw diagnostic, and the
operator collapsed every provider inventory exception into one warning. Its first plan review
BLOCKED the missing short not-authorized return path. The corrected plan expanded that path to
the same complete result contract and received **SHIP** before implementation.

Committed TDD from clean base `1eb811e86aca7857e0cb49cb19d41c9d797175c0`:

- RED `0d3b79039dc52adbdb51fd5dc35ebfcf0fb92942` added the complete schema-2
  result, independent store/file/document inventory failures, the five existing safe Gemini
  categories, not-authorized validation, blocked raw retention, and unknown diagnostic-pair
  rejection. The focused target produced 9 intended failures and 10 passes before production
  changes.
- GREEN `ba30310c51a52f49b5297074e8d7ee37d516df72` reuses the existing
  `translate_gemini_error` classifier, emitting only its fixed category with one fixed inventory
  stage. It changes no shared classifier or worker behavior. The reconciliation operator result
  is schema 2 with always-present `inventory_failure_stage` and `provider_error_category`; the
  evidence envelope and wrapper status remain schema 1.
- The wrapper now retains its current-SID-only protected diagnostic whenever a usable operator
  result is not `passed`. Usable passed results without an unexpected prefix retain the existing
  automatic deletion behavior.
- The evidence validator accepts only the exact schema-2 keys and fixed stage/category pairs.
  It also admits the complete not-authorized state/warning while rejecting unknown or mismatched
  pairs before safe output.
- The first expanded Windows harness run exposed only an exhausted eight-request synthetic
  health fixture. An isolated prefix-only reproduction passed with correct retained diagnostics.
  Commit `9037c82f2940ae6158805cdb8c1d8f405f959670` increased that offline fixture
  budget to 16 requests; no production code changed.

Exact offline verification at `9037c82`:

- locked focused reconciliation/evidence target: `19 passed`;
- locked provider/indexing/live/scripts lane: `337 passed, 1 skipped`; the skip is the required
  live Gemini opt-in;
- reconciliation operator branch coverage: `83%`;
- Ruff, strict mypy with the source tree, and diff hygiene: passed;
- exact local/NUC SHA-256 hashes matched for the evidence module, wrapper, and Windows harness;
- PowerShell `5.1.26100.8875` emitted `RECONCILIATION_EVIDENCE_HARNESS_VERIFIED` using only a
  synthetic Python emitter and local health fixture;
- the isolated NUC test root was deleted, both observed Hub health endpoints remained `ok`, and
  the V2 Hub task remained `Running`. The Hub was not stopped or restarted.

No credential was read, no Gemini/provider operation ran, no private source was accessed, and no
provider resource, production data, or Gate state was changed. Exact-tree Terra specification,
Terra quality/security, and Sol-9 reviews remain required before any new reconciliation may be
considered. Gate 2B, cleanup acceptance, and the private shadow remain blocked.

Exact-tree reviews of clean candidate `225181b5c4fa4ccbfe290f2d7a76c23c56c09b82`:

- `Review blocked gate summary`: **SHIP**, no residual findings; independently reproduced the
  genuine RED (`9 failed, 10 passed`) and GREEN (`19 passed`) and confirmed every result path
  now uses the complete schema-2 builder contract;
- Terra specification: **SHIP**;
- Terra quality/security: **SHIP**, confirming only fixed stage/category pairs reach safe
  evidence and protected blocked retention does not weaken passed-result cleanup;
- Sol-9 evidence/Gate: **SHIP for offline implementation only**, confirming ancestry, tests,
  handoff truth, Hub/temp cleanup, and unchanged Gate state.

This closes the offline implementation and review work only. Before another reconciliation,
Program Sol-0 must compose and verify the exact source/runtime/launcher hashes and Connor must
freshly authorize that bounded stored-key list/delete/relist operation. No private shadow or
Gate mutation is included. Any cleanup acceptance still requires usable schema-2 evidence with
`provider_cleanup_complete=true`, zero remaining disposable resources, reviewed diagnostic
disposition, complete local artifact cleanup, and Hub health `ok` before and after. Gate 2B
remains closed.

## Authorized schema-2 reconciliation result

Connor freshly authorized exactly one bounded stored-key reconciliation after the offline
diagnostic correction. Program Sol-0 produced an independently SHIP-reviewed zero-code
composition at `bacc354725f5651fcafe2e94d2fa640567f61e1a` from exact Sol-2 source
`b0adfee026d87ef3d9019aee88988b5a2073a6c4`. The protected composition bound 670 source
files, 11,868 isolated runtime files, and launcher SHA-256
`3ac495bd26a4f79ac0a316681fdf77be1f5b7d189794e1035d2be357f54a63b4`; both independent
Sol-0 reviewers returned **SHIP** before execution.

One current-user `Interactive`/`Limited` scheduled task invoked that exact launcher once. The
sanitized evidence was usable and localized the failure:

- wrapper schema 1: `wrapper_stage=complete`, `exit_code=1`, `evidence_usable=true`;
- operator schema 2: `status=blocked`, `provider_operation_states=[inventory_failed]`;
- `inventory_failure_stage=store_list`, `provider_error_category=provider`;
- `provider_cleanup_complete=false`, zero delete attempts;
- all inspected, matched, and remaining counts were zero placeholders;
- warning `provider_reconciliation_incomplete`;
- `raw_diagnostic_retained=true`, `operator_artifacts_deleted=false` during review;
- Hub health `ok` before and after; the V2 Hub task remained `Running`.

The protected raw metadata added no further cause: stdout was exactly one sanitized JSON line,
stderr was empty, and the diagnostic ACL remained protected. No raw stream, provider identity,
response payload, credential material, or private data was copied into Git or this handoff.

Final evidence verdicts:

- Terra specification: **SHIP as truthful blocker evidence**;
- Terra quality/security: **SHIP as truthful blocker evidence**;
- Sol-9 evidence/Gate: **SHIP as blocker evidence / BLOCK for provider cleanup acceptance,
  the private shadow, and Gate 2B**.

The zero counts do not prove resource absence, stored-key-read occurrence, or provider cleanup.
No delete was attempted, no provider mutation may be claimed, and no retry was made. After
review, the exact reconciliation task and entire protected composition/runtime/diagnostic root
were removed; both were proven absent. Hub health remained `ok` on both observed endpoints and
the V2 task remained `Running`; the Hub was never stopped or restarted.

The next Sol-2 owner action is offline committed RED/GREEN diagnostics that split `store_list`
into client construction, list request/pager collection, and client/context close, using only a
separately reviewed fixed allowlist of safe provider diagnostics. Locked Python and real Windows
harness verification plus Terra specification, Terra quality/security, and Sol-9 SHIP are
required before Program Sol-0/Connor may consider any fresh bounded authorization. Gate 2B,
cleanup acceptance, and the private shadow remain closed.

## Offline store-list lifecycle diagnostics

The single schema-2 reconciliation localized the live blocker only to generic `store_list` /
`provider`. Sol-2 returned to offline-only implementation from clean blocker handoff
`84d224872620be48162bc3fa7f99e7a67fb92b61` without reading a key, contacting Gemini, or
accessing private data.

The independent `Review blocked gate summary` task first **BLOCKED** the implementation plan
because existing generic `store_list` fixtures would have invalidated the blocked-retention
harness and weakened an unknown-category negative case. The corrected plan retargeted every
fixture, kept `_collect` inside the async context body, and made the private carrier explicitly
non-serializable; the reviewer then returned **SHIP** with no residual plan defect.

Committed TDD:

- RED `940a255d91a68557c1f6eb6036a2cdc28fe8cfc0` added offline real-factory seams for
  client construction/context entry, list request, pager collection, successful collection plus
  context-close failure, and primary request/pager failure plus close failure. It also required
  an untagged session failure to fail closed, preserved bounded overflow, replaced the evidence
  allowlist fixtures, and rejected legacy generic `store_list`. The focused target produced
  7 intended failures and 17 passes before production edits.
- GREEN `7355176b510b7195b026632e07cdcae3aa7f219c` added one private fixed-message
  `_StoreListFailure` carrier. It is caught and unwrapped inside `reconcile_resources` and is
  never returned or serialized. The operator emits only `store_client`, `store_request`, or
  `store_close` plus the existing `translate_gemini_error(error).category`; it adds no field,
  classifier, provider status, request ID, exception text/type, identity, or payload.
- Both the list call and pager `_collect` remain inside the reviewed `GeminiClientFactory`
  context body. Its existing `body_failed` rule therefore prevents a context-close failure from
  masking a request or pager primary failure. `OverflowError` still produces bounded
  `provider_reconciliation_scope_exceeded`; an untagged session exception now produces
  `provider_reconciliation_contract_invalid` with `not_applicable` / `none`.
- The schema-2 eleven-key result shape, schema-1 wrapper/evidence envelope, raw-retention rule,
  file/document stages, and every delete/relist behavior remain unchanged. The fixed evidence
  stage allowlist replaces generic `store_list` with `store_client`, `store_request`, and
  `store_close`.

Exact offline verification at `7355176`:

- locked reconciliation/evidence target: `24 passed`;
- locked provider/indexing/live/scripts lane: `342 passed, 1 skipped`; the skip is the required
  live Gemini opt-in;
- reconciliation operator coverage: `85%`;
- Ruff, strict mypy with `MYPYPATH=src`, and diff hygiene: passed;
- exact local/NUC SHA-256 hashes matched for the evidence module, wrapper, and harness;
- PowerShell `5.1.26100.8875` emitted `RECONCILIATION_EVIDENCE_HARNESS_VERIFIED` using only
  synthetic data and local health;
- the exact NUC test root was removed and proven absent; the V2 Hub task remained `Running` with
  health `ok` and was not stopped or restarted.

No credential/provider/private-source/resource/production/Gate operation occurred.

Exact-tree review of clean candidate `f15dce6b4e5617ca2e2b90f7a88630a2b9423095`:

- Terra specification: **SHIP**;
- Terra quality/security: **SHIP** — the carrier remains private/non-serializable, only fixed
  lifecycle stages and existing allowlisted categories are emitted, generic/unknown/mismatched
  diagnostics are rejected, and primary-failure precedence and blocked retention remain intact;
- Sol-9 evidence/Gate: **SHIP for the offline implementation only** — reproduced `24 passed`,
  Ruff, strict mypy, and diff hygiene; verified RED/GREEN ancestry, unchanged schema-2 output,
  no Gate-file diff, and clean worktree.

Any later provider attempt still requires a fresh Program Sol-0 exact hashed composition and
fresh Connor authorization. Gate 2B, provider cleanup acceptance, and the private shadow remain
closed.

## Authorized lifecycle-diagnostic reconciliation result

Program Sol-0 composed exact Sol-2 source `c839657910a4e2a31db34c4ee950a37ebde259c0`
at reviewed composition handoff `7edfe8cc93f7ffa90f6c26d5d9c71e728dce30d1`. The
launcher SHA-256 matched the reviewed value, no prior reconciliation task existed, the Hub task
was `Running`, and both active health endpoints returned `ok` before execution.

The first registration command used an SSH-session account label that Windows could not map to
a SID. It failed before task creation, stored-key access, or provider access. The corrected task
reused the existing Hub task's translated `Interactive` / `Limited` principal. Exactly one
provider-capable task then ran; no retry ran.

Sanitized schema-1 evidence:

- `wrapper_stage=complete`, `exit_code=1`, and `evidence_usable=true`;
- raw diagnostics were retained only in the protected local root during evidence review;
- Hub health was `ok` before and after.

Sanitized schema-2 evidence:

- `status=blocked`, `provider_operation_states=[inventory_failed]`;
- `inventory_failure_stage=store_request`, `provider_error_category=provider`;
- all inspected, matched, delete-attempt, and remaining counts were zero placeholders;
- `provider_cleanup_complete=false` and warning `provider_reconciliation_incomplete`.

No delete was attempted. The placeholders prove neither provider-resource absence nor cleanup.
No private source, production data, deployment, push, merge, or Gate action occurred.

Independent exact-evidence verdicts:

- Terra specification: **SHIP as truthful blocker evidence only**;
- Terra quality/security: **SHIP as truthful blocker evidence only**;
- Sol-9 evidence/Gate: **SHIP as truthful blocker evidence / BLOCK for provider-cleanup
  acceptance, the private shadow, and Gate 2B**.

After review, the exact temporary task and protected composition/runtime/raw-diagnostic root
were removed and proven absent. The Hub task remained `Running`, and both health endpoints
returned `ok`. The consumed authorization does not permit a retry. Any correction requires
offline committed RED/GREEN, exact-tree review, a fresh Program Sol-0 hashed composition, and
fresh Connor authorization. Gate 2B remains closed.

## Offline store-request diagnostic correction

The independent `Review blocked gate summary` task reviewed the usable
`store_request` / `provider` blocker at `4e7f41ebe0cae5629370cd6d60f19975dac1701f`.
It **BLOCKED** another reconciliation because google-genai 2.14.0 HTTP 400 and 404 failures both
collapsed to the generic category, while requiring different owner action. The reviewer accepted
a schema-preserving reconciliation-local projection as the minimum correction.

Committed TDD:

- RED `004d664551db51824575dd62e13fc6e2cb44e880` used the real pinned SDK through
  `httpx.MockTransport` and produced three intended failures: 400 and 404 still serialized as
  `provider`, and the PowerShell evidence contract did not allow the two safe categories.
- GREEN `d68eb6d12d85a008271a98115f9554eb2b8db4ee` calls
  `translate_gemini_error` exactly once. Only translated `provider` plus status 400 projects to
  `provider_bad_request`; only translated `provider` plus 404 projects to
  `provider_not_found`. No numeric status, status text, body, request ID, identity, credential,
  exception detail, or new field is serialized.
- Type-safety correction `27c24241f751000e9759a95dd78b26f629a5f2d3` replaced an optional-key
  dictionary lookup after strict mypy rejected it. The same focused target remained green.
- The evidence validator adds only the two fixed categories. Schema 2 retains its exact eleven
  keys; schema 1, raw retention, primary-over-close, delete/relist, synthetic runner, cleanup,
  and Gate behavior are unchanged.

Exact offline verification at `27c2424`:

- locked reconciliation/evidence target: `26 passed`;
- affected provider/indexing/live/scripts lane: `336 passed, 1 skipped`; the skip is the required
  live Gemini opt-in;
- reconciliation operator coverage: `86%`;
- Ruff, strict mypy, and diff hygiene: passed;
- PowerShell `5.1.26100.8875` emitted `RECONCILIATION_EVIDENCE_HARNESS_VERIFIED`;
- local/Windows hashes matched for the evidence module, wrapper, and harness, and the exact
  Windows test root was removed.

Exact-candidate verdicts:

- independent `Review blocked gate summary` post-review: **SHIP**, no residual findings;
- Terra specification: **SHIP**;
- Terra quality/security: **SHIP**;
- Sol-9 evidence/Gate: **SHIP for the offline implementation only**.

No credential, provider, private-source, production, or Gate action occurred. Any provider
confirmation requires a fresh Program Sol-0 exact hashed composition and fresh Connor
authorization. Gate 2B, provider cleanup acceptance, and the private shadow remain closed.

## Authorized store-request provider confirmation

Connor authorized exactly one bounded reconciliation using immutable Sol-2 source
`47c8d32d9c8cfe2a46690b3ea3e23535757d7fb0` and independently reviewed Program Sol-0
composition `0975a1c7e60c5b4030da87debd80e259fc203dd2`. The launcher SHA-256 matched
`f945beceee5a06ba1b5af075ffbff7e5360157cc95cdea6bbcf54b03d6fc510e`.

The protected preflight passed before execution: 670 source files, 11,868 runtime files,
runtime-manifest SHA-256 `b7c06a2913f9666b3e363bd4f1f7bcf9dc535fe7c8c3e4c8712ffbdb76ba4ec6`,
google-genai 2.14.0, exact script and launcher hashes, root/evidence ACL and reparse checks,
reconciliation task absence, and Hub `Running` / health `ok`.

Exactly one limited-interactive reconciliation task ran; no retry ran. Sanitized evidence:

- schema 1: `wrapper_stage=complete`, `exit_code=1`, `evidence_usable=true`, and Hub health
  `ok` before and after;
- schema 2: `status=blocked`, `provider_operation_states=[inventory_failed]`,
  `inventory_failure_stage=store_request`, and
  `provider_error_category=provider_bad_request`;
- all inspected, matched, delete-attempt, and remaining counts were zero placeholders;
- `provider_cleanup_complete=false` and warning `provider_reconciliation_incomplete`.

No delete was attempted. The placeholders prove neither provider-resource absence nor cleanup.
No private source, production data, feature activation, deployment, push, merge, or Gate action
occurred.

Exact-evidence verdicts:

- Terra specification: **SHIP as truthful blocker evidence**;
- Terra quality/security: **SHIP as truthful blocker evidence**;
- Sol-9 evidence/Gate: **SHIP as truthful blocker evidence / BLOCK for provider-cleanup
  acceptance, the private shadow, and Gate 2B**.

After review, the exact temporary reconciliation task and protected
composition/runtime/diagnostic root were removed and proven absent. The Hub task remained
`Running`, and both active health endpoints returned `ok`.

The authorization is consumed. The next Sol-2 action is an offline committed RED/GREEN audit of
the exact pinned-SDK store-list request preparation to determine the invalid request shape. If
that request is correct and the failure is account/API capability, Program Sol-0 must resolve
the external capability instead. Either outcome requires exact-tree review, a fresh Program
Sol-0 hashed composition, and fresh Connor authorization before any provider confirmation.
Gate 2B remains closed.

## Offline exact store-list request audit

The `store_request` / `provider_bad_request` evidence at blocker handoff
`c623d68dab343c7e67d6e0ae6d829911e13cf834` triggered an offline-only audit of the exact
google-genai 2.14.0 transport preparation. No key, provider, private source, or production data
was accessed.

The real pinned client through `httpx.MockTransport` prepares the current call as:

- method and path: `GET /v1beta/fileSearchStores`;
- query: `pageSize=100`;
- body: empty.

The installed official SDK defines `page_size` as an optional integer, maps it to `pageSize`,
and its packaged File Search tests exercise explicit page sizes. The generated model contains no
maximum or validation proving that 100 is invalid. Its asynchronous pager follows page tokens,
so removing the override would retain collection behavior, but no offline evidence establishes
that the override caused the observed 400 rather than project/account/API capability.

The independent `Review blocked gate summary` task returned **SHIP for plan B: no Sol-2 code
change**. It rejected a speculative page-size edit under the explicit fix-only-proven-defect
rule. No new RED/GREEN pair was created: the committed live RED remains `c623d68`, while the
offline audit found no candidate-local defect that could truthfully produce a GREEN. Fabricating
one would weaken the evidence boundary.

Locked verification on the unchanged candidate:

- reconciliation/evidence target: `26 passed`;
- affected provider/indexing/live/scripts lane: `336 passed, 1 skipped`; the skip is the required
  live Gemini opt-in;
- Ruff, strict mypy, and diff hygiene: passed.

Exact Program Sol-0 / Connor owner action:

1. Confirm administratively, without reading or copying the key, that the stored credential
   belongs to the intended Google Cloud / AI Studio project.
2. Confirm that project has the Generative Language Gemini Developer API enabled and that
   File Search / `fileSearchStores` is available to its account tier, region, and organization
   policy in v1beta mode.
3. Confirm API-key restrictions allow the Generative Language endpoint from the NUC execution
   environment, including API, application/IP/referrer, billing/account, and organization-policy
   restrictions. Retain only safe yes/no outcomes and non-sensitive owner/remediation labels.
4. Correct any administrative defect out of band and review it. If these checks pass, preserve
   the blocker because there remains no proven Sol-2 request-construction defect.

No provider retry is authorized. Any future reconciliation requires reviewed capability
evidence, a fresh exact Sol-0 hashed composition, and fresh Connor authorization. Gate 2B,
provider cleanup acceptance, and the private shadow remain closed.

Exact-candidate review of clean doc-only head
`08874c04ded72bdc17c5be1ed88fb00332aae604`:

- independent `Review blocked gate summary`: **SHIP**, no residual findings;
- Terra specification: **SHIP**;
- Terra quality/security: **SHIP**;
- Sol-9 evidence/Gate: **SHIP for the external capability blocker / BLOCK for provider-cleanup
  acceptance, the private shadow, and Gate 2B**.

Sol-9 independently reproduced the exact pinned-SDK request preparation and confirmed that the
handoff does not fabricate a candidate GREEN. The candidate authorizes no retry or Gate action.
