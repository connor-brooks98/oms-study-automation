# Task 1.8 Gate 2A evidence correction

Status: **HELD / unapplied**. This is a proposal to Program Sol-0, not an
applied integration-file change.

## Program Sol-0 intake metadata

- Originating Sol: `Sol-1`.
- Originating task: `Task 1.8 — Deliver Source Trust Gate 2A`.
- Exact integration application base: commit
  `7603bc690dd00d4dd59ac933d50a96b20558bc4a` / tree
  `78b749f17b0e6af1cdab6a68a8e9c50c7c7d0a8f`.
- Sol-1 correction base: commit
  `5ee3259e3b6fa2a2becaa8e226d91ab0f29944a8` / tree
  `b384eea07a31331c516a21248dff1651b65fd475`.
- This proposal does not authorize a Gate 1 checksum edit or the CP-0002
  Sol-2 consumer test; those remain with their assigned owners.

## Exact proposed shared files

Program Sol-0 may apply the replacements below to exactly:

1. `artifacts/acceptance/grounded-learning/gate-2a-source-trust.json`
2. `docs/implementation/handoffs/1.8.md`

Sol-1 does not modify either integration-owned file in this commit.

## Exact proposed replacements

These replacements remove private topic/operational narrative, keep only the
permitted hashes, counts, locator distribution, and warnings, and make the
private dry-run command executable. They intentionally do not reproduce the
private token being removed, change the gate decision, or claim a new Windows
result.

Replace the complete JSON object whose `id` is
`lecture13_private_dry_run` with:

```json
{
  "id": "lecture13_private_dry_run",
  "command": "python -m oms_hub.knowledge.backfill --dry-run --limit 1",
  "status": "pass",
  "evidence": "counts={examined:1,created:1,already_present:0,failed:0,evidence_count:180}; locator_distribution={slide:180}; warnings=[]; hashes={source_sha256:de14c3131badbb834e94cfbda7c433c2facead5db46dae970094bab5bf70e6e5,derived_pdf_sha256:3e25615cf3f19121c247165b9a2b64dded0644e92eea3ce22448dab401c5bcaf,database_backup_sha256:4a031f9c0a2dd9f1ea59ae11fa602e7fab9a93d3bd05a7d8c2c0e6650578d4ba}",
  "scope": "Task 1.8 Step 4 private dry-run report."
}
```

Replace the private dry-run row in `docs/implementation/handoffs/1.8.md`
with exactly:

```text
| Private Lecture 13 dry run | **PASS**: counts `examined=1`, `created=1`, `already_present=0`, `failed=0`, `evidence_count=180`; locator distribution `slide=180`; warnings `[]`; source/PDF/backup hashes recorded in the machine artifact |
```

Remove the private-run operational sentence from the final safety paragraph so
the paragraph ends exactly:

```text
production mutation, or private source text exposure occurred. Gate 2A remains
closed only pending final independent review.
```

## Sol-1 Windows proof correction

The accompanying Sol-1 change replaces the one-slide smoke test in
`tests/document_processing/test_windows_office.py` with one committed test that:

- builds exactly three slides with `SlideFixture` and `build_pptx`;
- uses `SerialOfficeConverter`;
- reads the exported PDF with the already-installed `pypdf`;
- asserts exactly three pages; and
- proves each of three unique markers occurs only on its corresponding page,
  including slide 3 only on PDF page 3.

No helper, production implementation, or dependency is added. Program Sol-0
must run the committed test on the controlled Windows Office host after
integrating the reviewed Sol-1 commit; this proposal does not claim that future
run passed.

This correction is acceptance-test-only. There are no production implementation
changes, so no artificial RED/GREEN production checkpoint is manufactured; the
native Windows execution remains the runtime gate.

## Verification commands

Executed on the Sol-1 macOS review host:

```text
python -m pytest tests/document_processing/test_windows_office.py -q -rs
SKIPPED: requires Windows desktop Microsoft Office

ruff check tests/document_processing/test_windows_office.py
All checks passed!
```

Required after Program Sol-0 integrates the reviewed Sol-1 commit:

```text
python -m pytest tests/document_processing/test_windows_office.py -q -rs
```

The applied shared-file patch must also receive JSON parsing, privacy-token,
and independent Terra review before Gate 2A can be reconsidered. This proposal
does not resolve or weaken the separately owned Gate 1 checksum and CP-0002
consumer-test blockers.

## Independent review

- Terra specification: **APPROVED**. The reviewer confirmed the exact
  three-marker/page mapping contract, aggregate-only private evidence, exact
  executable command, HELD proposal state, and ownership boundaries.
- Terra quality/release: **APPROVED**. The reviewer confirmed deterministic
  marker isolation, `pypdf` file lifetime, the honest macOS skip, matching
  base/tree metadata, and the absence of central, Gate 1, Sol-2, dependency,
  or production changes.
- These approvals cover this Sol-1 correction candidate only. They are not
  Gate 2A gate-open approval.
